---
id: T-084
title: "checkout-guard — deny checkout-mutating git in the shared primary"
type: feature
scope: hooks
epic: enforced-isolation
plan: enforced-isolation
points: 3
value: 8
time_criticality: 8
risk_opportunity: 5
wsjf: 7.0
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-084
files_owned:
  - kit/ops/hooks/checkout-guard.sh
  - kit/.claude/settings.json
  - kit/ops/install.sh
context_files:
  - kit/ops/hooks/readonly-allow.sh
  - kit/ops/hooks/ownership-guard.sh
  - ops/contracts/shared-checkout.md
depends_on: []
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/hooks/checkout-guard.sh
  - printf '{"cwd":"/tmp/fakerepo","tool_input":{"command":"git switch x"}}' | bash kit/ops/hooks/checkout-guard.sh | grep -q 'primary checkout is shared'
  - test -z "$(printf '{"cwd":"/tmp/fakerepo/.polaris/wt/T-000","tool_input":{"command":"git switch x"}}' | bash kit/ops/hooks/checkout-guard.sh)"
  - test -z "$(printf '{"cwd":"/tmp/fakerepo","tool_input":{"command":"git status"}}' | bash kit/ops/hooks/checkout-guard.sh)"
  - grep -q 'checkout-guard.sh' kit/.claude/settings.json
  - python -c "import json,io;json.load(io.open('kit/.claude/settings.json',encoding='utf-8'))"
  - grep -q 'checkout-guard.sh' kit/ops/install.sh
map_delta: "kit/ops/hooks/checkout-guard.sh — new PreToolUse deny hook: checkout-mutating git is refused in the shared primary, allowed inside .polaris/wt/<ID>"
---
## Why
Five parallel sessions share ONE primary checkout, and today any of them can `git switch` it out
from under the others — that is exactly how one PR silently carried another session's commits.
This task ships the guard that makes the primary untouchable: a new PreToolUse Bash hook that
DENIES checkout-mutating git commands (`switch`, `checkout`, `reset`, `stash`, `merge`, `rebase`,
`cherry-pick`, `worktree add`, `branch -D/-d/-m/-M`) when the session's cwd is the primary
worktree, and stays silent (allow) inside a task worktree `.polaris/wt/<ID>` or for read-only
git. Contract § v2.1 pins the deny message, the detection rule (a `/.polaris/wt/` path segment in
cwd means worktree; otherwise primary), and the exact top-level fn names. Register the hook in
the kit settings template and add it to install.sh's chmod line — the settings hook-merge repair
picks it up by path identity on `update` with no merge-code change.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] pure bash, no fork to ops/polaris, no python — the readonly-allow timeout lesson (slow
      hook = killed = FAILS OPEN) sizes the budget; only fast builtins + at most trivial git
- [ ] every deny command form from contract § v2.1 is refused in the primary; the same commands
      inside .polaris/wt/<ID> pass silently; `git status`/`log`/`diff`/non-git always pass
- [ ] deny is the PreToolUse JSON permissionDecision shape ownership-guard already emits, and the
      message is the contract's pinned ONE-LINE string, byte-exact
- [ ] top-level fns are EXACTLY deny · jstr · mutating_git (api-kit pins — T-086 writes the
      golden from the contract, never from your diff)
- [ ] kit/.claude/settings.json registers it beside ownership-guard (matcher Bash, sane timeout);
      file stays valid JSON
- [ ] kit/ops/install.sh:106 chmod line gains ops/hooks/checkout-guard.sh
- [ ] NOT wired into readonly-allow.sh — that hook only ever ALLOWS, by contract

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- bash 3.2: no `case` inside `$(...)`; split `local` declarations.
- Scratch dirs are per-lane: namespace everything under scratchpad/T-084/.
- Copy jstr verbatim from readonly-allow.sh — do not invent a new JSON reader.
- ops/tests/api-kit.expected is NOT yours — T-086 owns it this wave; your fn names are pinned in
  the contract so the golden moves without your diff.
- assumed: `git stash list`/`show` stay ALLOWED (default-safe) — the only two read-only stash forms, and the two readonly-allow.sh's git_ok already allows; denying them would break the disjointness the two hooks rely on.
- deny JSON is readonly-allow.sh's hookSpecificOutput shape: ownership-guard denies by exit 2 + stderr, which the pinned verify[2] grep of stdout could never see.
- the one git probe fails CLOSED — an unreadable or absent repo at cwd counts as primary. verify[2]'s cwd (/tmp/fakerepo) is not a repo and must still deny.
- a separator riding on the subcommand token (`git fetch;git rebase x`) must be handed BACK to the tokenizer unconsumed; consuming it hid the second invocation entirely.
- `find --api` anchors to the PRIMARY index by design (ops/lib/search.sh:13), so api-kit stays green from a worktree. Queried the index db directly: the extractor yields exactly the three lines § v2.3 pins for T-086.
