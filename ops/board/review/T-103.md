---
id: T-103
title: "Install side — VISUAL.md ships, new hooks are executable, keep-awake reaches every armed machine, uninstall refuses archives and strips every hook entry"
type: feature
scope: install
epic: cant-eat-itself
plan: cant-eat-itself
points: 3
value: 5
time_criticality: 5
risk_opportunity: 3
wsjf: 4.3
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-103
files_owned:
  - kit/ops/install.sh
  - kit/ops/bootstrap.py
  - kit/ops/lib/admin.sh
  - kit/.claude/skills/polaris-install/SKILL.md
approved:
context_files:
  - ops/contracts/keep-awake.md
  - ops/contracts/worktree-liveness.md
  - ops/contracts/role-handover.md
  - ops/contracts/install-parity.md
  - ops/contracts/key-registry.md
depends_on:
  - T-095
  - T-101
  - T-102
  - T-110
contract: ops/contracts/keep-awake.md
verify:
  - bash -n kit/ops/install.sh
  - bash -n kit/ops/lib/admin.sh
  - python -c "import ast;ast.parse(open('kit/ops/bootstrap.py',encoding='utf-8').read())"
  - grep -q 'VISUAL.md' kit/ops/install.sh
  - grep -q 'ops/hooks/awake-hook.sh' kit/ops/install.sh
  - grep -q 'ops/hooks/handover-hook.sh' kit/ops/install.sh
  - grep -q 'def merge_awake_hooks' kit/ops/bootstrap.py
  - test "$(python -c "import ast;t=ast.parse(open('kit/ops/bootstrap.py',encoding='utf-8').read());print(len([n for n in t.body if isinstance(n,ast.FunctionDef)]))")" = "$(git show main:kit/ops/bootstrap.py | python -c "import ast,sys;t=ast.parse(sys.stdin.read());print(len([n for n in t.body if isinstance(n,ast.FunctionDef)])+1)")"
  - grep -q 'awake-press.ps1' kit/ops/bootstrap.py
  - grep -q 'awake-hook.sh' kit/ops/lib/admin.sh
  - grep -q 'awake-press.ps1' kit/ops/lib/admin.sh
  - grep -q 'archived worktree(s) in .polaris/wt-archive/ — uncommitted work lives there' kit/ops/lib/admin.sh
  - grep -q 'awake/repos' kit/ops/lib/admin.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/lib/admin.sh)" = "$(git show main:kit/ops/lib/admin.sh | grep -c '^[a-z_]*()')"
  - grep -q 'keep-awake hooks' kit/.claude/skills/polaris-install/SKILL.md
  - grep -q 'arms the machine' kit/.claude/skills/polaris-install/SKILL.md
  - diff <(grep '^#' kit/.claude/skills/polaris-install/SKILL.md | tr -d '\r') <(git show main:kit/.claude/skills/polaris-install/SKILL.md | grep '^#' | tr -d '\r')
  - bash -c "$(cat ops/tests/perm-tools.cmd)" | diff - ops/tests/perm-tools.expected
  - bash -c "$(cat ops/tests/output-style-installed.cmd)" | diff - ops/tests/output-style-installed.expected
  - bash -c "$(cat ops/tests/adhd-skill-installed.cmd)" | diff - ops/tests/adhd-skill-installed.expected
map_delta: "install.sh ships ops/VISUAL.md and chmods awake-hook.sh + handover-hook.sh; bootstrap.py arm_machine and admin.sh refresh copy the two awake files to ~/.claude/polaris/ and merge the four machine hooks (merge_awake_hooks); uninstall dies on a non-empty .polaris/wt-archive/, deregisters the repo from the awake registry, and strips every ops/hooks/ entry across all hook events"
---
## Why
Everything the sprint built has to reach installed repos and armed machines, and be removable.
`install.sh`: `VISUAL.md` joins the `KIT_CODE` manifest (so `ops/VISUAL.md` exists in every repo and
is RULES-guarded like its siblings) and the chmod list gains the two new hooks. `bootstrap.py`
(`arm_machine`, the installer's machine step): extract `awake-hook.sh` + `awake-press.ps1` from the
archive into `~/.claude/polaris/` and run the hook's `install` through ONE new fn
`merge_awake_hooks` (fails open with one ⚠ line — a machine problem never fails a repo install).
`admin.sh` (`refresh_machine_kit`, the `update` path): copy the same two files (source `kit/ops/hooks/`
first, `ops/hooks/` fallback — the existing two-path pattern) and run `install`. `cmd_uninstall`:
die BEFORE the worktree check when `.polaris/wt-archive/` is non-empty (uncommitted work lives there
— the human moves it out), deregister this repo from `~/.claude/polaris/awake/repos/` (never the
daemon or the hooks — other repos use them), and strip EVERY `ops/hooks/` entry across ALL hook
events (Stop, SessionStart, UserPromptSubmit, PreToolUse …), foreign entries kept — today it strips
only the ownership guard, and a dangling Stop hook would error on every turn of an uninstalled
repo. The machine-level install skill (`kit/.claude/skills/polaris-install/SKILL.md`) says what
"arms the machine" now includes: its sentence gains the keep-awake hooks — one prose edit, no
heading change. Specs: keep-awake.md § installers, worktree-liveness.md § wt-archive,
role-handover.md (uninstall strip), visual-check.md (manifest).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `install.sh`: `KIT_CODE` += `VISUAL.md`; chmod line (:106) += `ops/hooks/awake-hook.sh` and `ops/hooks/handover-hook.sh`; fresh and live-board paths both copy it (they share the list); nothing else changes
- [ ] `bootstrap.py`: `merge_awake_hooks(bash_path)` (the ONLY new def; no new module-level constant) extracts both files to `~/.claude/polaris/`, chmods the .sh, runs `bash <abs>/awake-hook.sh install`; called from `arm_machine`; any failure ⇒ one ⚠ line, install continues; `PERMS` untouched
- [ ] `admin.sh` `refresh_machine_kit`: copies both files (two-path fallback like :204-207) to `~/.claude/polaris/` and runs `install`; fails open; no new fn
- [ ] `cmd_uninstall`: the pinned wt-archive die (count in the message) BEFORE the existing worktree-count die; removes `~/.claude/polaris/awake/repos/<cksum>` for THIS primary only; the settings-strip python removes every entry whose command matches `ops/hooks/` under EVERY hook event, keeps foreign entries and empties-out events with no entries left; existing prompts/notes unchanged
- [ ] proven by hand in a throwaway repo (scratchpad/T-103/): `bash kit/ops/install.sh <dir>` then plant a foreign Stop entry + the three handover entries + the awake-style entries in its `.claude/settings.json`, `uninstall --yes` ⇒ exactly the foreign entry remains; with a non-empty `.polaris/wt-archive/x-1/` ⇒ the pinned die; `HOME=$T/home python kit/ops/bootstrap.py`-style arm on a fixture home ⇒ `~/.claude/polaris/awake-hook.sh` present and four hook entries merged (never against your real `~/.claude`)
- [ ] `kit/.claude/skills/polaris-install/SKILL.md`: the "arms the machine" sentence mentions the keep-awake hooks (plans/v2.md WS3); the `^#` line set byte-identical to `main` (the skill is indexed by api-kit); nothing else in the file changes
- [ ] `bash kit/ops/polaris doctor --selftest --only upgrade,adopt` green (foreground, ≥600000 ms timeout); `python kit/ops/pack.py --allow-dirty` green (the build:)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: exactly ONE new python def (`merge_awake_hooks`) — T-104 owns `ops/tests/api-kit.expected` this wave and writes that row; no new module-level constant (they are indexed as `const`), no new sh fn, no heading.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-103` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- NEVER run `arm_machine`/`install`/`uninstall` against your real `~/.claude/settings.json` or this repo — every proof uses `HOME=<scratch>` and a throwaway repo. The one real rewrite of the live settings file is the release tail's dogfood click (G11).
- `kit/.claude/skills/polaris-install/SKILL.md` IS yours (board amendment 2026-09-01): ONE sentence edit, bold/plain prose only — `kit/.claude/skills/polaris/SKILL.md` (the project skill) is T-107's, leave it alone.
- Installer-output tripwires (install-parity.md): the quiet-line count above the epilogue and the INIT kickoff phrase are goldened by CI drills — add no output lines to install.sh. Scratch dirs per-lane: scratchpad/T-103/. Windows: `python`, never `python3`. bash 3.2.
- assumed: `merge_awake_hooks(archive, bash_path)` — the contract names `(bash_path)`, but the fn has to read both hook files out of the archive; arity is not part of the indexed surface (default-safe).
- `index.py`'s python fn pattern is `^\s*def`, so an INDENTED helper would add a second api-kit row — `merge_awake_hooks` uses one try/except rather than a nested `warn()`.
- `merge_awake_hooks` captures `awake-hook.sh install`'s stdout (shown only under --verbose): a quiet install still prints 1 line above the epilogue, under ci.yml:330's cap of 2. Proved on a fixture HOME; the real `~/.claude` was never written.
- drill `upgrade` is RED on `main` too (remote.sh:240, the uninstall pre-confirm summary) — T-096's liveness keeps a live worktree past `done`, so the PRE-EXISTING "N POLARIS worktree(s) still checked out" die fires before the summary. Not caused by T-103; `adopt`, `fmlist,tcm,brain,grant` and all 12 install drills are green.
- `cmd_uninstall`'s prompt and say/note wording left byte-identical (acceptance box), so "the write-guard hook entry" now under-describes what is actually stripped.
- scratch ran outside the repo (`%TEMP%/claude/.../scratchpad/T-103`), not `scratchpad/T-103/`: that path is not in `.gitignore` and would have made the tree dirty at handoff.
