---
id: T-111
title: "Drill handover + goldens handover-route / handover-stop — every verb, --do, the hook ladder, hop cap, handover: off; cli-help-parity learns next"
type: chore
scope: selftest
epic: cant-eat-itself
plan: cant-eat-itself
points: 3
value: 8
time_criticality: 5
risk_opportunity: 2
wsjf: 5.0
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-111
files_owned:
  - kit/ops/lib/selftest/board.sh
  - ops/tests/handover-route.cmd
  - ops/tests/handover-route.expected
  - ops/tests/handover-stop.cmd
  - ops/tests/handover-stop.expected
  - ops/tests/cli-help-parity.cmd
  - ops/tests/cli-help-parity.expected
approved:
context_files:
  - ops/contracts/role-handover.md
  - ops/tests/triage-lane.cmd
  - ops/tests/checkout-guard-denies.cmd
  - kit/ops/lib/handover.sh
  - kit/ops/hooks/handover-hook.sh
depends_on:
  - T-109
  - T-110
contract: ops/contracts/role-handover.md
verify:
  - bash -n kit/ops/lib/selftest/board.sh
  - grep -q '^drill_handover()' kit/ops/lib/selftest/board.sh
  - test "$(grep -c '^drill_[a-z_]*()' kit/ops/lib/selftest/board.sh)" = 6
  - grep -q 'CLAUDE_CODE_SESSION_ID=drill-sid' kit/ops/lib/selftest/board.sh
  - grep -q 'nothing to promote' kit/ops/lib/selftest/board.sh
  - grep -q 'allow:consumed' kit/ops/lib/selftest/board.sh
  - grep -q 'allow:cap' kit/ops/lib/selftest/board.sh
  - grep -q 'allow:off' kit/ops/lib/selftest/board.sh
  - grep -q 'allow:subagent' kit/ops/lib/selftest/board.sh
  - test -f ops/tests/handover-route.cmd
  - test -f ops/tests/handover-stop.cmd
  - grep -q 'POLARIS_HANDOVER_NEXT' ops/tests/handover-stop.cmd
  - grep -q 'pinned-reason-lines: 1' ops/tests/handover-stop.expected
  - grep -q '"decision"' ops/tests/handover-stop.expected
  - grep -q '^build T-' ops/tests/handover-route.expected
  - grep -q '^promote' ops/tests/handover-route.expected
  - grep -q '^nothing to promote' ops/tests/handover-route.expected
  - grep -q 'chore(board): promote' ops/tests/handover-route.expected
  - grep -q '|next) ' ops/tests/cli-help-parity.cmd
  - test "$(cat ops/tests/cli-help-parity.expected | tr -d ' \r')" = 10
  - test "$(cat ops/tests/handover-route.expected ops/tests/handover-stop.expected | grep -c '[0-9]\.[0-9]\.[0-9]')" = 0
  - bash -c "$(cat ops/tests/cli-help-parity.cmd)" | diff - ops/tests/cli-help-parity.expected
  - bash -c "$(cat ops/tests/handover-stop.cmd)" | diff - ops/tests/handover-stop.expected
  - bash -c "$(cat ops/tests/handover-route.cmd)" | diff - ops/tests/handover-route.expected
map_delta: "doctor --selftest gains the handover drill body (board.sh; label registered by T-104); goldens handover-route + handover-stop pin every next verb and hook rung; cli-help-parity counts next (10)"
---
## Why
`polaris next` is now the router every role follows and the Stop hook's oracle — a wrong line 1
misroutes a whole session, and a hook that blocks when it should allow traps a chat in a loop. Both
must be proven where prose cannot hold them. `drill_handover` (board.sh, `export
CLAUDE_CODE_SESSION_ID=drill-sid`, hermetic on the spine repo) walks the contract's assertion list
in order: help lists `next`; a ready task ⇒ `build`; claim ⇒ `task` + `last-event` + `resume`;
self-land handoff ⇒ `done` event ⇒ `finish` and the hook says `block:finish` with `hops`=1; `finish`
rc 0 ⇒ `finished` ⇒ `allow:finished`; a backlog task whose dep is done ⇒ `promote`, `--do` moves it
with the event and the board subject, a second `--do` ⇒ `nothing to promote`, an overlapping one is
held; an un-hopped fresh event ⇒ `block:build` then `allow:consumed`; release ⇒ `avoid`; hop cap ⇒
`stop` + `allow:cap`; foreign lock ⇒ `wait`; live foreign lease ⇒ `wait`, stale ⇒ `integrate`;
risk-high review alone ⇒ `finish` + the approve note; `handover: off` ⇒ `allow:off`;
`stop_hook_active` ⇒ `allow:harness-cap`; a newer `subagents/x.jsonl` ⇒ `allow:subagent`; `--brief`
≤8 lines with the markers and no `|`. Two goldens carry the cheap half on every `check`:
`handover-route` (the `triage-lane` fixture pattern: per board state line 1, the verb regex count,
the 3-space count; the `--do` state adds `ls ready`, the promote-event count and the board subject)
and `handover-stop` (a fixture state dir, `POLARIS_HANDOVER_NEXT` for the rail cases both ways, one
un-stubbed real-`next` case via `POLARIS_HANDOVER_CLI`, one raw JSON invocation pinning the shipped
block shape, `pinned-reason-lines: 1`). `cli-help-parity`'s alternation gains `next` (expected 10).
Spec: role-handover.md § executable check.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `drill_handover` asserts every item of role-handover.md § drill, in order, by rc and file state (never message presence alone); fixtures declare `risk:` explicitly; the sh forwarder to `$SELF` is planted as `ops/polaris` in the throwaway repo (the hook and `next` need a CLI at `<primary>/ops/polaris`, or `POLARIS_HANDOVER_CLI=$SELF`); hermetic — `git status --porcelain` unchanged across it, green twice in a row, no daemon or job left behind
- [ ] `handover-route.{cmd,expected}` and `handover-stop.{cmd,expected}` as pinned; machine-specific bytes normalized; no kit version number; each < 10 s and byte-identical on a second run
- [ ] `cli-help-parity.cmd` alternation `…|finish|next)`, expected `10`; every other byte of the `.cmd` unchanged
- [ ] each new assertion watched RED once under a semantic sabotage in a throwaway kit copy (e.g. drop the `consumed` rung; swap rows 3/4 in `next_route`), then green on restore — read the sabotage diff
- [ ] `bash kit/ops/polaris doctor --selftest --only handover` green (foreground, ≥600000 ms timeout) once T-104's label has landed (before that, run the drill body directly from a scratch spine copy and say so in Notes); goldens proven from the worktree with the `.cmd`-body form; after landing, the integrator sabotages each golden red on the primary and restores green

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: exactly ONE new fn (`drill_handover`) — T-104 owns `ops/tests/api-kit.expected` this wave and writes that row; the label `handover` in `SELFTEST_LABELS` and its `drill_on` block are T-104's (spine.sh) — do NOT touch spine.sh.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-111` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- Spine state reaches drills by bash dynamic scoping — NO `local` in drill bodies. The hook you test is NOT active in this repo (it lives in `kit/.claude/settings.json` until dogfood) — nothing hops your own session. `polaris check` is PRIMARY-anchored; new goldens are auto-LF-pinned.
- Never edit kit/ops/lib/*.sh while a selftest is RUNNING. Scratch dirs per-lane: scratchpad/T-111/. Windows/Git Bash: never assert on CR-sensitive output via grep/sed — use awk/od. bash 3.2.
- A self-land BEATS its own worktree seconds before `done` asks to remove it, so wt_remove refuses the live lane, `feat/<ID>` stays checked out, drift calls it CRUFT and `finish` reds — the drill clears the beat and runs `sweep --fix` before `finish`, which is also what makes it honest under `--only handover` (the history drills that sweep the spine's own done branches never run in a subset).
- `find --api` extracts NESTED functions too (cmd_finish's fin_* are api-kit rows), so drill_handover defines no helper at all — every hook invocation is inline, and the api surface gains exactly the one name T-104 writes.
- The hook's `allow:consumed` rung sits ABOVE `allow:finished`, so after the block at step 5 the same event can never reach the finished rung — the drill clears `hopped-event` (and backdates the event file) to read the rung under test.
- Row 3 over row 4 is invisible unless a claimable task and a promotable one are on the board AT ONCE; handover-route gained that state, and it is the only line the rows-3/4 swap sabotage reds.
- Measured on this machine (Git Bash, ~0.7 s of module loading per CLI call): handover-route 21 s · handover-stop 12 s · drill ~4 min, against the peer router golden triage-lane at 15 s. The acceptance line's "< 10 s" is not reachable for a golden that also drives `--do` (promote scan + board commit + drift); the goldens are in the same order as the golden they are modelled on.
- Proven pre-T-104 from a throwaway kit copy with `handover` sed into SELFTEST_LABELS + a drill_on block after selfland: green twice, and red under four semantic sabotages — consumed rung dropped (drill `HANDOVER HOOK CONSUMED FAIL`, handover-stop red x2), next_route rows 3/4 swapped (handover-route red), block JSON nested under hookSpecificOutput (handover-stop red), usage line renamed (cli-help-parity 9 != 10).
