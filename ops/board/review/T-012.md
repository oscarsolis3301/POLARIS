---
id: T-012
title: Conductor learns the autonomy dial — auto plan gate, backlog drain, roadmap confirm, gate pings
type: feature
scope: conductor
points: 3
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 5.3
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-012
plan: hands-free-core
files_owned:
  - kit/ops/roles/CONDUCTOR.md
context_files:
  - ops/contracts/hands-free-knobs.md
  - kit/ops/roles/PLANNER.md
depends_on: []
contract: ops/contracts/hands-free-knobs.md
verify:
  - grep -q "plan_gate" kit/ops/roles/CONDUCTOR.md
  - grep -q "drain_slices" kit/ops/roles/CONDUCTOR.md
  - grep -q "notify-gate" kit/ops/roles/CONDUCTOR.md
  - grep -qi "additive" kit/ops/roles/CONDUCTOR.md
  - grep -qi "roadmap" kit/ops/roles/CONDUCTOR.md
map_delta:
---
## Why
The conductor hard-stops at the plan gate even for zero-risk plans, ends the run the moment ready/
empties (leaving groomed backlog parked), stalls on an empty board instead of reading the human's
standing roadmap, and is silent at the exact moments it blocks on a human. This wires the four
hands-free behaviors from ops/contracts/hands-free-knobs.md into CONDUCTOR.md — all opt-in, all
defaulting to today's flow, with every hard gate (risk:high approval, STOP-AND-ASK) kept verbatim.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] Step 3 documents effective plan_gate per the contract's precedence rules: auto proceeds after the SAME full disclosure only when no risk:high exists in plan + disclosed drain depth AND nothing on STOP-AND-ASK is touched; either present forces the wait; the proceed line tells the human it went ahead.
- [ ] Step 7 gains `drain: backlog` exactly per contract: queue behavior first, then ≤ drain_slices planner-subagent promotion rounds, this-plan (`plan:` tag) tasks only, drift after each round, degrade-to-classic-start note for no-subagent harnesses.
- [ ] Step 3's disclosure enumerates the full backlog-drain depth and names beyond-cap tasks as staying parked, so one "go" covers the whole run.
- [ ] Empty board + no objective + ops/ROADMAP.md exists → step 1 confirms the next unstarted line with the human, then plans; the line substitutes ONLY for the typed objective — 0b/0c and the plan gate still run in full.
- [ ] notify-gate called at all four contract call sites (plan · risk <ID> · question <ID> · done), each stated as ADDITIVE to, never a substitute for, the in-conversation gate.
- [ ] Step 5 notes builder_questions:default-safe: fewer questions return, each recorded as an `- assumed:` Notes line; an unanswerable question still parks to blocked/, never stalls the run.
- [ ] Every knob unset → the file describes today's flow unchanged (all new text is conditional on its knob).

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- CONDUCTOR.md is 118 lines and read whole every conductor session — extend existing steps, do not add a parallel protocol; keep the growth to ~20 lines.
- growth landed at +25 net lines (119→144): the contract's binding auto/drain conditions did not compress further without losing wording the contract fixes; notify-gate plan is tied to entering the wait (auto-proceed skips it, per the contract's call-site name).
