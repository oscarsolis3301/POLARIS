---
id: T-081
title: "6.0.0 — BREAKING changelog, the ask entry it never got, and the release ritual"
type: chore
scope: release
epic: autonomy-by-default
plan: autonomy-by-default
points: 2
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 8.0
risk: high
status: review
owner: Owner@DESKTOP
branch: feat/T-081
files_owned:
  - CHANGELOG.md
context_files:
  - ops/contracts/hands-free-knobs.md
  - ops/contracts/key-registry.md
  - ops/contracts/ask-approval.md
  - ops/CONVENTIONS.md
depends_on:
  - T-048
  - T-049
  - T-050
  - T-074
  - T-075
  - T-076
  - T-077
  - T-078
  - T-079
  - T-080
  - T-082
  - T-083
contract: ops/contracts/key-registry.md
verify:
  - grep -q '6.0.0' CHANGELOG.md
  - grep -q 'BREAKING' CHANGELOG.md
  - grep -qF "autonomy: standard" CHANGELOG.md
  - grep -q 'ask' CHANGELOG.md
map_delta:
---
## Why
6.0.0 changes the safety posture of every installed repo, and a breaking change nobody can read
about is a trap, not a release. The builder's half is CHANGELOG.md: a BREAKING section for the
autonomy flip naming the one-line revert (`autonomy: standard`), entries for the key registry /
doctor drift / adopt / hook-merge repair, AND the entry the `ask` rule kind never got — it landed
2026-07-28 (commit 64c3742) undocumented. The ritual's other half is HUMAN by the write-routing
table and stays that way.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] CHANGELOG.md gains a 6.0.0 section with an explicit BREAKING block: unset autonomy knobs
      now compose auto / default-safe / auto-reversible; `autonomy: standard` is the one-line
      revert; hard gates unchanged (name them)
- [ ] entries for: KEYS.tsv registry + doctor drift line + `polaris adopt` · settings.json
      hook-merge repair (path identity, field updates) · `polaris approve` + the `ask` rule kind
      (backfilling the undocumented 2026-07-28 landing) · ask at the plan gate (ready-gate
      finding, triage three-way)
- [ ] HUMAN steps, per ops/CONVENTIONS.md:75-85 — the builder does NOT do these; the run's close
      does, with the human, in order:
      - [ ] bump kit/ops/VERSION to 6.0.0 (HUMAN-ONLY per the write-routing table)
      - [ ] commit · `git tag v6.0.0 && git push --tags` (CI publishes the zip)
      - [ ] `python kit/ops/pack.py --dogfood` · commit the refreshed ops/ (cli-help goldens
            regenerate here)
- [ ] END-TO-END acceptance (the question the human actually asked), at the close, after
      --dogfood: run `ops/polaris update` in polaris-testbed; from a cold session its doctor
      reports plan_gate=auto · builder_questions=default-safe · evolve_apply=auto-reversible
      with testbed's CONVENTIONS.md STILL UNMODIFIED; the BREAKING banner printed at update; and
      .claude/settings.json's ownership-guard entry moved to timeout: 20 with any user-added
      hook untouched

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- risk: high — the Integrator merges this only with the human's explicit yes in the conversation,
  and the release acts (VERSION, tag, dogfood) are the human's own. A landed VERSION bump without
  its tag reds CI's one-version-everywhere gate — the four release places move TOGETHER.
- Match the existing CHANGELOG house style; newest section on top.
- Do not golden or hardcode-test any line embedding the version number.
- Planner 2026-08-04: T-082/T-083 added to depends_on so 6.0.0 SHIPS the radically plain
  `standard` voice rather than following it. The CHANGELOG entry should mention the
  plain-language rewrite (new `standard` register, Pre-send check, plain worked examples;
  see ops/contracts/output-style.md § v2).
- assumed: entry dated 2026-08-04 — today; the human's tag at the close is the release act (default-safe)
- verified in-tree: 28 labeled drills (spine.sh SELFTEST_LABELS) and 17 golden pairs (ops/tests/) — the counts the entry states
- ops/MAP.md:13 still reads "27 at 6.0" — labeled-drill count is now 28; for EVOLVE's MAP fold, not this task
