---
id: T-159
title: "Prove the skills — the skills drill (gaps · propose · promote · hit · prune · restore end to end), the fast-tier section, and the two goldens that pin the budget and the foreign-skill survival"
type: feature
scope: selftest
plan: sprint-c
points: 3
value: 8
time_criticality: 5
risk_opportunity: 8
wsjf: 7.0
risk: normal
model: strong
status: review
owner: Owner@DESKTOP
branch: feat/T-159
files_owned:
  - kit/ops/lib/selftest/policy.sh
  - kit/ops/lib/selftest/fast.sh
  - kit/ops/lib/selftest/spine.sh
  - ops/tests/skill-budget.cmd
  - ops/tests/skill-budget.expected
  - ops/tests/skill-install.cmd
  - ops/tests/skill-install.expected
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/self-skills.md
  - ops/tests/triage-lane.cmd
  - ops/tests/handover-route.cmd
  - kit/ops/lib/selftest/remote.sh
  - kit/ops/lib/skills.sh
depends_on: [T-156, T-157, T-152, T-158]
contract: ops/contracts/self-skills.md
verify:
  - bash -n kit/ops/lib/selftest/policy.sh
  - bash -n kit/ops/lib/selftest/fast.sh
  - bash -n kit/ops/lib/selftest/spine.sh
  - grep -q '^drill_skills()' kit/ops/lib/selftest/policy.sh
  - grep -q "autoupdate surfaces skills'" kit/ops/lib/selftest/spine.sh
  - grep -q 'drill_on skills' kit/ops/lib/selftest/spine.sh
  - grep -q 'ft_section skills' kit/ops/lib/selftest/fast.sh
  - test -s ops/tests/skill-budget.expected
  - test -s ops/tests/skill-install.expected
  - bash ops/tests/skill-budget.cmd | diff - ops/tests/skill-budget.expected
  - bash ops/tests/skill-install.cmd | diff - ops/tests/skill-install.expected
  - bash kit/ops/polaris doctor --fast
  - grep -qE 'policy.sh.fn.drill_skills' ops/tests/api-kit.expected
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/selftest/policy.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/selftest/policy.sh' ops/tests/api-kit.expected)
  - test "$(wc -l < ops/tests/api-kit.expected | tr -d ' ')" = 694
  - test "$(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/*' | tr -d '\r' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected | grep -c '^<')" = 0
  - bash ops/tests/startup-budget.cmd | diff - ops/tests/startup-budget.expected
map_delta:
  - "selftest gains the skills drill (label skills: gaps · propose · twin-or-not per the probe · promote refusals and pass · claim's skill-hit · prune demote/archive · restore byte-identical) and the fast-tier section skills (constants · skill_bytes vs slim_scan on the same fixtures · skill_paths); goldens skill-budget (hermetic: constants, shelf, reserved-name and TODO refusals) + skill-install (a foreign skill survives install.sh and uninstall byte-identical); labels 37"
---
## Why
A budget nobody measures is a wish, and an eviction nobody has watched run is a promise. This task
is the proof for the whole skills seam, in the three tiers the kit already has: a drill that walks
one skill from gap to archive and back on a hermetic board, asserting rc and bytes at every step; a
fast-tier section that proves the byte counter in milliseconds — including that `skill_bytes` and
`slim_scan` agree on the same three fixtures, so the shelf and the machine tax can never disagree;
and two goldens that pin the constants and the refusals forever at the cost of a subprocess, plus
the promise `update`/`uninstall` make to a skill they did not write. You are the W3 api-kit owner;
your single row is `drill_skills`.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `drill_skills` (policy.sh, gated after `drill_qa` in spine.sh, label `skills` appended to `SELFTEST_LABELS`): self-skills § 9's sequence — the hermetic board, `gaps` lists only `src/search/`, `propose --write` (file, TODO, seven headings, flag true; the twin asserted EITHER way from the `probe:` line), promote refuses TODO and passes after the description is rewritten, a third promote past 1600 refuses, `claim` writes ONE skill-hit line, 41 synthetic done events ⇒ prune says demote (rc 1) and `--apply` flips, 81 ⇒ archive moves the dir and `git ls-files` drops it, `restore` ⇒ byte-identical. rc + bytes, never message text
- [ ] `selftest_fast` section `skills`: `skill_consts` values · `skill_bytes` on the three fixture frontmatters (0 / ≤ 320 / 827) · `slim_scan` over a fixture `CLAUDE_CONFIG_DIR` holding the same three files reports the same numbers · `skill_paths` parses flow and block lists
- [ ] `ops/tests/skill-budget.{cmd,expected}` hermetic per § 9; `ops/tests/skill-install.{cmd,expected}` per § 8/§ 9; no path, timestamp, user or version in either `.expected`; both LF (`.gitattributes` pins `ops/tests/*` already)
- [ ] both goldens proven RED once (sabotage the fixture skill's byte count / delete know-x mid-run, watch the red, restore) — read the sabotage diff before believing the green (calibration 2026-08-04); paste the two red lines into Notes
- [ ] the drill green in your worktree: `bash ops/polaris bg run t159 -- bash kit/ops/polaris doctor --selftest --only skills` + chunked `bg wait t159 --max 300`; paste the `selftest passed` line into Notes
- [ ] `ops/tests/api-kit.expected` = 694 lines (your one row added in order)
- [ ] `polaris verify` green

## Notes
- A NEW golden is invisible to `check` from a worktree (primary-anchored — it passes vacuously); your verify lines run the `.cmd` bodies directly, and the integrator runs `check --only skill-budget,skill-install` on the primary after you land, sabotages each red, restores green.
- `claude_home` reads `CLAUDE_CONFIG_DIR` — point it at a temp dir for the fast-tier `slim_scan` half; never let anything here touch the owner's `~/.claude`.
- The label list order matters to nothing but the count; append `skills` at the END so `--only surfaces` globs are unaffected. Recount the MAP's label tell in your map_delta (36).
- Per-lane scratch (`mktemp -d`), namespaced — never a shared bare path.
- selftest passed (subset: skills — 1 of 37 labeled drills; spine always runs) · 't159' green — rc 0 in 6m (spine + drill, with `verify` running beside it)
- profile: the drill alone is 106 s here — 23 CLI spawns at ~4 s each on this box (Windows process spawn, sys-bound: the same tax triage-lane pays) — over CONVENTIONS' ~44 s line and beside the last two drills (63 s / 79 s); the spawns ARE the sequence (gaps · propose ×2 · promote ×3 · budget · claim · 5 feat/* refusals · prune ×6 · restore ×3) and each asserts a refusal or a byte, so nothing was trimmed; the per-drill line is the Planner's to re-measure
- fast tier: 259 checks in 58 s (was 229 — the skills section adds 30)
- red once (sabotage: fx-small `pad 271` → `pad 270`, diff read first): skill-budget — `< shelf: 1126 B of 1600 (2 tier-1 skill(s)) · tier 0: 1 (0 B injected)` vs `> shelf: 1127 B …`, then `< fx-small  tier 1 · 299 B …`; .cmd restored byte-identical
- red once (sabotage: `rm -rf know-x` right after install.sh, diff read first): skill-install — `< know-x CHANGED BY install` vs `> know-x byte-identical after install`, then `< KNOW-X DIR GONE`; .cmd restored byte-identical
- measured: `gaps` folds src/other/ (1/80) into src/, which yields to the kept src/search/ (6/80) — ONE candidate; the drill pins the § 3 line verbatim: `src/search/  6/80 tasks · 0 kickbacks · skill: none`
- measured: after the demote at 41 done events `prune` says keep — the one hit is inside 2W, which is all history until 80 done events exist — and archive comes due only once 80 done events post-date the hit, so the drill writes 41, then 40 more; `--apply` keeps rc 1 whenever a verdict was due
- twin: plans/self-skills.md records `probe: rules-paths-fires-on-read: yes` → the drill asserts it present after `propose --write`, moved to .polaris/skills-archived/search.rule.md by archive, and back byte-identical by restore on BOTH paths (archive, git history); an installed repo has no probe file, so the drill defaults to the shipped answer
- found: restore-from-git is a `git checkout`, so on an autocrlf box (this one) the twin comes back CRLF — git's checkout-time conversion, never the kit's bytes; the drill fixture sets core.autocrlf false. Planner: `git show sha^:path > file` would make restore EOL-independent
- found: slim_scan's corpus is awk's NR==FNR first file, so an EMPTY corpus swallows the first definition — unreachable in a repo (CLAUDE.md and the roles always exist); the fast fixture points CONV at the tier's cfg fixture so the corpus is non-empty
- found: § 9's golden arithmetic cannot reach the shelf refusal — three fixtures top out at 1127 B and a ≤ 320-B candidate cannot cross 1600 from there (the per-skill cap fires first); the golden adds fx-wide (200 B, tier 1) to lift the shelf to 1327 before fx-ready (300 B) is refused — names from the contract, numbers from the running code
- goldens: skill-budget = 12 CLI spawns ≈ 54 s here, skill-install ≈ 26 s (install.sh --quiet · the uninstall preview · uninstall --yes); both hermetic, POLARIS_AWAKE_HOME pointed at the fixture so nothing registers in ~/.claude; the per-skill cap (400 B > 320), the shelf crossing, the over-budget `budget` rc 1 + its demotion candidate, and demote as the free reverse are all pinned there too
