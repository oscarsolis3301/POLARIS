---
id: T-140
title: "The scaffold engine — lib/surfaces.sh proposes a repo's test map from its own layout, and the entry learns surfaces --scaffold and interview"
type: feature
scope: surfaces
plan: spend-less
points: 5
value: 13
time_criticality: 13
risk_opportunity: 8
wsjf: 6.8
risk: normal
model:
status: review
owner: Owner@DESKTOP
branch: feat/T-140
files_owned:
  - kit/ops/lib/surfaces.sh
  - kit/ops/polaris
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/test-surfaces.md
  - ops/contracts/first-run.md
  - ops/contracts/module-layout.md
  - kit/ops/lib/core.sh
  - kit/ops/lib/observe.sh
depends_on: []
contract: ops/contracts/test-surfaces.md
verify:
  - bash -n kit/ops/lib/surfaces.sh && bash -n kit/ops/polaris
  - test "$(wc -l < kit/ops/lib/surfaces.sh)" -le 350
  - grep -q '_mods="core ownership workspace surfaces builder integrate knowledge search observe admin bg awake handover' kit/ops/polaris && grep -q '_match|_rules|_guard) _mods="core ownership" ;;' kit/ops/polaris
  - test "$(grep -c '^[a-z_][a-z_]*() *{' kit/ops/lib/surfaces.sh)" = 3
  - bash -c 'POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; . kit/ops/lib/ownership.sh; . kit/ops/lib/surfaces.sh; for f in surfaces_runner surfaces_pairs surfaces_proposal; do type "$f" >/dev/null 2>&1 || exit 1; done'
  - bash -c 'd=$(mktemp -d); printf "pytest.ini\nsrc/api/x.py\ntests/api/test_x.py\n" > "$d/ls"; : > "$d/pytest.ini"; POLARIS_TAB="$(printf "\t")"; t="$POLARIS_TAB"; . kit/ops/lib/core.sh; . kit/ops/lib/ownership.sh; . kit/ops/lib/surfaces.sh; PRIMARY="$d"; test "$(surfaces_runner "$d/ls")" = "pytest${t}pytest {tests}" && surfaces_proposal "$d/ls" | grep -q "^ROW${t}src/api/${t}tests/api/${t}pytest tests/api/${t}api tests$"; r=$?; rm -rf "$d"; exit $r'
  - bash -c 'd=$(mktemp -d); printf "Makefile\nbin/tool.sh\n" > "$d/ls"; : > "$d/Makefile"; POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; . kit/ops/lib/ownership.sh; . kit/ops/lib/surfaces.sh; PRIMARY="$d"; ! surfaces_runner "$d/ls" >/dev/null && surfaces_proposal "$d/ls" | head -1 | grep -q "^NORUNNER.*seen: Makefile$"; r=$?; rm -rf "$d"; exit $r'
  - bash kit/ops/polaris help | grep -q '^  surfaces \[--scaffold \[--apply\]\]$' && bash kit/ops/polaris help | grep -q '^  interview \[--pending | --set <key>=<value> …\]$'
  - grep -q 'interview)  shift; cmd_interview "$@";;' kit/ops/polaris && grep -q 'surfaces)   shift 2>/dev/null || true; cmd_surfaces "$@";;' kit/ops/polaris
  - bash -c "$(cat ops/tests/startup-budget.cmd)" | diff - ops/tests/startup-budget.expected
  - bash -c "$(cat ops/tests/cli-help-parity.cmd)" | diff - ops/tests/cli-help-parity.expected
  - grep -q 'surfaces.sh.fn.surfaces_runner$' ops/tests/api-kit.expected && grep -q 'surfaces.sh.fn.surfaces_pairs$' ops/tests/api-kit.expected && grep -q 'surfaces.sh.fn.surfaces_proposal$' ops/tests/api-kit.expected && grep -q 'bootstrap.py.fn.arm_file$' ops/tests/api-kit.expected && grep -q 'core.sh.fn.board_pull$' ops/tests/api-kit.expected
  - POLARIS_ROOT=$PWD python kit/ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | grep -vE 'bootstrap.py.fn.arm_file$|core.sh.fn.board_pull$' | diff - <(grep -vE 'bootstrap.py.fn.arm_file$|core.sh.fn.board_pull$' ops/tests/api-kit.expected)
  - bash kit/ops/polaris doctor --fast | tail -1 | grep -q 'fast tier passed'
map_delta: "kit/ops/lib/surfaces.sh (NEW, loader slot between workspace and builder): the scaffold engine — surfaces_runner (pytest · jest · vitest · go, grep-only), surfaces_pairs (name-based pairing, ambiguity/breadth/ancestor filters), surfaces_proposal (RUNNER/ROW/SKIP as data); entry gains surfaces [--scaffold [--apply]] and interview usage + dispatch"
---
## Why
Sprint A made `qa` and `land --express` able to run only the tests a change can break — and, by
design, switched nothing on: `test_select:` unset plus an empty `ops/SURFACES.tsv` is byte-identical
to 6.3. `update --auto` therefore delivers 6.4.0 to every installed repo and makes none of them
faster. Somebody has to write the rows, and in a repo nobody is watching that somebody is POLARIS.
This task is the engine that proposes them: given the repo's manifests and its tracked-file list, it
names the test runner it can PROVE takes path arguments (pytest, jest, vitest — and go, whose rows
carry complete package commands), pairs test dirs to source dirs only where the stack's own
convention implies the pairing (`tests/api/` ↔ exactly one `api/` dir; `tests/test_util.py` ↔
exactly one `util.py`; `src/foo/__tests__/` ↔ `src/foo/`; a dir with `*_test.go`), and refuses to
guess: two candidate dirs is a SKIP that says so, a surface over 200 paths is a SKIP, a narrower
pairing under an emitted one is folded. Mis-mapping is the unsafe failure (a row that skips real
coverage while reporting green), so the whole engine leans toward emitting three honest rows and
admitting the rest. It is pure — no git, no network, no map read — so the fast tier proves it in
milliseconds and the golden pins every byte. Contract § 13 is the spec: three functions, exactly.

You are also this wave's single owner of `kit/ops/polaris` and `ops/tests/api-kit.expected`: add
`surfaces` to the loader (§ 13, module-layout v6), write the `surfaces [--scaffold [--apply]]` usage
entry (§ 14 replaces the v1 lines verbatim) and the `interview` entry + dispatch (first-run.md § 2,
verbatim — `cmd_interview` lands in W2; naming an undefined function until then is accepted), and
write the api-kit rows for T-143's `arm_file` and T-148's `board_pull` from their pinned names (§ 18),
never from a diff you cannot see.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/ops/lib/surfaces.sh` exists, ≤ 350 lines, exactly three top-level functions (`surfaces_runner` · `surfaces_pairs` · `surfaces_proposal`), bash 3.2 clean, matching only via `match_one` with args
- [ ] `surfaces_runner` answers the § 13 table in the § 13 order (node by grep on package.json → pytest by its five tells → go by go.mod); everything else rc 1
- [ ] `surfaces_pairs` emits the per-stack rows and the three shared filters in order (ambiguity → breadth → ancestor), sorted `LC_ALL=C`, SKIP lines as data; `surfaces_proposal` adds `already mapped` and `tests glob covers its own surface`, and prints `NORUNNER … seen: <manifests>` as its only line when nothing scopes
- [ ] contract § 17 case 1 by hand in a scratch repo (`scratchpad/T-140/…`): FOUR rows, TWO skips, byte-exact per the contract — the golden itself is T-142's, but its case 1 is your engine's spec
- [ ] loader: `surfaces` between `workspace` and `builder`; the `_match|_rules|_guard` list untouched; `startup-budget` green
- [ ] usage: the § 14 `surfaces` block replaces v1's, the first-run.md § 2 `interview` block sits directly after `adopt`; dispatch lines exactly as pinned; `cli-help-parity` still 10; `bash kit/ops/polaris help | diff - ops/tests/cli-help.expected` shows the v1 hunks + the two new ones and nothing else
- [ ] `api-kit.expected`: your three rows + `arm_file` + `board_pull` added by content diff (§ 18); no other hunk
- [ ] `doctor --fast` green; the `surfaces` and `qa` drills green at the wave gate (`bg run test`)

## Notes
- Scratch paths under `scratchpad/T-140/…` only; never run `doctor --selftest` from the primary while a sibling lane's suite runs (selftest mid-edit trap) — run it in your worktree via `bg run test` + chunked `bg wait test --max 300`. A subagent never ends its turn with a job still running.
- The verify api-kit line deliberately excludes `arm_file` and `board_pull` from BOTH sides: their files land in parallel lanes this wave and your worktree cannot index them (CONVENTIONS § Planner calibration 2026-09-14). The strict diff is the wave gate's.
- `surfaces_runner` reads manifests under `$PRIMARY` and takes the tracked list as a FILE — never call git inside the engine; the fast tier hands it a hand-written list.
- Bash ≥ 3.2: no `case` inside `$(...)`, no mapfile, no assoc arrays. Sort with `LC_ALL=C sort` so the golden is byte-stable across platforms.
- Trust rc and file state, never a printed line alone (T-089).
- assumed: a test file under `<dir>/__tests__/` is rule (a)'s pairing, not a co-located one — so § 17 case 2 yields exactly its two pinned rows and no narrower skip (default-safe)
- assumed: the ANCESTOR fold also requires the survivor's cmd to run the folded tests dir (its tests glob up to the first `*` is a prefix) — the literal surface-only fold would silently drop `src/api/`↔`src/api/tests/` under `src/api/`↔`tests/api/`, D6's failure; such pairs stay as two rows and qa runs both (default-safe)
- found: an idempotent re-run over § 17 case 1 reports 6 skips (4 already mapped + the ambiguity + the ancestor line), not the 4 the contract counts — `surfaces_pairs` is map-agnostic by contract, so its skips stay
- found: `scratchpad/T-140/harness.sh` proves § 17 cases 1-5 plus two mixed layouts byte-identical under `gawk --posix` (the bash-3.2/BSD proxy); the runner reads jest from a devDependencies key when the test script names nothing
