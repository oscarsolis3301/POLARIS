---
id: T-138
title: "Prove it — the surfaces drill and the fast-tier sections"
type: feature
scope: selftest
plan: spend-less
points: 5
value: 8
time_criticality: 5
risk_opportunity: 8
wsjf: 4.2
risk: normal
model:
status: review
owner: Owner@DESKTOP
branch: feat/T-138
files_owned:
  - kit/ops/lib/selftest/policy.sh
  - kit/ops/lib/selftest/spine.sh
  - kit/ops/lib/selftest/fast.sh
approved:
context_files:
  - ops/contracts/test-surfaces.md
  - ops/contracts/fast-tier.md
  - kit/ops/lib/selftest/history.sh
  - kit/ops/lib/core.sh
  - kit/ops/lib/ownership.sh
depends_on: [T-135, T-136, T-137]
contract: ops/contracts/test-surfaces.md
verify:
  - bash -n kit/ops/lib/selftest/policy.sh && bash -n kit/ops/lib/selftest/spine.sh && bash -n kit/ops/lib/selftest/fast.sh
  - grep -q '^drill_surfaces()' kit/ops/lib/selftest/policy.sh
  - grep -q "autoupdate surfaces'" kit/ops/lib/selftest/spine.sh && grep -q 'drill_on surfaces' kit/ops/lib/selftest/spine.sh
  - bash kit/ops/polaris doctor --selftest --only nonsense-label 2>&1 | grep -q 'surfaces'
  - bash kit/ops/polaris doctor --fast | grep -c 'fast: surfaces-tsv\|fast: surfaces-match\|fast: surfaces-select\|fast: surfaces-item\|fast: stamp-scope' | grep -qx 5
  - bash kit/ops/polaris doctor --fast | tail -1 | sed 's/.*passed — \([0-9]*\) checks.*/\1/' | awk '$1 >= 110 {ok=1} END {exit !ok}'
map_delta: "selftest gains the surfaces drill (label surfaces: gate · exemption · selection · stamp scope · health · drift) and five fast-tier sections (surfaces-tsv/match/select/item, stamp-scope)"
---
## Why
Three waves of code are only as good as the proof that they stay honest. This task writes it at both
tiers: five in-process fast sections (seconds, every change) over the pure functions — TSV parsing,
glob matching, selection algebra, the item grammar, stamp scope — and ONE end-to-end drill,
`surfaces`, in a throwaway repo that walks the whole path: a task's `surface:` row written by `done`,
the stale-tests gate refusing and then passing, the comment-only exemption, the human's `approve`
exemption, `qa` running only the mapped command and stamping `scoped`, the whole suite on an unmapped
change, `--full`, a `-` template row, and the health check refusing a self-covering row through
`drift --strict`. Contract § 10 lists the minimum assertions; every one checks rc, file state or qa's
own report line — never qa's exit code and never a printed refusal alone (T-089, T-131). Keep the
drill hermetic and under the ~44s budget, and record its measured seconds in Notes.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `drill_surfaces` in policy.sh with every § 10 assertion (1–7); label `surfaces` appended LAST in `SELFTEST_LABELS`; gate directly after the `rules` gate; hermetic restore of SURFACES.tsv (header-only), CONVENTIONS keys, tasks, branches, stamp; a sabotage in a throwaway copy (e.g. skip the tests change) reds the drill — record the sabotage diff in Notes
- [ ] fast.sh sections `surfaces-tsv` · `surfaces-match` · `surfaces-select` · `surfaces-item` · `stamp-scope`, ≥4 asserts each, fixtures under `$FT_TMP`, globals overridden inside the section subshell only; no new top-level fn in fast.sh
- [ ] `bash kit/ops/polaris doctor --selftest --only surfaces` green (600000 ms or `bg run`); full `bg run test` green at the wave gate; measured drill seconds in Notes
- [ ] no new top-level fn beyond `drill_surfaces` (T-139 writes its api-kit row); no heading; bash 3.2 clean (no `case` inside `$(...)`)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Scratch paths under `scratchpad/T-138/…` only; never edit kit/ops/lib/*.sh while a selftest runs from the primary (mid-edit trap fabricates a moving failure point); a sabotage green counts only after reading the sabotage diff.
- measured 2026-09-14 (Windows/Git Bash): `--only surfaces` 121s wall vs the bare spine (`--only fmlist`) 58s → the drill is ~63s serial; phase profile from timestamp echoes in a throwaway clone: claim→verify→handoff→land→seal→done 18.6s · stale-tests gate 10.0s · approve exemption 6.5s · the five qa runs 20.9s (each pays doctor's ~3s env check) · health + drift 6.5s · cleanup 0.6s; every qa run proves a distinct § 10.6 verdict, so ~60s is this box's floor for the contract's minimum set — over the ~44s line by the contract's own shape, not by waste; the CONVENTIONS per-drill comment is the Integrator/EVOLVE's to re-measure
- sabotage 1 (throwaway clone, kit/ops/lib/ownership.sh check_freshness): `[ -n "$s" ] && [ -z "$t" ] || continue` → `continue` (the gate never denies) → the drill reds at SURFACES STALE FAIL; sabotage 2 (policy.sh, the acceptance box's own example): `echo more >> tests/sf/a_test.txt && git commit -qam 'sf: tests too'` → `git commit -q --allow-empty -m 'sf: tests too (skipped)'` → reds at SURFACES FRESH FAIL; both diffs read before either red counted
- the fixtures carry NO risk: key on purpose — self_land fails closed on an unclassified task, so handoff stays classic and the drill drives land → seal → done itself (the spine's T-W shape); a risk: normal fixture would self-land inside handoff and hide the docs(surfaces) commit assertion behind the tail
- hermetic shape: the sealed wave stays (as the spine's own and drill_selfland); the probe commits go by `git reset --hard` to the post-done sha, ops/SURFACES.tsv comes back from the pre-drill commit as 'surfaces drill cleanup', CONVENTIONS restored byte-exactly; `--only rules,surfaces,drift,qa,finish` green in 204s — the labels on either side of the gate see the fixture as before
- fast tier 89 → 150 checks in 6s: `[T-140]` inside a `${var#pattern}` expansion is a glob bracket set — quote the literal (`${ft_out#*"…[T-140]"}`); the memo assert primes with a bare `ft_assert … surfaces_lines` in the section shell (T-134's lesson holds — a `$(...)` primes only its own subshell); the stamp-scope default-path case borrows the quiescent fixture's bgrc/.polaris/ so the tier pays no extra mkdir fork
- `bash -x` with a timestamped PS4 perturbs the spine's 8-way claim race (2 winners) — profile a drill with `date` echoes in a clone, never with the tracer
- api-kit calibration (CONVENTIONS 2026-09-14): `find --api` shows exactly ONE new row, `kit/ops/lib/selftest/policy.sh fn drill_surfaces` — the row T-139 pins; excluded from both sides the diff against ops/tests/api-kit.expected is empty, so no unexpected hunk rides this lane
