---
id: T-075
title: "Flip the autonomy defaults — unset composes trusted; doctor always says so"
type: feature
scope: autonomy
epic: autonomy-by-default
plan: autonomy-by-default
points: 3
value: 13
time_criticality: 8
risk_opportunity: 8
wsjf: 9.7
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-075
files_owned:
  - kit/ops/lib/observe.sh
context_files:
  - ops/contracts/hands-free-knobs.md
  - kit/ops/lib/selftest/remote.sh
  - kit/ops/lib/core.sh
depends_on: []
contract: ops/contracts/hands-free-knobs.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - bash kit/ops/polaris doctor | grep -q 'plan_gate=auto'
  - bash kit/ops/polaris doctor | grep -q 'opt out. autonomy. standard'
  - bash ops/polaris check --only triage-lane
  - bash kit/ops/polaris doctor --selftest --only notify
map_delta: "BREAKING 6.0: autonomy knobs default to auto/default-safe/auto-reversible when unset; `autonomy: standard` is the one-line opt-out; doctor prints the effective composition unconditionally (hands-free-knobs.md v2)"
---
## Why
The hands-free machinery shipped in 5.13.0 and stayed OFF in exactly the repos that never learned
it existed — unset knobs composed to confirm/ask/confirm, and doctor printed the composition ONLY
when a knob was already set (observe.sh:265), so the repos most needing the message were guaranteed
not to get it. This is the heart of 6.0.0: invert the three fallbacks in kit CODE so `update`
delivers autonomy through the mechanism it already refreshes, and delete the silence. Nothing here
writes into anyone's CONVENTIONS.md, and no hard gate softens.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] the three `if [ -z … ]` fallbacks (observe.sh:273/277/281) invert: unset resolves to
      `auto` / `default-safe` / `auto-reversible`; `autonomy: standard` restores
      `confirm` / `ask` / `confirm`; `autonomy: trusted` stays legal and equals the new default
- [ ] precedence unchanged: an explicit individual knob beats `autonomy:` in BOTH directions —
      the drill assertions at remote.sh:139-141 stay green untouched
- [ ] fail-safe unknowns per v2: unknown `autonomy:` → warn once, behave as `standard`; unknown
      individual knob → warn once, behave as that knob's STANDARD value (a typo must never grant
      autonomy)
- [ ] the `if [ -n "$a$pg$bq$ea$dr$ds" ]` guard at observe.sh:265 is deleted — the composition
      line prints UNCONDITIONALLY when CONVENTIONS.md exists, in the v2 pinned shape ending
      `(explicit > autonomy > default · opt out: autonomy: standard)`, with `autonomy: default`
      shown when the key is unset
- [ ] the `drain:` note keeps its own `[ -n "$dr$ds" ]` condition (drain is never composed and
      stays silent when unset); the integration-knob checks below the block are untouched
- [ ] the stale comment block at observe.sh:258-261 ("Silence = every default = today's
      behavior… print the EFFECTIVE composition only when a knob is set") is rewritten to state
      the v2 rule — never left to rot
- [ ] surface-frozen (key-registry.md § 5): no new top-level fn — the flip lives inside
      cmd_doctor's existing block

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Hard gates are NOT in scope and must not move: risk:high approval, STOP-AND-ASK, RULES.tsv,
  ready gate, contract-before-code, green-before-review (hands-free-knobs.md:120-123).
- THIS repo sets `autonomy: trusted`, so the second verify grep passes before and after — the
  drill (`--only notify`) is what proves the flip against empty/standard fixtures once T-080
  lands; until then remote.sh's existing three assertions prove precedence held.
- `--only notify` FOREGROUND with an explicit ≥600000ms timeout (spine 144s + ~44s/drill), or
  log-and-poll for `selftest passed`. Run `bash kit/ops/polaris …` — the kit driver, never the
  installed one (label-count tell: kit registers 27 labels).
- bash 3.2: no `case` inside `$(...)`.
- The deleted knob guard is replaced by `if [ -f "$CONV" ]` wrapping the whole block, so the
  composition prints on every CONVENTIONS.md and stays silent in a repo where INIT never ran
  (which already gets its own "INIT has not run" warning two lines above).
- Unknown-knob warns now end "behaving as confirm/ask/confirm", not v1's "behaving as the
  default": v2 fails to the STANDARD value, so the v1 wording would name the wrong behavior.
  The v1 shape (one `⚠ <key>: '<v>' unknown (a | b) — behaving as <x>` line) is kept.
- assumed: an unknown `autonomy:` value prints as `standard` in the composition line — its
  EFFECTIVE value, following v1's precedent — rather than echoing the typo (default-safe).
- Verified against the contract's truth table before handoff: unset → auto/default-safe/
  auto-reversible · standard → confirm/ask/confirm · trusted = the default · explicit beats
  autonomy in BOTH directions · unknown pg/bq/ea under `trusted` still resolve to the standard
  value · drain silent when unset. remote.sh:139-141 green untouched.
