---
id: T-082
title: "Radically plain `standard` voice — new row, re-cut rules, Pre-send check, plain examples, plain-voice golden"
type: feature
scope: voice
epic: plain-voice
plan: plain-voice
points: 3
value: 8
time_criticality: 5
risk_opportunity: 2
wsjf: 5.0
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-082
files_owned:
  - kit/ops/PROTOCOL.md
  - kit/.claude/output-styles/polaris.md
  - ops/tests/plain-voice.cmd
  - ops/tests/plain-voice.expected
context_files:
  - ops/contracts/output-style.md
  - ops/tests/output-style-installed.cmd
  - ops/tests/adhd-skill-installed.cmd
  - ops/tests/api-kit.expected
  - ops/contracts/golden-eol.md
depends_on: []
contract: ops/contracts/output-style.md
verify:
  - bash ops/polaris check --only plain-voice
  - bash ops/polaris check --only output-style-installed
  - bash ops/polaris check --only adhd-skill-installed
map_delta:
---
## Why
With `voice: standard` ("plain, friendly English") the owner's summaries come back short but dense
and technical — a real close read "Wave 1 is sealed as sprint/10, the tree is clean, and waves 2–5
sit on the board with their dependencies satisfied", every noun jargon. Root causes are in the texts
themselves: the `standard` row bans only POLARIS jargon and its "unless you explain it in the same
breath" escape hatch licenses jargon; the 7 rules optimize volume, not simplicity; and the style's
own worked examples — the model's imitation target — say "Full suite is green on `main`". This task
replaces the words in BOTH copies (they are one artifact under contract invariant 6) and gives
invariant 6 its first mechanical pin. Every replacement text is byte-exact in
`ops/contracts/output-style.md` § v2 — write those bytes, do not compose your own.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `standard` row in `kit/ops/PROTOCOL.md` § VOICE (currently line 235) replaced with the § v2
      register-ban text as ONE physical table-row line; the same sentences, unwrapped, replace the
      opening paragraph of the style's § Voice
- [ ] zero occurrences of "in the same breath" in either file — the escape hatch is dead
- [ ] the 7 rules re-cut per § v2, byte-identical in both files, still EXACTLY 7 lines matching
      `^[0-9]\. \*\*` in the whole style file (ops/tests/output-style-installed.cmd counts them);
      no new numbered-bold list anywhere in the style file
- [ ] Pre-send check per § v2 added IDENTICALLY to both files: bold-led paragraph + dash bullets,
      under an EXISTING heading — zero new `##`/`###` headings, zero renames, in either file
      (ops/tests/api-kit.expected pins every kit heading and this task does NOT own it)
- [ ] both worked examples under the existing `## What a close reads like` heading replaced with
      the § v2 closes; heading byte-identical, `# 🎉 Complete!` literal kept
- [ ] `voice: technical` untouched — row and behavior
- [ ] new golden `ops/tests/plain-voice.cmd` + `.expected` with the SEVEN assertions specified in
      § v2 (bar sentence in both · escape hatch zero · Pre-send in both · examples-scoped jargon
      grep zero · rule lines diffed IDENTICAL, PROTOCOL side sed-scoped to § VOICE because § LONG
      COMMANDS has its own numbered-bold lines); both files LF per ops/contracts/golden-eol.md
- [ ] sabotage-proven: reintroduce one jargon word into an example and one drifted rule line →
      `check --only plain-voice` goes RED; revert → green. Record the sabotage runs in Notes
- [ ] `output-style-installed` and `adhd-skill-installed` goldens still green (pinned literals
      `^name: POLARIS$` · `^keep-coding-instructions: true$` · `🎉 Complete!` · `OUTPUT
      DISCIPLINE` all survive; the vendored SKILL.md is untouchable)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Wording only — no behavior, no mechanism, no installer change. If a § v2 text seems to conflict
  with a pinned literal, STOP and hand back; do not improvise around it.
- sabotage-proven: all 7 plain-voice assertions red individually in a scratch copy of the two files, green on restore — bar/PROTOCOL RED · bar/style RED · escape-hatch reintroduced RED · Pre-send/PROTOCOL RED · Pre-send/style RED · "branch" into the not-complete example RED · rule 2 reverted to its v1 wording in the style only RED.
- `polaris check` is PRIMARY-anchored (kit/ops/lib/observe.sh:1252 `cd "$PRIMARY"`), so a brand-new golden reports "no goldens matched" from a worktree and verify passes vacuously; every run above therefore emulated cmd_check exactly — run the .cmd from the tree root, byte-diff stdout against .expected. Real coverage starts the moment the Integrator lands.
- the contract's examples regex is `merged?`, so plural trade words slip through: "merges a real spreadsheet file" in an example stays GREEN, "branch" reds. Written verbatim as specified; widening it to `merge[ds]?|branch(es)?` is a one-line follow-up if the owner wants it.
- api-kit is red on this branch for an unrelated lane (`kit/ops/selftest-install.sh fn drill_hookmerge`); this diff adds, removes and renames zero `##`/`###` headings in either file.
- assumed: Pre-send check sits at the END of its existing section in both files (§ VOICE in PROTOCOL, `## Output discipline` in the style) — § v2 pins the bytes and "an existing heading" but not the slot (default-safe).
