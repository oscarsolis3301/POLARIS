---
id: T-126
title: "Prove auto-update the way the kit proves everything — a drill, plus the gates in the fast tier"
type: feature
scope: selftest
plan: feel-fast
points: 5
value: 8
time_criticality: 5
risk_opportunity: 8
wsjf: 4.2
risk: normal
model:
status: review
owner: Owner@DESKTOP
branch: feat/T-126
files_owned:
  - kit/ops/lib/selftest/remote.sh
  - kit/ops/lib/selftest/spine.sh
  - kit/ops/lib/selftest/fast.sh
approved:
  -
context_files:
  - ops/contracts/auto-update.md
  - ops/contracts/fast-tier.md
  - ops/contracts/selftest-sharding.md
  - kit/ops/lib/selftest/history.sh
  - kit/ops/lib/admin.sh
depends_on: [T-122, T-124]
contract: ops/contracts/auto-update.md
verify:
  - bash kit/ops/polaris doctor --fast | grep -q 'fast: quiescent'
  - bash kit/ops/polaris doctor --fast | grep -q 'fast: dirt'
  - bash kit/ops/polaris doctor --fast | tail -1 | grep -qE 'fast tier passed — [0-9]+ checks in [0-9]+s'
  - grep -q '^drill_autoupdate()' kit/ops/lib/selftest/remote.sh
  - grep -qE "SELFTEST_LABELS='.* handover autoupdate'" kit/ops/lib/selftest/spine.sh
  - grep -q 'autoupdate (' kit/ops/lib/selftest/spine.sh
  - test "$(grep -c '^[a-z_]*() *{' kit/ops/lib/selftest/fast.sh)" = 3
  - bash -n kit/ops/lib/selftest/remote.sh
  - bash -n kit/ops/lib/selftest/spine.sh
map_delta:
---
## Why
An update that applies itself is exactly the kind of thing that must be proven by a test, not by a
promise: the gates that keep it from firing mid-task (`board_quiescent`) and from stepping on
someone's edits (`update_dirt_overlaps_kit`) are pure functions, so they go into the new in-process
tier (T-122) where they are checked in seconds on every change. The end-to-end behavior — a real
`update --auto` against a `file://` channel and tarball, applying on a quiet board, refusing on a
busy one, obeying `auto_update: off`, asking on a major bump, the explicit `update` no longer dying
on app-only dirt, and `update --all` walking a registry — is a labeled drill in the selftest,
sharded and run in CI on three OSes like every other mechanic. Nothing here touches admin.sh: the
drill tests what T-124 shipped, and a red is a finding, never a weakened assertion.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `selftest_fast` (fast.sh) gains sections `quiescent` and `dirt` with every case pinned in ops/contracts/auto-update.md § Executable check, INSIDE the existing fn — the module keeps exactly three top-level fns; the whole tier still under 15s on this machine (record the new number in Notes)
- [ ] `drill_autoupdate` (remote.sh) implements the contract's six numbered assertions, `--repo-only` on every apply, rc AND file state asserted (`ops/VERSION` value, `.polaris/update.log`, `git stash list`), never message presence alone; hermetic — `ops/VERSION`, CONVENTIONS and `src/a.txt` restored, no stash left, the fixture registry under the drill's own `POLARIS_AWAKE_HOME` temp dir
- [ ] spine.sh: `autoupdate` appended to `SELFTEST_LABELS` after `handover`; its `drill_on` gate placed after the handover drill's; the full-run pass line gains an `autoupdate (…)` clause; `--only autoupdate` stands alone on the bare spine
- [ ] `bash kit/ops/polaris doctor --selftest --only autoupdate` green, and `--parallel 3` green with the new label (acceptance: 600000 ms timeouts or `bg run` + chunked `bg wait`); a `--parallel 2 --only 'autoupdate,fmlist'` run green proves partition-invariance
- [ ] sabotage evidence in Notes: with `board_quiescent` forced to rc 0 in a throwaway kit copy, assertion (2) goes RED at the `ops/VERSION` check; with the dirt overlap forced, the fast `dirt` section reds
- [ ] surface-frozen: no new top-level fn beyond `drill_autoupdate` (T-125 writes its api-kit row); no heading

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- fast tier now 89 checks in 5s on this machine (was 54; budget 15s) — the quiescent fixture dirs are the tier's one `mkdir -p` (a directory cannot be made with builtins); every file is `: >`/printf, every stdin a here-string
- Windows: curl is a native binary and cannot open `file:///tmp/…` — the drill spells the channel/tarball as `file:///$(cygpath -m "$T")/…` when cygpath exists, `file://$T` verbatim on POSIX
- fixture = a REAL install (install.sh's live-board path) pinned to 0.0.1 and committed: an apply then changes only tracked files, so `git reset --hard <pin>` is the whole reset; leaving the installed kit untracked would read as kit dirt (`?? ops/polaris`) and block (5)/(6) itself
- `.polaris/update-cache` is removed before every `--auto` call — the daily throttle would otherwise answer the previous channel value (the MAJOR case would still read 0.0.2)
- park announces only the stash NAME (`parked as polaris/park-…`); the why rides `git stash list` — (5b) asserts both
- beyond the six: (2b) `--auto` with kit dirt → `skipped: uncommitted changes inside the kit's paths`, no stash · (5b) explicit `update` with kit dirt → parked (one stash) AND applied — the two halves of the dirt rule
- sabotage (throwaway kit copy): `board_quiescent` forced rc 0 → `AUTOUPDATE (2) APPLY FAIL (a busy board must leave ops/VERSION at 0.0.1)`, rc 1 · `update_dirt_overlaps_kit` forced rc 0 → 10 `⛔ FAST DIRT FAIL` lines, tier rc 1
- `--only autoupdate` on the bare spine: green in 3m44s · `--parallel 2 --only autoupdate,fmlist`: both shards green in 6m (partition-invariance)
- api-kit golden: the `drill_autoupdate` row is T-125's to write (auto-update.md § api-kit rows) — `check` reds on api-kit until it lands
- hermetic teardown lesson (shard 2 of --parallel 3 red once): after `git reset --hard` (autocrlf writes CRLF) a byte copy of CONVENTIONS.md back over it reads ` M` in `git status` with IDENTICAL blob hashes, mode and bytes (porcelain=2 `.M`, i/lf w/lf, filemode false) — restoring through `git checkout HEAD --` is the one form that leaves status clean (the handover drill's lesson, now the autoupdate drill's too); a byte copy only when the file was dirty or untracked at entry
- the fixture commit stages ONLY paths absent from the entry status snapshot — a blanket `git add ops` would sweep a neighbour's untracked file into the pin commit and the teardown's reset would delete it
- final proof on this machine: `--only autoupdate` 3m44s · `--parallel 2 --only autoupdate,fmlist` 3m (both shards green) · `--parallel 3` full suite 12m, all three shards green with autoupdate riding shard 2
