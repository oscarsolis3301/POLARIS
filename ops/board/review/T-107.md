---
id: T-107
title: "Role prose for 6.2.0 — see your work, the port rule, Loop mode becomes handover, Invariant 5 per context, an approved plan is a kickoff"
type: chore
scope: roles
epic: cant-eat-itself
plan: cant-eat-itself
points: 3
value: 8
time_criticality: 3
risk_opportunity: 2
wsjf: 4.3
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-107
files_owned:
  - kit/ops/roles/BUILDER.md
  - kit/ops/roles/SOLO.md
  - kit/ops/roles/PLANNER.md
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/roles/INTEGRATOR.md
  - kit/ops/roles/INIT.md
  - kit/CLAUDE.md
  - kit/.claude/skills/polaris/SKILL.md
approved:
context_files:
  - ops/contracts/role-handover.md
  - ops/contracts/visual-check.md
  - ops/contracts/permission-rules.md
  - ops/contracts/output-style.md
  - kit/ops/PROTOCOL.md
depends_on:
  - T-095
  - T-098
  - T-109
contract: ops/contracts/role-handover.md
verify:
  - diff <(grep '^#' kit/ops/roles/BUILDER.md | tr -d '\r') <(git show main:kit/ops/roles/BUILDER.md | grep '^#' | tr -d '\r')
  - diff <(grep '^#' kit/ops/roles/SOLO.md | tr -d '\r') <(git show main:kit/ops/roles/SOLO.md | grep '^#' | tr -d '\r')
  - diff <(grep '^#' kit/ops/roles/PLANNER.md | tr -d '\r') <(git show main:kit/ops/roles/PLANNER.md | grep '^#' | tr -d '\r')
  - diff <(grep '^#' kit/ops/roles/CONDUCTOR.md | tr -d '\r') <(git show main:kit/ops/roles/CONDUCTOR.md | grep '^#' | tr -d '\r')
  - diff <(grep '^#' kit/ops/roles/INTEGRATOR.md | tr -d '\r') <(git show main:kit/ops/roles/INTEGRATOR.md | grep '^#' | tr -d '\r')
  - diff <(grep '^#' kit/ops/roles/INIT.md | tr -d '\r') <(git show main:kit/ops/roles/INIT.md | grep '^#' | tr -d '\r')
  - diff <(grep '^#' kit/CLAUDE.md | tr -d '\r') <(git show main:kit/CLAUDE.md | grep '^#' | tr -d '\r')
  - diff <(grep '^#' kit/.claude/skills/polaris/SKILL.md | tr -d '\r') <(git show main:kit/.claude/skills/polaris/SKILL.md | grep '^#' | tr -d '\r')
  - grep -q '^\*\*4b\. See your work\.\*\*' kit/ops/roles/BUILDER.md
  - grep -q 'never reclaim a port by killing' kit/ops/roles/BUILDER.md
  - grep -q 'never reclaim a port by killing' kit/ops/roles/SOLO.md
  - grep -q 'no prompt: the kit' kit/ops/roles/BUILDER.md
  - grep -q 'no prompt: the kit' kit/ops/roles/SOLO.md
  - test "$(cat kit/ops/roles/BUILDER.md kit/ops/roles/SOLO.md | grep -c 'this file is your instruction to run it')" = 0
  - grep -q 'handover: off' kit/ops/roles/BUILDER.md
  - grep -q '^## Loop mode' kit/ops/roles/BUILDER.md
  - grep -q 'the capture, when `pack` printed one' kit/ops/roles/SOLO.md
  - grep -q '^7c\. \*\*A visual surface names its capture\.\*\*' kit/ops/roles/PLANNER.md
  - grep -q 'may continue as BUILDER' kit/ops/roles/PLANNER.md
  - grep -q 'Touching a visual: path? run the shot: line pack printed' kit/ops/roles/CONDUCTOR.md
  - grep -q '^\*\*Entered from an approved plan?\*\*' kit/ops/roles/CONDUCTOR.md
  - grep -q 'the anchor hook already re-read the board' kit/ops/roles/CONDUCTOR.md
  - grep -q '^\*\*Open the capture named in the handoff\*\*' kit/ops/roles/INTEGRATOR.md
  - grep -q 'bash ops/polaris next --do' kit/ops/roles/INTEGRATOR.md
  - grep -q 'on Windows write python, never python3' kit/ops/roles/INIT.md
  - grep -q '^shot: <cmd or omit>' kit/ops/roles/INIT.md
  - grep -q '^visual: <globs or omit>' kit/ops/roles/INIT.md
  - grep -q '^port_base: <number or omit>' kit/ops/roles/INIT.md
  - grep -q '^serve: <cmd or omit>' kit/ops/roles/INIT.md
  - grep -q 'One task per context, one role per context' kit/CLAUDE.md
  - grep -q 'An approved plan is a kickoff, not a report' kit/CLAUDE.md
  - grep -q 'a plan you wrote was just approved' kit/CLAUDE.md
  - grep -q 'a plan you wrote was just approved' kit/.claude/skills/polaris/SKILL.md
  - grep -q 'subagent never ends a run' kit/CLAUDE.md
  - grep -q '🎉 Complete!' kit/CLAUDE.md
  - test "$(grep -c 'One task per session, one role per session' kit/CLAUDE.md)" = 0
  - bash -c "$(cat ops/tests/output-style-installed.cmd)" | diff - ops/tests/output-style-installed.expected
map_delta:
---
## Why
Every mechanic this sprint ships needs one sentence in the role that uses it, or it is a feature
nobody follows. Bold paragraphs and list items ONLY — the api-kit golden records every `#` line
(fenced ones included), so the heading set of all eight files stays byte-identical to `main`.
BUILDER: `**4b. See your work.**` between §4 and §5 (run the printed `shot:` line, READ the image,
one `saw:` line, blank = failure, never build your own capture tool, `handoff` refuses without the
capture), the port sentence in §3, step 1b's "no prompt" reason becomes the kit's own rule (6.2.0),
`## Loop mode`'s body becomes the default under `handover: auto` (run `next` after every handoff and
follow it; `off` restores one task per session), and "you never end the run" narrows to
conductor-entered builders. SOLO: step 4 + "What you must NOT skip" + the port bullet + the close
follows `next`. PLANNER: 7c (a visual surface names its capture) + may continue as BUILDER at the
boundary. CONDUCTOR: the visual kickoff line, the dead-lane sentence, "compacted mid-run?" → the
anchor already re-read the board, panes loop via `next`, and **Entered from an approved plan?** —
the plan IS the brief. INTEGRATOR: open the capture before landing; §5 promote → `next --do`. INIT:
the DERIVE row for the four keys + four skeleton lines beside `runnable:` + the §2c allow-rule note.
`kit/CLAUDE.md`: Invariant 5 rewritten per context (verbatim from role-handover.md), the :45-47
bullet, the "approved plan" dispatch row + bullet; `SKILL.md`:5 + the step-1 routing row. Every
line is pinned verbatim in visual-check.md § role prose, role-handover.md § role prose / § WS8 and
permission-rules.md § role prose — copy them, do not paraphrase.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] every pinned line landed byte-exact in its named place (visual-check.md, role-handover.md, permission-rules.md); the `^#` line set of all eight files identical to `main`'s
- [ ] BUILDER `## Loop mode` heading kept, body rewritten; "You never end the run" paragraph narrowed to conductor-entered builders; `kit/CLAUDE.md` keeps `subagent never ends a run` and `🎉 Complete!` (`output-style-installed` golden green)
- [ ] `kit/CLAUDE.md` § ROLE DISPATCH table gains the approved-plan row (3 columns, the table still renders) and the bold bullet; Invariant 5 reads exactly the pinned text; the :45-47 bullet reworded per role-handover.md
- [ ] INIT skeleton lines carry trailing `#` comments only, in the `runnable:` style; the DERIVE row fits the table; §2c note added
- [ ] `kit/CLAUDE.md` byte budget respected: the file is injected into every subagent — the two additions total ≤ 6 lines
- [ ] `python kit/ops/pack.py --allow-dirty` green; `bash kit/ops/polaris doctor --selftest --only claudemd,hint` green (foreground, ≥600000 ms timeout)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: SURFACE-FROZEN — no new `#` line anywhere (a `###` or a `#` inside a fence counts), no fn, no KEYS row. T-104 owns `ops/tests/api-kit.expected` this wave (key-registry.md §7).
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-107` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- Working-copy `.md` files are CRLF on this Windows checkout — never `$`-anchor a grep on them; the `^#` diffs strip `\r` on both sides. `ops/roles/` is the RULES-guarded INSTALLED copy — you edit `kit/ops/roles/` only.
- Scratch dirs per-lane: scratchpad/T-107/.
