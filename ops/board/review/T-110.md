---
id: T-110
title: "handover-hook.sh — the Stop backstop, the compaction anchor and the prompt clock; kit settings entries; readonly-allow learns next"
type: feature
scope: hooks
epic: cant-eat-itself
plan: cant-eat-itself
points: 3
value: 5
time_criticality: 3
risk_opportunity: 3
wsjf: 3.7
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-110
files_owned:
  - kit/ops/hooks/handover-hook.sh
  - kit/ops/hooks/readonly-allow.sh
  - ops/tests/readonly-allow.cmd
  - ops/tests/readonly-allow.expected
  - kit/.claude/settings.json
approved:
context_files:
  - ops/contracts/role-handover.md
  - kit/ops/hooks/checkout-guard.sh
  - kit/ops/hooks/awake-hook.sh
  - kit/ops/lib/handover.sh
depends_on:
  - T-095
contract: ops/contracts/role-handover.md
verify:
  - bash -n kit/ops/hooks/handover-hook.sh
  - bash -n kit/ops/hooks/readonly-allow.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/hooks/handover-hook.sh)" = 12
  - grep -q '^hh_emit()' kit/ops/hooks/handover-hook.sh
  - grep -q '^hh_stop()' kit/ops/hooks/handover-hook.sh
  - grep -q '^hh_anchor()' kit/ops/hooks/handover-hook.sh
  - grep -q '^hh_prompt()' kit/ops/hooks/handover-hook.sh
  - test "$(wc -l < kit/ops/hooks/handover-hook.sh | tr -d ' ')" -le 220
  - test "$(grep -c 'You are a BUILDER (hop' kit/ops/hooks/handover-hook.sh)" = 1
  - grep -q 'You are the INTEGRATOR (hop' kit/ops/hooks/handover-hook.sh
  - grep -q 'Backlog work is unblocked' kit/ops/hooks/handover-hook.sh
  - grep -q "This run's board is done" kit/ops/hooks/handover-hook.sh
  - grep -q 'Your landing tail' kit/ops/hooks/handover-hook.sh
  - grep -q 'allow:consumed' kit/ops/hooks/handover-hook.sh
  - grep -q 'allow:subagent' kit/ops/hooks/handover-hook.sh
  - grep -q 'allow:question' kit/ops/hooks/handover-hook.sh
  - grep -q 'block:collect' kit/ops/hooks/handover-hook.sh
  - grep -q 'POLARIS_HANDOVER_NEXT' kit/ops/hooks/handover-hook.sh
  - grep -q 'POLARIS_HANDOVER_CLI' kit/ops/hooks/handover-hook.sh
  - test "$(printf '{"session_id":"nostate","cwd":"%s","stop_hook_active":false}' "$PWD" | bash kit/ops/hooks/handover-hook.sh --test stop)" = allow:no-state
  - D=$(mktemp -d) && mkdir -p "$D/ops/board" "$D/.polaris/handover/s1" && echo '1 done T-1' > "$D/.polaris/handover/s1/last-event" && test "$(printf '{"session_id":"s1","cwd":"%s","stop_hook_active":false}' "$D" | POLARIS_HANDOVER_NEXT=finish bash kit/ops/hooks/handover-hook.sh --test stop)" = block:finish && test "$(cat "$D/.polaris/handover/s1/hops" | tr -d ' \r')" = 1 && test "$(printf '{"session_id":"s1","cwd":"%s","stop_hook_active":false}' "$D" | POLARIS_HANDOVER_NEXT=finish bash kit/ops/hooks/handover-hook.sh --test stop)" = allow:consumed
  - D=$(mktemp -d) && mkdir -p "$D/ops/board" "$D/.polaris/handover/s2" && echo '1 handoff T-2' > "$D/.polaris/handover/s2/last-event" && printf 'handover: off\n' > "$D/ops/CONVENTIONS.md" && test "$(printf '{"session_id":"s2","cwd":"%s","stop_hook_active":false}' "$D" | bash kit/ops/hooks/handover-hook.sh --test stop)" = allow:off
  - D=$(mktemp -d) && mkdir -p "$D/ops/board" "$D/.polaris/handover/s3" && echo '1 done T-3' > "$D/.polaris/handover/s3/last-event" && test "$(printf '{"session_id":"s3","cwd":"%s","stop_hook_active":true}' "$D" | bash kit/ops/hooks/handover-hook.sh --test stop)" = allow:harness-cap
  - D=$(mktemp -d) && mkdir -p "$D/ops/board" "$D/.polaris/handover/s4" && test "$(printf '{"session_id":"s4","cwd":"%s"}' "$D" | bash kit/ops/hooks/handover-hook.sh prompt 2>&1 | wc -c | tr -d ' ')" = 0 && test -s "$D/.polaris/handover/s4/prompted-at"
  - python -c "import json;json.load(open('kit/.claude/settings.json'))"
  - grep -q 'handover-hook.sh\\" stop' kit/.claude/settings.json
  - grep -q 'handover-hook.sh\\" anchor' kit/.claude/settings.json
  - grep -q 'handover-hook.sh\\" prompt' kit/.claude/settings.json
  - grep -q '"compact|resume"' kit/.claude/settings.json
  - grep -q '"EnterWorktree"' kit/.claude/settings.json
  - test "$(bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris next')" = allow
  - test "$(bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris next --brief')" = allow
  - test "$(bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris next --do')" = ask
  - grep -q 'bash ops/polaris next --do' ops/tests/readonly-allow.cmd
  - bash -c "$(cat ops/tests/readonly-allow.cmd)" | diff - ops/tests/readonly-allow.expected
  - bash -c "$(cat ops/tests/perm-tools.cmd)" | diff - ops/tests/perm-tools.expected
  - bash -c "$(cat ops/tests/output-style-installed.cmd)" | diff - ops/tests/output-style-installed.expected
map_delta: "kit/ops/hooks/handover-hook.sh — Stop (block once per board-proven completion event with the next role's instruction), SessionStart compact|resume (re-anchor via next --brief), UserPromptSubmit (prompted-at clock); wired in kit/.claude/settings.json; readonly-allow auto-approves next and next --brief"
---
## Why
The loop lives in role prose (every role runs `polaris next` at its boundary); this hook is the
backstop for a model that stopped anyway, and the re-entry after a compaction. `stop` runs a ladder
of cheap gates — no state dir (ordinary Q&A pays ~150 ms and is allowed), `handover: off`, an event
that is not a completion, an event already hopped (string equality of `last-event` and
`hopped-event`: ONE event, ONE hop), a finished run, the harness cap, our own hop cap, a subagent's
event in the parent's dir (a conductor is never hopped into BUILDER), a session that stopped to ask
(`⛔` line or trailing `?`), a `ship-<ID>` job still running (block with "collect it") — and only then
asks `polaris next`, blocking with the pinned reason for `build`/`integrate`/`promote`/`finish` and
allowing `wait`/`stop`/`resume`. It NEVER writes the board: `promote` tells the model to run
`next --do` in its own turn. `anchor` prints `next --brief` after a compaction or resume when the
state dir exists; `prompt` writes `prompted-at` with zero stdout (UserPromptSubmit stdout enters the
model's context). The three entries land in `kit/.claude/settings.json` (the same file T-095 gave
the seven tool names in W1); `readonly-allow.sh` auto-approves `next` and `next --brief` (reads)
and keeps asking for `--do` (a board write), with two golden lines. Every rung word, reason template
and file is pinned in role-handover.md; T-111 goldens the ladder in W3.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] exactly the 12 pinned fns; subcommands `stop|anchor|prompt|--test [stop|anchor|prompt]`; ≤220 lines; bash 3.2; reads `session_id` `transcript_path` `cwd` `stop_hook_active` from stdin JSON (`hh_jstr`, the checkout-guard reader); primary per `hh_primary`'s pinned order (cwd segment · cwd with `ops/board` · `$CLAUDE_PROJECT_DIR` · rev-parse); `hh_cfg` reads CONVENTIONS with `sed`, never `ops/polaris`, on the allow path
- [ ] the ladder in the pinned order with the pinned `--test` words; a block writes `hops` (+1) and `hopped-event` BEFORE emitting; `--test` differs from live ONLY in output shape; `POLARIS_HANDOVER_NEXT` replaces the `next` call; `POLARIS_HANDOVER_CLI` replaces `<primary>/ops/polaris`
- [ ] `hh_emit` is the ONE emitter: block = `{"decision":"block","reason":"…"}` on stdout, exit 0 — or the shape the harness docs specify at build (verify and record in Notes; exit 2 + stderr as the documented fallback); allow = no output, exit 0; the five reason templates byte-exact with N/<cap>/IDs/PRIMARY substituted
- [ ] `anchor`: prints `bash <primary>/ops/polaris next --brief` output only when the state dir exists (else nothing); `prompt`: builtins only, writes `prompted-at`, zero stdout, rc 0
- [ ] `kit/.claude/settings.json`: Stop (30 s) · SessionStart matcher `compact|resume` (10 s) · UserPromptSubmit (5 s) entries with `bash "$CLAUDE_PROJECT_DIR/ops/hooks/handover-hook.sh" <sub>`; the seven tool names and every existing entry untouched; `perm-tools` + `output-style-installed` goldens green
- [ ] `readonly-allow.sh` `polaris_ok`: `next` bare and `--brief` allow, `--do` asks; golden +2 lines (`allow  bash ops/polaris next` · `ask  bash ops/polaris next --do`); every existing line byte-identical
- [ ] the four build-time verifications recorded in Notes: `stop_hook_active`/cap semantics, the Stop block JSON shape, subagents-dir mtime freshness, the last-assistant-text shape; a fact that does not hold ⇒ that rung fails OPEN (allow) and Notes say so
- [ ] never mutates the board; never forks `ops/polaris` before the `block:collect` rung

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: exactly the 12 pinned `hh_*` fns — T-101 owns `ops/tests/api-kit.expected` this wave and writes those rows from role-handover.md; `readonly-allow.sh` gains no fn; settings.json is not indexed.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-110` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name. The INSTALLED `ops/polaris` has no `next` — the real `polaris next` arrives with T-109 in THIS SAME WAVE (board amendment 2026-09-02: under batch integration `done` only runs at the seal, so a same-wave dep could never be promoted mid-wave), so `lib/handover.sh` and the `next` dispatch may not exist in your worktree — only the stubbed `--test` ladder (`POLARIS_HANDOVER_NEXT`) is provable at handoff; build the real-`next` rung against role-handover.md's table, and the one un-stubbed real-`next` golden case is T-111's (W3). If T-109 and T-101 have landed by the time you claim, `POLARIS_HANDOVER_CLI="$PWD/kit/ops/polaris"` proves it live — optional, never required.
- The hook you ship is NOT active in this repo until dogfood (it lives in `kit/.claude/settings.json`, not `.claude/settings.json`) — no Stop hook fires on your own session this sprint.
- `hh_question` reads the transcript's last assistant text; `hh_subagent` compares `<transcript dir>/<sid>/subagents/*.jsonl` mtimes to the event's ts — verify both shapes against a real transcript on this machine before pinning.
- Scratch dirs per-lane: scratchpad/T-110/. bash 3.2 (CI runs macOS /bin/bash). Windows: `python`, never `python3`.
