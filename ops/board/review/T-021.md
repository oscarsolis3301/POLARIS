---
id: T-021
title: quiet board lifecycle — upgrade migration, fresh-clone materialization, uninstall, primary-anchored paths
type: feature
scope: cli
points: 5
value: 8
time_criticality: 8
risk_opportunity: 5
wsjf: 4.2
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-021
plan: one-pr-clean-graph
files_owned:
  - kit/ops/polaris
context_files:
  - ops/contracts/quiet-board.md
depends_on: [T-020]
contract: ops/contracts/quiet-board.md
verify:
  - grep -q "board moves to polaris/board" kit/ops/polaris
  - bash kit/ops/polaris doctor --selftest
map_delta: upgrade migrates a 5.13 board to polaris/board; doctor/resume materialize a fresh clone's board from the ref; uninstall deletes the branch (5.14.0-unreleased)
---
## Why
T-020 re-targets board commits for NEW installs; this task makes the change survive the real
world: existing 5.13 boards must migrate (once, idempotently), a fresh clone must be able to
rebuild ops/board/ + SPRINT.md from origin/polaris/board, uninstall must take the branch with it,
and a Builder — whose worktree no longer contains ops/board — must be told the task file's real
(primary-anchored) path by claim/resume. All semantics are pinned in ops/contracts/quiet-board.md.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] cmd_upgrade gains the idempotent 5.13→5.14 migration per contract: seed polaris/board (orphan) from the current moved-set state · `git rm -r --cached` the set on base · append the .gitignore entries · ONE final base commit `chore(board): board moves to polaris/board`. Re-run = no-op; no history rewritten; runs only when polaris/board is absent AND the set is tracked.
- [ ] doctor: ops/board/ missing on disk + polaris/board present (local, else origin — local ref created from origin's) → materializes the moved set into the working tree per contract (plumbing, no branch switch) and says what it did.
- [ ] resume performs the same materialization before task lookup, so a fresh clone can resume an active task.
- [ ] uninstall deletes refs/heads/polaris/board (and pushes the deletion when a remote exists); the branch is named in the pre-confirm summary.
- [ ] claim and resume print the task file as a primary-anchored absolute path (under $PRIMARY); the "read:" hint names that location; contract paths stay repo-relative.
- [ ] New drill (upgrade-migration): scratch 5.13-shaped repo (moved set tracked on base, chore(board) history present) → upgrade → branch exists with the board tree · set untracked + ignored on base · exactly ONE new base commit · re-run upgrade = no-op; then claim → handoff → done on a fixture task stays green and base first-parent gains no chore(board) after the migration commit.
- [ ] Bash 3.2 compatible (NO `case` inside `$(...)` — hard parse error, see SPRINT Learned), POSIX awk, no new dependencies; full selftest green.

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Second in the kit/ops/polaris chain (T-020 → T-021 → T-022 → T-023 → T-024).
- Landmarks: cmd_upgrade :1869 · cmd_doctor :1046 · cmd_resume :625 · cmd_claim say/note :444-457 · cmd_uninstall :2148.
- upgrade already calls cmd_init_board (:1870) — after T-020 that appends the gitignore entries; your migration must still handle the tracked→untracked flip and the seed commit.
- verify runs the KIT script's selftest — drills run pre-dogfood; the installed ops/polaris stays 5.13 for this board.
- migration orphan seed reuses the base-commit subject "chore(board): board moves to polaris/board" (contract pins only the base one; kept identical for greppability).
- migration refuses a dirty index — staged changes would otherwise ride the ONE base commit.
- uninstall's POLARIS-lines .gitignore cleanup now also drops the T-020 entries ops/board/ + ops/SPRINT.md (lifecycle completeness; ops/ is deleted anyway).
- materialization = read-tree into a secondary index + checkout-index --prefix="$PRIMARY/" — absolute prefix works on Windows drive-letter paths; leading dirs auto-created.
- resume in a fresh clone falls back to -b feat/<ID> from base when the local branch is missing (pre-existing fallback; drill asserts rc 0 + materialization only).
