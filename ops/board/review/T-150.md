---
id: T-150
title: "PROBE — does a .claude/rules file with paths: actually fire when a matching file is read? Three headless sessions in a throwaway repo, the answer recorded before any skill code exists"
type: spike
scope: skills
plan: sprint-c
points: 1
value: 3
time_criticality: 8
risk_opportunity: 5
wsjf: 16.0
risk: normal
model: strong
status: review
owner: Owner@DESKTOP
branch: feat/T-150
files_owned:
  - plans/self-skills.md
approved:
context_files:
  - ops/contracts/self-skills.md
  - plans/v3.md
  - kit/.claude/skills/i-have-adhd/SKILL.md
depends_on: []
contract: ops/contracts/self-skills.md
verify:
  - grep -Eq '^probe: rules-always-fires: (yes|no)$' plans/self-skills.md
  - grep -Eq '^probe: rules-paths-fires-on-read: (yes|no)$' plans/self-skills.md
  - grep -Eq '^probe: rules-paths-fires-on-edit: (yes|no|unknown)$' plans/self-skills.md
  - grep -q '^probe: hidden-skill-absent: yes$' plans/self-skills.md
  - grep -q '^probe: nested-dir-prefix: kit:$' plans/self-skills.md
  - test "$(grep -c '^probe: ' plans/self-skills.md)" = 5
map_delta:
---
## Why
The owner's original ask for self-written skills, in its most literal form, was: change the
universal search bar, and what POLARIS knows about it is already in context — at zero standing
cost in every session that never touches it. Skills cannot do that (they have no `paths:`);
Claude Code's `.claude/rules/<name>.md` files with a `paths:` frontmatter are documented to load
only when a matching file is read. The T-146 spike INFERRED that from the documentation; nobody
has observed it. The owner chose "prove it first, then build": this task is the proof, and it runs
before a line of skill code exists. If the rule fires, `skill propose --write` writes a twin rule
beside every skill (contract § 6). If it does not, SK-1..SK-4 ship as plain skills and the twin is
dropped — said out loud, never shipped as a trigger nobody verified. Either answer is a success;
a guess is the only failure.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] a throwaway repo built EXACTLY as `ops/contracts/self-skills.md` § "The probe" describes — two rule files, one matching file, everything committed BEFORE the first session starts
- [ ] three headless sessions run FROM INSIDE that repo (`cd <repo>; unset CLAUDECODE; claude -p …` — the contract's three prompts A/B/C verbatim), each capped at 60 s, their raw replies pasted verbatim under `## Measurements` in `plans/self-skills.md`
- [ ] the five `probe:` lines appended under `## Measurements`, exact keys from the contract, one per line, values derived ONLY from what the sessions echoed: `rules-always-fires` (A shows SK0-ALWAYS) · `rules-paths-fires-on-read` (B shows SK0-PATHS and A did not) · `rules-paths-fires-on-edit` (C; `unknown` when C could not be arranged) · `hidden-skill-absent: yes` · `nested-dir-prefix: kit:`
- [ ] one sentence under the lines saying what the answer means for SK-1: twin or no twin (contract § 0 OPEN-1)
- [ ] `polaris verify` green

## Notes
- WHO CAN RUN THIS (the T-087 lesson: a verification requirement must name a caller who can): a pinned-cwd subagent cannot see another repo's injected context — but it CAN launch a fresh top-level session there. `claude -p` works from a Bash tool call once `CLAUDECODE` is unset in the child's env (measured 2026-09-14 from inside a session: `PONG`, rc 0). So YOU run the probe; nobody has to open a terminal.
- Build the throwaway repo OUTSIDE this checkout: `R="$(mktemp -d)"` — never under `scratchpad/` (it is tracked-dirty and `handoff` refuses). `git init -q "$R"`, write the three files, `git -C "$R" add -A && git -C "$R" commit -qm probe`, THEN run the sessions. The harness scans `.claude/rules/` at session start — a rule created mid-session proves nothing.
- Run A first (the control). If A already echoes SK0-PATHS, `paths:` rules are loaded unconditionally on this harness version — record `rules-paths-fires-on-read: no` (it did not fire ON READ; it was already there) and say so in the sentence.
- Run C needs `--allowedTools Read,Edit` so the headless session may edit without a prompt; if the flag is refused by this `claude` version, record `unknown` — never guess.
- A session that refuses, hangs past 60 s or answers nonsense is an HONEST `unknown` on that line; return the question in your report rather than inventing a yes.
- `hidden-skill-absent` and `nested-dir-prefix` were observed 2026-09-14 by T-146 and by this sprint's planner from their own injected lists (i-have-adhd absent, polaris present, `kit:polaris` present); record them as observed, no session needed.
- `plans/` is outside the api-kit index; this task adds no row and touches no golden.
