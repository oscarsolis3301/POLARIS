---
id: T-080
title: "Drills for the flip and the stub-writer — defaults, standard, adopt idempotence"
type: chore
scope: selftest
epic: autonomy-by-default
plan: autonomy-by-default
points: 3
value: 5
time_criticality: 3
risk_opportunity: 5
wsjf: 4.3
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-080
files_owned:
  - kit/ops/lib/selftest/remote.sh
  - kit/ops/lib/selftest/spine.sh
  - ops/tests/api-kit.expected
context_files:
  - ops/contracts/hands-free-knobs.md
  - ops/contracts/key-registry.md
  - kit/ops/lib/selftest/policy.sh
depends_on:
  - T-075
  - T-077
  - T-049
contract: ops/contracts/hands-free-knobs.md
verify:
  - bash -n kit/ops/lib/selftest/remote.sh
  - bash -n kit/ops/lib/selftest/spine.sh
  - bash ops/polaris check --only api-kit
  - bash kit/ops/polaris doctor --selftest --only notify,adopt
map_delta: "doctor --selftest gains label `adopt` (drill_adopt in remote.sh); drill_notify's knob block asserts the 6.0 defaults and the `autonomy: standard` opt-out"
---
## Why
The flip's whole claim is behavioral — "unset now means auto" — and prose cannot hold that. Two
machine checks can: extend drill_notify's existing knob-awareness block (remote.sh:135-142, which
already proves precedence and trusted-fill) with the two new compositions, and add `drill_adopt`
proving the stub-writer against the REAL registry — adopt twice, second run a byte-identical
no-op, an existing live value untouched.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] drill_notify knob block gains: EMPTY CONVENTIONS → doctor prints `plan_gate=auto`,
      `builder_questions=default-safe`, `evolve_apply=auto-reversible`
- [ ] and: `autonomy: standard` alone → `plan_gate=confirm`, `builder_questions=ask`,
      `evolve_apply=confirm`
- [ ] the three EXISTING assertions (explicit beats trusted · trusted fills unset · unknown
      warns) stay byte-unchanged and green
- [ ] `drill_adopt` in remote.sh (beside drill_upgrade; fn name pinned in key-registry.md § 5):
      scratch repo, real KEYS.tsv — first run appends stubs (rc 0), second run prints the
      nothing-to-adopt line and leaves CONVENTIONS.md byte-identical, a pre-set live value
      survives untouched
- [ ] label `adopt` added to SELFTEST_LABELS (spine.sh:46); `--only adopt` selects it;
      full-run drill count goes 27 → 28
- [ ] hermetic: both drills restore every fixture file they touch (`git status --porcelain`
      empty on return), green on a second consecutive run
- [ ] W4 api-kit owner: ops/tests/api-kit.expected gains exactly `drill_adopt`; T-050 (same
      wave) is surface-frozen and never touches the golden

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Spine state reaches drills by bash dynamic scoping — NO `local` in drill bodies; follow the
  bare-name style (`gpre`, `fnpre`) of the neighbors.
- Never edit kit/ops/polaris or kit/ops/lib/*.sh while a selftest is RUNNING — mid-edit runs
  fabricate a different spurious drill failure each time (Learned log).
- `--only notify,adopt` FOREGROUND with an explicit ≥600000ms timeout, or log-and-poll for
  `selftest passed`; kit driver only (27+ labels is the tell).
- bash 3.2: no `case` inside `$(...)`.
- `polaris check` is PRIMARY-anchored (observe.sh:1314 reads `$OPS/tests`, :1321 runs the .cmd in
  `$PRIMARY`), so a golden edited in a worktree passes VACUOUSLY; prove it by hand with
  `POLARIS_ROOT=<worktree> python ops/index.py find --api 'kit/*'` and byte-diff against .expected.
- Every one of the 9 new assertions was watched fail under a SEMANTIC sabotage of observe.sh /
  admin.sh in a throwaway kit copy (a copy, never an edit outside files_owned), then watched pass.
- assumed: hermeticity asserted as `git status --porcelain` UNCHANGED across the drill rather than
  literally empty — same guarantee, and it holds under any --only partition (default-safe).
- Scratch dirs must be lane-namespaced: two lanes both using `scratchpad/sab` had one `rm -rf` the
  other's tree mid-run, fabricating a missing-file drill failure that looked like a regression.
