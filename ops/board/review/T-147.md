---
id: T-147
title: "Release 6.4.0 — spend less, stay current, everywhere: the kit that maps its own tests, asks its three questions, and follows you"
type: chore
scope: release
plan: spend-less
points: 2
value: 13
time_criticality: 13
risk_opportunity: 5
wsjf: 15.5
risk: normal
model:
status: review
owner: Owner@DESKTOP
branch: feat/T-147
files_owned:
  - kit/ops/VERSION
  - CHANGELOG.md
approved:
context_files:
  - CHANGELOG.md
  - kit/ops/VERSION
  - ops/board/done/T-132.md
  - ops/board/done/T-128.md
depends_on: [T-143, T-144, T-145, T-146, T-148]
contract: ops/contracts/test-surfaces.md
verify:
  - grep -q '^version: 6.4.0$' kit/ops/VERSION
  - test "$(grep -c '^channel:\|^zip:\|^tarball:\|^repo:\|^changelog:' kit/ops/VERSION)" = 5
  - grep -q '^## 6.4.0 — 2026-09-1[0-9]$' CHANGELOG.md
  - grep -q '^## 6.3.1 — 2026-09-08$' CHANGELOG.md
  - awk '/^## 6.4.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q 'surfaces --scaffold' && awk '/^## 6.4.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q 'interview' && awk '/^## 6.4.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q 'SURFACES.tsv'
  - awk '/^## 6.4.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -qi 'BREAKING'
map_delta:
---
## Why
Sprint A gave `qa` and `land --express` a test map to select from; Sprint B gave every repo a way to
get one without a human writing rows, an interview that asks the three questions a repo cannot
answer for itself, a machine that carries the voice, and a board that follows you across computers.
None of it reaches anyone until it is released — and `update --auto` only ever delivers a PUBLISHED
minor. Four places at once: `kit/ops/VERSION`, the CHANGELOG, the git tag, the GitHub release with
`polaris-v5.zip`. Sprint 13's lesson is on the board: 6.2.0, 6.2.1 and 6.3.0 were all tagged and
never shipped because a sharded green is not a CI green — the serial suite shares one fixture across
every drill and reds on what the shards never see. So this task bumps and documents, and it does not
hand off until the SERIAL suite is green in its own worktree, with nobody else editing `kit/`. The
CHANGELOG entry is written for the humans who will read it after `update --auto` prints one line:
what got faster, what now asks, what changed on the machine, in the house voice of the 6.3.x entries
— and a `BREAKING: none` line, because nothing in 6.4.0 changes a repo's behavior until it opts in
(`test_select:` unset stays byte-identical; the interview only asks; the style lands unselected).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/ops/VERSION` reads `version: 6.4.0`; every other line untouched
- [ ] CHANGELOG has `## 6.4.0 — <today>` above 6.3.1, covering Sprint A (SURFACES.tsv, the stale-tests gate, change-scoped `qa`/express, the stamp scope, triage pricing contexts, the one-copy lane rule) and Sprint B (`surfaces --scaffold [--apply]`, the interview, `adhd:`, the machine-armed style + skill, `board_pull`), a before/after table in the 6.3.0 style, and `BREAKING: none`; the 6.3.1 and 6.3.0 entries unchanged
- [ ] the SERIAL suite green in your worktree: `bash ops/polaris bg run serial -- bash kit/ops/polaris doctor --selftest` then chunked `bash ops/polaris bg wait serial --max 300` until it returns a verdict (expect 900–1000 s; three or four waits); paste the `selftest passed` line and `git rev-parse HEAD` into Notes — the serial run is the CI tier, and it must be green BEFORE the tag exists
- [ ] no other lane is active while the serial suite runs (this task depends on every other one, so `bash ops/polaris status` shows nothing in active/ besides you — check before starting; editing `kit/ops/lib/*.sh` mid-run fabricates a spurious failure)
- [ ] `bash kit/ops/polaris check` green (every golden, against your worktree) and `doctor --fast` green

## Notes
- THE RELEASE RITUAL after this task lands (CONVENTIONS § Release ritual — the conductor's, end to end, in order; pin the sha at each step):
  1. `finish` green on main (the express land carries its stamp; `finish` accepts it).
  2. `git tag v6.4.0 && git push origin main --tags` — CI's release job runs the serial suite on ubuntu and publishes the zip; "one version, everywhere" checks the four places.
  3. Wait for the release, in the background: poll `curl -fsSL https://api.github.com/repos/oscarsolis3301/POLARIS/releases/latest` every 60 s until `"tag_name": "v6.4.0"` (typically 12–20 min; `gh` is not authenticated on this machine — do not depend on it). Red CI ⇒ STOP: read the failing job, fix on a rider task, and the tag stays where it is (a published tag is never moved — the 6.3.1 precedent: the fix ships as 6.4.1).
  4. `python kit/ops/pack.py --dogfood` — downloads the PUBLISHED zip, installs it here, runs the serial selftest again (~805 s: top-level `run_in_background`, poll the log). Then `bash ops/polaris version` must say 6.4.0.
  5. `bash kit/ops/polaris help | diff - ops/tests/cli-help.expected` — review every hunk line by line (exactly the v1 § 4 hunks + `surfaces --scaffold` + `interview`), then `bash ops/polaris check --only cli-help --update`; `bash ops/polaris check` green.
  6. Commit the refreshed instance: `chore(polaris): the published 6.4.0 kit now runs this board` — ops/ + the cli-help golden; push.
  7. Activation dogfood, on this repo: `bash ops/polaris surfaces --scaffold` must answer NORUNNER (bash + drills — correct); `bash ops/polaris doctor` now prints `preferences never set here: adhd`. Both are expected; this repo's own rows are the next sprint's first task (IDEAS.md).
  8. Optional, owner's call: `bash ops/polaris update --all` walks the machine registry now instead of waiting for each repo's next session.
- `bg run <name> -- <cmd…>` accepts an arbitrary command (bg.sh line ~76), so the serial suite fits under the tool cap from a subagent; `bg run` executes in the primary checkout, which at W4 is `main` with everything landed — exactly the tree CI will test.
- Scratch paths under `scratchpad/T-147/…` only; a subagent never ends its turn with a job still running — the last `bg wait` returns the verdict before you hand off.
- SERIAL suite GREEN — the CI tier, run in the primary checkout on main: `✅ selftest passed` / `SELFTEST-PASS`, rc 0 in 20m (log .polaris/bg/serial/log:131). Tree tested: git rev-parse HEAD = 3332cbf8be558d3f993520022748999c38f7c953. Board had nothing in active/ but T-147, and no kit/ edit ran against it. `check` 27/27 golden; `doctor --fast` 217 checks / 21 sections in 12s.
- The bump is inert to the suite by construction: the only selftest reference to kit/ops/VERSION writes its own fixture (policy.sh:349 `version: 9.9.9`), so VERSION + CHANGELOG cannot move a drill.
- PROPAGATION, read off the code — the version bump alone reaches NOBODY. `cmd_update` installs from `tarball:` = codeload of main, and runs `$K/ops/install.sh` — the tarball's ROOT ops/, which in this self-hosting repo is our own INSTALLATION, still 6.3.1. `update_latest` (admin.sh:119) curls `channel:` = raw main/ops/VERSION — the same root file. release.yml's header says it outright: the update check reads ops/VERSION on main, "no second source of truth". So step 4 (dogfood) + step 6 (commit ops/) is what actually publishes 6.4.0 to every installed repo; steps 1-3 only make the artifact exist.
- `zip:` (releases/latest) is a DIFFERENT consumer from `tarball:`: it serves fresh installs (`python polaris-v5.zip`) and `refresh_machine_kit`'s re-cache into ~/.claude/skills/polaris-install/. An existing repo's `update` never reads it for kit bytes — only to refresh the machine cache AFTER the install.
- CI's one-version job checks 3/4/5 (tag · releases/latest · ops/VERSION == kit/ops/VERSION) run on schedule|workflow_dispatch ONLY, not on push (ci.yml:71-73) — so this push goes green with ops/ still at 6.3.1, and the DAILY run is what reds until tag + release + dogfood are all done. Docs check (ci.yml:51) is clean: no file under README.md/kit/.claude/skills/kit/ops/*.md quotes a version.
