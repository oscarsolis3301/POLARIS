---
id: T-174
title: "ownership-guard fast path — one git call and a rules prefilter, so an ordinary edit stops paying three to five seconds"
type: bug
scope: hooks
plan: v4-fast
points: 3
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 6.0
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-174
files_owned:
  - kit/ops/hooks/ownership-guard.sh
  - ops/tests/ownership-primary.cmd
  - ops/tests/ownership-primary.expected
approved:
context_files:
  - ops/contracts/speed.md
  - kit/ops/lib/ownership.sh
  - ops/RULES.tsv
  - ops/tests/guard-denies.cmd
depends_on: []
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/hooks/ownership-guard.sh
  - bash ops/tests/ownership-primary.cmd 2>/dev/null | diff - ops/tests/ownership-primary.expected
  - bash ops/tests/guard-denies.cmd 2>/dev/null | diff - ops/tests/guard-denies.expected
  - bash kit/ops/polaris doctor --selftest --only checkoutguard
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta:
surface:
  - kit/ops/hooks/ownership-guard.sh tests: ops/tests/ownership-primary.* cmd: bash ops/polaris check --only ownership-primary note: the edit guard's fast path and its pinned primary refusal
---
## Why
Every Edit and Write in a POLARIS repo goes through `ownership-guard.sh`. It used to take about
1 s; it now takes 3.2–4.9 s, because one content rule for one file sends every write through
python, and even the fast path costs about 1.9 s since it starts three git processes and the whole
`polaris _guard` CLI. A session with 55 writes waits 80–100 s on this hook alone.

The fix does the cheap work first. One `git rev-parse` call returns everything the hook needs. The
hook then sources `ops/lib/ownership.sh` (function definitions only, no processes) and walks
`RULES.tsv` with a builtin `while read` loop, using the same `match_one` the CLI uses, so it only
calls the CLI when a rule could actually fire. Python starts only when a content rule's scope matches
the path being written, and the payload is scanned with the existing bash `jstr` and `grep -E`,
falling back to python only for `\u` escapes. Every verdict stays exactly what it is today.

Budget (enforced by T-182): a non-builder edit ≤ floor + 600 ms (`ops/contracts/speed.md` § 1).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] The common path starts one git process (the three calls merged into one `rev-parse --show-toplevel --git-common-dir --abbrev-ref HEAD`).
- [ ] The RULES prefilter runs in-hook with `match_one` in a builtin loop; the CLI runs only when a rule's scope matches the path.
- [ ] Python runs only when a content rule's scope matches the path; payload text is scanned with `jstr` + `grep -E`, python only on `\u` escapes.
- [ ] `guard-denies` verdicts unchanged; `ownership-primary` gains a row proving the fast exit exists, and the pinned refusal line is unchanged.
- [ ] `drill_checkoutguard` (which drives this hook) is green unchanged.
- [ ] Non-builder Edit timed best-of-5 before and after, both numbers recorded in Notes.
- [ ] No new api rows (speed.md § 5).

## Notes
- Fail direction: a guard that errors must still DENY where it denies today. Trust the exit code, not the refusal text (Learned: a neutered guard still talks).
- `RULES.tsv` is read-only context here; no rule line changes in this task.
- timed: non-builder Edit, cwd = primary, best-of-5 with old/new interleaved, 5 lanes live. docs/bench-probe.md (bench.sh's input, includes primary_gate's ls-files): 2528 ms before, 516 ms after (floor 70 ms, budget 670). ops/board/backlog/IDEAS.md: 3137 ms before, 418 ms after (floor 75 ms).
- verdicts: a 56-case old-vs-new matrix gave identical rc and stderr in every case. It covered the real primary, this worktree, pinned-cwd writes, and fixtures (ask, CRLF path, glob, `*`-content, with and without lib, no RULES, PWD ≠ cwd), plus MultiEdit, NotebookEdit, `\u`, and payloads over 4 KB.
- jstr is O(n²): 4 KB 430 ms, 16 KB 5.6 s, 64 KB 80 s. A late or missing key (`${s#*"key"}`) on a 150 KB payload took 30–36 s. So the path is parsed from the first 4 KB of stdin only, and the payload is decoded in bash only when the whole input is ≤ 4 KB.
- The fast exit needs `$PWD -ef cwd` and `<primary>/.git -ef` the common dir, because `_guard` finds RULES.tsv from its own cwd. Without both, or without the lib's `match_one` plus core.sh, the CLI is always called.
- Broken install (a real CLI copy with its lib missing, with or without RULES.tsv, or with core.sh missing): old and new both deny (rc 2). Self-review caught one gap: the absent-RULES branch had skipped the matcher check, and it is now fixed.
- assumed: python also takes MultiEdit, NotebookEdit and unknown tools, inputs over 4 KB, and a missing common dir, not only `\u`. Each re-runs the hook with POLARIS_GUARD_PY=1, the old python path whole, never a partial payload (default-safe).
- The Write/Edit tools turn a literal backslash-u0062 in file text into the character itself. The golden builds its escape with `printf '\134u0062ad'`.
- Evidence (plans/audit-0924.md, verbatim):
  - **hooks-1** · CONFIRMED · ownership-guard regressed from 976 ms to ~3–4.9 s per Edit/Write: one content rule for one file sends every write through python — ≈1.4–1.8 s saved per Edit/Write in this repo (up to ~3 s under heavy load). session-H's 55 writes ≈ 80–100 s of total wait. Repos with `*`-scoped content rules get nothing from this fix. To help them, pull new_string/content out with the existing bash jstr and scan it with `grep -E`, falling back to… — fix: In kit/ops/hooks/ownership-guard.sh: 1. Parse file_path/cwd with the existing pure-bash `jstr` first. 2. Compute REL. 3. Set NEED_BODY=1 only when a content rule's scope matches REL, using `. $TOP/ops/lib/ownership.sh; match_one "$REL" "$scope"` inside a builtin `while IFS=$'\t' read` loop over RUL… (kit/ops/hooks/ownership-guard.sh, ops/RULES.tsv)
  - **hooks-2** · CONFIRMED · Make the ownership-guard common path cheap: one git call and an in-hook RULES prefilter, with `polaris _guard` only when a rule could fire — About 2.3–2.8 s saved per non-Builder write, measured with my candidate; ~0.4–0.6 s is plausible with the git calls merged. This number already includes hooks-1's saving, so do not add the two. Once it lands, the wait on an Edit becomes model-guard's 0.4–2.2 s, which runs in parallel, unless hooks-… — fix: kit/ops/hooks/ownership-guard.sh: 1. Merge the three git calls into one `rev-parse --show-toplevel --git-common-dir --abbrev-ref HEAD`. 2. Source `$TOP/ops/lib/ownership.sh` (function definitions only, zero forks) and prefilter with the same `match_one` the CLI uses. 3. Exit 0 right away when ID='-… (kit/ops/hooks/ownership-guard.sh, kit/ops/polaris, kit/ops/lib/ownership.sh)
  - **The ownership-guard fast path is itself about twice the recorded 976 ms.** Even with no content rule it takes 1.86–2.19 s at moderate load. `polaris _guard` alone takes 1.0–1.16 s, against the 537 ms in the hook's own header trace. The auditor blamed the whole slowdown on the content rule.
