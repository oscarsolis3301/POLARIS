---
id: T-101
title: "polaris awake + polaris next reach the CLI — lib/awake.sh, loader +awake +handover, dispatch, usage blocks, preamble beat, awake_ensure; api-kit owner W2"
type: feature
scope: cli
epic: cant-eat-itself
plan: cant-eat-itself
points: 5
value: 13
time_criticality: 8
risk_opportunity: 5
wsjf: 5.2
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-101
files_owned:
  - kit/ops/lib/awake.sh
  - kit/ops/polaris
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/keep-awake.md
  - ops/contracts/module-layout.md
  - ops/contracts/role-handover.md
  - ops/contracts/key-registry.md
  - kit/ops/lib/bg.sh
depends_on:
  - T-096
contract: ops/contracts/keep-awake.md
verify:
  - bash -n kit/ops/lib/awake.sh
  - bash -n kit/ops/polaris
  - test "$(grep -c '^[a-z_]*()' kit/ops/lib/awake.sh)" = 5
  - grep -q '^cmd_awake()' kit/ops/lib/awake.sh
  - grep -q '^awake_ensure()' kit/ops/lib/awake.sh
  - grep -q '^awake_home()' kit/ops/lib/awake.sh
  - grep -q '^awake_conf()' kit/ops/lib/awake.sh
  - grep -q '^awake_status_line()' kit/ops/lib/awake.sh
  - test "$(wc -l < kit/ops/lib/awake.sh | tr -d ' ')" -le 150
  - grep -q 'awake: idle — not running' kit/ops/lib/awake.sh
  - grep -q 'awake: off (disabled)' kit/ops/lib/awake.sh
  - grep -q 'admin bg awake handover' kit/ops/polaris
  - grep -q '_match|_rules|_guard) _mods="core ownership" ;;' kit/ops/polaris
  - grep -q 'awake) shift; cmd_awake "$@";;' kit/ops/polaris
  - grep -q 'next) shift; cmd_next "$@";;' kit/ops/polaris
  - grep -q 'polaris-beat' kit/ops/polaris
  - test "$(grep -c 'awake_ensure || true' kit/ops/polaris)" -ge 5
  - test "$(wc -l < kit/ops/polaris | tr -d ' ')" -lt 500
  - bash kit/ops/polaris help | grep -q '^  awake \[status|start|stop\]      ONE keep-awake daemon per machine (~/.claude/polaris/awake): presses'
  - bash kit/ops/polaris help | grep -q '^  next \[--do|--brief\]            what this session does NEXT, read off the board — line 1 is ONE verb:'
  - bash kit/ops/polaris help | grep -q '^                                 --do performs a promote under the board lock · --brief re-anchors a chat'
  - bash -c "$(cat ops/tests/startup-budget.cmd)" | diff - ops/tests/startup-budget.expected
  - test "$(wc -l < ops/tests/api-kit.expected | tr -d ' ')" = 625
  - test "$(grep -c '^kit/ops/hooks/awake-hook.sh' ops/tests/api-kit.expected)" = 19
  - test "$(grep -c '^kit/ops/hooks/handover-hook.sh' ops/tests/api-kit.expected)" = 12
  - test "$(grep -c '^kit/ops/lib/awake.sh' ops/tests/api-kit.expected)" = 5
  - test "$(grep -c '^kit/ops/lib/handover.sh' ops/tests/api-kit.expected)" = 8
  - grep -q 'ah_verdict' ops/tests/api-kit.expected
  - grep -q 'hh_emit' ops/tests/api-kit.expected
  - grep -q 'next_route' ops/tests/api-kit.expected
  - test "$(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected | grep -c '^<')" = 0
map_delta: "new module lib/awake.sh (awake status|start|stop|disable|enable|install, awake_ensure); entry loader +awake +handover after bg; dispatch awake + next; preamble beats the worktree on every CLI call; awake_ensure fires from claim/status/doctor/handoff/bg run"
---
## Why
Two new commands reach the CLI here: `polaris awake` (the machine's keep-awake face, T-102 builds
the hook it drives) and `polaris next` (the handover router, T-109 builds the module). This task owns
the entry script — the ONE owner of `kit/ops/polaris` this sprint — and `lib/awake.sh`: `awake_home`,
`awake_conf`, `awake_ensure` (fork-free when the daemon's beat is fresh; a silent no-op on an unarmed
machine or in CI), `awake_status_line` (three pinned shapes) and `cmd_awake` (status · start · stop =
60 minutes off · disable · enable · install). The loader gains `awake` after `bg` and `handover`
after `awake` (module-layout v5); the guard path stays `core ownership`. Dispatch: `awake`, `next`;
the two byte-pinned usage blocks (`cli-help-parity` learns `next` in W3; `cli-help.expected` moves
only after dogfood — T-108). The preamble gets the builtins-only worktree beat below `EVENTS=`, so
every CLI call from inside a worktree keeps it live; `awake_ensure || true` fires from the dispatch
arms of `claim`, `status`, `doctor`, `handoff` and `bg run`. This task is also the W2 api-kit owner:
it writes the whole 44-row union — its own 5, the 19 awake-hook fns (T-102), the 8 handover fns
(T-109) and the 12 handover-hook fns (T-110) — from the names the contracts pin. Specs: keep-awake.md
(awake.sh + entry wiring + usage bytes), role-handover.md (next usage bytes + dispatch + the
parallel-build note), module-layout.md v5, key-registry.md §7.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `lib/awake.sh`: exactly the five fns, ≤150 lines; `awake_ensure` returns 0 without forking when `daemon/beat` < 3×TICK old, returns 0 silently when neither `$POLARIS_AWAKE_HOME` nor `~/.claude/polaris/awake-hook.sh` exists, else backgrounds `bash <hook> ensure "$PRIMARY"`; `stop` = stop flag + kill MSYS pid + rm lock + `disabled` with a 60-min expiry; status line shapes byte-exact
- [ ] entry: loader `… admin bg awake handover` (guard list untouched); `awake)` and `next)` dispatch arms (no `update_check_maybe` on `next`); the preamble beat `case` below `EVENTS=` and above the dispatch; `awake_ensure || true` inside the five arms; both usage blocks byte-exact (3 lines each, description column 34); entry < 500 lines; `startup-budget` unchanged
- [ ] api-kit.expected = 625 lines: the 44 pinned W2 rows in `find --api` order; completeness `<` count 0 in this worktree; sibling `>` rows are exactly the awake-hook/handover/handover-hook rows until the wave lands
- [ ] `bash kit/ops/polaris awake status` prints one of the three shapes (tested with the untracked stub described in Notes, then the stub deleted); `bash kit/ops/polaris doctor --selftest --only fmlist,newcmds` green after T-109 lands (or with the stub, foreground ≥600000 ms)
- [ ] no other fn anywhere; no heading; no KEYS row

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit OWNER, W2 — write the whole 44-row union up front from key-registry.md §7 + keep-awake.md + role-handover.md fn lists (never from a sibling's diff); the ROW LIST wins over the count; discrepancies go in the handoff Notes.
- PARALLEL-BUILD TRAP (role-handover.md § testing before T-101 lands): `lib/handover.sh` is T-109's and does not exist in YOUR worktree, and the loader dies on a missing module by design — so no verify: line above runs a loader-dependent command (`help` exits before the loader; the index probe is python). For local testing only, drop an UNTRACKED empty `kit/ops/lib/handover.sh` (`: > kit/ops/lib/handover.sh`), never commit it, and delete it before `verify`/`handoff`. The integrator lands T-109 before you.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-101` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree (the stub included); never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- The `awake_ensure` call sits BELOW `EVENTS=` in the dispatch arms — `startup-budget` counts `$(cfg ` forks in the resolve-env block and would red on a global. The preamble beat is `case "$PWD" in */.polaris/wt/*) …` with string ops + `: >` only.
- Scratch dirs per-lane: scratchpad/T-101/. Never edit kit/ops/polaris while a selftest is RUNNING. bash 3.2 (CI runs macOS /bin/bash).
