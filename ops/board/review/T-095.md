---
id: T-095
title: "Auto mode prompts for nothing it doesn't have to — seven bare tool-name rules in kit settings + bootstrap PERMS, golden perm-tools"
type: feature
scope: perms
epic: cant-eat-itself
plan: cant-eat-itself
points: 2
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 8.0
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-095
files_owned:
  - kit/.claude/settings.json
  - kit/ops/bootstrap.py
  - ops/tests/perm-tools.cmd
  - ops/tests/perm-tools.expected
approved:
context_files:
  - ops/contracts/permission-rules.md
  - ops/tests/output-style-installed.cmd
  - kit/ops/lib/admin.sh
depends_on: []
contract: ops/contracts/permission-rules.md
verify:
  - python -c "import json;json.load(open('kit/.claude/settings.json'))"
  - python -c "import ast;ast.parse(open('kit/ops/bootstrap.py',encoding='utf-8').read())"
  - grep -q '"EnterWorktree"' kit/.claude/settings.json
  - grep -q '"ExitWorktree"' kit/.claude/settings.json
  - grep -q '"Workflow"' kit/.claude/settings.json
  - grep -q '"SendMessage"' kit/.claude/settings.json
  - grep -q '"EnterWorktree"' kit/ops/bootstrap.py
  - grep -q '"TodoWrite"' kit/ops/bootstrap.py
  - test "$(cat kit/.claude/settings.json kit/ops/bootstrap.py | grep -c 'ExitPlanMode')" = 0
  - test "$(cat kit/.claude/settings.json kit/ops/bootstrap.py | grep -c 'AskUserQuestion')" = 0
  - test "$(cat kit/.claude/settings.json kit/ops/bootstrap.py | grep -c '"NotebookEdit"')" = 0
  - grep -q 'added = \[rule for rule in PERMS' kit/ops/bootstrap.py
  - test "$(wc -l < ops/tests/perm-tools.expected | tr -d ' ')" = 4
  - bash -c "$(cat ops/tests/perm-tools.cmd)" | diff - ops/tests/perm-tools.expected
  - bash -c "$(cat ops/tests/output-style-installed.cmd)" | diff - ops/tests/output-style-installed.expected
map_delta: "kit settings + bootstrap PERMS pre-authorize the harness's own tools (EnterWorktree ExitWorktree Workflow Task Agent TodoWrite SendMessage); golden perm-tools pins the set and the two human gates' absence"
---
## Why
In auto mode a POLARIS session still stops to ask "Do you want to proceed with EnterWorktree?" —
every one of five parallel chats, on the same click. Every permission rule the kit ships is a
`Bash(...)`/`PowerShell(...)` prefix; nothing pre-authorizes the harness's OWN tools. The fix is
data: seven bare tool names appended to `permissions.allow` in `kit/.claude/settings.json` and to
`PERMS` in `kit/ops/bootstrap.py` (the machine-level list `arm_machine` unions into
`~/.claude/settings.json`). Bare = every invocation of that tool. Two names are deliberately NOT
added — `ExitPlanMode` and `AskUserQuestion` are the human gates POLARIS must keep — and
`NotebookEdit` stays out because the ownership guard denies it anyway. `readonly-allow.sh` is not
touched: it is a Bash-read approver, not a tool-permission store. A new golden `perm-tools` pins the
exact set in both files and the absence of the gates, so the rule never drifts in either direction.
The `EnterWorktree({path: ".polaris/wt/<ID>"})` entry the sprint-11 contract left open is settled by
the tool's own description: it accepts any `git worktree list` path on first entry from the launch
dir — the missing rule was the whole defect. Spec: permission-rules.md.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/.claude/settings.json` `permissions.allow` ends with, in order after the last PowerShell rule: "EnterWorktree", "ExitWorktree", "Workflow", "Task", "Agent", "TodoWrite", "SendMessage" — valid JSON, nothing else changed
- [ ] `bootstrap.py` `PERMS` gains the same seven as its final block, preceded by a comment block stating: why bare names, why the two gates and NotebookEdit are absent (without spelling the gate names — the golden counts them as 0), and that unknown names are inert on older builds; no new module-level constant or def (the index records both)
- [ ] `ExitPlanMode`, `AskUserQuestion` appear in NEITHER file; the union paths (`added = [rule for rule in PERMS` in bootstrap.py, `for rule in rules:` in admin.sh) untouched
- [ ] golden `ops/tests/perm-tools.{cmd,expected}`: exactly the four pinned lines of permission-rules.md; greps the two kit files only (hermetic); proven from the worktree with the `.cmd`-body diff; sabotaged red once (remove one name) before trusting green
- [ ] `output-style-installed` and `readonly-allow` goldens byte-identical (settings.json's outputStyle and hook entries untouched)
- [ ] the pre-approval below is understood: it covers exactly these seven names; anything else is a plan-gate decision

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- OWNER PRE-APPROVAL, verbatim from plans/v2.md (2026-09-01): "Owner decisions carried from v1 (2026-09-01): the permission-rules task (`risk: high`) merge is PRE-APPROVED for exactly the bare tool names listed in WS2; the product-repo step is in scope." Plan-gate decision 1, approved with the plan: "Default: `risk: normal`, `approved:` empty, your 2026-09-01 pre-approval quoted verbatim in the task Notes and `permission-rules.md`; the real human gate on the live settings file is the ONE classifier click at dogfood." — so this task is `risk: normal` and self-lands; the human's click happens once, when dogfood rewrites `~/.claude/settings.json`.
- api-kit: SURFACE-FROZEN — no new fn/def/const, no new `#` line, no new KEYS row (key-registry.md §7). T-096 owns `ops/tests/api-kit.expected` this wave. `bootstrap.py` constants ARE indexed (`const` rows) — add none.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-095` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- This task edits `kit/.claude/settings.json` (the PRODUCT), never `.claude/settings.json` (this repo's live file) — no classifier click is expected in W1. Scratch dirs per-lane: scratchpad/T-095/. Windows: `python`, never `python3`.
- assumed: verify[11] grepped 'NotebookEdit"' across both files, which can never be 0 — the ownership-guard hook matcher "Edit|Write|MultiEdit|NotebookEdit" contains that string and acceptance criterion 5 requires the hook entries untouched; narrowed to '"NotebookEdit"' (the bare-rule form the Why actually forbids, 0 in both files, still red if anyone adds it) (default-safe)
- kit/.claude/settings.json is CRLF in the working tree while bootstrap.py and ops/tests/* are LF — patch it byte-exactly or the whole file shows up in the diff
