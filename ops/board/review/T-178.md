---
id: T-178
title: "next in about a second — no process per background job, held tasks say why they are held, and the brain finally reads kickbacks"
type: bug
scope: cli
plan: v4-fast
points: 3
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 6.0
risk: normal
status: review
owner: Owner@DESKTOP
branch: feat/T-178
files_owned:
  - kit/ops/lib/handover.sh
  - kit/ops/lib/bg.sh
  - kit/ops/lib/knowledge.sh
  - kit/ops/lib/selftest/brain.sh
  - ops/tests/handover-route.cmd
  - ops/tests/handover-route.expected
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/role-handover.md
  - ops/contracts/bg-jobs.md
  - ops/contracts/brain.md
depends_on: []
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/lib/handover.sh
  - bash -n kit/ops/lib/bg.sh
  - bash -n kit/ops/lib/knowledge.sh
  - bash -n kit/ops/lib/selftest/brain.sh
  - bash ops/tests/handover-route.cmd 2>/dev/null | diff - ops/tests/handover-route.expected
  - test "$(grep -c 'held' ops/tests/handover-route.expected | tr -d ' ')" -ge 4
  - s=$(date +%s%N); bash kit/ops/polaris next >/dev/null 2>&1; test $(( ($(date +%s%N) - s) / 1000000 )) -lt 5000
  - s=$(date +%s%N); bash kit/ops/polaris next --brief >/dev/null 2>&1; test $(( ($(date +%s%N) - s) / 1000000 )) -lt 10000
  - bash kit/ops/polaris doctor --selftest --only brain
  - bash ops/tests/handover-stop.cmd 2>/dev/null | diff - ops/tests/handover-stop.expected
  - bash ops/tests/bg-lifecycle.cmd 2>/dev/null | diff - ops/tests/bg-lifecycle.expected
  - bash ops/tests/startup-budget.cmd 2>/dev/null | diff - ops/tests/startup-budget.expected
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta:
surface:
---
## Why
`polaris next` answers "what does this session do now?" and the compaction and Stop hooks call it
too. It takes 23–28 s, because it starts one `basename` process for each of the 153 background-job
folders in `.polaris/bg` — folders that are never cleaned up. That is past the anchor hook's 10 s
limit, so the re-anchor after compaction silently never arrives.

Replace every `basename` in those loops (handover, bg and knowledge) with shell builtins, skip the
`.prev` archive folders before doing any other work, and replace the `cat | tr` pipelines with `read`.
Let the existing age-based cleanup also remove `.prev` folders older than 7 days, keeping every
`bg-jobs.md` rule (never a live job).

Two quiet failures ride along. When `next` decides not to promote a planned task, it drops the
reason for three of the four gate failures, so planned work can vanish without a word: record a
held reason for all four (contract missing · points outside 1–5 · a dependency not done · an
ownership overlap), and print the held lines from bare `next` too. And the brain's kickback parser
can never match a kickback event, so the "what came back" lesson log has always been empty: fix the
parser and prove it with a seeded kickback in the brain drill.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] Every bg-folder loop in handover.sh, bg.sh and knowledge.sh uses `${d%/}` / `${n##*/}`; `.prev` is skipped first; no `cat | tr` pipeline is left in those loops.
- [ ] Folders older than 7 days (`.prev` and `.archive/*`) are pruned inside the existing prune path; a live job is never touched; the 7 is an in-code constant, not a KEYS row.
- [ ] `next_promote` records a held reason for all four gate failures; bare `next` prints held lines; `handover-route` pins all four.
- [ ] `brain_learned` parses kickback events; `drill_brain` asserts a seeded kickback's id and note appear.
- [ ] On a quiet box with the 153 bg folders: `next` < 3 s and `next --brief` < 10 s — before and after recorded in Notes (verify allows 5 s for load).
- [ ] No new api rows (speed.md § 5).

## Notes
- Held-reason wording is yours (the golden you own pins it); keep it plain English, one line per task, and include the task id.
- Evidence (plans/audit-0924.md, verbatim):
  - **close-4** · CONFIRMED · `polaris next` takes 24.5 s: a `basename` process per .polaris/bg job folder (153, never pruned). That exceeds the 10 s anchor-hook timeout and approaches the … — `next` takes 23-28 s and `next --brief` 29.5 s on today's box, against 10 s (anchor) and 30 s (stop) hook timeouts. The builtin fix brings that loop from about 24 s to about 0.02 s. Anchor re-orientation is lost only for sessions that reach row 5. — fix: In all four scans (handover.sh:236-244, observe.sh:2311-2314, observe.sh:330-333, bg.sh:146-149) replace `$(basename "$d")` with `n="${d%/}"; n="${n##*/}"` and do the `*.prev` case before any other work, exactly as hh_bg_live does. Better still, iterate `"$root"/*/` but `case "$d" in *.prev/) conti… (kit/ops/lib/handover.sh, kit/ops/lib/observe.sh, kit/ops/lib/bg.sh)
  - **quality-5** · CONFIRMED · Planned work can vanish silently: next_promote drops 3 of 4 gate failures, bare `next` never prints held lines, and `finish` never reads backlog/ (IDEAS:30/47,… — Frequency is unmeasured. There are 0 stranded tasks today, and one historical bite (the unset-contract case T-130 fixed, IDEAS:30). Impact stays an estimate. — fix: In next_promote, replace the three `ok=0` sets with held reasons ('held: <ID> — contract <path> missing', '— points <p> must be 1-5', '— waits on <dep> (<col>)') appended to NX_HELD, keeping promotion semantics identical. In next_route's finish branch, print NX_HELD after the verb line. In cmd_fini… (kit/ops/lib/handover.sh, kit/ops/lib/observe.sh, ops/tests/handover-route.cmd, ops/tests/handover-route.expected, kit/ops/roles/CONDUCTOR.md)
  - **quality-6** · CONFIRMED · The quality feedback loop is blind: kickback rate is structurally 0, the brain's kickback parser can never match, and Learned is dry despite the 6.5.0 fix — The parser bug and the dry Learned log are real and cheap to fix. The escape rate is not measured. By subject reading, about 5 of the 20 fix tasks since 6.1.0 repair lane-landed work. — fix: (1) One line in knowledge.sh brain_learned: `if ($i=="id" && $(i+1)==":") id=$(i+2); if ($i=="note" && $(i+1)==":") note=$(i+2)`, or reuse next_brief's sed extraction (handover.sh:283-285). Add a brain drill assertion that a seeded kickback's id and note appear. (2) An escape signal: a `fixes: [T-x… (kit/ops/lib/knowledge.sh, kit/ops/lib/observe.sh, kit/ops/lib/integrate.sh, kit/ops/roles/EVOLVE.md, kit/ops/templates/TASK.md, kit/ops/lib/selftest/brain.sh)
- conductor's decision (option A): retention only removes archived runs in .polaris/bg/.archive that finished more than 7 days ago. It never touches a `<name>.prev` slot, a run with no rc, or a name's newest run. So the roughly 120 old top-level .prev folders on this machine stay; a later task can decide on them with this data.
- timing on a busy shared machine (5 lanes; a bare `bash -c true` took 0.1-1 s). On a test board at row 5 with a 169-folder registry: before, next took 7.1-11.9 s and next --brief 7.9-13.7 s. After, next took 1.8-2.2 s and --brief 1.7-1.9 s. The bg-folder scan alone over the real 169-folder registry went from 6655 ms to 79 ms. There was no quiet window today to take the quiet-machine number.
- retention was proven on a throwaway registry. Removed: an old archived run whose .prev slot was newer, the older of two archives with the same name, and qa-final-5000. Kept: a 1-day-old run, a run with no rc, a live pid with no rc, each name's newest run (lone, pair-2000, qa-1000, since qa-final is a different name), entries not named `<name>-<epoch>`, every .prev slot, and the live job.
- the brain's kickback list now shows the NEWEST 12 (tail, not head). The drill seeds its kickback last, and head would hide it on a board that already has 12.
- the points check is unchanged: only blank, 8 and 13 fail. The held line still says "must be 1-5" because the scale is Fibonacci, so 8 and 13 are the only values above 5.
- observe.sh still starts one basename process per bg folder, in the sweep (about lines 333-336) and in the finish guard (about 2311-2314). That file is not owned here; the same two-line fix applies.
