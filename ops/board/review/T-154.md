---
id: T-154
title: "next --do keeps its hands off foreign plans — the promote pass holds a backlog task whose plan: is not this run's under drain: plan, and says so"
type: bug
scope: handover
plan: sprint-c
points: 2
value: 5
time_criticality: 3
risk_opportunity: 3
wsjf: 5.5
risk: normal
model: mid
status: review
owner: Owner@DESKTOP
branch: feat/T-154
files_owned:
  - kit/ops/lib/handover.sh
  - kit/ops/lib/selftest/board.sh
approved:
context_files:
  - ops/contracts/role-handover.md
  - ops/tests/handover-route.cmd
  - kit/ops/lib/builder.sh
depends_on: []
contract: ops/contracts/role-handover.md
verify:
  - bash -n kit/ops/lib/handover.sh
  - bash -n kit/ops/lib/selftest/board.sh
  - bash kit/ops/polaris help >/dev/null
  - test "$(grep -c '^[a-z_][a-z_]*() {' kit/ops/lib/handover.sh)" = 8
  - grep -q "is not this run's" kit/ops/lib/handover.sh
  - grep -q "is not this run's" kit/ops/lib/selftest/board.sh
  - test "$(wc -l < kit/ops/lib/handover.sh | tr -d ' ')" -le 340
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/handover.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/handover.sh' ops/tests/api-kit.expected)
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/selftest/board.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/selftest/board.sh' ops/tests/api-kit.expected)
  - bash ops/tests/startup-budget.cmd | diff - ops/tests/startup-budget.expected
map_delta:
  - "next_promote (handover.sh) holds a backlog candidate whose plan: is set and differs from the run's plan under drain: plan (`held: <ID> — plan <slug> is not this run's (<P>) — drain: plan`); the run's plan = the session's handover plan file, else the one slug carried by ready ∪ active ∪ review; role-handover.md v2"
---
## Why
`drain: plan` exists so one "go" authorizes the plan the human approved, not the whole board. The
build scan (`next_claimable`) honours it; the promote pass (`next_promote`) does not — it promotes
any backlog task whose dependencies landed, whatever plan it belongs to. Harmless in sprint 14
(every task carried `plan: spend-less`); the first board with two plans on it silently adopts
foreign work, exactly what the knob is for. The fix mirrors the build scan's own foreign rule: know
the run's plan (the session's handover plan file, else the single slug the in-flight tasks carry),
and under `drain: plan` hold a candidate whose `plan:` is set and different — reported as a `held:`
line like the overlap hold, never a silent drop (T-130's lesson). A candidate with no `plan:` is
never foreign, so riders still flow.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `next_promote`: P per contract v2 (session plan file → single in-flight slug → none); under `drain: plan` with P known, a candidate whose `plan:` is set and ≠ P goes to `NX_HELD` with the pinned line and is NOT promoted; no `plan:` ⇒ eligible; `drain: queue|backlog` ⇒ no filtering; P unknown ⇒ byte-identical to today
- [ ] the module still has EXACTLY eight top-level fns (inline change) and stays ≤ 340 lines
- [ ] `drill_handover` (board.sh): the contract's fixture — under `drain: plan`: ready `plan: alpha`, backlog dep-satisfied `alpha` + `beta` + unplanned → `--do` promotes two, holds beta's with the pinned line, ONE board commit; under `drain: queue` all three promote; asserts `ls ready/`, the promote event count and rc
- [ ] `bash ops/tests/handover-route.cmd | diff - ops/tests/handover-route.expected` is empty (run once from your worktree root — ~47 s, so it is an acceptance check, not a verify line)
- [ ] the drill green in your worktree: `bash ops/polaris bg run t154 -- bash kit/ops/polaris doctor --selftest --only handover` + chunked `bg wait t154 --max 300`; paste the `selftest passed` line into Notes
- [ ] `polaris verify` green

## Notes
- The `promote` alias (entry dispatch + usage) is T-155's; CONDUCTOR.md step 7's prose is T-158's. Touch neither `kit/ops/polaris` nor any role file.
- `next_claimable` already reads `<next_dir>/plan` and `cfg drain plan`; reuse those reads verbatim — one definition of "foreign" for both passes.
- The fixture's session plan file: `mkdir -p .polaris/handover/<sid>; printf 'alpha\n' > .polaris/handover/<sid>/plan` with `CLAUDE_CODE_SESSION_ID=<sid>` exported (handover-route.cmd shows the exact shape).
- Per-lane scratch (`mktemp -d`); trust `ls ready/` + rc over printed text (T-089).
- drill green: `✅ selftest passed (subset: handover — 1 of 36 labeled drills; spine always runs)` (rc 0).
- `handover-route` golden byte-identical: its fixture never claims, so no session `plan` file exists and no task carries `plan:` — P is unknown there and v1 behaviour is exact.
- `claim` stamps `<sid>/plan` even when the task has NO `plan:` (builder.sh:148), so an EMPTY plan file is the ordinary state. It yields no P and falls through to the in-flight scan — reading it as "P is the empty string" would have made every planned task foreign to an unplanned session.
- P is resolved AFTER the `[ -n "$cands" ] || return 1` early return and inside `--do`'s mutex, so an empty backlog still costs nothing and the read is taken under the board lock.
- still-silent drops, left alone on purpose (contract v2 pins only the plan hold, and held lines for these would move `handover-route.expected`, which T-155 owns this wave): a named-but-missing `contract:`, `points` ∈ {8, 13, ''}, and any dep not in `done/` all `continue` before the accounting. One for IDEAS.md.
