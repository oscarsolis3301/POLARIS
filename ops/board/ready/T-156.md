---
id: T-156
title: "Wire the skills in — claim records a skill-hit, pack prints the SKILLS section and whole trap bullets, slim counts a hidden skill as 0 bytes, doctor and uninstall say what the shelf holds"
type: feature
scope: skills
plan: sprint-c
points: 3
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 5.3
risk: normal
model: mid
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/builder.sh
  - kit/ops/lib/admin.sh
  - kit/ops/lib/observe.sh
approved:
context_files:
  - ops/contracts/self-skills.md
  - kit/ops/lib/skills.sh
  - ops/contracts/context-pack.md
depends_on: [T-155, T-151, T-152]
contract: ops/contracts/self-skills.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - bash -n kit/ops/lib/admin.sh
  - bash -n kit/ops/lib/observe.sh
  - bash kit/ops/polaris help >/dev/null
  - grep -q 'SKILLS — what POLARIS already knows about these paths' kit/ops/lib/builder.sh
  - grep -q 'evt skill-hit' kit/ops/lib/builder.sh
  - test "$(grep -c 'disable-model-invocation' kit/ops/lib/admin.sh)" -ge 5
  - grep -q '29,124' kit/ops/lib/admin.sh
  - grep -q 'POLARIS wrote stay' kit/ops/lib/admin.sh
  - grep -q 'command -v skill_budget' kit/ops/lib/observe.sh
  - grep -q 'due for eviction' kit/ops/lib/observe.sh
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/builder.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/builder.sh' ops/tests/api-kit.expected)
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/admin.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/admin.sh' ops/tests/api-kit.expected)
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/observe.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/observe.sh' ops/tests/api-kit.expected)
  - bash ops/tests/pack-visual.cmd | diff - ops/tests/pack-visual.expected
  - bash ops/tests/startup-budget.cmd | diff - ops/tests/startup-budget.expected
map_delta:
  - "claim emits one skill-hit event per POLARIS-written skill whose paths overlap files_owned (the only skills telemetry); pack prints SKILLS — what POLARIS already knows about these paths (omitted when none overlap) and KNOWN TRAPS now prints WHOLE bullets (≤8/40 lines); slim_scan counts a disable-model-invocation: true definition as 0 B; uninstall's preview names the skills that stay; doctor prints the shelf's over-budget/eviction lines (command -v-guarded)"
---
## Why
The module (T-155) can write, budget and evict skills, but nothing yet DELIVERS one or records that
it was used. This task is the wiring, surface-frozen (no new fn anywhere): `claim` emits a
`skill-hit` event per skill whose paths overlap the task's files — the one piece of telemetry the
eviction rule needs; `pack` gains the SKILLS section so a builder is told which skill to read before
its first edit, and its KNOWN TRAPS producer prints whole bullets — the best lesson on this board is
19 lines and a builder gets one; `slim` learns that a hidden skill costs nothing, so its number and
`skill budget`'s can never disagree; `uninstall`'s preview names the skills that stay because they
are the repo's knowledge; `doctor` prints the shelf's one-line warnings.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `cmd_claim`: beside `evt claim`, inside the mutex, one `evt skill-hit "<skill>" "<ID>"` per POLARIS-written skill whose `skill_paths` overlap `files_owned` (`pat_overlap`, both directions) — rides the same board commit
- [ ] `cmd_pack`: the SKILLS section after KNOWN TRAPS and before SURFACES, one pinned line per overlapping skill (`read: .claude/skills/<name>/SKILL.md · tier <t> · <h>/<W> hits · <L> lines`), the WHOLE section omitted when nothing overlaps — `pack-visual` stays byte-identical
- [ ] `pack_brain_grep`: whole bullets (a bullet is emitted when any of its lines matches), ≤ 8 bullets / 40 lines per file
- [ ] `slim_scan`: a flagged definition counts 0 B (one awk clause); the rationale comment keeps the dated 251 / 34,119 figure and gains `223 / 29,124 / 7,281 tok on 2026-09-14`
- [ ] `cmd_uninstall` preview: the pinned `<n> skill(s) POLARIS wrote stay …` line when n > 0
- [ ] `cmd_doctor`: `command -v skill_budget`-guarded; the `⛔ shelf over budget …` line when `skill_budget` is rc 1 and `⚠ <n> skill(s) due for eviction — ops/polaris skill prune` when `skill_prune` is rc 1
- [ ] no new top-level fn in any of the three files — their api-kit rows byte-identical
- [ ] `bash kit/ops/polaris doctor --fast` green; `bash kit/ops/polaris pack T-156` (from your worktree; the primary's board holds you in active/) shows KNOWN TRAPS with whole bullets
- [ ] `polaris verify` green

## Notes
- The twin's existence is T-155's business (inside `skill propose`); nothing here writes rules files.
- `skill_hits <name> <events>` and `skill_paths <name>` are the module's (T-155, landed before you) — call them, never re-implement; `skill_budget`/`skill_prune` return rc 1 exactly when doctor should speak.
- awk paragraph mode (`RS=""`) splits on blank lines; brain bullets are `- ` items separated by newlines, not blank lines — split on `^- ` instead and keep continuation lines (two-space indent).
- `ops/tests/pack-visual.cmd` builds its own fixture and runs the worktree's kit — that is why it is a verify line here; it slices only the SEE YOUR WORK section, so a SKILLS section printed for a fixture with no skills would red it (the omit-when-empty rule is what keeps it green).
- Per-lane scratch (`mktemp -d`); trust rc + bytes (T-089).
