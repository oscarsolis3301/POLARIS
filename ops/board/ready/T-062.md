---
id: T-062
title: "Four isolation drills — park, claimguard, busyint, pushdegrade — and the drill-surface golden"
type: test
scope: selftest
epic: n-chats-one-repo
plan: n-chats-one-repo
points: 5
value: 8
time_criticality: 5
risk_opportunity: 8
wsjf: 4.2
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/selftest/spine.sh
  - kit/ops/lib/selftest/board.sh
  - kit/ops/lib/selftest/history.sh
  - kit/ops/lib/selftest/policy.sh
  - kit/ops/lib/selftest/remote.sh
  - ops/tests/api-kit.expected
  - ops/tests/triage-lane.cmd
  - ops/tests/triage-lane.expected
context_files:
  - ops/contracts/shared-checkout.md
  - kit/ops/lib/workspace.sh
depends_on:
  - T-057
  - T-058
  - T-059
  - T-060
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/selftest/spine.sh
  - bash -n kit/ops/lib/selftest/board.sh
  - bash -n kit/ops/lib/selftest/history.sh
  - bash -n kit/ops/lib/selftest/policy.sh
  - bash -n kit/ops/lib/selftest/remote.sh
  - grep -q 'drill_park' ops/tests/api-kit.expected
  - bash ops/tests/triage-lane.cmd | diff - ops/tests/triage-lane.expected
  - bash kit/ops/polaris doctor --selftest --only park,claimguard,busyint,pushdegrade
map_delta:
---
## Why
The isolation features land in waves 1-2; this task is the proof that keeps them true forever. Four
new labeled drills, exactly as shared-checkout v1 § Executable check specifies:
`park` (park a dirty tree tracked+untracked → clean; unpark → byte-identical restore; a dirty tree
at `land` parks + proceeds + prints `parked as polaris/park-`) · `claimguard` (invalid ID dies via
id_ok pre-lock; a planted ref literally named `feat`, local + scratch origin, is renamed
`stray/feat-<sha7>` and claim succeeds; two overlapping tasks → the second auto-moves to blocked/
with the remedy note) · `busyint` (a held lease → wait notes then rc 3 + `queued: `; a stale lease
is stolen with a note; re-land of a landed ID → `already landed — skipped` rc 0; an open non-ff
integrate/<date> is ADOPTED and sealed once) · `pushdegrade` (a planted `feat` ref on the scratch
origin breaks the push → handoff retries, repairs, and with push forced dead the board move still
happens + the task Note + the `push-fail` event exist). Labels register on spine.sh's one-line
SELFTEST_LABELS literal (:46). The four drill fns are new top-level functions, and the surface
golden records every one — so this task owns `ops/tests/api-kit.expected` for its second and final
delta of the sprint (T-057 authored the first; the golden was frozen in between).

Plus one repair from the wave-2 gate (11/12): the triage-lane golden runs `triage` against the LIVE
board, so ANY session filing a task flips its answer — T-064's filing turned the pinned `full` into
`solo`, red on main too, sealed without kickback. Under N chats that golden reds every time anyone
files a task. This task re-founds it on a hermetic fixture board.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] all four labels registered in SELFTEST_LABELS (one line, literal); an unknown label still
      dies before the spine
- [ ] each drill asserts exactly its contract bullet list and leaves the throwaway repo
      assertions-clean (T-046 hermeticity — no stray rules, tasks, refs or stashes)
- [ ] drill placement: claimguard→board.sh · busyint→history.sh · park→policy.sh ·
      pushdegrade→remote.sh (one fn per label, module-internal order preserved)
- [ ] drill fns add NO `local` declarations that shadow spine state (dynamic scoping —
      module-layout invariant)
- [ ] `--only park,claimguard,busyint,pushdegrade` green; a full serial selftest stays green
      (proven at the wave gate, not in verify:)
- [ ] `ops/tests/api-kit.expected` gains exactly the four drill_ fn lines, hand-authored
      byte-exact (recipe in Notes)
- [ ] the triage-lane golden is HERMETIC: `ops/tests/triage-lane.cmd` constructs its own fixture
      board (throwaway repo the CLI anchors to, independent of live board state) so cross-session
      board writes can never red it; it still asserts triage's line-1 vocabulary
      (`solo`/`express`/`full`), the three-space note-indent shape, and keeps the existing
      `pts" -le 3 ]` source grep; running the .cmd twice from any board state is byte-identical

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Golden recipe (T-047, proven): replicate the generator — `python ops/index.py` with POLARIS_ROOT
  at your worktree, emit the `--api 'kit/*'` shape, byte-exact diff — and hand-author the delta.
  The wave gate's uat proves it on the integrate checkout.
- Scratch-origin drills: reuse `ensure_origin` (spine.sh:25) — pushdegrade and claimguard's origin
  cases build on it exactly like the remote/syncrace drills do.
- Hermetic-fixture pattern for triage-lane.cmd: mirror the selftest spine — init a throwaway git
  repo with a minimal board fixture and run the CLI from INSIDE it (polaris anchors to the
  worktree-list primary, which becomes the fixture repo). Live board state then cannot leak in.
- OPTIONAL, only if near-free (T-060's finding): the update-park path is proven manually but
  undrillable without a fake release channel; a fixture-served channel file could drill it. Skip
  freely — not acceptance material.
- Windows/Git Bash: never assert on CR-sensitive bytes via grep/sed — awk/od only (T-056).
- The installed `ops/polaris` lags your kit edits all sprint: prove behavior with
  `bash kit/ops/polaris …`.
- bash 3.2: no `case` inside `$(...)`; split `local` declarations.
- Never run `doctor --selftest` while your kit edits are half-written — a mid-edit suite fabricates
  a different spurious failure each run.
- Long suites FOREGROUND with an explicit ≥600000ms tool timeout (test_fast ≈320s; the 4-drill
  --only above ≈320s too); past the cap → log to a file and poll for `selftest passed`. Never end
  a turn waiting on a notification.
