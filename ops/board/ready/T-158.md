---
id: T-158
title: "Teach the roles — the SKILL.md template, EVOLVE proposes a skill and never promotes one, CONDUCTOR step 7 names next --do, INTEGRATOR lands with amend and learned, PROTOCOL and MANUAL carry the new commands"
type: chore
scope: roles
plan: sprint-c
points: 2
value: 5
time_criticality: 5
risk_opportunity: 2
wsjf: 6.0
risk: normal
model: mid
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/templates/SKILL.md
  - kit/ops/roles/EVOLVE.md
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/roles/INTEGRATOR.md
  - kit/ops/roles/INIT.md
  - kit/ops/roles/PLANNER.md
  - kit/ops/PROTOCOL.md
  - kit/ops/MANUAL.md
  - kit/.claude/skills/polaris/SKILL.md
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/self-skills.md
  - ops/contracts/grant.md
  - ops/contracts/role-handover.md
  - ops/contracts/sprint-report.md
  - ops/contracts/auto-update.md
depends_on: [T-155]
contract: ops/contracts/self-skills.md
verify:
  - test -f kit/ops/templates/SKILL.md
  - grep -q '^# <glob> — what POLARIS already knows$' kit/ops/templates/SKILL.md
  - test "$(grep -c '^## ' kit/ops/templates/SKILL.md)" = 6
  - grep -q 'Promotion is one command' kit/ops/roles/CONDUCTOR.md
  - grep -q 'skill propose' kit/ops/roles/EVOLVE.md
  - grep -q 'skill demote' kit/ops/roles/EVOLVE.md
  - grep -q 'skill promote' kit/ops/roles/EVOLVE.md
  - grep -q 'polaris learned -m' kit/ops/roles/INTEGRATOR.md
  - grep -q 'amend <ID> --verify' kit/ops/roles/INTEGRATOR.md
  - grep -q 'next --do' kit/ops/roles/PLANNER.md
  - grep -q 'polaris learned' kit/ops/roles/INIT.md
  - grep -q 'ops/polaris amend' kit/ops/PROTOCOL.md
  - grep -q 'ops/polaris skill' kit/ops/PROTOCOL.md
  - grep -q 'ops/polaris learned' kit/ops/PROTOCOL.md
  - grep -q -- '--major' kit/ops/PROTOCOL.md
  - grep -q 'amend' kit/ops/MANUAL.md
  - sed -n '3,$p' kit/.claude/skills/polaris/SKILL.md | grep -q 'skill'
  - ! git diff main -- kit/.claude/skills/polaris/SKILL.md | grep -q '^[-+]description:'
  - for f in EVOLVE CONDUCTOR INTEGRATOR INIT PLANNER; do test "$(grep -c '^#' kit/ops/roles/$f.md)" = "$(git show main:kit/ops/roles/$f.md | grep -c '^#')" || exit 1; done
  - test "$(grep -c '^#' kit/ops/PROTOCOL.md)" = "$(git show main:kit/ops/PROTOCOL.md | grep -c '^#')"
  - test "$(grep -c '^#' kit/ops/MANUAL.md)" = "$(git show main:kit/ops/MANUAL.md | grep -c '^#')"
  - bash ops/tests/plain-voice.cmd | diff - ops/tests/plain-voice.expected
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/templates/SKILL.md' | tr -d '\r' | diff - <(grep '^kit/ops/templates/SKILL.md' ops/tests/api-kit.expected)
  - grep -qE 'integrate.sh.fn.cmd_amend' ops/tests/api-kit.expected
  - grep -qE 'integrate.sh.fn.amend_verify' ops/tests/api-kit.expected
  - test "$(wc -l < ops/tests/api-kit.expected | tr -d ' ')" = 693
  - test "$(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/*' | tr -d '\r' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected | grep -c '^<')" = 0
map_delta:
  - "kit/ops/templates/SKILL.md (NEW — the skeleton's static half, seven headings); EVOLVE may propose ONE skill per run and `skill demote` joins its inert allowlist while `skill promote` never does; CONDUCTOR step 7 names `next --do`; INTEGRATOR § 1/§ 6 name `amend` and `learned`; PROTOCOL THE TOOL gains skill/amend/learned/promote rows and the update --major note; MANUAL gains the amend recipe"
---
## Why
Every command this sprint adds is only as real as the prose that names it: a conductor spent a whole
planner context in sprint 14 moving files by hand because CONDUCTOR.md step 7 named no command for
the promote pass. So the roles learn their new lines from the contracts, verbatim: the CONDUCTOR
promotes with `next --do`; the INTEGRATOR amends an unsatisfiable verify line with `amend` and records
a lesson with `learned` instead of hand-editing SPRINT.md; EVOLVE may write ONE skill per run and
propose its promotion — never apply it, because promotion spends every future session's budget and
EVOLVE never self-escalates, while `demote` (free, reversible) may sit in its inert allowlist: the
asymmetry is deliberate and the prose says so. The SKILL.md template is the skeleton's static half,
so the generator and the humans read the same seven headings. PROTOCOL's tool table and MANUAL's
recipes carry the rest. You are the W2 api-kit owner: the template's seven headings are your rows,
T-157's two functions are the sibling rows you write from the contract.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/ops/templates/SKILL.md`: the frontmatter + the seven headings of self-skills § 5, each heading followed by a one-line `<!-- producer -->` comment naming its source; nothing else
- [ ] EVOLVE.md: a "legal targets" bullet for ONE skill per run (`skill gaps` → `skill propose <surface> --write` → write the `description:` TRIGGER/DO NOT TRIGGER sentence ≤ 290 chars and the `## What keeps going wrong` distillation); `skill promote <name>` is ALWAYS a numbered proposal with its `hits/W` evidence line, never applied by EVOLVE; allowlist item 5 = `skill demote <name>` when `prune` says demote; the asymmetry stated in one sentence with "deliberate"
- [ ] CONDUCTOR.md step 7 opens with role-handover.md v2's pinned sentence; PLANNER.md step 13 gains its parenthetical; INTEGRATOR.md § 1 gains grant.md v2's sentence and § 6 gains sprint-report.md v3's; INIT.md's write-routing skeleton row names `seal` and `polaris learned`
- [ ] PROTOCOL.md THE TOOL: rows for `skill …`, `amend`, `learned`, `promote`; the `version / update` row gains auto-update.md v2's `--major` note; TOKEN DISCIPLINE gains one bullet ("a skill `pack` names is read before the first edit")
- [ ] MANUAL.md: the by-hand `amend` recipe beside grant's; `learned` beside the board-commit recipe
- [ ] `kit/.claude/skills/polaris/SKILL.md` body line 3 lists `skill` among the commands; its `description:` byte-identical (827 B)
- [ ] NO new `#` line in any role file, PROTOCOL, MANUAL or the polaris SKILL.md (list items and paragraphs only); `plain-voice` golden byte-identical
- [ ] `ops/tests/api-kit.expected` = the W2 union (key-registry.md § 9): T-157's two integrate.sh rows + your seven template rows, in `find --api` order, 693 lines
- [ ] `polaris verify` green

## Notes
- Role prose co-changes as a trio (BUILDER/SOLO/CONDUCTOR); this task touches CONDUCTOR only and leaves BUILDER/SOLO byte-identical on purpose — the builder-facing behaviour did not change.
- `output-style-installed` pins CLAUDE.md phrases and `plain-voice` diffs PROTOCOL § VOICE — neither section is yours to edit; `kit/CLAUDE.md` is untouched (paid 6-8× a run).
- The index records `#` lines inside fenced blocks too; the template's `<!-- -->` comments are not headings, but a `# ` inside a fence would be.
- Per-lane scratch (`mktemp -d`).
