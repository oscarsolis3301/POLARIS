---
id: T-083
title: "Role prose speaks the plain voice — conductor close, solo report, builder progress line, INIT picker, CLAUDE.md clause"
type: chore
scope: voice
epic: plain-voice
plan: plain-voice
points: 2
value: 5
time_criticality: 5
risk_opportunity: 1
wsjf: 5.5
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/roles/SOLO.md
  - kit/ops/roles/BUILDER.md
  - kit/ops/roles/INIT.md
  - kit/CLAUDE.md
context_files:
  - ops/contracts/output-style.md
  - ops/tests/output-style-installed.cmd
  - ops/tests/api-kit.expected
depends_on: []
contract: ops/contracts/output-style.md
verify:
  - grep -qF '≤8 lines' kit/ops/roles/CONDUCTOR.md
  - grep -qF 'every check passed' kit/ops/roles/CONDUCTOR.md
  - grep -qF 'Plain English' kit/ops/roles/INIT.md
  - grep -qF 'no git/test vocabulary' kit/CLAUDE.md
  - bash ops/polaris check --only output-style-installed
map_delta:
---
## Why
T-082 rewrites what `voice: standard` means; this task makes the role files ASK for it at the spots
where a human actually reads agent prose. Each edit is a small in-line wording change — zero new or
renamed `##`/`###` headings anywhere (ops/tests/api-kit.expected pins every kit heading and this
task does not own it). The register itself lives in ops/contracts/output-style.md § v2 — these
edits point at it, they never restate it.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] CONDUCTOR.md step 8 (≈lines 249-251) first sentence becomes: In `voice:`, **≤8 lines**
      (INIT.md's cap, same reason): what got built and what it means for them, what was set aside
      and why, whether every check passed, one thing to try right now — then EVOLVE's numbered
      proposals
- [ ] CONDUCTOR.md relay example (≈line 146) becomes: `"✅ 2 of 5 done — the new navigation is in,
      every check passed"`
- [ ] SOLO.md report line (≈lines 62-63) becomes: what changed, how you know it works, what you
      left alone — plus a clause that under `standard` this is everyday words, outcomes not git
- [ ] BUILDER.md `✅` progress line (≈line 26) gains: the words follow the repo's `voice:`; in
      panes mode a human reads these live. The four-field handoff report (≈line 49) stays
      machine-shaped — the conductor consumes it, do NOT soften it
- [ ] INIT.md voice-picker (≈line 46) standard option becomes: `**Plain English** — friendly,
      everyday words, nothing technical. *(default)*` — "I explain as we go" is dropped (the new
      row forbids explaining)
- [ ] kit/CLAUDE.md § PROGRESS FORMAT sentence becomes: Keep the shape; the words inside follow
      `voice:` — `standard` = everyday words, no git/test vocabulary. EXACTLY this one clause and
      nothing more — this file is paid 6-8x per run
- [ ] pinned literals survive in kit/CLAUDE.md: `🎉 Complete!` and `subagent never ends a run`
      (ops/tests/output-style-installed.cmd greps both)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Line numbers are anchors, not gospel — match the quoted sentence if the file has drifted a line
  or two. Wording only; no behavior, no step reordering, no heading changes.
