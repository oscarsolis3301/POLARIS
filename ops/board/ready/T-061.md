---
id: T-061
title: "The sprint-report writer commits its own file when it is the only dirt"
type: feature
scope: report
epic: n-chats-one-repo
plan: n-chats-one-repo
points: 2
value: 5
time_criticality: 3
risk_opportunity: 5
wsjf: 6.5
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/knowledge.sh
  - kit/ops/lib/selftest/report.sh
context_files:
  - ops/contracts/sprint-report.md
  - ops/contracts/shared-checkout.md
depends_on: []
contract: ops/contracts/sprint-report.md
verify:
  - bash -n kit/ops/lib/knowledge.sh
  - bash -n kit/ops/lib/selftest/report.sh
  - bash kit/ops/polaris doctor --selftest --only report
map_delta:
---
## Why
Contract sprint-report v1.1 documented this failure instead of fixing it: a re-render after `done`
leaves `docs/sprints/sprint-<n>.md` dirty on `<base>`, and the NEXT land/seal dies "working tree not
clean" with no visible cause. In a one-chat world the hint was enough; with N chats, session B pays
for session A's forgotten hint. v2 (this task) kills it at source: after writing, `cmd_report`
SELF-COMMITS its file(s) when — and only when — it runs in the primary checkout, on `$BASE`, and
`git status --porcelain` shows NOTHING but the report file(s) this invocation just wrote. Subjects:
`docs(sprint-<n>): report refresh` (single sprint) · `docs(sprint): report refresh --all` (--all).
Any other dirty path → commit NOTHING, print v1.1's two-remedy hint verbatim, exactly as today.
Seal's own report commit on integrate/<date> is untouched. The `report` drill gains both sides:
only-dirt → commit exists with the pinned subject and the tree is clean; mixed-dirt → no commit,
hint printed.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] only-dirt case on $BASE in the primary: report writes, commits with the pinned subject, says
      what it committed, leaves a clean tree
- [ ] mixed-dirt case: no commit, v1.1 hint text unchanged, the foreign dirty path untouched
- [ ] off-$BASE, in a worktree, or at seal time: behavior byte-identical to today (no new commit path)
- [ ] `--all` commits once with subject `docs(sprint): report refresh --all` in the only-dirt case
- [ ] the `report` drill (selftest/report.sh) asserts both the only-dirt commit and the mixed-dirt
      hint, and leaves its throwaway repo assertions-clean
- [ ] no new top-level function in knowledge.sh — inline the porcelain check (the api-kit surface
      golden is frozen this wave; T-057 owns its only delta)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- The relevant seam is report_dirty_hint / report_one / cmd_report (kit/ops/lib/knowledge.sh:142-177);
  seal_report_commit must stay untouched.
- The installed `ops/polaris` lags your kit edits all sprint: prove behavior with
  `bash kit/ops/polaris …`.
- bash 3.2: no `case` inside `$(...)`; split `local` declarations.
- Never run `doctor --selftest` while your kit edits are half-written — a mid-edit suite fabricates
  a different spurious failure each run.
- Long suites FOREGROUND with an explicit ≥600000ms tool timeout (test_fast ≈320s); past the cap →
  log to a file and poll for `selftest passed`. Never end a turn waiting on a notification.
