---
id: T-065
title: "tier_for + `polaris route` + fleet --model + pack tier + finish bg-guard — routing becomes code"
type: feature
scope: cli
epic: routing-and-bg
plan: routing-and-bg
points: 5
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 3.6
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/core.sh
  - kit/ops/lib/observe.sh
  - kit/ops/lib/builder.sh
  - kit/ops/polaris
  - ops/tests/api-kit.expected
context_files:
  - ops/contracts/model-routing.md
  - ops/contracts/bg-jobs.md
  - ops/contracts/module-layout.md
  - ops/board/done/T-062.md
depends_on: []
contract: ops/contracts/model-routing.md
verify:
  - bash -n kit/ops/polaris
  - bash -n kit/ops/lib/core.sh
  - bash -n kit/ops/lib/observe.sh
  - bash -n kit/ops/lib/builder.sh
  - bash kit/ops/polaris route --points 5 --risk normal | head -1 | grep -qx strong
  - bash kit/ops/polaris route --points 3 --risk normal | head -1 | grep -qx mid
  - bash kit/ops/polaris route --points 1 --risk normal | head -1 | grep -qx cheap
  - bash kit/ops/polaris route --points 2 --risk high | head -1 | grep -qx strong
  - bash kit/ops/polaris route --role PLANNER | head -1 | grep -qx strong
  - grep -qF -- '--model' kit/ops/lib/observe.sh
  - grep -qF "· tier" kit/ops/lib/builder.sh
  - grep -qF 'background job' kit/ops/lib/observe.sh
  - grep -q tier_for ops/tests/api-kit.expected
  - grep -q cmd_route ops/tests/api-kit.expected
  - grep -qF 'LONG COMMANDS' ops/tests/api-kit.expected
map_delta: "polaris gains route [<ID>|--role R|--points N --risk R] — mechanical model tier (line 1 bare word, optional model: note from CONVENTIONS knobs); fleet injects --model (max tier over ready, tmux + wt.exe tokens); pack header gains · tier; finish pends on running bg jobs (5.24.0-unreleased)"
---
## Why
`PROTOCOL.md § MODEL ROUTING` already states the rule (strong models where mistakes multiply, cheap
where they don't), but it is prose: nothing computes a tier and nothing passes a model, so every
conductor spawn and every fleet pane runs on whatever the platform defaults to. This task makes the
rule executable. `tier_for <points> <risk>` and `model_for_tier <tier>` go in core.sh (tiny and
fork-free — core.sh rides the write-guard's hot path); `cmd_route` goes in observe.sh beside
`cmd_triage` and answers with a machine-parseable bare tier word, plus a `   model: <name>` note
line only when the repo's CONVENTIONS maps that tier to a real model. Three consumers wire in:
`fleet` injects `--model <name>` into both the tmux command and the wt.exe pane token list built by
`find_claude_windows` (MAX tier over ready tasks — panes claim racily, any pane may get any task;
`--dry-run` previews it), `pack`'s task header gains `· tier <t>`, and `finish` gains the bg-jobs
pending line (per `ops/contracts/bg-jobs.md` § finish — it simply never fires until T-070's bg.sh
exists, because no `.polaris/bg/` job dirs exist). Dispatch + usage entries for `route` go in
kit/ops/polaris. Everything is spec'd in `ops/contracts/model-routing.md` — code to it, invent
nothing.

This task also owns the wave's `ops/tests/api-kit.expected` delta (the surface golden — one owner
per wave, the Learned-log rule): its own three fn lines PLUS the PROTOCOL heading swap that T-066
makes in a parallel lane. Both heading texts are pinned verbatim in the contract § Surface pins, so
the two lanes agree without touching each other's files.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `tier_for` + `model_for_tier` in core.sh exactly per contract (risk≠normal → strong · ≥5 →
      strong · ≤1 → cheap · else mid; empty/non-numeric points → mid, never an error)
- [ ] `cmd_route` in observe.sh: line 1 always one bare tier word; `   model:` note ONLY when the
      winning tier's knob is set; precedence flags → --role → ID (frontmatter `model:` override:
      tier word or literal name); bare `route` → ⛔ usage rc 1; unknown ID → ⛔ rc 1
- [ ] `route` dispatch + usage entry in kit/ops/polaris (cli-help goldens do NOT move — they record
      the INSTALLED 5.23.0 CLI and regenerate at dogfood)
- [ ] fleet: `--model` in tmux AND wt.exe pane paths; max tier over ready/; `--dry-run` shows it;
      knob unset → command byte-identical to today
- [ ] pack header carries `· tier <t>` for the packed task
- [ ] finish: pending line for any rc-less `.polaris/bg/<name>/` job dir, wording per bg-jobs.md
- [ ] api-kit.expected: hand-authored delta = `tier_for` + `model_for_tier` + `cmd_route` fn lines
      + the heading swap + `LONG COMMANDS` line, byte-exact in sorted position (recipe in Notes);
      NO top-level functions beyond the pinned three
- [ ] `route` stays read-only: no lock, no board write, no hook fire

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- The installed `ops/polaris` is 5.23.0 all sprint: `route` does not exist at runtime — prove every
  new behavior with `bash kit/ops/polaris …`; board mechanics (claim/verify/handoff) via the
  installed CLI as usual.
- api-kit golden, worktree-local proof (T-062, proven): `POLARIS_ROOT=<your worktree> python
  ops/index.py` and emit the `--api 'kit/*'` shape, byte-exact diff vs your hand-authored delta —
  em-dashes normalize to `-`, lines sort within a file. `check`/`find` are PRIMARY-anchored, so
  never use `polaris check` to prove your own golden; the wave gate's uat is the byte-exact referee.
- Long suites FOREGROUND with an explicit ≥600000ms tool timeout (spine 144s, ~44s per drill); past
  the cap → log to a file and poll for `selftest passed` INSIDE your turn — never end a turn
  waiting on a notification.
- Never run `doctor --selftest` while your kit edits are half-written — a mid-edit suite fabricates
  a different spurious failure each run.
- bash 3.2: no `case` inside `$(...)`, no mapfile/assoc arrays, split `local` declarations
  (one per line when a value derives from an earlier one).
- Verify runs while wave-1 siblings are unlanded: the primary's CONVENTIONS has no model knobs yet,
  so `route` prints tier words only — every verify line above asserts line 1 only, on purpose.
