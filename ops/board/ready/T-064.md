---
id: T-064
title: "The board mutex learns ownership — pid-guarded mutex_off, no more cross-session lock eating"
type: bug
scope: core
epic: n-chats-one-repo
plan: n-chats-one-repo
points: 2
value: 8
time_criticality: 8
risk_opportunity: 3
wsjf: 9.5
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/core.sh
context_files:
  - ops/contracts/shared-checkout.md
  - kit/ops/lib/workspace.sh
depends_on: []
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/core.sh
  - bash -c 'T=$(mktemp -d); MUTEX="$T/.board-mutex"; mkdir -p "$MUTEX"; date +%s > "$MUTEX/epoch"; echo 999999 > "$MUTEX/pid"; . kit/ops/lib/core.sh; mutex_off; test -d "$MUTEX"'
  - bash -c 'T=$(mktemp -d); MUTEX="$T/.board-mutex"; mkdir -p "$MUTEX"; date +%s > "$MUTEX/epoch"; echo $$ > "$MUTEX/pid"; . kit/ops/lib/core.sh; mutex_off; test ! -d "$MUTEX"'
  - bash -c 'T=$(mktemp -d); MUTEX="$T/.board-mutex"; mkdir -p "$MUTEX"; . kit/ops/lib/core.sh; mutex_off; test -d "$MUTEX"'
  - bash -c 'T=$(mktemp -d); LOCKS="$T"; MUTEX="$T/.board-mutex"; . kit/ops/lib/core.sh; mutex_on; test -s "$MUTEX/pid"'
  - bash kit/ops/polaris doctor --selftest --only grant
map_delta:
---
## Why
The integrator's audit of T-058 found the one lock in the kit without an ownership check — and it
is the board's most important one. `mutex_off` (core.sh:167) is an unconditional `rm -rf "$MUTEX"`;
`mutex_on` writes only `epoch`, so nothing records WHOSE mutex it is; and `on_die`'s EXIT trap is
never disarmed. Pre-existing in main — but T-057 armed `trap on_die EXIT` for the whole integration
lease lifetime, so under N chats ANY process exit (a finished land, a died command, a Ctrl-C) can
now delete a board mutex a DIFFERENT session legitimately holds, un-serializing two board mutations
mid-flight. The lease already got this right (pid file, pid-guarded release — workspace.sh); this
task makes the board mutex mirror it, per shared-checkout v1.1: `mutex_on` writes `$MUTEX/pid`
beside `epoch`; `mutex_off` becomes a NO-OP unless that pid exists and matches the calling process
(foreign, missing or unreadable → leave it); removing one's OWN mutex is byte-identical to today.
The waiter-side staleness steal in `mutex_on` (epoch age > 120s) stays exactly as is — deliberately
pid-blind, it IS the crashed-holder recovery. No trap rewiring: the pid guard is what makes the
existing trap safe.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `mutex_on` writes `$MUTEX/pid` containing the acquiring process's pid, beside `epoch`
- [ ] `mutex_off` with a foreign pid leaves the mutex directory untouched
- [ ] `mutex_off` with the calling process's own pid removes it, as today
- [ ] `mutex_off` with a missing or unreadable pid file leaves the mutex in place (the staleness
      steal in `mutex_on` remains the only recovery for legacy/crashed mutexes)
- [ ] the staleness steal (age > 120s → remove + retake) is byte-identical to today
- [ ] `on_die` needs no edit — its `mutex_off` call is now safely ownership-guarded
- [ ] NO new top-level function in core.sh — the guard is inlined in `mutex_off`/`mutex_on`
      (the kit surface golden is frozen until T-062's delta)
- [ ] every board-mutating command still round-trips (the grant drill exercises the full
      mutex_on → mutate → board_commit → mutex_off path)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Mirror, don't invent: the lease's pid guard in kit/ops/lib/workspace.sh is the pattern — same
  shape, board-mutex names.
- `rm -rf` only after the pid check passes; the check-then-remove race is safe because only the
  owning process takes that branch.
- The installed `ops/polaris` lags your kit edits all sprint: prove behavior with
  `bash kit/ops/polaris …` and the inline assertions above.
- bash 3.2: no `case` inside `$(...)`; split `local` declarations.
- Never run `doctor --selftest` while your kit edits are half-written — a mid-edit suite fabricates
  a different spurious failure each run.
- Long suites FOREGROUND with an explicit ≥600000ms tool timeout (the --only grant run ≈190s);
  past the cap → log to a file and poll for `selftest passed`. Never end a turn waiting on a
  notification.
