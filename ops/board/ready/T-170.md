---
id: T-170
title: "The golden that walks it — shots-gallery proves the slug, the three-place search, before-and-after, the escape hatch, the index and the published gallery, hermetically"
type: test
scope: tests
plan: gallery-and-bar
points: 5
value: 8
time_criticality: 5
risk_opportunity: 8
wsjf: 4.2
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - ops/tests/shots-gallery.cmd
  - ops/tests/shots-gallery.expected
approved:
context_files:
  - ops/contracts/visual-check.md
  - ops/tests/pack-visual.cmd
  - ops/tests/keys-drift.cmd
depends_on: [T-169]
contract: ops/contracts/visual-check.md
verify:
  - bash -n ops/tests/shots-gallery.cmd
  - bash ops/tests/shots-gallery.cmd 2>/dev/null | diff - ops/tests/shots-gallery.expected
  - grep -q 'mktemp -d' ops/tests/shots-gallery.cmd
  - grep -q 'landing: integrator' ops/tests/shots-gallery.cmd
  - grep -q 'gallery: docs/screens' ops/tests/shots-gallery.cmd
  - test "$(grep -c 'POLARIS_AWAKE_HOME' ops/tests/shots-gallery.cmd | tr -d ' ')" = 1
  - test "$(grep -cE '\$\(pwd\)/ops/board|/c/Users|C:\\\\Users' ops/tests/shots-gallery.cmd | tr -d ' ')" = 0
map_delta:
surface:
  - kit/ops/lib/visual.sh tests: ops/tests/shots-gallery.* cmd: bash ops/polaris check --only shots-gallery note: the shots tree, the before/after gate, the index and the gallery publish
---
## Why
Four waves of code have shipped a feature that THIS repo never runs: POLARIS sets no `visual:` key,
so every line of it is code only other people's repos execute, and the first they would hear of a
regression is a broken page. `ops/tests/pack-visual.*` already proves the section and the gate; this
golden proves everything the last three waves added, end to end, in a throwaway repo — and once
written it costs a subprocess forever instead of a subagent.

Follow the `pack-visual` fixture pattern exactly: ONE repo under `mktemp -d`, carrying its own
CONVENTIONS, its own board and its own tasks, with the CLI run from INSIDE it so POLARIS anchors to
the fixture as PRIMARY. Nothing may read the live board, config, registry or shots directory — the
golden must be byte-identical on a second run from any board state. `POLARIS_AWAKE_HOME` points at
the fixture so no daemon touches the real machine, and `landing: integrator` is load-bearing: under
the default `landing: self` a passing handoff would go on to take the integration lease and land, and
you would be testing the integrator instead of the thing you wrote.

`ops/contracts/visual-check.md` § v2 § 13 lists what to assert.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] A `screen:` on a task produces the slugged shotdir, and `pack` prints it.
- [ ] `screen:` unset files under `misc/` and `pack` says so.
- [ ] A traversal-shaped `screen:` (`../../etc`) does not escape `.polaris/shots/` — it falls back to `misc/`.
- [ ] ONE fresh capture is REFUSED with the two-capture sentence, rc 1, and the task stays in `active/`.
- [ ] TWO fresh captures plus `--saw "…"` pass, rc 0, task in `review/`.
- [ ] Two captures with NO `--saw` are refused with the pinned sentence, rc 1.
- [ ] `--no-before "<why>"` with one capture plus `--saw` passes, and the reason appears in the caption file.
- [ ] A FLAT `.polaris/shots/<ID>-*.png` — what a pre-6.6 `shot:` line writes — counts toward the gate, and after the handoff it has been FILED into the shotdir. This is the backward-compatibility assertion and it is the most important one in the file.
- [ ] `.polaris/shots/INDEX.md` contents are pinned: the screen heading, the task heading, the caption text, and the before/after table.
- [ ] `polaris shots` output is pinned.
- [ ] After `done`, `docs/screens/` holds the curated image and caption, `docs/screens/INDEX.md` exists, and the base branch carries a `docs(screens):` commit.
- [ ] Every path the golden prints is repo-relative. The one machine-specific byte any of this could emit is the `mktemp` path — carry `pack-visual`'s `N()` normaliser so a future absolute path cannot quietly make this golden machine-bound.

## Notes
- **A NEW golden is invisible to its own author's verify.** `cmd_check` is PRIMARY-anchored: a pair
  born in a worktree matches nothing in the primary's `ops/tests/` and `check --only shots-gallery`
  passes VACUOUSLY with `no goldens matched`. That is why `verify:` runs the `.cmd` file directly and
  diffs it. Run it with `2>/dev/null` so it matches what `check` will do (stderr is not captured).
- **For the Integrator, after this lands:** run `check --only shots-gallery`, then SABOTAGE it — make
  a real edit, read the diff to confirm the edit took, watch it go red — and restore it green. A
  golden nobody has seen fail is not evidence. An off-by-one `sed` is a no-op whose restore-green is
  vacuous; verify the edit landed before believing the red.
- **Per-lane scratch.** Namespace any scratch path under your own ID. Never delete a shared scratch
  path another lane might be standing in — one lane's `rm -rf` of a bare shared path once deleted
  another lane's in-use fixture mid-selftest and fabricated a failure that read exactly like a
  regression. A failure point that MOVES between runs is corruption, not code.
- Budget: `pack-visual` costs about 28s because its asserts are real `handoff` runs. This one walks
  further, so expect more. That is the right trade — a mocked gate proves nothing about the gate —
  but keep the fixture to ONE repo and reuse it across states, the way `keys-drift` does.
- The `surface:` row on this task is the sprint's only one, and it lands LAST on purpose: a row
  written earlier would have demanded that every wave touching `visual.sh` also touch a golden that
  did not exist yet, and would have blocked its own chain.
