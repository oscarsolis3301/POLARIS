---
id: T-020
title: quiet board core — board_commit/sync_board target refs/heads/polaris/board
type: feature
scope: cli
points: 5
value: 13
time_criticality: 8
risk_opportunity: 8
wsjf: 5.8
risk: normal
status: ready
owner: null
branch: null
plan: one-pr-clean-graph
files_owned:
  - kit/ops/polaris
context_files:
  - ops/contracts/quiet-board.md
  - ops/contracts/clean-history.md
depends_on: []
contract: ops/contracts/quiet-board.md
verify:
  - grep -q "polaris/board" kit/ops/polaris
  - grep -q "GIT_INDEX_FILE" kit/ops/polaris
  - grep -q "docs(map)" kit/ops/polaris
  - bash kit/ops/polaris doctor --selftest
map_delta: board state lives on refs/heads/polaris/board — board_commit/sync_board use secondary-index plumbing; base first-parent stays chore(board)-free; done's map_delta is a docs(map) base commit (5.14.0-unreleased)
---
## Why
Every claim/handoff/done/release/grant/kickback today commits `chore(board): …` on the base branch
(~3 chores per task), and `sync_board` pushes base after each one — so a hosted repo's history
drowns in board noise, and on a protected main every push fails and the whole board stops syncing.
This is the prerequisite for PR-based publishing: board mutations move to a dedicated ref,
`refs/heads/polaris/board`, committed via secondary-index git plumbing (files stay at their on-disk
paths; no second worktree, no branch switching), and base first-parent stays clean product history.
Everything is specified in ops/contracts/quiet-board.md — implement it, do not redesign it.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] board_commit commits ONLY the moved set (ops/board/** + ops/SPRINT.md) to refs/heads/polaris/board via GIT_INDEX_FILE plumbing per contract: no second worktree, no checkout; working tree and primary index untouched; commit subjects unchanged; contention retry kept.
- [ ] sync_board pushes polaris/board (never $BASE), bounded retry; on rejection it union-appends missing remote EVENTS.ndjson lines, re-parents on the fetched tip, retries — per contract.
- [ ] init-board appends .gitignore entries `ops/board/` + `ops/SPRINT.md`; the branch is created (orphan) by the first board_commit.
- [ ] All board-file state transitions use plain `mv` — no `git mv` on board paths remains (claim, handoff, release, kickback, done).
- [ ] done: non-empty map_delta becomes ONE separate `docs(map): <ID> <first delta line>` commit on $BASE; empty map_delta commits nothing on $BASE; no other board mutation commits on $BASE.
- [ ] Existing selftest drills that assert chore(board) commits on the scratch base (grant drill exact-subject check, seal subjects, history filter fixtures — drill region ~:1149-1550) are re-targeted to polaris/board and stay green.
- [ ] New drill `quiet-board`: scratch repo → claim → handoff → land → seal → done → polaris/board log carries the chore(board) commits · base first-parent gains ZERO chore(board) commits · a fixture map_delta yields exactly one docs(map) commit on base.
- [ ] seal's clean-tree gate stays satisfiable throughout the drill flow (moved set ignored on base, never dirty).
- [ ] Bash 3.2 compatible (NO `case` inside `$(...)` — hard parse error, see SPRINT Learned), POSIX awk, no new dependencies; full selftest green.

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Chain head on the hotspot file: T-020 → T-021 → T-022 → T-023 → T-024 all edit kit/ops/polaris — never parallel.
- Landmarks: board_commit :163 · sync_board :154 · cmd_init_board :349 · cmd_done map_delta :699-717 · `git mv` sites :420 :478 :512 :650 :702 · grant subject assert :1467 · drill region :1149-1550.
- claim/resume primary-anchored OUTPUT paths and the upgrade migration belong to T-021, NOT here.
- `git update-index --add` bypasses gitignore — that is why the plumbing path works with the set ignored on base.
- verify runs the KIT script's selftest (`bash kit/ops/polaris doctor --selftest`) — your new drills run pre-dogfood; the installed ops/polaris stays 5.13 for this board.
