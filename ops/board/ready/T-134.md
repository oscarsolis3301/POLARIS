---
id: T-134
title: "The surface map's data plane — SURFACES.tsv, the test_select key, and the pure functions every gate reads"
type: feature
scope: surfaces
plan: spend-less
points: 5
value: 8
time_criticality: 13
risk_opportunity: 5
wsjf: 5.2
risk: normal
model:
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/core.sh
  - kit/ops/lib/admin.sh
  - kit/ops/polaris
  - kit/ops/KEYS.tsv
  - ops/SURFACES.tsv
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/test-surfaces.md
  - kit/ops/lib/ownership.sh
  - ops/tests/startup-budget.cmd
  - ops/contracts/key-registry.md
  - kit/ops/lib/selftest/fast.sh
depends_on: []
contract: ops/contracts/test-surfaces.md
verify:
  - bash -n kit/ops/lib/core.sh && bash -n kit/ops/lib/admin.sh && bash -n kit/ops/polaris
  - bash -c 'POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; for f in surfaces_lines surfaces_seed surface_row_matches surface_rows_for surface_row_from_item surface_change_set surface_select_cmd suite_stamp_scope; do type "$f" >/dev/null 2>&1 || exit 1; done'
  - bash -c 'POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; SURFACES=ops/SURFACES.tsv; test -z "$(surfaces_lines)"'
  - bash -c 'POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; test "$(surface_row_from_item "src/api/ tests: tests/api/ cmd: pytest tests/api note: the API" T-9 t)" = "$(printf "src/api/\ttests/api/\tpytest tests/api\tthe API [T-9]")"'
  - bash -c 'POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; test "$(surface_row_from_item "src/api/ tests: tests/api/" T-9 "My title")" = "$(printf "src/api/\ttests/api/\t-\tMy title [T-9]")"'
  - bash -c 'POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; surface_row_from_item "src/api/ cmd: x" T-9 t >/dev/null; test $? = 1'
  - bash -c 'f=$(mktemp); printf "abc1234 1700000000\n" > "$f"; POLARIS_TAB="$(printf "\t")"; . kit/ops/lib/core.sh; test "$(suite_stamp_scope "$f")" = full; printf "abc1234 1700000000 scoped\n" > "$f"; test "$(suite_stamp_scope "$f")" = scoped'
  - test "$(grep -c '_RULES_CACHED' kit/ops/lib/core.sh)" = 3
  - awk -F'\t' '$1=="test_select" && $2=="6.4.0"' kit/ops/KEYS.tsv | grep -q .
  - bash kit/ops/polaris help | grep -q '^  surfaces '
  - bash kit/ops/polaris help | grep -q '^  qa \[--force\] \[--full\]'
  - bash kit/ops/polaris help | grep -c '1 task ≤3pts' | grep -qx 1
  - grep -q '^SURFACES="\$OPS/SURFACES.tsv"' kit/ops/polaris
  - grep -q 'surfaces)   cmd_surfaces;;' kit/ops/polaris
  - grep -q 'surfaces_seed' kit/ops/lib/admin.sh
  - test -f ops/SURFACES.tsv && head -1 ops/SURFACES.tsv | grep -q '^# POLARIS SURFACES' && ! grep -v '^#' ops/SURFACES.tsv | grep -q .
  - POLARIS_ROOT=$PWD python kit/ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
  - bash kit/ops/polaris doctor --fast | tail -1 | grep -q 'fast tier passed'
map_delta: "core.sh gains the surface-map data plane (surfaces_lines/seed, surface_row_matches/rows_for/row_from_item, surface_change_set/select_cmd, suite_stamp_scope) reading ops/SURFACES.tsv + CONVENTIONS test_select:; KEYS gains test_select; init-board seeds ops/SURFACES.tsv"
---
## Why
POLARIS runs the whole 12-minute test suite for a one-line change, and nothing checks that a
feature's tests moved when the feature did. Both fixes need the same fact — which tests cover which
source paths — so this task lays down the ONE data file that answers it, `ops/SURFACES.tsv`
(TAB-separated, modeled on `ops/RULES.tsv`), the `test_select:` CONVENTIONS key that switches
change-scoped selection on, and the small pure functions every later gate reads through. Nothing
here changes behavior on its own: with the key unset and the file header-only, every command is
byte-identical to today. That is deliberate — it is what lets the next wave build the gate, the
selection and the health check in parallel against these names.

Read `ops/contracts/test-surfaces.md` § 1–4 and § 11 first; every name, signature, message and the
seeded header are pinned there. You are also this wave's single owner of the derived golden
`ops/tests/api-kit.expected`: write the nine pinned rows (eight fns + the KEYS key) by content
diff, sorted into place. `cli-help.expected` is NOT yours — it regenerates at the release dogfood.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/ops/lib/core.sh` defines exactly the eight pinned functions with the pinned rc/stdout semantics (contract § 4); `surfaces_lines` memoizes on `_SURFACES_CACHED`/`_SURFACES_CACHE` and never touches `_RULES_CACHED` (startup-budget `rules-lines-memoized 3` unchanged); `surface_row_matches` uses args only — no pipe into `owned_match`/`match_one`
- [ ] `surface_change_set`: on a non-base branch prints `$BASE...HEAD` paths minus the three board-noise paths; on base uses an ancestor stamp as baseline (rc 0, `SURFACE_BASELINE`/`SURFACE_BASELINE_SCOPE` set) and is rc 1 for `--force`, no stamp, or a non-ancestor sha — prove each case in a scratch repo and record it in Notes
- [ ] `surface_select_cmd`: dedupes complete cmds in first-appearance order, folds `-` rows into ONE `{tests}`-substituted template line, and returns rc 1 with the pinned reason for `test_select unset` · `no rows` · `no changed paths` · `unmapped: <p> (+n more)`
- [ ] `kit/ops/polaris`: `SURFACES="$OPS/SURFACES.tsv"` directly under `RULES=`; dispatch `surfaces)   cmd_surfaces;;` (no update_check_maybe); the three usage hunks byte-exact per contract § 4, `surfaces` directly after `rules`; loader lists untouched; `startup-budget` stays green (no `$(cfg ` in the globals block)
- [ ] `kit/ops/KEYS.tsv`: the pinned `test_select` row (TAB-separated), placed after `test_fast`; `adopt-stub` and `keys-drift` goldens unchanged and green
- [ ] `cmd_init_board` calls `surfaces_seed` after the RULES seed, and the seeded RULES header gains the ONE commented `#ops/SURFACES.tsv<TAB>path<TAB>-<TAB>…` example line (commented, never armed)
- [ ] `ops/SURFACES.tsv` exists in this repo = the seeded header, no rows
- [ ] `ops/tests/api-kit.expected` carries exactly the nine W1 rows pinned in contract § 11, refreshed BY CONTENT DIFF with the worktree-honest `index.py` recipe; an unexpected hunk is a STOP, not a refresh
- [ ] `bash kit/ops/polaris doctor --fast` green; `bash kit/ops/polaris check --only startup-budget,adopt-stub,keys-drift,cli-help-parity` green after landing (integrator re-proves — goldens are primary-anchored and pass vacuously in a worktree)
- [ ] bash 3.2 clean: no `case` inside `$(...)`, no `mapfile`; `bash -n` on every touched script

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Run long suites via `bash ops/polaris bg run test` + chunked `bg wait --max 300`; never a foreground call over 600s. Scratch paths under `scratchpad/T-134/…` only — never a shared scratch path.
- `check` is primary-anchored: a golden edited in this worktree is invisible to `polaris check` here; the `index.py` verify line is the honest local diff.
