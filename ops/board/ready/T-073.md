---
id: T-073
title: "finish's bg guard pends forever on .prev archives — scan live job dirs only"
type: bug
scope: cli
epic: routing-and-bg
plan: routing-and-bg
points: 1
value: 5
time_criticality: 5
risk_opportunity: 3
wsjf: 13.0
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/observe.sh
context_files:
  - ops/contracts/bg-jobs.md
  - kit/ops/lib/bg.sh
depends_on: []
contract: ops/contracts/bg-jobs.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - awk '/^cmd_finish/,/^}/' kit/ops/lib/observe.sh | grep -qF '.prev'
  - awk '/^cmd_finish/,/^}/' kit/ops/lib/observe.sh | grep -qF 'background job'
map_delta:
---
## Why
Found by T-071's builder, outside its ownership, recorded in its Notes. `cmd_finish`'s bg guard
(T-065) loops every `.polaris/bg/*/` dir and pends any one without an `rc` file. But rotation
archives a job by MOVING its dir to `<name>.prev` — and a `--force`-killed job's archive has no
`rc` by design. Result: one `--force` replace leaves a permanent rc-less `.prev`, the guard pends
it forever, and `finish` can never exit 0 again — a false wall on the run-over gate. `bg_status`
and `sweep` both already skip `*.prev`; the finish guard is the one scanner that does not. Fix:
skip `*.prev` dirs in the guard's loop — archives are history, never pending. Live rc-less dirs
keep pending exactly as before (alive pid → "still running" line; dead pid → the "crashed?"
wording). Contract updated to match: `ops/contracts/bg-jobs.md` § v1.2 — v1's "any job dir with no
rc" prose is what the current code faithfully implements, so the spec moves with the fix.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] the guard's loop skips `*.prev` dirs: an rc-less archive NEVER produces a pending line
- [ ] live rc-less dirs still pend, both wordings intact (alive → still-running + `bg wait`;
      dead → crashed? + `bg status`)
- [ ] NO top-level function added or removed (the fix lives inside `cmd_finish` — api-kit's
      surface golden is not yours) and no other `cmd_finish` gate or wording changes
- [ ] worktree-local fixture demo pasted into Notes: `.polaris/bg/t073fx.prev/` (empty, no rc) →
      finish output contains NO `t073fx` line; `.polaris/bg/t073live/` with `pid` = your live
      shell's pid and no rc → finish output pends `t073live`; both fixture dirs removed after

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Demo recipe (fixtures land in the PRIMARY's gitignored `.polaris/bg/`, harmless, clean up after):
  `mkdir -p .polaris/bg/t073fx.prev` · `mkdir -p .polaris/bg/t073live && echo $$ >
  .polaris/bg/t073live/pid` · `bash kit/ops/polaris finish 2>&1 | grep t073` · expect ONLY the
  t073live line · `rm -rf .polaris/bg/t073fx.prev .polaris/bg/t073live`.
- Mid-wave `finish` is rc 1 on board gates long before qa, so the demo is fast — but give the call
  a generous explicit timeout anyway per ops/PROTOCOL.md § LONG COMMANDS, and pipe it as above so
  the rc 1 does not trip a chained command.
- Do NOT touch kit/ops/lib/selftest/policy.sh — T-071 (in review) owns it; the drill_bg case for
  .prev-skip is that lane's follow-up, not yours.
- The installed `ops/polaris` is 5.23.0: prove behavior with `bash kit/ops/polaris finish`, never
  the installed copy.
