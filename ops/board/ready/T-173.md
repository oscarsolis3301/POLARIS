---
id: T-173
title: "model-guard v2 — the model ban reads a tiny per-session file instead of scanning the transcript, checks every spawn, and stops failing open when many agents start at once"
type: feature
scope: hooks
plan: v4-fast
points: 5
value: 13
time_criticality: 13
risk_opportunity: 8
wsjf: 6.8
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/hooks/model-guard.sh
  - kit/ops/bootstrap.py
  - kit/ops/bench.sh
  - ops/tests/model-guard-v2.cmd
  - ops/tests/model-guard-v2.expected
  - ops/tests/machine-armed.cmd
  - ops/tests/machine-armed.expected
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/model-routing.md
  - ops/tests/checkout-guard-denies.cmd
  - ops/tests/awake-hook.cmd
depends_on: []
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/hooks/model-guard.sh
  - bash -n kit/ops/bench.sh
  - python -c "import ast; ast.parse(open('kit/ops/bootstrap.py', encoding='utf-8').read())"
  - bash ops/tests/model-guard-v2.cmd 2>/dev/null | diff - ops/tests/model-guard-v2.expected
  - grep -q 'claude-code-guide' ops/tests/model-guard-v2.expected
  - grep -q 'mktemp -d' ops/tests/model-guard-v2.cmd
  - bash ops/tests/machine-armed.cmd 2>/dev/null | diff - ops/tests/machine-armed.expected
  - grep -q 'availableModels' ops/tests/machine-armed.expected
  - bash ops/tests/perm-tools.cmd 2>/dev/null | diff - ops/tests/perm-tools.expected
  - bash kit/ops/bench.sh guards | awk -F'\t' '$1=="model-guard"{s=$4} END{exit s!="ok"}'
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta: kit/ops/hooks/model-guard.sh v2 — also handles SessionStart, PreModelSwitch, PostModelSwitch and PostToolUse(Agent); the per-call verdict reads ~/.claude/polaris/model-state/<session_id>; bench.sh gains a `guards` mode; new golden ops/tests/model-guard-v2
surface:
  - kit/ops/hooks/model-guard.sh tests: ops/tests/model-guard-v2.* cmd: bash ops/polaris check --only model-guard-v2 note: the per-call model ban and its spawn, switch and caveat layers
---
## Why
The model guard runs before EVERY tool call on this machine, in every project. Today it works out
which model the session is on by scanning the last 64 KB of the conversation transcript in pure
bash, which costs 0.5–2.3 s per call. When a conductor starts several agents at once, those scans
pile up past the hook's 5-second limit, and a hook that times out lets the call through — so the
ban on Fable and Haiku is not enforced at exactly the moment it matters most. It also cannot see a
subagent's own model, so the built-in `claude-code-guide` agent (pinned to Haiku) slips past it.

This task makes the common case a single small file read. New hook branches record the model in
`~/.claude/polaris/model-state/<session_id>` when a session starts and whenever the model is
switched; every tool call then reads that one line. Only when the file is missing does the guard fall
back to the transcript, and then with one `grep` instead of a bash loop. Subagent calls read their
OWN transcript. Spawns are checked at the door: a requested Fable or Haiku model, or the
`claude-code-guide` agent type, is refused before anything starts. A switch to a forbidden model is
refused, and if a subagent still somehow ran on one, the parent is told to discard its result. The
installer registers the new events and writes `availableModels` so the harness itself excludes Fable.

Everything is specified in `ops/contracts/speed.md` § 1 and § 2 — the exact messages, the order of
checks, the state-file rule and the bench output. Haiku stays refused everywhere this sprint.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] PreToolUse with a state file naming an allowed model exits 0 without reading the transcript.
- [ ] Five transcript tail shapes, including a 64 KB Workflow line whose model field sits more than 4 KB before the line's end, give the same verdict on the fallback path — pinned in `model-guard-v2`.
- [ ] `Agent` with `subagent_type` `claude-code-guide` → exit 2 with the § 2 message; `Agent`/`Task` asking for a haiku or fable model → exit 2; a `Workflow` input carrying a forbidden model → exit 2; an `Agent` with no model → exit 0.
- [ ] A subagent call (`agent_id` present) whose own transcript shows Haiku → exit 2, and the parent's transcript is not what decided it.
- [ ] `PreModelSwitch` to haiku or fable → exit 2; to opus or sonnet → exit 0. `SessionStart` and `PostModelSwitch` write the state file; a `session_id` containing `/`, `\` or `..` writes nothing.
- [ ] `PostToolUse` on `Agent` with a forbidden resolved model prints the § 2 caveat JSON and exits 0.
- [ ] Every fail-open path still exits 0: no stdin, no transcript, unreadable transcript, missing subagent transcript.
- [ ] `arm_machine` writes `availableModels` per § 2 layer 1 and the installer registers the four new events idempotently; `machine-armed` pins both on its fixture HOME.
- [ ] `bash kit/ops/bench.sh guards` prints exactly the § 1 lines, and model-guard is `ok`.
- [ ] No new api rows (speed.md § 5): new logic lives in `case` branches and inside existing functions.
- [ ] The golden touches nothing real: HOME and `POLARIS_AWAKE_HOME` point into its `mktemp -d`.

## Notes
- Worktree-true checks only (speed.md § 6): `bash ops/polaris check --only` passes vacuously from your worktree.
- The installed copy at `~/.claude/polaris/model-guard.sh` is refreshed by the release checkpoint (re-arm), not by you. Never run the install branch against the real `~/.claude` from this task.
- No model is ever named in kit code except inside a deny list. Never spawn `claude-code-guide` to look anything up — it runs Haiku.
- The bench line in verify checks only model-guard: ownership-guard is still slow until T-174 lands. T-182 enforces every budget.
- Evidence (plans/audit-0924.md, verbatim):
  - **hooks-3** · OVERSTATED · model-guard (matcher "*", every tool call on the machine) spends 0.7–1.4 s in a pure-bash scan of 64 KB; reading 4 KB first gives the same answer 3x faster — Grep-only saves ≈0.2–1.9 s per non-edit call, depending on the transcript. For session-H that is ≈0.35 s × 812 ≈ 5 min of total hook time; since subagents run in parallel, wall-clock time drops by much less than the 5–15 min claimed. More important, it keeps model-guard under its 5 s limit at fan-ou… — fix: Edit kit/ops/hooks/model-guard.sh (the installer re-copies it to ~/.claude/polaris). 1. `TAIL=$(tail -c 4096 "$TP")`. If it contains `"model":"`, extract the last occurrence with the existing loop, which is only 1–4 iterations on 4 KB. 2. Otherwise fall back to `tail -c 65536 "$TP" | grep -o '"mode… (kit/ops/hooks/model-guard.sh, ~/.claude/polaris/model-guard.sh)
  - **hooks-4** · CONFIRMED · model-guard cannot see a subagent's model: a Haiku subagent (built-in claude-code-guide) passes — Closes a latent hole in the absolute ban (0 cases observed since the guard shipped). A side benefit: a subagent's own transcript is small when it starts, so reading it instead of the parent's 64 KB tail also removes the fan-out cost behind today's 6 timeouts. — fix: In kit/ops/hooks/model-guard.sh: 1. `jstr agent_id`. When present, point TP at `${TP%.jsonl}/subagents/agent-$AID.jsonl`, or at the first match of the glob `${TP%.jsonl}/subagents/workflows/*/agent-$AID.jsonl`. If neither exists, fail open as today. 2. When tool_name is Agent or Task, deny with no … (kit/ops/hooks/model-guard.sh, ~/.claude/polaris/model-guard.sh)
  - **telemetry-11** · CONFIRMED · The model ban has a hole: the built-in claude-code-guide agent type runs Haiku whatever the parent's model, and model-guard only checks the session's own model — Closes a policy hole of negligible token cost (about 0.7M tokens across 2 spawns in 3 weeks). It matters for owner policy, not for speed. — fix: In model-guard.sh (kit copy + refresh_machine_kit), add a check for tool_name Agent/Task whose tool_input.subagent_type is claude-code-guide: exit 2 with 'claude-code-guide runs a forbidden model — use a general-purpose subagent or WebFetch'. Add one line to PROTOCOL.md § MODEL ROUTING and a golden… (kit/ops/hooks/model-guard.sh, kit/ops/PROTOCOL.md, kit/ops/lib/admin.sh)
  - **model-guard fails open at subagent fan-out.** It hit its 5 s timeout 6 times today: 5 at 17:39:03–04Z as this workflow's subagents started, and 1 at 18:23Z in a another install subagent (hook_cancelled, timedOut:true). So the absolute ban on Fable and Haiku is not enforced exactly when many agents start at once, and each affected call waits 5 s. I reproduced it: I re…
