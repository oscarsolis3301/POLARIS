---
id: T-072
title: "check's write flags lose the hook's silent pass — --update/--scaffold refuse auto-approval"
type: bug
scope: hooks
epic: routing-and-bg
plan: routing-and-bg
points: 1
value: 5
time_criticality: 3
risk_opportunity: 3
wsjf: 11.0
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/hooks/readonly-allow.sh
  - ops/tests/readonly-allow.cmd
  - ops/tests/readonly-allow.expected
context_files:
  - ops/contracts/bg-jobs.md
depends_on:
  - T-067
contract: ops/contracts/bg-jobs.md
verify:
  - bash -n kit/ops/hooks/readonly-allow.sh
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris check' | grep -qx allow
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris check --only api-kit' | grep -qx allow
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris check --update' | grep -qx ask
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris check --update --only api-kit' | grep -qx ask
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris check --scaffold' | grep -qx ask
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris check --scaffold --app' | grep -qx ask
  - bash ops/tests/readonly-allow.cmd | diff - ops/tests/readonly-allow.expected
map_delta:
---
## Why
Pre-existing gate-integrity hole, found by T-067's builder and deliberately left out of that lane
because it changes a verdict outside its remit. `check` sits in `polaris_ok`'s tail-IGNORING plain-
read list, so `check --update` and `check --scaffold` are auto-approved inside compound commands —
yet `--update` rewrites the goldens from actual output, and observe.sh's own help calls that
"ALWAYS a human/Builder decision, never automatic", while `--scaffold` writes new pairs. An agent
that can silently rewrite the goldens gating its own work is the `.github/` Invariant-11 problem in
miniature. The fix: `check` moves out of the plain list into a flag-inspecting arm, per
`ops/contracts/bg-jobs.md` § v1.1.

Scope decision, recorded: READ flags allow — bare `check` and `--only <glob>` tails; WRITE flags
refuse — any tail containing `--update` or `--scaffold`; UNKNOWN flags refuse (fail-closed, the
hook's deny-by-default contract). A refusal is the NORMAL PROMPT, not a block — and the standing
settings.json rule still covers a bare `bash ops/polaris check --update`, which remains the
sanctioned, visible Builder path (T-062 precedent: the golden's owner runs `--update` deliberately).
What this closes is the SILENT pass inside compound reads, where nobody chose it.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `check` and `check --only <glob>` (any glob) still verdict `allow`
- [ ] any tail containing `--update` or `--scaffold` verdicts `ask`, in every position and
      combination; any flag outside the read set verdicts `ask` (fail-closed)
- [ ] golden battery gains BOTH directions (≥2 allow, ≥3 refuse incl. one compound line);
      the full old battery still passes byte-identical apart from the new cases
- [ ] NO new top-level functions (extend `polaris_ok`'s arm in place — api-kit surface untouched)
      and every other subcommand's verdict is unchanged
- [ ] hook stays fork-free pure bash

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Mirror the `slim`/`bg` two-arm split T-067 lands at readonly-allow.sh:206-214 — your arm is the
  third of that pattern; T-067 finishes before you claim (it is your depends_on).
- ops/tests/*.cmd and *.expected are eol=lf-pinned; no CR-sensitive asserts via grep/sed — awk/od
  only. The .cmd already invokes the KIT hook, so edit and proof travel together.
- Docs-for-this-lane: `ops/contracts/bg-jobs.md` § v1.1 is the spec; the arm's allow-list is
  `--only` + its value and NOTHING else.
