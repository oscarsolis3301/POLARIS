---
id: T-109
title: "lib/handover.sh — polaris next: the seven-verb router off the board, --do promotes under the lock, --brief re-anchors a compacted chat"
type: feature
scope: handover
epic: cant-eat-itself
plan: cant-eat-itself
points: 5
value: 13
time_criticality: 5
risk_opportunity: 3
wsjf: 4.2
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/handover.sh
approved:
context_files:
  - ops/contracts/role-handover.md
  - kit/ops/lib/observe.sh
  - kit/ops/lib/builder.sh
  - kit/ops/lib/core.sh
  - ops/tests/triage-lane.cmd
depends_on:
  - T-096
  - T-097
contract: ops/contracts/role-handover.md
verify:
  - bash -n kit/ops/lib/handover.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/lib/handover.sh)" = 8
  - grep -q '^cmd_next()' kit/ops/lib/handover.sh
  - grep -q '^next_dir()' kit/ops/lib/handover.sh
  - grep -q '^next_route()' kit/ops/lib/handover.sh
  - grep -q '^next_landable()' kit/ops/lib/handover.sh
  - grep -q '^next_claimable()' kit/ops/lib/handover.sh
  - grep -q '^next_promote()' kit/ops/lib/handover.sh
  - grep -q '^next_budget()' kit/ops/lib/handover.sh
  - grep -q '^next_brief()' kit/ops/lib/handover.sh
  - test "$(wc -l < kit/ops/lib/handover.sh | tr -d ' ')" -le 300
  - grep -q 'nothing to promote' kit/ops/lib/handover.sh
  - grep -q 'chore(board): promote' kit/ops/lib/handover.sh
  - grep -q 'evt promote' kit/ops/lib/handover.sh
  - grep -q 'budget: ' kit/ops/lib/handover.sh
  - grep -q 'say start to continue' kit/ops/lib/handover.sh
  - grep -q 'read ops/roles/' kit/ops/lib/handover.sh
  - grep -q 'mutex_on' kit/ops/lib/handover.sh
  - grep -q 'cmd_drift' kit/ops/lib/handover.sh
  - grep -q 'pat_overlap' kit/ops/lib/handover.sh
  - grep -q 'rules_gate' kit/ops/lib/handover.sh
  - T=$(mktemp -d) && cp -R kit "$T/kit" && sed -i -e 's/admin bg$/admin bg handover/' -e '/^  triage)     cmd_triage;;$/a next) shift; cmd_next "$@";;' "$T/kit/ops/polaris" && git -C "$T" init -q -b main r && git -C "$T/r" config user.email t@t && git -C "$T/r" config user.name t && echo x > "$T/r/a" && git -C "$T/r" add -A && git -C "$T/r" commit -qm i && (cd "$T/r" && bash "$T/kit/ops/polaris" init-board >/dev/null 2>&1 && test "$(bash "$T/kit/ops/polaris" next | sed -n 1p)" = finish && printf '# fixture contract\n' > ops/contracts/fix.md && printf -- '---\nid: T-1\ntitle: fixture task\ntype: feature\npoints: 1\nwsjf: 5\nrisk: normal\nowner: null\nbranch: null\nstatus: ready\ncontract: ops/contracts/fix.md\nfiles_owned:\n  - a\nverify: []\n---\n' > ops/board/ready/T-1.md && test "$(bash "$T/kit/ops/polaris" next | sed -n 1p)" = 'build T-1' && test "$(bash "$T/kit/ops/polaris" next | sed -n '2,$p' | grep -vc '^   ')" = 0)
map_delta: "new module lib/handover.sh — polaris next [--do|--brief]: resume/integrate/stop/build/promote/wait/finish off the board (T-101 wires the dispatch + loader)"
---
## Why
A session ends with its task, so every next role needs a human kickoff — and a five-pane run needs
`start` nudges at every wave. `polaris next` reads the board and says, in ONE word on line 1, what
THIS session does next: `resume <ID>` (my own live lock — never a second task mid-task),
`integrate` (something landable in review/ and the lease is free, stale or mine — under both landing
modes, because a self-land tail that queued leaves its task for nobody), `stop` (a run budget hit —
the conductor's verbatim budget line), `build <ID>` (an unlocked, un-avoided, non-foreign ready
task, top wsjf), `promote` (backlog work that passes the full ready gate), `wait` (only with work
genuinely in flight — never with nothing), else `finish`. Notes follow on three-space lines (the
`triage` shape); `next` writes nothing. `--do` performs the promote reusing what exists — `mutex_on`,
drift's ready-gate checks, `rules_gate`, claim's disjointness loop (including tasks promoted earlier
in the same pass), `mv` + `set_fm` + `evt promote` + ONE `board_commit` + `sync_board`, then `drift`
as audit; a second `--do` says `nothing to promote`. `--brief` prints ≤8 lines (`role:` `task:`
`worktree:` `last:` `next:` and the role-file pointer) so a compacted chat re-anchors from disk.
Every rule, note text and marker is pinned in role-handover.md — the hook (T-110), the drill and
goldens (T-111) and the role prose (T-107) all build against that table, so no verb may drift.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] exactly the eight pinned fns, ≤300 lines, bash 3.2; line 1 always `<verb>[ <ID>]`, every other line `^   `, rc 0 (rc 1 only on a usage error); `next` never writes the board or the state dir
- [ ] `next_route` implements the seven rows IN ORDER with the pinned note texts; `wait` never with nothing in flight; `stop` only when a build/promote would otherwise fire; foreign = `drain: plan` ∧ task `plan:` set ∧ ≠ `<dir>/plan` (missing `plan` file ⇒ nothing foreign); `avoid` honored; budget = hops ≥ `run_max_tasks` (≠0) or minutes since max(`started`, `prompted-at`) ≥ `run_max_minutes` (≠0)
- [ ] `--do`: the promotion algorithm exactly as pinned (mutex, re-check inside, `held:` notes for ask scopes and overlaps, ONE `board_commit "chore(board): promote <IDs>"` + EVENTS, `sync_board`, `mutex_off; trap - EXIT`, `cmd_drift` audit); nothing eligible ⇒ `nothing to promote` rc 0; `evt promote "$id" "deps done: <list>" "$pts"`
- [ ] `--brief`: ≤8 lines, no `|`, markers `role:` `task:` `worktree:` `last:` `next:` + `read ops/roles/<ROLE>.md if this context lost it`
- [ ] proven in a throwaway kit copy (the verify harness): empty board ⇒ `finish`; one ready task ⇒ `build T-1`; a backlog task with its dep in done/ ⇒ `promote`, then `--do` moves it to ready/ with the promote event and board subject, and a second `--do` prints `nothing to promote`; an overlapping backlog task is held with the note; a lock with a foreign sid on an active task ⇒ `wait`; `run_max_tasks: 1` with `hops`=1 ⇒ `stop` + the budget line
- [ ] no other fn; nothing in this task touches `kit/ops/polaris` (T-101 wires dispatch/loader/usage)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: exactly the 8 pinned fns — T-101 owns `ops/tests/api-kit.expected` this wave and writes those rows from role-handover.md; a different name is a contract break.
- PARALLEL-BUILD TRAP (role-handover.md § testing before T-101 lands): your worktree has no `next` dispatch and no `handover` in the loader, and the entry is T-101's file. Test in a THROWAWAY KIT COPY exactly as the verify harness does (`cp -R kit "$T/kit"` + the two `sed` edits in the COPY); never edit the real entry, never leave a stub. The integrator lands you before T-101.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-109` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- Reuse, do not reimplement: `mutex_on`/`mutex_off` (core.sh:214/191), the disjointness loop shape (builder.sh:67-86), drift's ready-gate checks (observe.sh:584+), `rules_gate` (ownership.sh), `fm_get`/`fm_list`/`set_fm`, `board_commit`/`sync_board`, `lock_age`, the lease reader in workspace.sh. `find <symbol>` before any Grep.
- Scratch dirs per-lane: scratchpad/T-109/. Never edit kit/ops/lib/*.sh while a selftest is RUNNING. bash 3.2 (CI runs macOS /bin/bash): no `case` inside `$(...)`.
