---
id: T-087
title: "Worktree entry is mechanical — claim instructs, fleet and roles carry the step"
type: feature
scope: roles
epic: enforced-isolation
plan: enforced-isolation
points: 2
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 8.0
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/builder.sh
  - kit/ops/lib/observe.sh
  - kit/ops/roles/BUILDER.md
  - kit/ops/roles/SOLO.md
  - kit/ops/roles/CONDUCTOR.md
context_files:
  - kit/ops/lib/workspace.sh
  - ops/contracts/shared-checkout.md
depends_on:
  - T-086
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - bash -n kit/ops/lib/observe.sh
  - grep -q 'now enter the worktree' kit/ops/lib/builder.sh
  - grep -q EnterWorktree kit/ops/lib/builder.sh
  - grep -q EnterWorktree kit/ops/lib/observe.sh
  - grep -q 'pinned-cwd subagent' kit/ops/lib/builder.sh
  - grep -q EnterWorktree kit/ops/roles/BUILDER.md
  - grep -q 'absolute paths under .polaris/wt/' kit/ops/roles/BUILDER.md
  - grep -q EnterWorktree kit/ops/roles/SOLO.md
  - grep -q 'EnterWorktree will refuse' kit/ops/roles/CONDUCTOR.md
  - grep -q 'absolute paths under .polaris/wt/' kit/ops/roles/CONDUCTOR.md
map_delta:
---
## Why
Root cause #1 of the collisions: claim creates `.polaris/wt/<ID>` and then only PRINTS
`claimed <ID> → cd "<path>"` — entering the worktree is prose a session can skip, and a session
that skips it works in the shared primary on `main`. Make entry mechanical everywhere the
kickoff is generated — and there are TWO callers (contract v2.4, wave-1 field finding): a
top-level session (the human's ~5-chat workflow, and every fleet pane) enters via
EnterWorktree({path: ".polaris/wt/<ID>"}) with no prompt; a conductor-spawned subagent has its
cwd PINNED at launch and EnterWorktree REFUSES there, so its PRIMARY instruction is absolute
paths under .polaris/wt/<ID> (or cd — the shell's cwd persists between calls). `cmd_claim`
closes with the two contract-pinned lines (the second names both callers); `cmd_fleet` pane
kickoffs carry the EnterWorktree line; `BUILDER.md` step 1b and `SOLO.md` carry BOTH caller
lines; `CONDUCTOR.md`'s builder-kickoff template carries ONLY the subagent form and never
instructs EnterWorktree — an instruction that always fails teaches builders to ignore the step,
the exact failure mode this sprint removes.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] claim output ends with BOTH pinned lines, byte-exact per contract v2.4, each on one line
- [ ] fleet's printed/pane kickoff (top-level panes) includes the EnterWorktree line per task
- [ ] BUILDER.md step 1b and SOLO.md carry BOTH caller lines as a numbered step
- [ ] CONDUCTOR.md kickoff template carries the pinned subagent line (absolute paths;
      "EnterWorktree will refuse") and never instructs EnterWorktree as an action
- [ ] the top-level EnterWorktree entry is RE-VERIFIED LIVE before pinning (contract v2.4:
      the tool only accepts paths under .claude/worktrees/, so .polaris/wt/<ID> may be
      structurally outside for ANY caller); if it refuses, the absolute-paths/cd form becomes
      the sole pinned instruction for all callers, EnterWorktree is dropped from every kickoff,
      and the finding goes in the handoff report
- [ ] no new top-level fns; no new H2 headings in any role file (api-kit records headings)
- [ ] claim/fleet drills stay green (claimguard, fmlist)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Never edit kit/ops/lib/*.sh while a selftest runs (Learned log); scratch under scratchpad/T-087/.
- bash 3.2 rules. Kit driver for any drill run, foreground ≥600000ms timeout.
- Keep role-file wording in each file's existing voice; a numbered step, not a paragraph.
- Sharpened wave-1 cause: EnterWorktree refused a pinned-cwd subagent because it only accepts
  paths under .claude/worktrees/ — not permissions or timing. Test the top-level case first-hand
  before writing any line that names the tool; the EnterWorktree greps in verify: bind only to
  the lines you actually pin, so if the tool is dropped per the contract contingency, hand back
  with the finding so the Planner re-pins verify: to the cd-only form.
