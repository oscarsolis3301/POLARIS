---
id: T-177
title: "drift and check in seconds — one pass over the dependency graph, branch checks that loop over branches, and a check that tests the worktree it runs in"
type: bug
scope: cli
plan: v4-fast
points: 3
value: 13
time_criticality: 8
risk_opportunity: 5
wsjf: 8.7
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/observe.sh
  - ops/tests/drift-deps.cmd
  - ops/tests/drift-deps.expected
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/worktree-liveness.md
  - ops/tests/handover-route.cmd
  - kit/ops/lib/core.sh
depends_on: []
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - bash ops/tests/drift-deps.cmd 2>/dev/null | diff - ops/tests/drift-deps.expected
  - grep -qF 'DEP MISSING: ' ops/tests/drift-deps.expected
  - grep -qF 'DEP CYCLE: ' ops/tests/drift-deps.expected
  - grep -qF 'CRUFT orphan: feat/' ops/tests/drift-deps.expected
  - grep -q 'mktemp -d' ops/tests/drift-deps.cmd
  - bash kit/ops/polaris check --only drift-deps
  - bash kit/ops/polaris check --only no-such-golden-anywhere >/dev/null 2>&1; test $? -eq 1
  - s=$(date +%s); bash kit/ops/polaris drift >/dev/null 2>&1; test $(( $(date +%s) - s )) -lt 15
  - bash kit/ops/polaris doctor --selftest --only drift
  - bash ops/tests/startup-budget.cmd 2>/dev/null | diff - ops/tests/startup-budget.expected
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta: new golden ops/tests/drift-deps — drift's dependency check is one awk pass over every column, its branch checks loop over feat/* refs, and `check` run from .polaris/wt/<ID> tests that worktree
surface:
---
## Why
`drift` is the board's hygiene audit, and it now takes about 7 minutes on this board. 94% of that
is section 7, the dependency check, which re-reads every task in every column — including 171
finished ones — once per dependency. Sprint 16 ran drift 17 times, about 30 minutes, 20 of them on
the critical path. One awk pass over all the columns returns the same verdict in under half a second.

The branch-cruft checks have the same shape: they loop over the `done/` folder and start a
`basename` process per file, when they only need to look at the few `feat/*` branches that exist.
Loop over the branches instead, and use shell builtins instead of `basename`. A `feat/*` branch
that matches no task at all is reported with one new line, so a stray branch is noticed instead of
silently slowing every `qa`.

Two more fixes to `check` ride along, because they live in this file: run from a builder's
worktree, `check` now tests THAT worktree (today it tests the primary and passes having proven
nothing), and `check --only <name>` that matches nothing now exits 1 instead of 0.

Every existing finding line stays byte-identical (`ops/contracts/speed.md` § 3).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] Section 7 is one inline awk over every column (DEP MISSING + cycles); `drift-deps` pins, on a fixture with one missing dep, one cycle and one orphan `feat/*` branch, the exact § 3 lines.
- [ ] Cruft, drift's cruft section and sweep loop over `feat/*` refs, never over `done/`; no `basename` fork inside any loop in observe.sh.
- [ ] `time bash kit/ops/polaris drift` on this board: before and after recorded in Notes; after < 15 s; the verdict is unchanged.
- [ ] `check` from `.polaris/wt/<ID>` runs that worktree's `ops/tests` from that worktree's root; `--only` matching nothing → rc 1; bare `check` unchanged.
- [ ] `drill_drift` green unchanged.
- [ ] No new api rows (speed.md § 5): the awk is inline in `cmd_drift`.

## Notes
- The `check --only drift-deps` verify line is itself the proof of the worktree fix: the pair exists only in your worktree until you land, so before the fix it prints `no goldens matched` and proves nothing.
- `ops/tests/triage-lane.cmd` greps this file for `pts" -le 3 ]` — leave triage's lines alone.
- T-179 edits this same file after you land (it depends on you). Keep your change to drift, cruft, sweep and check.
- Evidence (plans/audit-0924.md, verbatim):
  - **close-3** · CONFIRMED · drift's section 7 (dependency-graph check over ALL columns incl. 171 done tasks) is 94% of drift, and drift runs about 3+3W times per run — Section 7 costs about 430 s per drift call on today's box with no load from sibling agents, and about 100 s or less on a fast day (the 09-17 tail was 114 s for cruft+drift+doctor together). A one-pass awk brings it to about 0.4 s. The per-run total is an estimate. — fix: Replace observe.sh:981-993 with ONE awk over `$BOARD/{active,ready,review,blocked,backlog,done}/*.md` (list files with a builtin glob loop; skip any column with no files). It reads the depends_on block and flow lists exactly as fm_list does, then in END prints 'DEP MISSING' for any id not seen and … (kit/ops/lib/observe.sh, kit/ops/lib/selftest/board.sh)
  - **close-5** · CONFIRMED · Other loops that start a `basename` process per file: branch cleanup and drift's cruft check pay 171 per qa when ANY feat/* branch exists; check --only pays 31… — About 41-68 s per qa on today's box (about 7 s on a fast day) while any feat/* ref exists, which has been every qa here since 2026-07-14. About 4-5 s per `check --only`. — fix: (a) cruft_clear, drift's cruft section and sweep: loop over the refs, not the done folder. `for r in $crefs; do id=${r#feat/}; [ -f "$BOARD/done/$id.md" ] || continue; …` is O(branches) with zero process starts. (b) cmd_check: `name="${f##*/}"; name="${name%.cmd}"`, and filter before anything else.… (kit/ops/lib/observe.sh, kit/ops/lib/handover.sh, kit/ops/lib/knowledge.sh, kit/ops/lib/builder.sh)
  - **quality-3** · OVERSTATED · `check --only` and `find --api` are primary-anchored, so verify: lines run from a builder worktree prove base, not the branch (IDEAS:58, still open) — About 2% of done tasks (3 of 171), not 14%, had a check line that was vacuous end to end before landing. The rest were vacuous only at builder verify and handoff. It is still a real tooling gap: pre-land feedback is wrong and lanes have to be told workarounds. — fix: In cmd_check: when $PWD is under $PRIMARY/.polaris/wt/<ID> (the same case test the entry uses at polaris:360), set dir=<worktree>/ops/tests and run each .cmd from the worktree root. When --only was given explicitly and n=0, exit 1 ('no golden named <x> — a named check that runs nothing is red'). A … (kit/ops/lib/observe.sh, kit/ops/lib/search.sh, kit/ops/lib/selftest/board.sh)
