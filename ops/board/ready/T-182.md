---
id: T-182
title: "Re-pin the derived goldens — api-kit indexes the tree it runs in, and every guard is proven inside its time budget"
type: test
scope: tests
plan: v4-fast
points: 1
value: 3
time_criticality: 5
risk_opportunity: 3
wsjf: 11.0
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - ops/tests/api-kit.cmd
  - ops/tests/api-kit.expected
  - ops/tests/cli-help.expected
  - ops/tests/keys-drift.expected
  - ops/tests/startup-budget.expected
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/key-registry.md
  - ops/tests/startup-budget.cmd
  - ops/tests/keys-drift.cmd
depends_on: [T-173, T-174, T-175, T-176, T-177, T-178, T-179, T-180]
contract: ops/contracts/speed.md
verify:
  - grep -q 'POLARIS_ROOT' ops/tests/api-kit.cmd
  - bash ops/tests/api-kit.cmd 2>/dev/null | diff - ops/tests/api-kit.expected
  - bash ops/tests/cli-help.cmd 2>/dev/null | diff - ops/tests/cli-help.expected
  - bash ops/tests/keys-drift.cmd 2>/dev/null | diff - ops/tests/keys-drift.expected
  - bash ops/tests/startup-budget.cmd 2>/dev/null | diff - ops/tests/startup-budget.expected
  - bash kit/ops/bench.sh guards
map_delta:
surface:
---
## Why
Four goldens are DERIVED: they record a surface the rest of the kit produces (every function,
heading and settings key under `kit/`, the help text, the settings-drift report, the startup
budget). This sprint kept every lane surface-frozen so none of them moved mid-sprint
(`ops/contracts/speed.md` § 5). This task is the one owner at the end: it proves they still hold,
and fixes the one that could lie.

`api-kit` asks the INSTALLED CLI to index the kit, and that index is anchored to the primary
checkout, so run from a builder's worktree it silently checks the wrong tree. Make its `.cmd` index
the tree it runs in (`POLARIS_ROOT`). Then re-pin api-kit, cli-help, keys-drift and startup-budget
by comparing each golden with the live output — never by commit range — and STOP and hand back on
any hunk `speed.md` does not explain. Finally, prove every guard is inside its budget with
`bench.sh guards`, now that the three guard lanes have all landed.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `api-kit.cmd` indexes the tree it runs in, so it is not vacuous from a worktree.
- [ ] api-kit, cli-help, keys-drift and startup-budget match live output; every changed line is explained by speed.md, or the task went back.
- [ ] `bash kit/ops/bench.sh guards` exits 0: every § 1 budget met. Paste its output in Notes.
- [ ] The whole golden set is green from the primary — proven by this task's own land (`uat: bash kit/ops/polaris check`), never by a full-suite line in verify.

## Notes
- `cli-help` runs the INSTALLED `ops/polaris help` (6.5.0 until the release checkpoint's dogfood). Pin what it prints now; do not paste the kit's new help into it — that re-pin happens after dogfood.
- A golden nobody has seen fail is not evidence: after your change, break api-kit.expected by one line, watch it go red, restore it.
