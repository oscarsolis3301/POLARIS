---
id: T-093
title: "checkout-guard learns the other destroyers — worktree remove/prune/move, clean, push --delete, rm on .polaris, broad kills; both hooks beat"
type: feature
scope: hooks
epic: cant-eat-itself
plan: cant-eat-itself
points: 3
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 6.0
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/hooks/checkout-guard.sh
  - kit/ops/hooks/ownership-guard.sh
  - ops/tests/checkout-guard-denies.cmd
  - ops/tests/checkout-guard-denies.expected
approved:
context_files:
  - ops/contracts/shared-checkout.md
  - ops/contracts/worktree-liveness.md
  - kit/ops/hooks/readonly-allow.sh
  - ops/tests/readonly-allow.cmd
depends_on: []
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/hooks/checkout-guard.sh
  - bash -n kit/ops/hooks/ownership-guard.sh
  - grep -q '^mutating_other()' kit/ops/hooks/checkout-guard.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/hooks/checkout-guard.sh)" = 4
  - grep -q 'polaris-beat' kit/ops/hooks/checkout-guard.sh
  - grep -q 'polaris-beat' kit/ops/hooks/ownership-guard.sh
  - grep -q "a task worktree may be another session's whole working state" kit/ops/hooks/checkout-guard.sh
  - grep -q 'never delete origin refs by hand' kit/ops/hooks/checkout-guard.sh
  - grep -q 'never kill by name or kill the whole tree' kit/ops/hooks/checkout-guard.sh
  - test "$(grep -c 'permissionDecision' kit/ops/hooks/checkout-guard.sh)" = 1
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/tmp/fakerepo|git worktree remove .polaris/wt/T-001')" = deny:worktree-remove
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/x/.polaris/wt/T-000|rm -rf .polaris')" = deny:rm-polaris
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/tmp/fakerepo|Get-Process node | Stop-Process')" = deny:kill-broad
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/tmp/fakerepo|git push origin :refs/heads/feat/T-001')" = deny:push-delete
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/tmp/fakerepo|git clean -fdx')" = deny:clean
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/tmp/fakerepo|kill 1234')" = allow
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/tmp/fakerepo|git clean -n')" = allow
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/tmp/fakerepo|rm -rf node_modules')" = allow
  - test "$(bash kit/ops/hooks/checkout-guard.sh --test '/x/.polaris/wt/T-000|git switch main')" = allow
  - bash -c "$(cat ops/tests/checkout-guard-denies.cmd)" | diff - ops/tests/checkout-guard-denies.expected
  - bash -c "$(cat ops/tests/ownership-primary.cmd)" | diff - ops/tests/ownership-primary.expected
  - bash -c "$(cat ops/tests/readonly-allow.cmd)" | diff - ops/tests/readonly-allow.expected
map_delta: "checkout-guard.sh denies worktree remove/prune/move, clean (except -n), push --delete, rm/Remove-Item on .polaris and broad process kills (mutating_other); both guards touch the worktree beat"
---
## Why
Four things still destroy another session's work and nothing stops them: `git worktree remove`
(the ARC-428 incident), `git clean -fdx` in the primary (deletes the whole gitignored `.polaris/` —
every worktree, the index, the bg logs), `rm -rf .polaris` / `Remove-Item -Recurse .polaris`, and
by-name process kills (`taskkill /IM`, `pkill`, `killall`, `npx kill-port` — the "port 8001 instance
vanished" incident, a launcher reclaiming a port by killing whoever held it). The checkout-guard hook
already refuses checkout-mutating git in the primary; this task teaches it the rest, with the same
discipline: deny narrow, silence default, zero forks on the common path. Pid-targeted kills, dry-run
cleans and ordinary pushes stay allowed — you may kill what you started. Each new class has its own
pinned one-line message and `--test` label, and the golden grows to prove every case in both
directions. Both hooks also gain a zero-fork touch of the worktree beat (worktree-liveness.md), so a
session that only edits files still counts as live. Spec: shared-checkout.md v2.5 (verbatim messages,
labels, golden lines) + worktree-liveness.md § beat writers (the two pinned touch lines).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] gate 1 widened to `*git*|*rm*|*Remove-Item*|*kill*|*Stop-Process*|*fuser*`; gate 2 (wt cwd) allows only when the command contains none of worktree/rm/Remove-Item/kill/Stop-Process/fuser, and touches the beat via the pinned string-ops line before deciding
- [ ] `mutating_git` grows `worktree remove|prune|move` (HIT `worktree-remove|prune|move`), `clean` (allow on `-n`/`--dry-run`), `push --delete|-d|:ref` (HIT `push-delete`); `git worktree list`/`add` behave as before (`add` stays `deny:worktree`)
- [ ] ONE new fn `mutating_other` (rm-polaris · kill-broad per the contract's allow/deny lists); top-level fns are exactly deny · jstr · mutating_git · mutating_other
- [ ] worktree-*/rm-polaris/kill-broad deny EVERYWHERE (no placement probe); clean/push-delete deny in the primary only (probe unchanged); `deny <sub> <msg>` keeps ONE printf emitter; the v2 MSG byte-identical; MSG_WT/MSG_PUSH/MSG_KILL byte-exact per contract
- [ ] golden `checkout-guard-denies`: every existing line byte-identical in place; +20 deny lines, +9 allow lines, +3 wt-cwd deny lines, +4 count lines exactly as pinned in shared-checkout.md v2.5 §2; proven from the worktree with the `.cmd`-body diff (never `check --only`)
- [ ] `ownership-guard.sh`: exactly ONE added line after the `WT_ID=`/`PRIMARY=` anchor (:207-209) — the pinned beat touch; `ownership-primary` golden byte-identical; `readonly-allow.sh` untouched
- [ ] hermetic: `/tmp/fakerepo` still fails closed for primary-only classes; no live board or real repo read by the golden
- [ ] `bash kit/ops/polaris doctor --selftest --only checkoutguard` green (foreground, ≥600000 ms timeout) — the existing drill must not red on the widened gate 1

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: exactly ONE new fn (`mutating_other`) — T-096 owns `ops/tests/api-kit.expected` this wave and writes that row; no other new fn at any depth (key-registry.md §7).
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-093` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name — the very commands this task teaches the hook to refuse; the INSTALLED hook does not refuse them yet.
- Scratch dirs are per-lane: scratchpad/T-093/ only. Windows/Git Bash: never assert on CR-sensitive output via grep/sed — the goldens are LF-pinned. bash 3.2 (CI runs macOS /bin/bash). Sabotage each new class red once in a throwaway copy before trusting green (read the sabotage diff).
- The hook's beat path is string ops only: `${_p%%/.polaris/wt/*}/.git/worktrees/$_w/polaris-beat` — a missing dir is silently skipped (`2>/dev/null || true`); the guard forks nothing new.
