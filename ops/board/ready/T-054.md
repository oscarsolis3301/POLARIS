---
id: T-054
title: "Role files: the Planner asks, the Builder hands back, the Integrator sees it"
type: chore
scope: roles
epic: ask-rule-kind
plan: ask-rule-kind
points: 2
value: 5
time_criticality: 3
risk_opportunity: 3
wsjf: 5.5
risk: normal
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/roles/PLANNER.md
  - kit/ops/roles/SOLO.md
  - kit/ops/roles/BUILDER.md
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/roles/INTEGRATOR.md
context_files:
  - ops/contracts/ask-approval.md
  - kit/ops/roles/PLANNER.md
  - kit/ops/roles/BUILDER.md
depends_on: []
contract: ops/contracts/ask-approval.md
verify:
  - grep -q 'lifted only by a human' kit/ops/roles/PLANNER.md
  - grep -q 'A Builder never approves' kit/ops/roles/BUILDER.md
  - grep -q 'A Builder never approves' kit/ops/roles/SOLO.md
  - grep -q 'approve' kit/ops/roles/CONDUCTOR.md
  - grep -q 'approve' kit/ops/roles/INTEGRATOR.md
  - bash ops/polaris check --only cli-help-parity
map_delta:
---
## Why
The mechanism only works if the ask lands in the right session. Wrong session and it is either an
agent approving itself (the failure mode the whole design is built to prevent) or a human being
interrupted mid-build for a decision that was cheap ten minutes earlier and is expensive now.

The routing is: **the Planner asks, at the plan gate, before any task is promoted.** The Builder
never approves — it hands back exactly as it does today. The Integrator sees the approval named in
the handoff report and treats it as part of what it is landing. The Conductor knows an unapproved
`ask` scope is a human gate, like `risk: high`.

Five short insertions, each quoting the contract's pinned phrasing. This is not a rewrite of any role.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] PLANNER.md: the ready gate (Invariant 2 / protocol step 5) states that a task owning an `ask`
      scope needs a recorded approval before `ready/`, and that getting it is a plan-gate question —
      quotes the contract's pinned one-liner verbatim
- [ ] BUILDER.md: its existing "never touch RULES.tsv — hand back" paragraph gains the contract's
      pinned Builder line VERBATIM: `` A Builder never approves. Hand back — the approval is granted
      at the plan gate, not mid-build. ``
- [ ] SOLO.md: same pinned line, in its "a RULES rejection is an answer, not an obstacle" section —
      SOLO is one context doing the whole path, so it is the likeliest place to rationalize
- [ ] CONDUCTOR.md: an unapproved `ask` scope is a human gate at the plan gate, handled like any
      other `notify-gate question`; the conductor never approves on the human's behalf
- [ ] INTEGRATOR.md: an approval named in a handoff report is part of what is being landed — read it,
      and treat a `path`-rule violation exactly as before (an approval never clears `path`)
- [ ] no role file's ROLE header, its step numbering, or its report format changes

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Quote the contract's pinned strings VERBATIM. T-053 and T-055 land the same phrases in other files
  in parallel; identical strings merge without conflict, paraphrases do not.
- `kit/ops/roles/` is the PRODUCT copy. `ops/roles/` is the installed twin and RULES-guarded — read
  it, never write it. That also means these edits do NOT change the roles this sprint's own sessions
  are running; they ship at the next dogfood.
- BUILDER.md line 25 currently reads "never touch RULES.tsv", which predates Invariant 11 (RULES are
  agent-maintained since 2026-07-25). Correcting that sentence is IN scope for this task; nothing
  else on the line is.
