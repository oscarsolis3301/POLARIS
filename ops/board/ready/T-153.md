---
id: T-153
title: "The burndown writes itself — seal appends the wave's row to ops/SPRINT.md and any lane records a lesson with polaris learned -m"
type: feature
scope: sprint
plan: sprint-c
points: 5
value: 5
time_criticality: 3
risk_opportunity: 3
wsjf: 2.2
risk: normal
model: strong
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/lib/integrate.sh
  - kit/ops/lib/knowledge.sh
  - kit/ops/lib/selftest/history.sh
approved:
context_files:
  - ops/contracts/sprint-report.md
  - kit/ops/lib/core.sh
  - ops/SPRINT.md
depends_on: []
contract: ops/contracts/sprint-report.md
verify:
  - bash -n kit/ops/lib/integrate.sh
  - bash -n kit/ops/lib/knowledge.sh
  - bash -n kit/ops/lib/selftest/history.sh
  - bash kit/ops/polaris help >/dev/null
  - grep -q '^seal_burndown_row()' kit/ops/lib/integrate.sh
  - grep -q '^cmd_learned()' kit/ops/lib/knowledge.sh
  - grep -q 'chore(board): burndown' kit/ops/lib/integrate.sh
  - grep -q 'chore(board): learned' kit/ops/lib/knowledge.sh
  - grep -q 'learned anything?' kit/ops/lib/integrate.sh
  - grep -q 'Burndown' kit/ops/lib/selftest/history.sh
  - diff <(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/integrate.sh' | tr -d '\r' | grep -v 'seal_burndown_row') <(grep '^kit/ops/lib/integrate.sh' ops/tests/api-kit.expected | grep -v 'seal_burndown_row')
  - diff <(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/knowledge.sh' | tr -d '\r' | grep -v 'cmd_learned') <(grep '^kit/ops/lib/knowledge.sh' ops/tests/api-kit.expected | grep -v 'cmd_learned')
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/selftest/history.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/selftest/history.sh' ops/tests/api-kit.expected)
  - bash ops/tests/startup-budget.cmd | diff - ops/tests/startup-budget.expected
map_delta:
  - "seal (direct after the merge · pr at --sync) appends `| <date> | <done pts> | <remaining> |` to the current sprint's ## Burndown table via seal_burndown_row (integrate.sh) as chore(board): burndown, then nudges `learned -m`; knowledge.sh cmd_learned appends one dated bullet to ## Learned from any lane as chore(board): learned; sprint-report.md v3"
---
## Why
The burndown tables for sprints 12, 13 and 14 are empty and the Learned log has no bullet newer
than sprint 11, while the write-routing table still names the Integrator as the only writer — a
pen that went silent the day integration became a command (`landing: self`, `land --express`,
`next`). The owner loses the record of how work actually went, EVOLVE's "repeated Learned themes"
input is dry, and `metrics` reads "no reason recorded". The decision, owner-approved: the seal
writes the row it already has the numbers for (the wave's landed points, and what is still on the
board), and any lane records a lesson with one command instead of a hand edit of a board file.
The per-sprint report in `docs/sprints/` stays the narrative of record; SPRINT.md carries the
numbers and the lessons.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `seal_burndown_row <n> <date> <ids>` (integrate.sh) exactly as contract v3 pins it: done_pts from the wave's IDs (review/ first, done/ fallback), remaining over backlog ∪ ready ∪ active ∪ review minus the wave, the row appended as the LAST row of the TOP sprint's `## Burndown` table, the table created at the end of the top section when absent, `board_commit "chore(board): burndown <date>"` + `sync_board`, failures a ⚠ never a failed seal
- [ ] `cmd_seal` (direct) calls it right after the `--no-ff` merge; `seal_sync` (pr) after the `[<ID>]` verification; then the ONE pinned `learned anything?` nudge
- [ ] `cmd_learned` (knowledge.sh): `-m` required, ≤ 400 chars, no TAB; appends `- <YYYY-MM-DD> · <bullet>` as the last bullet of `## Learned` (created at EOF when absent); `evt learned`; `board_commit "chore(board): learned"`; `sync_board`; prints `✅ learned: <bullet>`; works from any branch and any cwd inside the repo
- [ ] `drill_express` (history.sh): after the express seal, SPRINT.md's `## Burndown` last row is `| <today> | <pts> | <remaining> |` with the fixture's numbers and the board ref's last subject is `chore(board): done <ID>` (asserts bytes + rc)
- [ ] the drill green in your worktree: `bash ops/polaris bg run t153 -- bash kit/ops/polaris doctor --selftest --only express,tcm` + chunked `bg wait t153 --max 300`; paste the `selftest passed` line into Notes
- [ ] `polaris verify` green

## Notes
- The `learned` DISPATCH line is the entry's and belongs to T-155; the `learned` DRILL assertions belong to T-157 (board.sh, `drill_grant`) because they need that dispatch to exist — do NOT add a `learned` call to history.sh, and do not touch `kit/ops/polaris` or board.sh. Your proof of `cmd_learned` is `bash -n` + reading + T-157's drill later; keep the fn small and pure enough that it needs no more.
- Drill fixtures write `# SPRINT <n> — … capacity: 5` with NO `## Burndown` table — the create-when-absent path is what the express drill exercises; the spine's `QUIET TREE` assertion already expects ops/SPRINT.md in the board ref's tree.
- `board_commit` uses a secondary index and works from the base checkout `seal` is on; `cmd_done` is the pattern (mutex_on · write · board_commit · sync_board · mutex_off).
- The burndown commit MUST land before `done`'s so drills that pin the board ref's last subject (`chore(board): done <ID>`) stay green — seal runs before done in every lane.
- Per-lane scratch (`mktemp -d`); trust rc + bytes, never printed text (T-089).
