---
id: T-160
title: "Release 6.5.0 — every remaining item: skills POLARIS writes for itself, an updater that reaches every install, and the four approved fixes; LAST, all four places at once"
type: chore
scope: release
plan: sprint-c
points: 2
value: 13
time_criticality: 13
risk_opportunity: 5
wsjf: 15.5
risk: normal
model: strong
status: ready
owner: null
branch: null
files_owned:
  - kit/ops/VERSION
  - CHANGELOG.md
approved:
context_files:
  - CHANGELOG.md
  - kit/ops/VERSION
  - ops/board/done/T-147.md
  - ops/contracts/self-skills.md
depends_on: [T-150, T-151, T-152, T-153, T-154, T-155, T-156, T-157, T-158, T-159]
contract: ops/contracts/self-skills.md
verify:
  - grep -q '^version: 6.5.0$' kit/ops/VERSION
  - test "$(grep -c '^channel:\|^zip:\|^tarball:\|^repo:\|^changelog:' kit/ops/VERSION)" = 5
  - grep -q '^## 6.5.0 — 2026-09-1[0-9]$' CHANGELOG.md
  - grep -q '^## 6.4.0 — 2026-09-14$' CHANGELOG.md
  - awk '/^## 6.5.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q 'skill propose' && awk '/^## 6.5.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q 'update --all' && awk '/^## 6.5.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q 'amend' && awk '/^## 6.5.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q 'learned' && awk '/^## 6.5.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -qi 'cruft'
  - awk '/^## 6.5.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -q '29,124'
  - awk '/^## 6.5.0/{f=1;next} /^## /{f=0} f' CHANGELOG.md | grep -qi 'BREAKING'
map_delta:
---
## Why
This is the last sprint of the owner's program, and nothing in it reaches anyone until it is
released: `update --auto` only ever delivers a PUBLISHED minor, and the fixed walker only reaches the
owner's other machines and projects once main's instance carries it. Four places at once —
`kit/ops/VERSION`, the CHANGELOG, the git tag, the GitHub release with `polaris-v5.zip` — and the
SERIAL suite green in its own lane before any tag exists, because a sharded green is not a CI green
(6.2.0, 6.2.1 and 6.3.0 were all tagged and never shipped). The CHANGELOG entry is written for the
humans who will read it after `update --auto` prints one line: what POLARIS now knows about your
repo (skills, born hidden, promoted by you), what reaches every install now (`update --all` runs its
own updater in each repo; `--major` on request), what stopped costing a suite (cruft), and the three
board commands that landed (`amend`, `learned`, `promote`). Both slim figures — the dated 251 /
34,119 B and today's 223 / 29,124 B — go in the entry (self-skills § 0 OPEN-7). `BREAKING: none`:
every skill is born hidden, the walker changes nothing a repo did not already ask for, and no gate
moved.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/ops/VERSION` reads `version: 6.5.0`; every other line untouched
- [ ] CHANGELOG has `## 6.5.0 — <today>` above 6.4.0 covering: skills (`skill gaps/propose/promote/demote/prune/restore`, the shelf, the twin iff the probe said yes — say which), `update --all` + `--major` + gone-entry pruning, `amend`, `learned` + the seal's burndown row, the cruft classes, `next --do`'s plan gate + `promote`, the `qa` flag fix; a before/after table in the 6.4.0 style; both slim figures; `BREAKING: none`; the 6.4.0 and 6.3.1 entries unchanged
- [ ] the SERIAL suite green in your worktree: `bash ops/polaris bg run serial -- bash kit/ops/polaris doctor --selftest` then chunked `bash ops/polaris bg wait serial --max 300` until it returns a verdict (expect 950–1100 s with the new drill; four waits); paste the `selftest passed` line and `git rev-parse HEAD` into Notes — the serial run is the CI tier and it must be green BEFORE the tag exists
- [ ] no other lane is active while the serial suite runs (this task depends on every other one, so `bash ops/polaris status` shows nothing in active/ besides you — check before starting; editing `kit/ops/lib/*.sh` mid-run fabricates a spurious failure)
- [ ] `bash kit/ops/polaris check` green (every golden, against your worktree) and `doctor --fast` green

## Notes
- THE RELEASE RITUAL after this task lands (CONVENTIONS § Release ritual — the conductor's, end to end, in order; pin the sha at each step). Copied from T-147's Notes, the version changed and the two new steps marked NEW:
  1. `finish` green on main (the express land carries its stamp; `finish` accepts it).
  2. `git tag v6.5.0 && git push origin main --tags` — CI's release job runs the serial suite on ubuntu and publishes the zip; "one version, everywhere" checks the four places.
  3. Wait for the release, in the background: poll `curl -fsSL https://api.github.com/repos/oscarsolis3301/POLARIS/releases/latest` every 60 s until `"tag_name": "v6.5.0"` (typically 12–20 min; `gh` is not authenticated on this machine — do not depend on it). Red CI ⇒ STOP: read the failing job, fix on a rider task, and the tag stays where it is (a published tag is never moved — the 6.3.1 precedent: the fix ships as 6.5.1).
  4. `python kit/ops/pack.py --dogfood` — downloads the PUBLISHED zip, installs it here, runs the serial selftest again (~1000 s: top-level `run_in_background`, poll the log). Then `bash ops/polaris version` must say 6.5.0.
  5. `bash kit/ops/polaris help | diff - ops/tests/cli-help.expected` — review every hunk line by line (exactly: `skill`, `amend`, `learned`, `promote`, the `update --major` line), then `bash ops/polaris check --only cli-help --update`; `bash ops/polaris check` green.
  6. Commit the refreshed instance: `chore(polaris): the published 6.5.0 kit now runs this board` — ops/ + the cli-help golden; push.
  7. NEW — the owner's propagation ask, finally answerable: `bash ops/polaris update --all` walks the machine registry with THIS kit; The Director (6.2.2) should update in place; pip (5.24.0) prints the MAJOR line — then `bash ops/polaris update --all --major` applies it (its dirt is app-only, tolerated since 6.3.0). Paste both walks' output into the close report. Other machines reach it through `channel:` on their next session start.
  8. NEW — this repo's own SPRINT.md burndown: the installed CLI was 6.4.0 for every seal of this sprint, so no row was written; add the sprint-15 rows by hand from `bash ops/polaris metrics` (the last time this is ever done by hand), then `bash ops/polaris learned -m "…"` for the sprint's lessons — the first bullets since sprint 11.
- `bg run <name> -- <cmd…>` accepts an arbitrary command, so the serial suite fits under the tool cap from a subagent; it executes in the primary checkout, which at W4 is `main` with everything landed — exactly the tree CI will test.
- The bump is inert to the suite by construction (the only selftest reference to kit/ops/VERSION writes its own fixture).
- PROPAGATION, read off the code (T-147's Notes): the version bump alone reaches NOBODY — `cmd_update` installs from `tarball:` = main's ROOT ops/, our own installation. Step 4 (dogfood) + step 6 (commit ops/) is what publishes 6.5.0 to every installed repo.
- Per-lane scratch (`mktemp -d`); a subagent never ends its turn with a job still running — the last `bg wait` returns the verdict before you hand off.
