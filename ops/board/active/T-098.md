---
id: T-098
title: "builder.sh — beats on every step, resume/release respect a live worktree, pack prints SEE YOUR WORK, handoff needs the capture"
type: feature
scope: builder
epic: cant-eat-itself
plan: cant-eat-itself
points: 5
value: 13
time_criticality: 5
risk_opportunity: 3
wsjf: 4.2
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-098
files_owned:
  - kit/ops/lib/builder.sh
approved:
context_files:
  - ops/contracts/worktree-liveness.md
  - ops/contracts/visual-check.md
  - ops/contracts/role-handover.md
  - kit/ops/lib/workspace.sh
  - kit/ops/lib/ownership.sh
depends_on:
  - T-092
  - T-096
  - T-097
contract: ops/contracts/worktree-liveness.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/lib/builder.sh)" = "$(git show main:kit/ops/lib/builder.sh | grep -c '^[a-z_]*()')"
  - test "$(grep -c 'beat_touch ' kit/ops/lib/builder.sh)" -ge 4
  - grep -q 'resume refused: .polaris/wt/' kit/ops/lib/builder.sh
  - grep -q 'release refused: .polaris/wt/' kit/ops/lib/builder.sh
  - grep -q 'wt_remove .* release' kit/ops/lib/builder.sh
  - test "$(grep -c 'worktree remove --force' kit/ops/lib/builder.sh)" = 0
  - grep -q 'wt_add "$id" resume' kit/ops/lib/builder.sh
  - grep -q 'SEE YOUR WORK — capture before handoff (ops/VISUAL.md)' kit/ops/lib/builder.sh
  - grep -q 'visual: unset — no capture step' kit/ops/lib/builder.sh
  - grep -q 'handoff refused: .* changed a visual: path but .polaris/shots/' kit/ops/lib/builder.sh
  - grep -q 'cfg visual' kit/ops/lib/builder.sh
  - grep -q 'cfg shot' kit/ops/lib/builder.sh
  - grep -q 'cfg port_base' kit/ops/lib/builder.sh
  - grep -q 'cfg serve' kit/ops/lib/builder.sh
  - grep -q 'the board hands you the next step: bash ops/polaris next' kit/ops/lib/builder.sh
  - test "$(grep -c 'say start (or: bash ops/polaris fleet' kit/ops/lib/builder.sh)" = 0
  - grep -q '\.polaris/handover' kit/ops/lib/builder.sh
  - K="$PWD/kit/ops/polaris" && R=$(mktemp -d) && git -C "$R" init -q -b main && git -C "$R" config user.email t@t && git -C "$R" config user.name t && mkdir -p "$R/web" && echo x > "$R/web/a.txt" && git -C "$R" add -A && git -C "$R" commit -qm i && (cd "$R" && bash "$K" init-board >/dev/null 2>&1; printf 'base: main\nvisual: web/*\nshot: snap {ID} {PORT}\nserve: dev {PORT}\nport_base: 4000\n' > ops/CONVENTIONS.md; printf -- '---\nid: T-207\ntitle: t\ntype: feature\npoints: 1\nrisk: normal\nstatus: ready\nfiles_owned:\n  - web/a.txt\nverify: []\n---\n' > ops/board/ready/T-207.md; bash "$K" pack T-207 2>/dev/null | grep -q 'shot: snap T-207 4007')
map_delta: "claim/resume/verify/handoff beat the worktree; resume and release refuse a live worktree (takeover = rm the beat file); pack prints the SEE YOUR WORK section from shot:/visual:/port_base:/serve:; handoff refuses a visual change without a fresh .polaris/shots/<ID>-*.png"
---
## Why
This is where the worktree-liveness primitives (T-092) and the capture step (visual-check.md) meet
the builder's own commands. Beats: `claim`, `resume`, `verify` and `handoff` each touch the worktree
beat so a working session is provably live. Resume: today it adopts ANY lock unconditionally — the
documented takeover path and the lock-side twin of the worktree bug; now a live beat from a
different session refuses with the pinned message naming the beat file (takeover is explicit:
`rm <beat>`), the same session (after a compaction) is allowed, an idle one is adopted, and its meta
rewrite writes all five lines. Release: the worktree goes through `wt_remove <ID> release` (dirty
own-lane ⇒ archive; outside + dirty + live ⇒ the pinned die BEFORE any board write). Pack: a new
"SEE YOUR WORK" section, driven by real `cfg` reads of `visual:`/`shot:`/`port_base:`/`serve:` with
`{ID}`/`{PORT}` substituted (per-task port = port_base + numeric tail mod 100 — T-207 ⇒ 4007), and
the unset line when a repo declares no visual surface. Handoff: when the diff touches a `visual:`
path and `shot:` is set, a non-empty `.polaris/shots/<ID>-*.png` newer than the branch base must
exist, else the pinned refusal (verify only warns). Claim records `task` and `plan` in the session's
handover state dir; the queue notice stops telling the HUMAN to "say start" — the board hands the
session its next step: `bash ops/polaris next`. Specs: worktree-liveness.md (beats, die texts),
visual-check.md (section lines, gate rule), role-handover.md (state dir, the notice).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `beat_touch "$id"` in cmd_claim (after wt_add), cmd_resume, cmd_verify, cmd_handoff — best-effort
- [ ] `cmd_resume`: `beat_live` AND lock meta line 4 ≠ my sid (or `-`) ⇒ die with the pinned resume text (beat age + absolute beat path); same sid ⇒ allowed; idle ⇒ adopt; the meta rewrite writes 5 lines; vanished worktree ⇒ `wt_add "$id" resume` (the recreate warning is workspace.sh's)
- [ ] `cmd_release`: `wt_remove "$id" release` replaces the `--force` line; from outside on dirty+live ⇒ the pinned release die BEFORE `mutex_on`/`mv`; the success line reads `worktree removed|archived|LEFT` per rc; lock dropped in every non-die path
- [ ] `cmd_pack`: the `SEE YOUR WORK — capture before handoff (ops/VISUAL.md)` section between §7 and §8 with the exact lines of visual-check.md § cmd_pack (unset line; `visual: <globs> · this task touches it: yes|no` via `owned_match` both directions; serve/shot substituted; `port:`; `proof:`; `read:`); inline, no new fn at any depth
- [ ] `cmd_handoff`: after `run_verify_cmds` — visual set AND shot set AND diff ∩ visual ≠ ∅ ⇒ require a non-empty `$PRIMARY/.polaris/shots/<ID>-*.png` with mtime ≥ merge-base commit time, else the pinned `⛔ handoff refused: …` die (rc 1, task stays active); `cmd_verify` prints the `⚠` variant and continues
- [ ] `cmd_claim` writes `.polaris/handover/<sid>/task` (every claim) and `plan` (first claim only, from the task's `plan:`), only when `$CLAUDE_CODE_SESSION_ID` is set; best-effort
- [ ] handoff queue notice (:239) reads `$nrdy ready task(s) still queued — the board hands you the next step: bash ops/polaris next`; the `integrate` notice unchanged
- [ ] the hermetic pack probe (verify) prints `shot: snap T-207 4007` and `this task touches it: yes`; a task owning `src/b.txt` prints `touches it: no`
- [ ] `bash kit/ops/polaris doctor --selftest --only claimguard,selfland,park` green (foreground, ≥600000 ms timeout); `resume` on a fresh-beat lock from a DIFFERENT sid dies, from the SAME sid succeeds (drill wtreap automates in W3)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: SURFACE-FROZEN — no new fn at any depth (the pack section is inline in `cmd_pack`; `pack_section` exists), no new `#` line, no new KEYS row. T-101 owns `ops/tests/api-kit.expected` this wave (key-registry.md §7).
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-098` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name. The installed `resume`/`release` still `--force` — yours is the kit copy.
- `owned_match` lives in ownership.sh:14-26 — reuse it for the visual glob test; `pat_overlap` for both-directions. `cfg` reads of the four keys are what `keys-drift` ties to T-096's rows (landed in W1).
- The port formula: numeric tail of the ID (`T-207` ⇒ 207) mod 100 ⇒ 7; no digits ⇒ port_base; port_base unset ⇒ `{PORT}` literal + `port: (port_base unset — {PORT} stays literal)`.
- Scratch dirs per-lane: scratchpad/T-098/. Never edit kit/ops/lib/*.sh while a selftest is RUNNING. Windows: `python`, never `python3`. bash 3.2.
