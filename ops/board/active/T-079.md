---
id: T-079
title: "The prose flips with the code — CONDUCTOR, BUILDER, EVOLVE, INIT skeleton"
type: chore
scope: roles
epic: autonomy-by-default
plan: autonomy-by-default
points: 2
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 8.0
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-079
files_owned:
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/roles/BUILDER.md
  - kit/ops/roles/EVOLVE.md
  - kit/ops/roles/INIT.md
context_files:
  - ops/contracts/hands-free-knobs.md
depends_on: []
contract: ops/contracts/hands-free-knobs.md
verify:
  - grep -qF 'else auto' kit/ops/roles/CONDUCTOR.md
  - grep -qF 'defaults to default-safe since 6.0' kit/ops/roles/BUILDER.md
  - grep -qF 'the default since 6.0' kit/ops/roles/EVOLVE.md
  - grep -q 'autonomy. standard' kit/ops/roles/INIT.md
map_delta:
---
## Why
The three gate knobs are ENFORCED by role-file prose — the conductor computes effective plan_gate
from CONDUCTOR.md:111-119, builders read BUILDER.md:36, EVOLVE reads EVOLVE.md:16. So this task is
not documentation trailing the code: for the roles, it IS the behavior change. Each surface gets
the byte-exact pinned sentence from hands-free-knobs.md v2 § Pinned strings, so the prose and
observe.sh state the same default and can never drift apart in this release.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] CONDUCTOR.md:111-113 — the effective-plan_gate sentence replaced with the v2 pinned
      sentence (else `confirm` under `autonomy: standard` · else `auto` — the default since 6.0)
- [ ] BUILDER.md:36 — opening clause replaced with the v2 pinned clause (`default-safe` is the
      default since 6.0; `ask` or `autonomy: standard` restores the old path); the REST of the
      paragraph is byte-unchanged, including "structural blocks … ALWAYS ask"
- [ ] EVOLVE.md:16 — the parenthetical replaced with the v2 pinned parenthetical
      (auto-reversible is the default; `confirm` or `standard` restores the approve queue);
      EVOLVE's no-self-escalation clause (:29) untouched
- [ ] INIT.md:120-134 — the four commented autonomy-stanza lines state the 6.0 facts: unset =
      auto / default-safe / auto-reversible, `autonomy: standard` = the confirm-everything
      opt-out; "Default: standard (today's behavior…)" claims removed
- [ ] no markdown heading added, removed, or renamed in any of the four files (api-kit records
      headings; this lane is surface-frozen per key-registry.md § 5)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Quote the pinned strings VERBATIM from hands-free-knobs.md v2 — paraphrase is how parallel doc
  surfaces drift (the sprint-4 lesson that pinned phrases merge zero-conflict).
- Hard-gate sentences in all four files (risk:high always asks, STOP-AND-ASK, RULES) are not
  yours to touch — the flip changes defaults, never gates.
