---
id: T-096
title: "Docs + keys for 6.2.0 — six KEYS rows, ops/VISUAL.md, PROTOCOL AWAKE + rows, rule 3 rewrite in both copies, MANUAL without --force; api-kit owner W1"
type: chore
scope: docs
epic: cant-eat-itself
plan: cant-eat-itself
points: 4
value: 13
time_criticality: 8
risk_opportunity: 5
wsjf: 6.5
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-096
files_owned:
  - kit/ops/KEYS.tsv
  - kit/ops/VISUAL.md
  - kit/ops/PROTOCOL.md
  - kit/ops/MANUAL.md
  - kit/.claude/output-styles/polaris.md
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/key-registry.md
  - ops/contracts/visual-check.md
  - ops/contracts/keep-awake.md
  - ops/contracts/role-handover.md
  - ops/contracts/worktree-liveness.md
depends_on: []
contract: ops/contracts/key-registry.md
verify:
  - test -f kit/ops/VISUAL.md
  - test "$(grep -c '^#' kit/ops/VISUAL.md)" = 7
  - grep -q '^# SEEING YOUR WORK — the capture is the proof' kit/ops/VISUAL.md
  - grep -q '^## Adding it to a repo' kit/ops/VISUAL.md
  - test "$(wc -l < kit/ops/VISUAL.md | tr -d ' ')" -le 40
  - test "$(sed -n '/^```/,/^```/p' kit/ops/VISUAL.md | grep -c '^#')" = 0
  - awk -F "\t" "\$1==\"wt_live_minutes\" && \$3==15 {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"handover\" && \$3==\"auto\" {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"shot\" && \$2==\"6.2.0\" {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"visual\" && \$2==\"6.2.0\" {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"port_base\" && \$2==\"6.2.0\" {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"serve\" && \$2==\"6.2.0\" {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$2==\"6.2.0\" {n++} END {exit n!=6}" kit/ops/KEYS.tsv
  - grep -q '^## AWAKE — one keep-awake daemon per machine' kit/ops/PROTOCOL.md
  - test "$(grep -c '^## ' kit/ops/PROTOCOL.md)" = 10
  - grep -q '^| a worktree that is not yours |' kit/ops/PROTOCOL.md
  - grep -q 'End with ONE concrete next step\*\* — one only the human can take' kit/ops/PROTOCOL.md
  - grep -q 'End with ONE concrete next step\*\* — one only the human can take' kit/.claude/output-styles/polaris.md
  - grep -q '^\*\*A hop is not an ending\.\*\*' kit/.claude/output-styles/polaris.md
  - test "$(grep -c '^[0-9]\. \*\*' kit/.claude/output-styles/polaris.md)" = 7
  - test "$(grep -c '^#' kit/.claude/output-styles/polaris.md)" = "$(git show main:kit/.claude/output-styles/polaris.md | grep -c '^#')"
  - test "$(grep -c '^#' kit/ops/MANUAL.md)" = "$(git show main:kit/ops/MANUAL.md | grep -c '^#')"
  - test "$(grep -c 'worktree remove.*--force' kit/ops/MANUAL.md)" = 0
  - grep -q 'wt-archive' kit/ops/MANUAL.md
  - bash -c "$(cat ops/tests/plain-voice.cmd)" | diff - ops/tests/plain-voice.expected
  - bash -c "$(cat ops/tests/output-style-installed.cmd)" | diff - ops/tests/output-style-installed.expected
  - bash -c "$(cat ops/tests/keys-drift.cmd)" | diff - ops/tests/keys-drift.expected
  - test "$(wc -l < ops/tests/api-kit.expected | tr -d ' ')" = 581
  - test "$(grep -c '^kit/ops/VISUAL.md' ops/tests/api-kit.expected)" = 7
  - grep -q 'wt_remove' ops/tests/api-kit.expected
  - grep -q 'mutating_other' ops/tests/api-kit.expected
  - grep -q 'AWAKE — one keep-awake daemon per machine' ops/tests/api-kit.expected
  - test "$(grep -c '^kit/ops/KEYS.tsv' ops/tests/api-kit.expected)" = 44
  - test "$(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected | grep -c '^<')" = 0
map_delta: "kit/ops/VISUAL.md — the SEEING YOUR WORK doctrine ships in the kit (installed as ops/VISUAL.md by T-103); KEYS.tsv gains wt_live_minutes shot visual port_base serve handover; PROTOCOL gains ## AWAKE"
---
## Why
Wave 1's prose and data, in one lane so five code lanes never touch a shared doc: (1) six new
CONVENTIONS keys registered in `kit/ops/KEYS.tsv` — `wt_live_minutes` (worktree liveness), `shot`
`visual` `port_base` `serve` (the capture step) and `handover` (role hops) — rows are inert data, and
`keys-drift` ties every future `cfg` read to its row, so they land BEFORE the code that reads them;
(2) a new kit doc `kit/ops/VISUAL.md` carrying the "seeing your work" doctrine the owner has been
pasting by hand (≤40 lines, seven pinned headings); (3) PROTOCOL.md gains the `## AWAKE` section
(one keep-awake daemon per machine), one N-CHATS table row (a worktree that is not yours), a bold
§ LANES paragraph extending the precedent chain to handover, and the rewritten § VOICE rule 3;
(4) the output style gets the SAME rule 3 bytes (the `plain-voice` golden diffs the two copies) and
one bold "A hop is not an ending" line in "How a session ends"; (5) MANUAL.md's by-hand recipes lose
`git worktree remove --force`. And because this repo's api-kit golden records every heading and
KEYS row, this task is the wave's ONE api-kit owner: it writes the whole 19-row W1 union up front —
its own 14 rows plus T-092's four workspace fns and T-093's `mutating_other` — from the names the
contracts pin. Specs: key-registry.md §7 (the union + owner recipe), visual-check.md (keys + VISUAL.md),
keep-awake.md (AWAKE section), role-handover.md (LANES paragraph, rule 3, the output-style line),
worktree-liveness.md (MANUAL recipes, the N-CHATS row).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] KEYS.tsv: the six rows appended after `landing`, in the pinned order and byte-exact text (visual-check.md § keys); `keys-drift` golden green
- [ ] VISUAL.md: exactly the seven pinned headings, ≤40 lines, every doctrine bullet present, no `#`-leading line inside a fence, "Adding it to a repo" names the four keys + python-not-python3 + the `.claude/settings.json` allow line
- [ ] PROTOCOL.md: `## AWAKE — one keep-awake daemon per machine` is the ONLY new H2 (10 H2s total), ≤12 lines with the pinned content; the N-CHATS row byte-exact; the § LANES bold paragraph; rule 3 rewritten — the numbered-rule set of § VOICE stays 7 lines and byte-identical to the output style's
- [ ] output style: rule 3 identical bytes; the pinned bold line placed in "How a session ends" ABOVE `## What a close reads like`; still 7 numbered rules; no heading change (`output-style-installed` + `plain-voice` green)
- [ ] MANUAL.md :100 and :122 recipes reworded per worktree-liveness.md (no `--force`; `sweep --fix` first; `wt-archive` for dirty); heading set unchanged
- [ ] api-kit.expected: 581 lines = 562 + the 19 pinned W1 rows inserted in `find --api` order (key-registry.md §7); the completeness check (`<` count) is 0 in this worktree; sibling rows (`>`) are exactly T-092's four + T-093's one until the wave lands
- [ ] no other `#` line added anywhere under kit/ by this task; no fn; no python

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit OWNER, W1 — write the whole 19-row union up front from key-registry.md §7 (never from a sibling's diff); the ROW LIST wins over the count: a discrepancy goes in the handoff Notes with the corrected count, never a silent re-pin. `find --api` is primary-anchored — the `POLARIS_ROOT="$PWD" python kit/ops/index.py` form is the worktree proof.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-096` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- Land FIRST in W1 if you can (the owner-lands-first variant of the Learned rule): siblings' handoffs each move the golden by their pinned rows, and the wave gate is the run that counts.
- Working-copy `.md` files are CRLF on this Windows checkout (autocrlf) — never `$`-anchor a grep on them; goldens and KEYS.tsv are LF-pinned. Scratch dirs per-lane: scratchpad/T-096/.
- `keys-drift` fixture uses FAKE keys and is byte-identical after this task; `startup-budget` is untouched (no entry-script edit here — that is T-101's).
