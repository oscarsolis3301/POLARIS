---
id: T-063
title: "The second-chat doctrine — PROTOCOL § N CHATS ONE REPO, CLAUDE.md stop-ask carve-out, role deltas, SKILL pointer"
type: docs
scope: docs
epic: n-chats-one-repo
plan: n-chats-one-repo
points: 2
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 8.0
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-063
files_owned:
  - kit/ops/PROTOCOL.md
  - kit/CLAUDE.md
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/roles/INTEGRATOR.md
  - kit/ops/roles/BUILDER.md
  - kit/ops/roles/SOLO.md
  - kit/ops/roles/PLANNER.md
  - kit/.claude/skills/polaris/SKILL.md
context_files:
  - ops/contracts/shared-checkout.md
  - ops/contracts/pipelined-integration.md
  - ops/contracts/express-lane.md
depends_on:
  - T-058
  - T-059
contract: ops/contracts/shared-checkout.md
verify:
  - grep -q 'git/workspace mechanics are never ask material after plan approval' kit/CLAUDE.md
  - grep -q 'N CHATS, ONE REPO' kit/ops/PROTOCOL.md
  - grep -q 'queued:' kit/ops/roles/CONDUCTOR.md
  - grep -q 'queued:' kit/ops/roles/INTEGRATOR.md
  - grep -q 'claim the next task' kit/ops/roles/BUILDER.md
  - grep -q 'bash ops/polaris park' kit/ops/roles/SOLO.md
  - grep -q 'disjointness gate' kit/ops/roles/PLANNER.md
  - grep -q 'N CHATS' kit/.claude/skills/polaris/SKILL.md
  - bash kit/ops/polaris doctor --selftest --only claudemd
map_delta: "kit/ops/PROTOCOL.md gains § N CHATS, ONE REPO — the second-chat decision table (ops/contracts/shared-checkout.md)"
---
## Why
The "ask the user" behavior this plan kills is EMERGENT: no instruction tells a second chat what to
DO when claim says taken, the lane is busy, or the tree is dirty — so it improvises a question. The
CLI half lands in waves 1-2; this task writes the doctrine half. New `kit/ops/PROTOCOL.md`
§ N CHATS, ONE REPO: the decision table (claim says taken → claim the next; integration busy →
bounded wait; rc 3 `queued:` → conductor polls at wave boundaries, a subagent reports queued and
ends its turn; dirty tree → park; another session's locks, leases and tasks → invisible, never
steal unless sweep flags STALE). `kit/CLAUDE.md` STOP-AND-ASK gains the two-line carve-out: git/
workspace mechanics are never ask material after plan approval — the CLI prints the next step;
follow it (real asks stay: spec ambiguity, `risk: high`, `ask` rules). 1-4 line deltas in
CONDUCTOR (poll queued lanes at wave boundaries) · INTEGRATOR (rc 3 = report queued, never spin) ·
BUILDER (taken → claim the next task) · SOLO (dirty tree → `bash ops/polaris park`) · PLANNER
(the claim-time disjointness gate backstops multi-planner races). SKILL.md gains the pointer.
Every pinned sentence comes VERBATIM from shared-checkout v1 § Pinned phrasing — ON ONE LINE,
NEVER HARD-WRAPPED, and the grep for each file rides verify: (the T-055 lesson).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] PROTOCOL § N CHATS, ONE REPO exists with the full decision table and the rc table (0/1/3)
- [ ] kit/CLAUDE.md STOP-AND-ASK carve-out present, +2 lines, managed-block shape intact
      (claudemd drill green)
- [ ] each role delta is 1-4 lines, carries its pinned fragment on ONE line, and names the exact
      command it routes to
- [ ] SKILL.md points at the new PROTOCOL section
- [ ] no behavioral claims beyond what T-058/T-059 shipped (docs describe the CLI that exists —
      the T-055 doc-before-code gap stays closed)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- kit/CLAUDE.md is a hotspot (chain edits, never parallel-own) — this is the sprint's ONLY task
  touching it, by design; keep the injected-bytes cost in mind: every line is paid 6-8× per run.
- Pinned sentences: copy from the contract byte-for-byte; hard-wrapping one mid-phrase is the
  exact failure T-055 shipped.
- The installed `ops/polaris` lags kit docs all sprint — that is expected; the doctrine ships at
  the combined 5.24.0 release.
- Long suites FOREGROUND with an explicit ≥600000ms tool timeout; past the cap → log and poll for
  `selftest passed`.
