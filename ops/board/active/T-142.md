---
id: T-142
title: "surfaces --scaffold proposes the map and --apply writes the unambiguous rows; doctor and qa say when a repo has no map"
type: feature
scope: observe
plan: spend-less
points: 5
value: 13
time_criticality: 13
risk_opportunity: 8
wsjf: 6.8
risk: normal
model:
status: active
owner: Owner@DESKTOP
branch: feat/T-142
files_owned:
  - kit/ops/lib/observe.sh
  - ops/tests/surfaces-scaffold.cmd
  - ops/tests/surfaces-scaffold.expected
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/test-surfaces.md
  - ops/contracts/first-run.md
  - kit/ops/lib/surfaces.sh
  - ops/tests/adopt-stub.cmd
  - kit/ops/lib/selftest/policy.sh
depends_on: [T-140, T-148]
contract: ops/contracts/test-surfaces.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - test "$(grep -c '^surfaces_apply()' kit/ops/lib/observe.sh)" = 1
  - grep -q 'usage: polaris surfaces \[--scaffold \[--apply\]\]' kit/ops/lib/observe.sh
  - grep -q 'runs on \$BASE only' kit/ops/lib/observe.sh && grep -q 'nothing was committed for you' kit/ops/lib/observe.sh && grep -q '\[scaffold\]' kit/ops/lib/observe.sh && grep -q 'kept (already set here)' kit/ops/lib/observe.sh
  - grep -q 'surfaces: none mapped' kit/ops/lib/observe.sh && grep -q 'ran the whole suite (' kit/ops/lib/observe.sh && grep -q 'command -v interview_pending' kit/ops/lib/observe.sh
  - bash kit/ops/polaris surfaces 2>&1 | grep -q 'no surfaces yet'
  - bash kit/ops/polaris surfaces --scaffold 2>&1 | grep -q 'no test runner this kit can scope'
  - bash kit/ops/polaris surfaces --bogus >/dev/null 2>&1; test $? = 1
  - bash -c "$(cat ops/tests/surfaces-scaffold.cmd)" | diff - ops/tests/surfaces-scaffold.expected
  - bash -c "$(cat ops/tests/triage-lane.cmd)" | diff - ops/tests/triage-lane.expected
  - bash -c "$(cat ops/tests/rules-health.cmd)" | diff - ops/tests/rules-health.expected
  - for r in 'observe.sh.fn.surfaces_apply$' 'admin.sh.fn.cmd_interview$' 'admin.sh.fn.interview_pending$' 'admin.sh.fn.interview_set$' 'KEYS.tsv.key.adhd$'; do grep -q "$r" ops/tests/api-kit.expected || exit 1; done
  - POLARIS_ROOT=$PWD python kit/ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | grep -vE 'admin.sh.fn.(cmd_interview|interview_pending|interview_set)$|KEYS.tsv.key.adhd$' | diff - <(grep -vE 'admin.sh.fn.(cmd_interview|interview_pending|interview_set)$|KEYS.tsv.key.adhd$' ops/tests/api-kit.expected)
  - bash kit/ops/polaris doctor --fast | tail -1 | grep -q 'fast tier passed'
map_delta: "observe.sh: surfaces --scaffold renders the engine's proposal (runner, rows, skips), --scaffold --apply (surfaces_apply; <base> only) writes the rows tagged [scaffold] and sets test_select: when unset — the second sanctioned writer of ops/SURFACES.tsv; doctor and qa print the one-line activation nudge only when a runner is detectable and the map is empty; doctor prints the interview's pending line; golden surfaces-scaffold"
---
## Why
T-140 built the engine; this is the command a human or a planner runs. `surfaces --scaffold` shows
the proposal — the runner it found, the rows it would write, and every pairing it skipped with the
reason — and writes nothing. `--scaffold --apply` writes exactly those rows into `ops/SURFACES.tsv`
tagged `[scaffold]` and sets `test_select:` when the repo has never set it (a stub is replaced in
place; a live value, even empty, is the human's and is kept), on `<base>` only, and then says
"review, then commit" instead of committing. That makes it the second sanctioned writer of a
RULES-guarded file, so the contract's § 16 widens D1/D2 for it and nothing else. The other half of
activation is telling a repo its map is empty at the moments it matters: `doctor` (one line, after
the config-drift line) and `qa` (one line, after a whole-suite run of a minute or more) — both gated
on the engine detecting a runner, so a repo the scaffold cannot help (this one) is never nagged.
`doctor` also prints the interview's `preferences never set here:` line (first-run.md § 2) through a
`command -v` guard, because T-141 lands beside you this wave. You are this wave's single owner of
`ops/tests/api-kit.expected`: write your `surfaces_apply` row and T-141's three fns + `adhd` key row
from the contract's names. The golden `surfaces-scaffold` (§ 17, five fixtures) is yours and is the
whole activation's regression lock — pin every byte the contract pins.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `cmd_surfaces` flags: none · `--scaffold` · `--scaffold --apply`; anything else dies with the pinned usage; plain `surfaces` byte-identical to v1 § 7 (the `surfaces` drill's steps 1–7 unchanged)
- [ ] `--scaffold` renders per § 14: the `runner:` note, the three-column table, `   ⚠ skipped:` lines, the three tails; NORUNNER ⇒ the pinned note; rc 0 always; writes nothing (assert the map and CONVENTIONS byte-identical after)
- [ ] `--scaffold --apply` per § 14: the `feat/*` and no-CONVENTIONS refusals; `surfaces_apply` (the ONLY new top-level fn) seeds, appends `[scaffold]` rows, handles `test_select:` live/stub/absent exactly as pinned, temp file + mv; the two tail lines; idempotent second run
- [ ] doctor: the surfaces nudge directly after the CONFIG DRIFT block, gated on `$CONV` + `cfg test` + runner + empty map; then the guarded preferences line; `qa`: the whole-suite nudge after `last-suite-seconds`, gated on `ran ≥ 1`, no selection, `t1 - t0 ≥ 60`, runner, empty map — every existing drill that greps doctor/qa lines still green
- [ ] golden `ops/tests/surfaces-scaffold.cmd/.expected` with the five § 17 fixtures — hermetic, run from the repo root, < 20 s; each fixture commits its files; case 1 asserts the four rows, the two skips, the apply, the health tail, the idempotent rerun, the stub replacement, the kept live value and the feat/* refusal
- [ ] `api-kit.expected`: `surfaces_apply` + T-141's four pinned rows by content diff; no other hunk; `rules-health` unchanged (16)
- [ ] `doctor --fast` green; `doctor --selftest --only surfaces,qa,finish,drift` green in the worktree (explicit 600000 ms timeout); the whole `test:` green at the wave gate via `bg run test`

## Notes
- The verify api-kit line excludes T-141's four rows from BOTH sides on purpose: admin.sh and KEYS.tsv land in a parallel lane this wave and your worktree cannot index them (CONVENTIONS § Planner calibration 2026-09-14). Assert presence, diff the rest; the strict diff is the wave gate's.
- `surface_change_set` sets globals as side effects — call it with a file redirect, never inside `$(...)` (T-136's note); `surfaces_proposal` is pure and safe in `$(...)`.
- The scaffold's `git ls-files` is the caller's ONE fork; hand the engine a temp file, never a pipe.
- In THIS repo `surfaces --scaffold` answers NORUNNER (`seen: no manifest`) — correct, and the reason this repo's own rows arrive by hand next sprint (IDEAS.md).
- Scratch paths under `scratchpad/T-142/…` only; never run `doctor --selftest` from the primary while a sibling lane's suite runs — in your worktree, via `bg run` or an explicit ≥ 600000 ms timeout; a subagent never ends its turn with a job running.
- Bash ≥ 3.2: no `case` inside `$(...)`, no mapfile, no assoc arrays. Trust rc and file state (T-089); never qa's rc inside a fixture (T-131).
