---
id: T-131
title: Prove the carried stamp, not the whole board — the express drill stops failing on other drills' cruft
type: bug
scope: selftest
points: 1
value: 8
time_criticality: 13
risk_opportunity: 5
wsjf: 26
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-131
files_owned:
  - kit/ops/lib/selftest/history.sh
approved:
context_files:
  - kit/ops/lib/selftest/board.sh
  - kit/ops/lib/selftest/policy.sh
depends_on: []
contract:
verify:
  - bash -n kit/ops/lib/selftest/history.sh
  - bash -c 'grep -c "EXPRESS QA" kit/ops/lib/selftest/history.sh'
---
## Why
The express drill added last sprint checks the right feature the wrong way. It asks whether the
whole `qa` command comes back happy, but `qa` also audits board tidiness, and by the time this drill
runs, earlier drills have left leftover branches lying around in the practice repo. So `qa` reports
untidiness, the drill calls that a failure, and the whole suite goes red on all three operating
systems — while the feature it was meant to check works perfectly. When the drills are split across
three parallel workers the leftovers land somewhere else, everything looks green, and that is why
this got through.

The drill now checks what it actually means: that the carried-over green result makes the checks get
skipped, and that one more change makes them run again. Both facts are read straight out of the
command's own report, so nothing another drill leaves behind can move them.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [x] The express drill no longer treats `qa`'s overall result as its assertion.
- [x] It still proves the skip happens, naming the exact sealed point.
- [x] It still proves one further change makes the checks run again.
- [x] The whole suite, run start to finish in one go, comes back clean.

## Notes
- Long suite: run it detached and collect the result in chunks; a foreground run is cut off.
- Route taken: NARROWED the assertions (the option the brief preferred) — no fixture surgery, so no other drill's behaviour moves. `qa`'s rc is no longer read; the skip line (naming the sealed sha) and the presence/absence of the suite's own green line are.
- Coverage trade, stated plainly: the old line 138 check that a re-run `qa` writes a fresh stamp is gone, because in this fixture `qa` always ends red on other drills' leftover branches and so never reaches the stamping step at all. It was never really testing that. What replaces it proves the same thing from the outside: the checks actually run again after one more change.
- Full serial suite (what CI runs) green: rc 0 in 20m, all 35 drills — no other serial-only coupling surfaced.
