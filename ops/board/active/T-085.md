---
id: T-085
title: "ownership-guard learns the primary — no gate-free writes while lanes run"
type: feature
scope: hooks
epic: enforced-isolation
plan: enforced-isolation
points: 3
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 6.0
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-085
files_owned:
  - kit/ops/hooks/ownership-guard.sh
context_files:
  - kit/ops/hooks/readonly-allow.sh
  - ops/MANUAL.md
  - ops/contracts/shared-checkout.md
depends_on: []
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/hooks/ownership-guard.sh
  - grep -q 'builders never edit the shared primary' kit/ops/hooks/ownership-guard.sh
  - grep -q '^primary_gate()' kit/ops/hooks/ownership-guard.sh
  - test -z "$(printf '{"cwd":"/tmp/not-a-repo","tool_input":{"file_path":"/tmp/not-a-repo/src/x.py"}}' | bash kit/ops/hooks/ownership-guard.sh)"
map_delta: "ownership-guard gains primary_gate — writes to tracked source in the shared primary are denied while any task lock exists and HEAD is not feat/*"
---
## Why
The write-guard keys ownership off the CURRENT DIRECTORY'S branch (`feat/*` = task, anything
else = no gate at all). A session that never entered its worktree sits in the primary on `main`
with the ownership system fully disengaged — the exact state five colliding sessions were in.
Add `primary_gate`, called before the existing branch-keyed path: when cwd is the primary
worktree AND HEAD is not `feat/*` AND at least one task lock dir exists under polaris-locks
(ignore `.int-lease` and the board mutex), deny writes to tracked source paths with the
contract's pinned remedy line. Allowlist first: `ops/board/`, `ops/contracts/`, `ops/*.md`,
`.polaris/`, untracked paths — a PLANNER or INTEGRATOR in the primary keeps working normally.
No locks (INIT, lone planner, empty board) = no gate: accepted trade, recorded in contract § v2.2.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] primary + planted lock + HEAD main + tracked source path (e.g. kit/ops/polaris) → deny
      carrying the pinned ONE-LINE message from contract § v2.2, byte-exact
- [ ] same payload but path under ops/board/ or ops/contracts/ or ops/*.md or .polaris/ → allow
- [ ] no lock dirs (or only .int-lease/board-mutex) → allow; cwd inside .polaris/wt/<ID> →
      primary_gate is a no-op and the existing branch-keyed path runs unchanged
- [ ] non-repo / unreadable cwd → fail OPEN (allow), like every other error path in this hook
- [ ] exactly ONE new top-level fn: primary_gate (api-kit pin; cleanup/jstr/lc/norm stay)
- [ ] existing behavior on feat/* branches byte-for-byte unchanged
- [ ] stays within the hook's configured timeout — no new subprocess storms (the guard once
      failed OPEN at 4s/edit; keep git calls minimal and short-circuited)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- bash 3.2 rules apply; scratch under scratchpad/T-085/ only.
- Locks live at "$(git rev-parse --git-common-dir)/polaris-locks" — see ops/MANUAL.md claim recipe.
- Functional drill coverage (planted locks, real repo fixtures) lands with T-089's checkoutguard
  drill; your verify proves shape + pinned string + fail-open.
- ops/tests/api-kit.expected is NOT yours — T-086 owns it this wave; primary_gate is pinned in
  the contract for it.
