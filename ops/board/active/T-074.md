---
id: T-074
title: "KEYS.tsv — the key registry ships with the kit"
type: feature
scope: keys
epic: autonomy-by-default
plan: autonomy-by-default
points: 2
value: 8
time_criticality: 8
risk_opportunity: 5
wsjf: 10.5
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-074
files_owned:
  - kit/ops/KEYS.tsv
  - kit/ops/install.sh
  - ops/RULES.tsv
  - ops/tests/rules-health.expected
context_files:
  - ops/contracts/key-registry.md
  - kit/ops/roles/INIT.md
  - ops/tests/rules-health.cmd
depends_on: []
contract: ops/contracts/key-registry.md
verify:
  - bash -n kit/ops/install.sh
  - grep -q 'KEYS.tsv' kit/ops/install.sh
  - "awk -F'\\t' '!/^#/ && NF>0 && NF!=4 {bad=1} END {exit bad}' kit/ops/KEYS.tsv"
  - "test \"$(awk -F'\\t' '!/^#/ && NF==4' kit/ops/KEYS.tsv | wc -l)\" -eq 37"
  - bash ops/polaris rules
  - bash ops/polaris check --only rules-health
map_delta: "kit/ops/KEYS.tsv added — the CONVENTIONS key registry (key · since · default · absent-cost), shipped via KIT_CODE, consumed by doctor drift + adopt (ops/contracts/key-registry.md)"
---
## Why
`update` refreshes kit code and never rewrites CONVENTIONS.md — correct, and exactly why every
capability gated on a NEW key ships dormant: nothing in the system compares an installed repo's
config against the kit's feature set (polaris-testbed is missing 19 keys on byte-identical kit
code). This file is the single source of truth both fixes read: doctor's one-line drift report
(T-076) and `polaris adopt` (T-077). One TAB-separated row per key: key · version introduced ·
effective default · what the repo loses while it is absent.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/ops/KEYS.tsv` exists with exactly the 37 rows named in key-registry.md § 1 (the
      INIT.md:104-195 skeleton keys + `autolaunch_max`, `integration_wait_minutes`,
      `integration_stale_minutes`), every row 4 TAB-separated fields
- [ ] the `default` column records the EFFECTIVE 6.0 value — for the three autonomy knobs that is
      auto / default-safe / auto-reversible per hands-free-knobs.md v2, NOT the pre-6.0 values
- [ ] `KEYS.tsv` added to `KIT_CODE` at kit/ops/install.sh:76 — one token; both install paths
      already loop that list, so fresh AND live-board installs ship/refresh `ops/KEYS.tsv`
- [ ] `ops/RULES.tsv` gains a `path` rule guarding `ops/KEYS.tsv` as an installed copy, matching
      the 14 sibling rules' message style, with a `#` comment naming who decided and why
      (Invariant 11)
- [ ] `ops/tests/rules-health.expected` updated for the new rule count (14 → 15) — this task
      changes RULES.tsv, so it owns the golden that records it
- [ ] surface-frozen (key-registry.md § 5): no new top-level fn, no new markdown heading under
      kit/ — never touch ops/tests/api-kit.expected

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Rows document; they never execute. Wording for `absent-cost` comes from the INIT.md skeleton
  comments — compress, don't invent.
- `kit/ops/selftest-install.sh` (CI + the wave gate) proves the file ships; you do not need to run
  the full install drill locally. If you do: FOREGROUND with an explicit ≥600000ms timeout, or
  log-and-poll.
- bash 3.2: no `case` inside `$(...)`.
