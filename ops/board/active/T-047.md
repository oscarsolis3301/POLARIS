---
id: T-047
title: "`ask` rule kind + task-ID threaded through RULES enforcement"
type: feature
scope: rules
epic: ask-rule-kind
plan: ask-rule-kind
points: 5
value: 13
time_criticality: 13
risk_opportunity: 13
wsjf: 7.8
risk: high
status: active
owner: Owner@DESKTOP
branch: feat/T-047
files_owned:
  - kit/ops/lib/ownership.sh
  - kit/ops/lib/integrate.sh
context_files:
  - ops/contracts/ask-approval.md
  - kit/ops/lib/builder.sh
  - ops/RULES.tsv
depends_on: []
contract: ops/contracts/ask-approval.md
verify:
  - bash -n kit/ops/lib/ownership.sh
  - bash -n kit/ops/lib/integrate.sh
  - bash kit/ops/polaris rules
  - bash ops/polaris check --only startup-budget
  - bash kit/ops/polaris doctor --selftest --only newcmds
map_delta:
---
## Why
`ops/RULES.tsv` has two kinds, `path` and `content`, and both mean **never**. A rule whose message
says "human decision, stop-and-ask" is enforced as a wall, so a directory under a `path` rule is
unbuildable by POLARIS forever — even after a human has approved the exact change at the plan gate.
That happened in the field (repo ARC): approval given, Builder still died on its first write, work
lost. This task adds the missing third kind and threads the one piece of information enforcement is
missing — WHICH TASK is writing — down to the matcher, so an approval recorded on that task can be
consulted.

You are the foundation of the sprint: T-048 (`polaris approve`) and T-049 (the plan gate) both build
on the helpers you add here. Read `ops/contracts/ask-approval.md` § 1 and § 4 — they are exact.

Two things this task must NOT do: weaken `path` or `content` in any way, and pass when no task ID is
supplied. No-ID is a session with nothing to carry an approval, so it **denies** — fail closed.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `rule_scan_path <path> [<ID>|-]` takes an optional second argument; `path` and `content`
      behaviour is byte-for-byte unchanged when it is absent
- [ ] an `ask`-kind rule denies exactly like `path` when no covering approval exists
- [ ] an `ask`-kind rule passes when the named task's `approved:` list covers the scope, matched with
      the ordinary `files_owned` matcher (`owned_match`), scope-for-scope equality NOT required
- [ ] the approval-coverage helper parses each `approved:` entry's leading whitespace-delimited token
      as the scope and ignores everything after it
- [ ] `ask` + ID `-` or omitted → deny (fail closed)
- [ ] a second helper answers "does this scope match at least one `ask` rule?" — T-048 calls it for
      its no-op precondition; export it plainly, do not inline the logic
- [ ] `check_rules <ref> [<ID>]` accepts and forwards the ID; `cmd_guard` forwards the ID it already
      receives; `cmd_rules_check` (`_rules`) passes `-`
- [ ] `cmd_audit` and `cmd_land` in `kit/ops/lib/integrate.sh` pass their ID to `check_rules`
- [ ] when a check passes BECAUSE of an approval, `check_rules` prints a line naming the scope and
      the approval entry (so it reaches the handoff report and the Integrator)
- [ ] guard exit codes unchanged: 0 clean · 1 rules deny · 3 ownership deny
- [ ] `bash ops/polaris check --only startup-budget` stays green — no `printf … | owned_match`
      per-rule pipe reintroduced, no new fork on the guarded-write path

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- bash 3.2 (macOS CI): a `case` statement inside `$(...)` is a HARD parse error. Keep `case` out of
  command substitution.
- `_guard` runs on EVERY Edit/Write against a 10s hook timeout, and each polaris startup is ~3.8s on
  Windows/Git Bash. Reading a task file per rule would blow that. Read `approved:` at most once per
  `rule_scan_path` invocation, and only when an `ask` rule actually matches.
- Run long suites FOREGROUND with an explicit ≥600000ms timeout. If the harness caps it, log to a
  file and poll for the `selftest passed` line — never end a turn waiting on a notification.
- `risk: high` — a defect here silently opens `ops/` and `.github/`. The Integrator needs explicit
  human approval in the conversation before merging.
