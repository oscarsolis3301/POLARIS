---
id: T-049
title: "Move the ask to the plan gate — ready gate, triage three-way, rules health"
type: feature
scope: board
epic: ask-rule-kind
plan: autonomy-by-default
points: 5
value: 13
time_criticality: 8
risk_opportunity: 8
wsjf: 5.8
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-049
files_owned:
  - kit/ops/lib/observe.sh
  - ops/tests/triage-lane.cmd
  - ops/tests/triage-lane.expected
  - ops/tests/api-kit.expected
context_files:
  - ops/contracts/ask-approval.md
  - kit/ops/lib/ownership.sh
  - ops/tests/triage-lane.cmd
depends_on:
  - T-047
  - T-048
  - T-076
contract: ops/contracts/ask-approval.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - bash kit/ops/polaris rules
  - bash kit/ops/polaris triage | sed -n 1p | grep -qE '^(solo|express|full)$'
  - bash ops/polaris check --only triage-lane
  - bash kit/ops/polaris doctor --selftest --only drift
  - bash ops/polaris check --only api-kit
map_delta:
---
## Why
This is the half that actually fixes the ARC failure. T-047 makes an approval *possible*; this makes
the board *ask for it before a Builder is ever spawned*.

The ARC sequence was: ready gate never consults RULES → task promoted to `ready/` with nothing
objecting → `triage` prints `full` → Builder claims it → dies on its first write. Break it at step 1
and the rest never happens. A task that needs a human's yes belongs in `blocked/` with a note, not in
`ready/` waiting to burn a Builder's whole context discovering it cannot write.

Read `ops/contracts/ask-approval.md` § 5 — the three triage cases and the finding text are exact.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `cmd_rules` accepts `ask` as a valid kind in its health check (alongside `path|content`), and
      does not demand an ERE for it — `-` is correct for `ask`, as for `path`
- [ ] `cmd_drift` step 2 gains a check: a `ready/` task whose `files_owned` intersects an `ask` scope
      with no covering `approved:` entry is a finding beginning `READY GATE:`, naming the task, the
      owned pattern and the scope
- [ ] that finding makes `drift --strict` exit 1
- [ ] a `ready/` task WITH a covering approval produces no finding
- [ ] `cmd_triage` distinguishes three cases instead of one: `path` scope → `full` +
      `cannot be built as specified` · `ask` without approval → `full` +
      `get the human's yes before starting` · `ask` WITH approval → fall through to ordinary
      points-based routing
- [ ] `triage` line 1 is still exactly one bare word (`solo`/`express`/`full`); every reason stays on
      a `   ` note line
- [ ] `bash ops/polaris check --only triage-lane` green — the golden asserts line-1 shape, the
      3-point SOLO threshold literal, and that help agrees with it

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Use T-047's approval-coverage helper. Do NOT re-implement the `approved:` parse here; two parsers
  for one field is exactly how the two halves drift apart.
- `cmd_triage`'s existing loop already calls `rule_scan_path` per owned pattern and treats any
  non-zero as "RULES-guarded". You need the *reason*, not just the rc — decide the shape (a second
  helper, or an exported variable) and keep `rule_scan_path`'s own contract unchanged.
- bash 3.2: no `case` inside `$(...)`.
- `drift` runs inside `qa` as `drift --strict`; a false finding reds the whole gate. The drill
  fixtures must leave `ops/RULES.tsv` pristine (T-046 hermeticity) — T-050 owns the drills, but do
  not leave a stray `ask` rule behind while testing by hand.
- Long suites FOREGROUND with an explicit ≥600000ms timeout, or log-and-poll for `selftest passed`.
- re-parked 2026-08-03 by the Planner per the approved isolation plan (`n-chats-one-repo`): sprint 8
  reworks `kit/ops/lib/observe.sh`, which this task owns. Re-groom after the 5.24.0 release —
  5.24.0 is now the COMBINED two-sprint release at the program's end; sprint 7's "5.24.0 at the
  close" target is superseded, and the `ask`-rule remainder (T-048/T-049/T-050) rides a later one.
- 2026-08-03: T-062 rewrote the triage-lane golden to run against a HERMETIC fixture board (the
  live-board version flipped on any ready-task filing — wave-2 gate 11/12). When this task is
  re-groomed, its triage three-way assertions extend the FIXTURE `ops/tests/triage-lane.cmd`;
  never re-anchor that golden to the live board.
- Integrator (sprint 9 W1 close): hold CONFIRMED for a second, independent reason — sprint 9's T-070
  owns `kit/ops/lib/observe.sh`, which this task also owns, so promoting on satisfied deps alone
  would have broken W2's disjointness. Deps (T-047) are green; ownership is what parks it.
- re-groomed 2026-08-03 by the Planner (plan `autonomy-by-default`): the T-070 ownership hold is
  lifted. Serial-chained behind T-076 on observe.sh and behind T-048 for the api-kit golden — this
  task is W3's sole api-kit.expected owner (key-registry.md § 5) and now owns the FIXTURE
  triage-lane pair for its three-way assertions (extend the fixture, never re-anchor to the live
  board). The triage decision the 6.0 brief flags is already contracted: an `ask` scope WITH a
  covering approval falls through to points-based routing (ask-approval.md § 5) — that stands;
  implementing it needs the ID-threaded scan, which is this task's work, not a new decision.
