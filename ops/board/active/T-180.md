---
id: T-180
title: "pack and builder rider — one index call per pack, no basename loops in the builder, and a correct note about the working folder at claim"
type: chore
scope: pack
plan: v4-fast
points: 1
value: 2
time_criticality: 1
risk_opportunity: 2
wsjf: 5.0
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-180
files_owned:
  - kit/ops/lib/builder.sh
  - kit/ops/index.py
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/code-index.md
  - ops/tests/pack-visual.cmd
depends_on: []
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - python -c "import ast; ast.parse(open('kit/ops/index.py', encoding='utf-8').read())"
  - bash ops/tests/index-selfcheck.cmd 2>/dev/null | tr -d '\r' | diff - ops/tests/index-selfcheck.expected
  - test "$(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api kit/ops/bench.sh kit/ops/hooks/update-hook.sh | cut -f1 | sort -u | wc -l | tr -d ' ')" = 2
  - test "$(grep -v '^[[:space:]]*#' kit/ops/lib/builder.sh | grep -c 'basename' | tr -d ' ')" = 0
  - bash ops/tests/pack-visual.cmd 2>/dev/null | diff - ops/tests/pack-visual.expected
  - bash ops/tests/startup-budget.cmd 2>/dev/null | diff - ops/tests/startup-budget.expected
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta:
surface:
---
## Why
`pack` is the one call a builder makes to load its whole task. For every path the task owns, it
starts a fresh polaris + python + index-refresh chain to list that path's public functions — about
1–2 s per cold start for a typical task. Let `find --api` take several globs in one call (one index
build, results printed in argument order) and have `pack` call it once with the whole owned list,
keeping the per-path grouping so `pack`'s output does not change by a single byte.

Two small things ride along in `builder.sh`: its three `basename` calls inside loops become shell
builtins, and the note `claim` prints about the working folder is corrected — a subagent's Bash
resets its working folder between calls, so the note must say to use absolute paths (or `cd` in
the same call), not that a `cd` sticks.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `find --api g1 g2 …` builds the index once and prints each glob's rows in argument order; one glob behaves exactly as today.
- [ ] `cmd_pack` calls it once with the whole owned list; `pack-visual` is byte-identical.
- [ ] No `basename` left in builder.sh code.
- [ ] `claim`'s working-folder note says subagent Bash resets the folder between calls.
- [ ] `index-selfcheck` green.
- [ ] No new api rows (speed.md § 5): the loop over globs lives inside the existing functions.

## Notes
- `index-selfcheck` prints CRLF through python on this machine; the verify line strips `\r`, as `check` does.
- Evidence (plans/audit-0924.md, verbatim):
  - **close-9** · OVERSTATED · pack starts a whole new polaris + python + index-refresh process chain for EVERY owned path — An estimate of about 1-2 s per Builder cold start for a typical task (about 1.85 fewer find calls), and 0 s for the third of tasks that own one path. — fix: Let index.py `find --api` take several globs (`--api g1 g2 …`, one build(), print in order), and call it once from cmd_pack with the full owned list. Keep the per-path grouping in the output so the pack-visual golden stays byte-identical. (kit/ops/lib/builder.sh, kit/ops/index.py)
