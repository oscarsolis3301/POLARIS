---
id: T-055
title: "MANUAL, PROTOCOL and the skill learn the third rule kind"
type: chore
scope: docs
epic: ask-rule-kind
plan: ask-rule-kind
points: 2
value: 5
time_criticality: 3
risk_opportunity: 3
wsjf: 5.5
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-055
files_owned:
  - kit/ops/MANUAL.md
  - kit/ops/PROTOCOL.md
  - kit/.claude/skills/polaris/SKILL.md
context_files:
  - ops/contracts/ask-approval.md
  - kit/ops/MANUAL.md
  - kit/ops/PROTOCOL.md
depends_on: []
contract: ops/contracts/ask-approval.md
verify:
  - grep -q 'lifted only by a human' kit/ops/MANUAL.md
  - grep -q 'lifted only by a human' kit/ops/PROTOCOL.md
  - grep -q 'lifted only by a human' kit/.claude/skills/polaris/SKILL.md
  - grep -c 'ask' kit/ops/PROTOCOL.md
map_delta:
---
## Why
Three surfaces still describe a two-kind world, and each one is load-bearing somewhere the CLI is not.

`kit/ops/MANUAL.md` § "Ownership + RULES proof" is the recipe an agent follows when it CANNOT execute
the CLI — it spells out the two kinds by hand. If it never learns the third, the manual path silently
enforces `ask` as `path`, which is the ARC failure with extra steps. `kit/ops/PROTOCOL.md` § LANES
says `solo` requires "nothing RULES-guarded", which after T-049 is no longer true — an `ask` scope
WITH an approval falls through to ordinary routing, and one fact must have one home. The skill file
tells every session "never edit RULES.tsv", which predates Invariant 11 and now also misses the
approval route.

Added by the Planner beyond the brief's file list: leaving these stale is documentation drift that
contradicts shipped behaviour, which is the exact failure mode `check --only triage-lane` exists to
catch on the CLI side.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] MANUAL.md § "Ownership + RULES proof" describes all three kinds and how to check an `ask` rule
      by hand: match the scope, then read the task's `approved:` list for a covering entry
- [ ] MANUAL.md's "never edit RULES.tsv" sentence is corrected — RULES are agent-maintained
      (Invariant 11) EXCEPT that converting a rule between `path` and `ask` is a human decision
- [ ] PROTOCOL.md's `RULES.tsv` line names three kinds, and the LANES table's `solo` row reads
      "nothing RULES-guarded, or `ask`-guarded with a recorded approval"
- [ ] SKILL.md's invariant summary names `ask` and the approval route, and drops "never edit
      RULES.tsv" in favour of the Invariant 11 wording
- [ ] all three quote the contract's pinned one-liner verbatim
- [ ] no line count grows by more than ~4 lines per file — these are clauses, not sections

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Quote the contract's pinned strings VERBATIM. T-053 and T-054 land the same phrases in other files
  in parallel; identical strings merge without conflict, paraphrases do not.
- Backlog on purpose: it is the lowest-wsjf item and holds `ready/` to five disjoint lanes, which is
  where this repo's calibration puts the evidence (0 kickbacks, n=29). Drains automatically on the
  `ask-rule-kind` plan.
