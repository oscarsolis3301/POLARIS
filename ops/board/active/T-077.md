---
id: T-077
title: "`polaris adopt` — commented stubs for every missing key, plus the 6.0 update banner"
type: feature
scope: adopt
epic: autonomy-by-default
plan: autonomy-by-default
points: 5
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 3.6
risk: normal
status: active
owner: Owner@DESKTOP
branch: feat/T-077
files_owned:
  - kit/ops/lib/admin.sh
  - kit/ops/polaris
  - ops/tests/adopt-stub.cmd
  - ops/tests/adopt-stub.expected
  - ops/tests/api-kit.expected
context_files:
  - ops/contracts/key-registry.md
  - ops/contracts/hands-free-knobs.md
  - ops/tests/route-tier.cmd
depends_on:
  - T-074
  - T-048
contract: ops/contracts/key-registry.md
verify:
  - bash -n kit/ops/lib/admin.sh
  - bash kit/ops/polaris help | grep -q 'adopt'
  - grep -qF "add 'autonomy: standard' to ops/CONVENTIONS.md" kit/ops/lib/admin.sh
  - bash ops/polaris check --only adopt-stub
  - bash ops/polaris check --only cli-help-parity
  - bash ops/polaris check --only api-kit
  - bash kit/ops/polaris doctor --selftest --only upgrade
map_delta: "polaris gains `adopt` — appends every KEYS.tsv key missing from CONVENTIONS.md as a commented stub (default + rationale), never edits a value, idempotent; `update` prints the BREAKING banner when the incoming kit is >=6.0.0 and no autonomy knob is set (key-registry.md § 3-4)"
---
## Why
Doctor's drift line (T-076) tells you keys are missing; this is the hand that fixes it — the
discovery mechanism, deliberately NOT how autonomy arrives. `adopt` appends commented stubs
carrying each key's default and one-line rationale from KEYS.tsv, so uncommenting a line is the
whole gesture of enabling a feature. And the same release must be LOUD at the moment it lands:
`update`'s `refreshed here:`/`untouched:` pair (admin.sh:403-404) gains the two pinned BREAKING
lines naming the one-line revert, exactly when a repo crosses to 6.0 with no knob set.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `cmd_adopt` in kit/ops/lib/admin.sh per key-registry.md § 3: appends stubs to the END of
      CONVENTIONS.md — marker heading once, then `# <key>: <default>   # <absent-cost> (since
      <since>)` per absent key, KEYS.tsv order
- [ ] never edits an existing line, never uncomments, never reorders, never writes a live value;
      an existing `key:` value and an existing `# key:` stub are both left byte-identical
- [ ] idempotent: second run appends nothing, file byte-identical, prints
      `nothing to adopt — all <m> known keys present or stubbed`, rc 0
- [ ] no KEYS.tsv → die naming `ops/polaris update`; no CONVENTIONS.md → die pointing at INIT
- [ ] dispatched in kit/ops/polaris beside `update`/`upgrade` (:311-315) and documented in
      `help` in the house style; the `help` fast path stays above the module loader
      (startup-budget golden)
- [ ] `cmd_update` prints the two pinned banner lines from hands-free-knobs.md v2 § Pinned
      strings, directly after `untouched:`, ONLY when incoming kit >= 6.0.0 AND none of
      autonomy/plan_gate/builder_questions/evolve_apply is set in the target CONVENTIONS.md;
      the banner is never goldened
- [ ] golden pair `ops/tests/adopt-stub.{cmd,expected}` — HERMETIC (fixture repo + fixture
      KEYS.tsv with fake keys; no version numbers): first-run stubs · second-run no-op ·
      existing value untouched
- [ ] W2 api-kit owner (key-registry.md § 5): ops/tests/api-kit.expected gains `cmd_adopt` AND
      T-078's pinned `drill_hookmerge` (kit/ops/selftest-install.sh) — written from the pin,
      without seeing T-078's diff

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- `kit/ops/polaris` is a hotspot; T-048 lands its `approve` dispatch first (depends_on) — add the
  `adopt` line beside it, touch nothing else in the entry script.
- api-kit may sit legitimately RED mid-wave until T-078 lands its half of the pinned pair — the
  wave gate is the run that counts (Learned log, sprint 9). Never `check --update`.
- `cli-help.expected` is generated from the INSTALLED ops/polaris and regenerates at the release
  dogfood — not yours.
- `--only upgrade` FOREGROUND with an explicit ≥600000ms timeout, or log-and-poll; kit driver only.
- bash 3.2: no `case` inside `$(...)`.
