---
id: T-089
title: "Prove it — checkoutguard, readyoverlap, selfland drills + refusal goldens"
type: chore
scope: selftest
epic: enforced-isolation
plan: enforced-isolation
points: 5
value: 5
time_criticality: 3
risk_opportunity: 5
wsjf: 2.6
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-089
files_owned:
  - kit/ops/lib/selftest/spine.sh
  - kit/ops/lib/selftest/policy.sh
  - kit/ops/lib/selftest/board.sh
  - kit/ops/lib/selftest/history.sh
  - ops/tests/checkout-guard-denies.cmd
  - ops/tests/checkout-guard-denies.expected
  - ops/tests/ownership-primary.cmd
  - ops/tests/ownership-primary.expected
  - ops/tests/api-kit.expected
context_files:
  - ops/tests/readonly-allow.cmd
  - ops/tests/readonly-allow.expected
  - ops/contracts/verification-tiering.md
  - ops/contracts/shared-checkout.md
depends_on:
  - T-084
  - T-085
  - T-088
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/selftest/spine.sh
  - bash -n kit/ops/lib/selftest/policy.sh
  - bash -n kit/ops/lib/selftest/board.sh
  - bash -n kit/ops/lib/selftest/history.sh
  - grep -q 'checkoutguard readyoverlap selfland' kit/ops/lib/selftest/spine.sh
  - test -f ops/tests/checkout-guard-denies.cmd
  - test -f ops/tests/ownership-primary.cmd
  - bash kit/ops/polaris doctor --selftest --only checkoutguard,readyoverlap,selfland
map_delta: "doctor --selftest gains checkoutguard, readyoverlap and selfland drills (labels 28 to 31); golden pairs checkout-guard-denies + ownership-primary pin the refusal wording"
landed: 260dfebb2236c514a5151e4d824248e14864d890
---
## Why
The three enforcement layers are exactly the kind of behavior prose cannot hold — they must be
drilled or they will silently regress. Add three labeled drills per contract v2.6:
drill_checkoutguard (policy.sh — primary deny + worktree allow + read-only allow, plus
primary_gate: planted lock/no-lock, allowlist, fail-open — AND the fallback entry every
conductor lane actually takes: cwd pinned at the primary, a write via an ABSOLUTE path under
.polaris/wt/<ID> is allowed and the commit lands on feat/<ID>), drill_readyoverlap (board.sh — two
overlapping ready tasks: explicit claim dies with `overlaps ready`, auto-pick parks to blocked/,
drift --strict exits nonzero, plain drift exits zero), drill_selfland (history.sh — unset knob
lands a normal task to done/ through the lease; risk: high stays in review/ with the pinned
refusal; landing: integrator gives the classic notice). Register all three labels at the END of
SELFTEST_LABELS in spine.sh (order: ... route bg checkoutguard readyoverlap selfland). Scaffold
golden pairs (polaris check --scaffold) grepping the two pinned refusal lines out of the hook
scripts themselves — hermetic, no live board. Own the final api-kit.expected delta: the three
drill fns.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] all three drills green via --only, each assertion watched RED under a semantic sabotage in
      a throwaway kit copy first (read the sabotage diff — a no-op sabotage green is vacuous),
      then green on restore
- [ ] checkoutguard asserts the FALLBACK path (contract v2.6): pinned-cwd session, absolute
      worktree path write → allowed by both hooks, resulting commit on feat/<ID>
- [ ] checkoutguard asserts the deny SHAPE: hookSpecificOutput JSON on STDOUT (contract v2.1 —
      NOT ownership-guard's exit-2+stderr; the two hooks deny by different mechanisms, both
      correct as shipped)
- [ ] checkoutguard and the goldens assert git stash list / git stash show in the primary are
      ALLOWED (v2.1 carve-out — a golden asserting all stash forms deny fails against correct
      behavior)
- [ ] hermetic: git status --porcelain unchanged across each drill; green on a second
      consecutive run; green under any --only partition
- [ ] labels registered; full-run drill count goes 28 to 31; --only selects each individually
- [ ] golden pairs checkout-guard-denies + ownership-primary green via bash ops/polaris check,
      and they grep the hook FILES, not a live board
- [ ] api-kit.expected delta is exactly drill_checkoutguard + drill_readyoverlap +
      drill_selfland, sorted in place
- [ ] drill budget respected: about 44s each — reuse the throwaway-repo spine, no new sleeps

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Spine state reaches drills by bash dynamic scoping — NO `local` in drill bodies; follow the
  bare-name style of the neighbors.
- Never edit kit/ops/polaris or kit/ops/lib/*.sh while a selftest is RUNNING (Learned log).
- --only checkoutguard,readyoverlap,selfland is ~144s spine + ~132s drills: FOREGROUND with an
  explicit >=600000ms timeout, or log-and-poll for `selftest passed`; kit driver only.
- Windows/Git Bash: never assert on CR-sensitive output via grep/sed — use awk/od; new goldens
  are auto-LF-pinned.
- Scratch dirs per-lane: scratchpad/T-089/ only; never rm -rf a bare shared path.
- `polaris check` is PRIMARY-anchored — worktree goldens pass vacuously; exercise for real by
  running the .cmd by hand in the worktree before trusting green.
- `find --api` is also PRIMARY-anchored (search.sh:13): a worktree-run api-kit check cannot see
  a file added in that worktree — prove the golden delta via the index db / POLARIS_ROOT recipe.
- INTEGRATOR (wave 3, 2026-08-23): selfland drill fixtures MUST declare `risk:` frontmatter
  explicitly — `self_land` fails CLOSED and refuses SILENTLY when a task carries no `risk:` at
  all (T-088's recorded `- assumed:` decision; that is why every pre-6.1 fixture kept the suite
  byte-identical). A fixture without `risk:` makes the drill silently test NOTHING — worse than
  a missing drill. Declare `risk: normal` (happy path) and `risk: high` (refusal path) per case.
- BUILDER (T-089): guard-drill payload paths must be GIT-CANONICAL (rev-parse --show-toplevel, C:/… on Windows), never $PWD — a /tmp-style cwd reads as outside-the-repo in ownership-guard's norm; the exact fragility that pulled the old wrapper harness
- BUILDER (T-089): the throwaway repo has no ops/polaris (init-board installs none) and ownership-guard stands down without one — drill_checkoutguard plants a sh forwarder to $SELF, removes it in teardown
- BUILDER (T-089): 4 semantic sabotages each red at the targeted assertion — stash carve-out dropped / primary_gate exit dropped (refusal still printed, rc 0: the rc assertion is what catches a fail-open) / ready sweep dropped / risk-high gate dropped (T-SL2 visibly self-landed)
- BUILDER (T-089): kit spine 31 labels vs installed 28 — recounted first-hand, the kit-vs-installed tell holds until next dogfood; single-label partitions proven green via --parallel 3
