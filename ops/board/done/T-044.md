---
id: T-044
title: extract lib/knowledge.sh + lib/observe.sh
type: chore
scope: cli
plan: many-hands
points: 3
value: 5
time_criticality: 2
risk_opportunity: 2
wsjf: 3.0
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-044
files_owned:
  - kit/ops/polaris
  - kit/ops/lib/knowledge.sh
  - kit/ops/lib/observe.sh
context_files:
  - kit/ops/lib/core.sh
  - ops/contracts/brain.md
depends_on: [T-043]
contract: ops/contracts/module-layout.md
verify:
  - bash -n kit/ops/polaris && bash -n kit/ops/lib/knowledge.sh && bash -n kit/ops/lib/observe.sh
  - grep -q '^cmd_brain()' kit/ops/lib/knowledge.sh && ! grep -q '^cmd_brain()' kit/ops/polaris
  - grep -q '^cmd_report()' kit/ops/lib/knowledge.sh && ! grep -q '^cmd_report()' kit/ops/polaris
  - grep -q '^cmd_doctor()' kit/ops/lib/observe.sh && ! grep -q '^cmd_doctor()' kit/ops/polaris
  - grep -q '^cmd_drift()' kit/ops/lib/observe.sh && ! grep -q '^cmd_drift()' kit/ops/polaris
  - test "$(wc -l < kit/ops/lib/knowledge.sh)" -le 1200 && test "$(wc -l < kit/ops/lib/observe.sh)" -le 1200
  - bash kit/ops/polaris help >/dev/null
  - bash kit/ops/polaris doctor --selftest --only brain
  - bash kit/ops/polaris doctor --selftest --only report
map_delta:
landed: ebc614affb08f329019c0ac48ae3b3533af6a332
---
## Why
Fifth link of the chain, two modules in one pass because they share no seam risk: the knowledge
generators (report_* and sprint_* helpers, ts_date, event_ts, resolve_sprint_ids, render_*,
report_dirty_hint, report_one, cmd_report, seal_report_commit, board_changed_touch,
brain_refresh_if_present, the six brain_* writers, cmd_brain — the contract's 26) move verbatim
into kit/ops/lib/knowledge.sh, and the read-only observers (cmd_notify_gate, status_brief,
cmd_status, cmd_sweep, cmd_doctor, pat_overlap, dep_ids, dep_reaches, cmd_drift, cmd_rules,
cmd_qa, cmd_metrics, cmd_why, cmd_dash, find_claude, find_claude_windows, cmd_fleet — the
contract's 17) into kit/ops/lib/observe.sh. cmd_doctor keeps calling selftest() across modules —
free at runtime, everything is sourced before dispatch. seal (integrate.sh, landed last wave)
keeps calling seal_report_commit/brain_refresh_if_present/board_changed_touch the same way.
Extend the loader list with `knowledge observe` at their final positions. Original relative
order, bodies untouched, zero behavior change.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] knowledge.sh holds exactly the contract's 26 functions; observe.sh exactly its 17; nothing else moved
- [ ] loader list reads `core ownership builder integrate knowledge observe selftest/…` (contract growth schedule)
- [ ] `report`/`brain`/`status --brief` outputs spot-diffed byte-identical against the pre-task script
- [ ] full `bash kit/ops/polaris doctor --selftest` green (handoff gate `test:`)

## Notes
Long suites: run FOREGROUND with an explicit 600000ms timeout; if the harness caps it, log to a file and poll for the `selftest passed` line.
- knowledge.sh = 26 fns verbatim (report/render helpers, cmd_report, seal_report_commit, brain_* writers, cmd_brain) incl. the "sprint reports (T-023)" + brain section comments; observe.sh = 17 fns, cmd_notify_gate first then status_brief..cmd_fleet.
- loader now `core ownership builder integrate knowledge observe selftest/…`.
- relocation diff-proven byte-identical: moved bodies == old entry L79-1158; retained entry == original minus L79-1159, ONLY the loader line changed.
- line counts: entry 1678→597, knowledge.sh 412, observe.sh 673; grand total 4008 (band [3750,4120]); full doctor --selftest green.
