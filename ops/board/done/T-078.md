---
id: T-078
title: "settings.json hook merge keys on script PATH and updates POLARIS-owned fields"
type: bug
scope: install
epic: autonomy-by-default
plan: autonomy-by-default
points: 3
value: 5
time_criticality: 3
risk_opportunity: 8
wsjf: 5.3
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-078
files_owned:
  - kit/ops/install.sh
  - kit/ops/selftest-install.sh
context_files:
  - ops/contracts/key-registry.md
  - kit/.claude/settings.json
depends_on:
  - T-074
contract: ops/contracts/key-registry.md
verify:
  - bash -n kit/ops/install.sh
  - bash -n kit/ops/selftest-install.sh
  - grep -q 'ops/hooks/' kit/ops/install.sh
  - grep -q 'drill_hookmerge' kit/ops/selftest-install.sh
map_delta: "install.sh settings merge: POLARIS-owned hook entries (identified by ops/hooks/ script path) are REPLACED with the kit's current fields on re-install; user-added hooks and all other keys keep skip-if-present (key-registry.md § 6)"
landed: 473b5e766c554818bb5c14a210752083d5fc3c9d
---
## Why
The merge at install.sh:267-297 keys on hook script BASENAME and `continue`s when present, so an
existing entry is never re-examined and a shipped field change can never arrive. Real cost,
measured: polaris-testbed is pinned at ownership-guard.sh `timeout: 10` while the kit ships 20,
against a guard core.sh measures at ~8s per write — parked on the fail-open margin permanently,
which means the ownership gate silently drops exactly when the machine is slow. Fix the identity
(script PATH under `ops/hooks/`, not basename), replace POLARIS-owned entries wholesale, and touch
nothing the user added.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] POLARIS-owned entry := any hooks entry whose `hooks[].command` contains the path segment
      `ops/hooks/` — basename matching is gone
- [ ] a target entry running the same `ops/hooks/<script>` as a kit entry is REPLACED wholesale
      (matcher, timeout, command) in place; no match → appended, as today
- [ ] user-added hook entries (no `ops/hooks/` in any command) are byte-untouched, whatever
      script they run; every non-hook key keeps today's merge semantics exactly
- [ ] idempotent — a second install changes nothing; the report line names updated entries as
      well as added ones
- [ ] `drill_hookmerge` in kit/ops/selftest-install.sh (fn name pinned in key-registry.md § 5-6;
      no other new top-level fn): POLARIS entry seeded at `"timeout": 10` + one user-added hook →
      re-install → POLARIS entry carries the kit's current fields, user entry byte-identical,
      re-run is a no-op
- [ ] `drill_live_board` green UNCHANGED — the preserve guarantee (board_snapshot,
      selftest-install.sh:262-292) is the thing this program must not dent

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Serial chain on install.sh: T-074's KIT_CODE token lands first — build on it, don't move it.
- Your new fn's api-kit golden line is written by T-077 (this wave's owner) from the pinned name —
  use `drill_hookmerge` exactly; a different name reds the wave gate.
- Full install drill (`bash kit/ops/selftest-install.sh`): FOREGROUND with an explicit
  ≥600000ms timeout, or log-and-poll — it runs several installs.
- The merge code is embedded Python (stdlib json only) inside a heredoc — keep it parseable by
  `python3 -c pass`-grade interpreters; never rewrite what cannot be parsed (existing stance).
- assumed: the merge reports via a mktemp file passed as argv[3], folded into the existing single
  `✅ .claude/settings.json: …` line as `— added <paths>; updated <paths>; N permission rule(s)`
  (default-safe). Python stdout stays silent — it is the quiet-line tripwire's contract.
- A user hook sharing OUR basename (`tools/ownership-guard.sh`) is the sharpest case: under the old
  matcher its presence SUPPRESSED our own entry entirely. drill_hookmerge seeds exactly that, and
  the pre-fix merge code fails the drill's first assertion — verified against HEAD~1.
- `[ -n "$X" ] && rm …` as a statement trips install.sh's `set -eu` when X is empty; the cleanup
  uses `if … then … fi` (install.sh:379).
