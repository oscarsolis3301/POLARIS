---
id: T-090
title: "Release 6.1.0 — enforced isolation ships to every POLARIS repo"
type: chore
scope: release
epic: enforced-isolation
plan: enforced-isolation
points: 2
value: 5
time_criticality: 5
risk_opportunity: 2
wsjf: 6.0
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-090
files_owned:
  - kit/ops/VERSION
  - CHANGELOG.md
context_files:
  - ops/CONVENTIONS.md
depends_on:
  - T-089
contract: ops/contracts/shared-checkout.md
verify:
  - grep -q '^version: 6.1.0$' kit/ops/VERSION
  - grep -q '6.1.0' CHANGELOG.md
  - grep -q 'checkout-guard' CHANGELOG.md
map_delta:
landed: 5a196d9201b7d641920432b887f5b957b3da5628
---
## Why
A release lands in all four places at once — kit/ops/VERSION, CHANGELOG, git tag, published
release — and the daily CI job fails if ops/VERSION lags what is published. This task carries
the first two: bump kit/ops/VERSION to 6.1.0 and write the CHANGELOG entry (the four layers: the
checkout-guard hook, the ownership-guard primary gate, ready-union-active disjointness + strict
drift, landing: self with the 5/20 defaults, plus the three drills). Minor, not major: every
change is additive and the one behavior flip (self-landing) composes from an unset key by the
6.0.0 default-in-code pattern with a one-line opt-out (landing: integrator). Tag, publish,
dogfood (pack.py --dogfood refreshes ops/ including ops/VERSION) and the rollout `bash
ops/polaris update` in each POLARIS repo happen at sprint finish per the release ritual in
ops/CONVENTIONS.md — never bump without tagging.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] kit/ops/VERSION reads exactly 6.1.0
- [ ] CHANGELOG.md gains a 6.1.0 entry covering all four layers + drills, matching the file's
      existing entry style and naming the landing: integrator opt-out
- [ ] no other file touched — tag/publish/dogfood happen at finish per the release ritual, not
      inside this task
- [ ] the published zip is refreshed by this release: polaris-v5.zip is currently STALE (built at
      4e9fa63, before the 6.0.0-era changes now on main) — the zip is not a repo file, CI rebuilds
      and publishes it from the tag, so ritual steps 3-4 at sprint finish (tag v6.1.0 -> CI publish
      -> dogfood) are what un-stale it; confirmed when post-dogfood `bash ops/polaris version`
      reports 6.1.0

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- kit/ops/VERSION rule was lifted 2026-07-15 for exactly this: bump ONLY as a full release
  (VERSION + CHANGELOG + tag + dogfood together).
- After dogfood, confirm bash ops/polaris version reports 6.1.0 (kit gotchas: update and dogfood
  refuse on dirty trees).
- verify[1] `test "$(cat kit/ops/VERSION)" = 6.1.0` is unsatisfiable: kit/ops/VERSION is a keyed
  config (version:/channel:/zip: … 14 lines), so cat never equals a bare version — the intent-true
  form is `grep -q '^version: 6.1.0$' kit/ops/VERSION`; a builder does not edit its own gate.
- polaris-v5.zip is confirmed STALE (built at 4e9fa63, pre-6.0.0-era main); not a repo file, so only
  the sprint-finish ritual (tag v6.1.0 -> CI publish -> dogfood) un-stales it.
- pre-existing, NOT this task: ops/CONVENTIONS.md lacks 14 of 37 KEYS.tsv keys; the Learned log sits
  at 10 bullets against drift's prune-to-<=5 target (EVOLVE's job).
- verify[1] amended by PLANNER 2026-08-23: cat-equality can never match the 14-line keyed VERSION
  file; gate is now `grep -q '^version: 6.1.0$' kit/ops/VERSION` — proven to pass on feat/T-090 and
  fail on main (6.0.0). Acceptance box unchanged; only the mechanical assertion was wrong.
