---
id: T-045
title: extract lib/admin.sh — entry under 500 lines, loader complete
type: chore
scope: cli
plan: many-hands
points: 2
value: 5
time_criticality: 2
risk_opportunity: 3
wsjf: 5.0
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-045
files_owned:
  - kit/ops/polaris
  - kit/ops/lib/admin.sh
context_files:
  - kit/ops/lib/core.sh
  - ops/contracts/install-parity.md
depends_on: [T-044]
contract: ops/contracts/module-layout.md
verify:
  - bash -n kit/ops/polaris && bash -n kit/ops/lib/admin.sh
  - grep -q '^cmd_update()' kit/ops/lib/admin.sh && ! grep -q '^cmd_update()' kit/ops/polaris
  - grep -q '^cmd_uninstall()' kit/ops/lib/admin.sh && ! grep -q '^cmd_uninstall()' kit/ops/polaris
  - grep -q '^cmd_init_board()' kit/ops/lib/admin.sh && ! grep -q '^cmd_init_board()' kit/ops/polaris
  - test "$(wc -l < kit/ops/lib/admin.sh)" -le 1200
  - test "$(wc -l < kit/ops/polaris)" -lt 500
  - t=$(cat kit/ops/polaris kit/ops/lib/*.sh kit/ops/lib/selftest/*.sh | wc -l); test "$t" -ge 3750 && test "$t" -le 4120
  - bash kit/ops/polaris help >/dev/null
  - bash kit/ops/polaris doctor --selftest --only upgrade
  - bash kit/ops/selftest-install.sh
map_delta:
landed: ddd7e17a99bd3913ae66a7f5016ec973a4d791e7
---
## Why
Last link: the lifecycle commands (cmd_init_board, cmd_upgrade, ver, semver_gt,
update_check_maybe, cmd_version, kit_zip_version, refresh_machine_kit, cmd_update,
cmd_uninstall — the contract's 10) move verbatim into kit/ops/lib/admin.sh, completing the
loader list to the contract's pinned final block. cmd_update's re-exec guard moves UNCHANGED —
it protects the entry file bash is still lazily reading; lib files are fully read at startup, so
overwriting them mid-update is already safe (contract invariant; .github CI asserts the guard
survives). This task also closes the sprint's line-budget gates: the entry script — globals +
loader + usage + dispatch, nothing else — under 500 lines, every module ≤1,200, and the grand
total inside [3750, 4120] (module-layout v1.1: relocation band 3,826 ±2% + the measured
~150-line --parallel feature + module headers): relocation, not rewrite, proven by wc. selftest-install reruns because uninstall and
the old-client update path both walk admin code.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] admin.sh holds exactly the contract's 10 functions; nothing else moved; re-exec guard byte-identical
- [ ] loader list matches the contract's pinned block byte-for-byte (all 13 names, final order)
- [ ] entry script contains ONLY: header/set -eu/POLARIS_V, loader, git guard, top-level assignments, usage(), dispatch
- [ ] wc gates green: entry <500 · modules ≤1200 · total in [3750, 4120] (module-layout v1.1)
- [ ] full `bash kit/ops/polaris doctor --selftest` green (handoff gate `test:`)

## Notes
Long suites: run FOREGROUND with an explicit 600000ms timeout; if the harness caps it, log to a file and poll for the `selftest passed` line.
- admin.sh = 10 fns verbatim (cmd_init_board, cmd_upgrade, ver, semver_gt, update_check_maybe, cmd_version, kit_zip_version, refresh_machine_kit, cmd_update, cmd_uninstall); cmd_update re-exec guard moved byte-identical.
- the generic `# ===== commands` divider (immediately preceded cmd_init_board) relocated into admin.sh as its leading comment — pure relocation, nothing deleted (matches ownership.sh/knowledge.sh precedent); entry keeps only globals→`# --- dispatch`→usage.
- loader now complete + matches contract pinned block byte-for-byte: `core ownership builder integrate knowledge observe admin selftest/…`.
- byte-identity: admin.sh body == old entry L46-478; retained entry == original minus L46-479, ONLY the loader line changed.
- line budgets: entry 597→163 (<500), admin.sh 436 (≤1200), all 7 modules ≤1200 (max observe 673); grand total 4017 (∈[3750,4120]; +T-046 hermetic-drill lines landed in base between T-044 and T-045).
- GOTCHA: pack.py packs via `git ls-files` (tracked only) — a NEW lib module must be COMMITTED before build/handoff or the zip omits it and the fresh-install loader dies "ops/lib/admin.sh is missing" (selftest-install fresh/uninstall drills). Commit-then-pack.
- full doctor --selftest green + selftest-install.sh all 8 drills green (pack/zip-purity/fresh/no-leaks/old-client/live-board/uninstall/repo-clean).
