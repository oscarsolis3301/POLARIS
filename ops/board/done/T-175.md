---
id: T-175
title: "checkout-guard covers PowerShell — the same protection against switching the shared checkout's branch, now for this machine's primary shell"
type: bug
scope: hooks
plan: v4-fast
points: 2
value: 5
time_criticality: 3
risk_opportunity: 5
wsjf: 6.5
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-175
files_owned:
  - kit/ops/hooks/checkout-guard.sh
  - kit/.claude/settings.json
  - kit/ops/install.sh
  - ops/tests/checkout-guard-denies.cmd
  - ops/tests/checkout-guard-denies.expected
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/shared-checkout.md
  - kit/ops/hooks/readonly-allow.sh
depends_on: []
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/hooks/checkout-guard.sh
  - bash -n kit/ops/install.sh
  - python -c "import json; json.load(open('kit/.claude/settings.json', encoding='utf-8'))"
  - grep -q 'Bash|PowerShell' kit/.claude/settings.json
  - bash ops/tests/checkout-guard-denies.cmd 2>/dev/null | diff - ops/tests/checkout-guard-denies.expected
  - grep -F 'git fetch; if ($?) { git switch main }' ops/tests/checkout-guard-denies.expected | grep -q '^deny'
  - grep -F 'git restore .' ops/tests/checkout-guard-denies.expected | grep -q '^deny'
  - grep -F 'git branch -d ' ops/tests/checkout-guard-denies.expected | grep -q '^allow'
  - grep -F 'git checkout -- .' ops/tests/checkout-guard-denies.expected | grep -q '^deny'
  - bash ops/tests/perm-tools.cmd 2>/dev/null | diff - ops/tests/perm-tools.expected
  - bash ops/tests/output-style-installed.cmd 2>/dev/null | diff - ops/tests/output-style-installed.expected
  - bash ops/tests/adhd-skill-installed.cmd 2>/dev/null | diff - ops/tests/adhd-skill-installed.expected
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta:
surface:
  - kit/ops/hooks/checkout-guard.sh tests: ops/tests/checkout-guard-denies.* cmd: bash ops/polaris check --only checkout-guard-denies note: which git and PowerShell lines may not switch the shared checkout
landed: 6507aa85803e5156992d2162ed7a5c6d10cce787
---
## Why
`checkout-guard.sh` stops any session from switching the branch of the shared primary checkout,
which is the one thing that would pull the floor out from under every other session. It is wired
only to the Bash tool, but on this machine PowerShell is the primary shell, so a PowerShell
`git switch main` passes untouched. The guard's parser already understands most PowerShell forms;
it just never sees them.

Wire the guard to `Bash|PowerShell` (readonly-allow stays Bash-only), and teach the parser the few
PowerShell shapes it misses: `{` and `if (…)` / `elseif (…)` / `foreach (…)` put the next word in
command position just like `;`, and a `;` inside quotes does not split a command. While here, deny
`git restore` that rewrites the working tree in the primary, and stop denying `git branch -d`, which
never touches the checkout. `install.sh` must merge the second matcher into repos that already have
the first, without duplicating it.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/.claude/settings.json` wires checkout-guard with matcher `Bash|PowerShell`; readonly-allow keeps `Bash`.
- [ ] Golden rows: `git fetch; if ($?) { git switch main }` → deny · `git restore .` → deny · `git branch -d x` → allow · `git checkout -- .` → still deny · every existing row unchanged.
- [ ] A `;` inside quotes no longer splits a command.
- [ ] `install.sh` merges the new matcher into an existing settings.json, and a second run changes nothing — shown on a `mktemp -d` fixture, output pasted in Notes.
- [ ] No new api rows (speed.md § 5).

## Notes
- Run the golden rows through `--test` exactly as the existing ones do; the JSON layer is covered by `drill_checkoutguard` at land.
- Evidence (plans/audit-0924.md, verbatim):
  - **hooks-7** · CONFIRMED · The PowerShell tool bypasses checkout-guard completely; its parser already understands most PowerShell forms — No incidents so far. It protects the shared-checkout guarantee if Opus starts using PowerShell more often. Cost: one ~0.1–0.2 s bash start per PowerShell call, run in parallel with model-guard. — fix: 1. In kit/.claude/settings.json, change the checkout-guard entry's matcher to "Bash|PowerShell". Leave readonly-allow on Bash. 2. In checkout-guard's mutating_git, treat `{` as a command-position reset like `;`, and treat `if`/`elseif`/`foreach` + `(…)` as non-command tokens, so `A; if ($?) { git s… (kit/.claude/settings.json, kit/ops/hooks/checkout-guard.sh, kit/ops/install.sh)
  - **quality-8** · OVERSTATED · checkout-guard is not wired for the PowerShell tool (this machine's primary shell) and over-matches non-switching git commands (IDEAS:23, 56, both still open) — Keep: add a PowerShell matcher and allow lowercase `branch -d`. Reverse: close the `git restore` hole (deny worktree-affecting restore in the primary) instead of opening `checkout -- <path>`. — fix: Add a second PreToolUse entry `{matcher: "PowerShell", hooks: [checkout-guard.sh]}` in kit/.claude/settings.json, plus the installer's merge identity (the hook already reads tool_input.command). In the tokenizer, treat `checkout` followed by a `--` pathspec (with or without a ref) as a file restore… (kit/.claude/settings.json, kit/ops/hooks/checkout-guard.sh, kit/ops/install.sh, ops/tests/checkout-guard-denies.cmd, ops/tests/checkout-guard-denies.expected)
- install.sh needed NO code change: its settings merge identifies a hook entry by its `ops/hooks/<script>` path and replaces an owned entry wholesale in place, so `Bash` → `Bash|PowerShell` lands as an update, never as a second entry (one matcher string, not two entries, is what keeps that identity sound).
- install fixture (mktemp -d, HOME inside the fixture, target seeded with HEAD's kit/.claude/settings.json, `bash kit/ops/install.sh <fixture>` twice), output verbatim:
  - `before : Edit|Write|MultiEdit|NotebookEdit -> ownership-guard.sh | Bash -> readonly-allow.sh | Bash -> checkout-guard.sh`
  - `✅ .claude/settings.json: hooks + permissions + output style merged (idempotent) — updated ops/hooks/checkout-guard.sh`
  - `run 1  : Edit|Write|MultiEdit|NotebookEdit -> ownership-guard.sh | Bash -> readonly-allow.sh | Bash|PowerShell -> checkout-guard.sh`
  - `✅ .claude/settings.json: hooks + permissions + output style merged (idempotent)`
  - `run 2  : Edit|Write|MultiEdit|NotebookEdit -> ownership-guard.sh | Bash -> readonly-allow.sh | Bash|PowerShell -> checkout-guard.sh`
  - `second run: settings.json byte-identical` · `checkout-guard entries: 1`
- quotes: the entry block turns every `; | & {` inside a quoted stretch into `_` before either parser runs (one expansion per quote, no per-character loop — this path sees long commit messages); the quotes stay, so the stretch is still opaque. `\"` never closes a stretch.
- `{` is split exactly like `;` (anywhere in a token, so a glued `if($?){git switch main}` denies too); a `}` glued to the subcommand ends it, which keeps `{git stash list}` allowed. `if`/`elseif`/`foreach` joined the command-position keywords, so bash `if git rebase main; then` now denies as well (pinned in the golden).
- restore denies unless `--staged`/`-S` is given WITHOUT `--worktree`/`-W` (index-only moves no file); inside `.polaris/wt/<ID>` it stays allowed like every other checkout verb.
