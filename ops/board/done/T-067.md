---
id: T-067
title: "readonly-allow arms route + bg read forms — with both-direction golden cases"
type: feature
scope: hooks
epic: routing-and-bg
plan: routing-and-bg
points: 2
value: 3
time_criticality: 3
risk_opportunity: 3
wsjf: 4.5
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-067
files_owned:
  - kit/ops/hooks/readonly-allow.sh
  - ops/tests/readonly-allow.cmd
  - ops/tests/readonly-allow.expected
context_files:
  - ops/contracts/bg-jobs.md
  - ops/contracts/model-routing.md
depends_on: []
contract: ops/contracts/bg-jobs.md
verify:
  - bash -n kit/ops/hooks/readonly-allow.sh
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris route --points 3 --risk normal' | grep -qx allow
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris bg status' | grep -qx allow
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris bg tail qa -n 40' | grep -qx allow
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris bg wait qa --max 120' | grep -qx allow
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris bg run qa' | grep -qx ask
  - bash kit/ops/hooks/readonly-allow.sh --test 'bash ops/polaris bg' | grep -qx ask
  - bash ops/tests/readonly-allow.cmd | diff - ops/tests/readonly-allow.expected
map_delta:
landed: b728461072fdfed6757aff81f3bf77227e5cb831
---
## Why
The auto-approver is what lets an agent's reads run without prompting a human, and it is deny-by-
default: any subcommand it has not proven read-only falls through to a prompt. T-065/T-070 add
commands that agents will run constantly INSIDE compound lines (`bash ops/polaris bg status x &&
…`, `route … | head -1`), where the standing settings.json allow rule cannot match. Extend
`polaris_ok` (readonly-allow.sh:206): `route` joins the plain read list (it computes from
frontmatter + CONVENTIONS, writes nothing); `bg` gets a two-arm split exactly like `slim`'s —
`status`/`tail`/`wait` return 0, EVERYTHING else (`run`, `--force`, bare `bg`, unknown words)
returns 1 and keeps its prompt, because `bg run` spawns processes and writes the registry. The
golden battery (readonly-allow.cmd/.expected) gains cases in BOTH directions — the hook's own
header says a new verb MUST arrive with its cases or it is not shipped, and the refuse cases are
the security half.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `route` allowed with any flag tail; `bg status|tail|wait` allowed with their flag tails
- [ ] `bg run` refused in ALL forms (with/without `--`, `--force`, suite keys); bare `bg` and
      unknown `bg <word>` refused — deny-by-default proven, not assumed
- [ ] golden gains ≥3 allow cases and ≥3 refuse cases covering the above, including one compound
      line (pipe or `&&`) per direction
- [ ] NO new top-level functions in the hook (extend `polaris_ok` in place — the api-kit surface
      golden is another lane's this wave) and no change to existing verdicts: the full old battery
      still passes byte-identical
- [ ] hook stays fork-free pure bash (its speed contract: ~100ms parse, no interpreter calls)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- The golden .cmd already invokes the KIT hook (`H=kit/ops/hooks/readonly-allow.sh`), so your edit
  and its proof travel together — no installed-copy lag for this lane.
- ops/tests/*.cmd and *.expected are pinned `eol=lf` in .gitattributes (T-056); write them LF and
  never assert CR-sensitive bytes via grep/sed — awk/od only.
- Mirror the `slim` split at readonly-allow.sh:211 — it is the in-file precedent for a
  subcommand-conditional verdict.
- Docs-for-this-lane: `ops/contracts/bg-jobs.md` § Interface — the commands (read-only forms list).
