---
id: T-086
title: "Invariant 2 becomes true — claim sweeps ready ∪ active, drift --strict fails overlap"
type: feature
scope: board
epic: enforced-isolation
plan: enforced-isolation
points: 3
value: 5
time_criticality: 5
risk_opportunity: 3
wsjf: 4.3
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-086
files_owned:
  - kit/ops/lib/builder.sh
  - kit/ops/lib/observe.sh
  - ops/tests/api-kit.expected
context_files:
  - kit/ops/lib/ownership.sh
  - ops/contracts/key-registry.md
  - ops/contracts/shared-checkout.md
  - ops/tests/api-kit.cmd
depends_on: []
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - bash -n kit/ops/lib/observe.sh
  - grep -q 'overlaps ready' kit/ops/lib/builder.sh
  - test "$(grep -c 'checkout-guard' ops/tests/api-kit.expected)" = 3
  - grep -q 'primary_gate' ops/tests/api-kit.expected
  - bash kit/ops/polaris doctor --selftest --only drift
map_delta: "claim disjointness gate sweeps ready/ as well as active/; drift --strict exits nonzero on OWNERSHIP OVERLAP"
landed: 4d9802bc8441ed02413c2e73758a9f3703abb3c1
---
## Why
Invariant 2 says no two claimable tasks ever share a file, but nothing enforces it end to end:
the claim-time gate (builder.sh, T-059 loop) compares the candidate only against `active/`, and
`drift` merely REPORTS an OWNERSHIP OVERLAP. Two planners racing can leave overlapping tasks in
`ready/`, and the loser surfaces two builds later as an integrator squash conflict. Extend the
existing loop to also iterate `ready/` (skipping the candidate itself) — same pat_overlap both
directions, same explicit-die vs auto-park-to-blocked/ behavior, message gains the `overlaps
ready` variant pinned in contract § v2.3. In observe.sh, make OWNERSHIP OVERLAP a FAILING finding
under `--strict` (CI already runs drift --strict), leaving plain `drift` output unchanged. This
task is also wave 1's sole owner of ops/tests/api-kit.expected: write EXACTLY the four pinned
additions from contract § v2.3 (three checkout-guard fns + primary_gate), sorted in place — from
the contract, never from the other lanes' diffs.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] explicit `claim <ID>` whose files_owned overlaps a READY task dies naming the task and
      both patterns with the `overlaps ready` fragment; overlap with active/ behaves as today
- [ ] auto-pick claim parks the overlapping candidate in blocked/ with the remedy note and keeps
      claiming, exactly like the active/ path today; candidate never compares against itself
- [ ] `drift` exit code unchanged without --strict; with --strict an OWNERSHIP OVERLAP finding
      exits nonzero (like the ready-gate findings)
- [ ] NO new top-level fns in builder.sh or observe.sh (surface-frozen except the golden)
- [ ] ops/tests/api-kit.expected delta is EXACTLY the 4 contract-pinned lines, sorted in place
- [ ] `bash kit/ops/polaris doctor --selftest` drills claimguard and drift stay green

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- pat_overlap forked TWICE per call (a `printf | owned_match` pipeline per direction); owned_match with one stdin line is exactly `match_one`, so both are now zero-fork case matching — 576-pair old-vs-new equivalence check: 0 mismatches; 600 comparisons 39.2s → 0.09s. cmd_drift runs that sweep, cmd_qa runs drift --strict, cmd_finish runs cmd_qa, so the fork tax was on every finish.
- TOCTOU closed: the claim scan sat between lock_take and mutex_on, so two sessions claiming two overlapping tasks could both scan a board showing neither and both mv. mutex_on is not re-entrant, so it is now ONE acquisition per candidate, taken before the scan and held through the ready→active mv; the blocked/ branch's own mutex_on was removed and the post-loop one deleted.
- `drift --strict` ALREADY exited nonzero on OWNERSHIP OVERLAP (finding() is uniform and drill_drift asserts it) — criterion 3 was true before this task; re-proved, no observe.sh change needed for it.
- EnterWorktree refuses a conductor-spawned subagent (pinned cwd → "requires a worktree under .claude/worktrees/"); this session worked by prefixing every command with an explicit cd into .polaris/wt/T-086 — worth knowing for T-087's worktree-entry step.
- Never edit kit/ops/polaris or kit/ops/lib/*.sh while a selftest is RUNNING — mid-edit runs
  fabricate a different spurious drill failure each time (Learned log); a failure point that
  MOVES between runs is corruption, not code.
- Run `--only drill` suites FOREGROUND with an explicit ≥600000ms timeout, or log-and-poll for
  `selftest passed`; kit driver only (label-count tell: kit has more labels than installed).
- bash 3.2: no `case` inside `$(...)`. Scratch under scratchpad/T-086/ only.
- `polaris check` is PRIMARY-anchored — a golden edited in a worktree passes VACUOUSLY; prove the
  api-kit delta by hand: POLARIS_ROOT=<worktree> python ops/index.py find --api 'kit/*' and
  byte-diff against your .expected.
- Mid-wave api-kit red on main is EXPECTED until your golden lands (one-owner-per-wave, sprint 9
  prescription); if a sibling lands first the integrator may record the landed surface — your
  final delta must still match the contract pins exactly.
