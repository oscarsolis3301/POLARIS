---
id: T-059
title: "Claim/handoff hardening — id_ok pre-lock, the claim-time disjointness gate, pushes that degrade instead of stranding"
type: feature
scope: builder
epic: n-chats-one-repo
plan: n-chats-one-repo
points: 5
value: 13
time_criticality: 8
risk_opportunity: 8
wsjf: 5.8
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-059
files_owned:
  - kit/ops/lib/builder.sh
  - kit/ops/lib/selftest/board.sh
  - kit/ops/lib/selftest/remote.sh
context_files:
  - ops/contracts/shared-checkout.md
  - kit/ops/lib/workspace.sh
  - kit/ops/lib/core.sh
depends_on:
  - T-057
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - bash -n kit/ops/lib/selftest/board.sh
  - bash -n kit/ops/lib/selftest/remote.sh
  - bash kit/ops/polaris doctor --selftest --only grant,remote,syncrace
map_delta:
landed: c5d1605d168bed1c85492218f1024b7a5db1403f
---
## Why
A Builder's two worst multi-session failures are both in builder.sh. First, claim: IDs are never
validated (a bad one becomes a bad ref name deep inside `worktree add`) and nothing re-checks
ownership disjointness at claim time, so two planners racing can put overlapping tasks on the board
and the overlap is only discovered as an integrator squash conflict two builds later. Second,
handoff: the push (builder.sh:111) has zero retry and no fallback — one network hiccup strands a
FINISHED task in active/ with its lock held. Per shared-checkout v1: `cmd_claim` runs `id_ok`
BEFORE lock_take and, after locking a candidate, checks its `files_owned` against EVERY active/
task via `pat_overlap` (both directions) — overlap on auto-pick → move the candidate to blocked/
with a ⛔ note naming the active task, both patterns and the remedy, ONE board commit
`chore(board): block <ID> (ownership overlap)`, release its lock, claim the next candidate;
overlap on an explicit ID → die naming the same. `wt_add` replaces the inline retry loop (:62-71)
and cmd_resume's recreate block (:369-371). `cmd_handoff`'s push gets 3 attempts with one
`stray_feat_repair` between them; still failing → PROCEED with the board move (direct-mode landing
merges the LOCAL branch — the work is safe), append the contract's ⚠ push-fail Note to the task,
emit `evt push-fail`, and say so. A finished task is never stranded by the network again.
Claim/handoff assertions live in selftest/board.sh and selftest/remote.sh — update any that the
new claim/handoff output touches (existing drills only; new drills are T-062's).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] an invalid ID dies via id_ok BEFORE any lock exists; every historical ID shape still claims
- [ ] the disjointness gate: auto-pick moves the overlapping candidate to blocked/ (note + one
      board commit + lock released) and claims the next; explicit ID dies naming task + patterns
- [ ] a claim with no overlap behaves byte-identically to today (gate is silent when clean)
- [ ] wt_add serves both claim and resume; the old retry loops are gone; a non-index.lock failure
      re-emits real stderr instead of "git index busy"
- [ ] handoff push: 3 attempts, one stray-feat repair between, degrade → board move + task Note +
      `push-fail` event; publish: pr path (no push) is untouched
- [ ] existing drills green with updated assertions (grant · remote · syncrace)
- [ ] no new top-level function in builder.sh — inline the glue; call workspace.sh fns directly
      (the api-kit surface golden is frozen this wave)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- `pat_overlap` is defined in observe.sh and is sourced before dispatch — call it, do not copy it.
- OUT OF SCOPE: threading the task ID into `check_rules` at cmd_verify/cmd_handoff (:95/:104) —
  that is T-048's recorded scope; it is parked in backlog and will ride a later release.
- The installed `ops/polaris` lags your kit edits all sprint: prove behavior with
  `bash kit/ops/polaris …`.
- bash 3.2: no `case` inside `$(...)`; split `local` declarations.
- Never run `doctor --selftest` while your kit edits are half-written — a mid-edit suite fabricates
  a different spurious failure each run.
- Long suites FOREGROUND with an explicit ≥600000ms tool timeout (test_fast ≈320s); past the cap →
  log to a file and poll for `selftest passed`. Never end a turn waiting on a notification.
- degrade drill blocks ONLY the feat ref (origin refs/heads/feat/T-PF/x): a fully unreachable origin dies in sync_board first — board-sync resilience is not this task
- stray-feat detection is stderr grep + an ls-remote probe fallback: git version phrasing of the D/F error drifts, the probe does not
