---
id: T-092
title: "workspace.sh — beats, wt_remove (archive, never --force), pid-aware lease steal, owned parks"
type: feature
scope: workspace
epic: cant-eat-itself
plan: cant-eat-itself
points: 5
value: 13
time_criticality: 8
risk_opportunity: 8
wsjf: 5.8
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-092
files_owned:
  - kit/ops/lib/workspace.sh
approved:
context_files:
  - ops/contracts/worktree-liveness.md
  - kit/ops/lib/core.sh
  - kit/ops/lib/bg.sh
  - ops/contracts/shared-checkout.md
depends_on: []
contract: ops/contracts/worktree-liveness.md
verify:
  - bash -n kit/ops/lib/workspace.sh
  - grep -q '^beat_touch()' kit/ops/lib/workspace.sh
  - grep -q '^beat_age()' kit/ops/lib/workspace.sh
  - grep -q '^beat_live()' kit/ops/lib/workspace.sh
  - grep -q '^wt_remove()' kit/ops/lib/workspace.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/lib/workspace.sh)" = 14
  - grep -q 'cfg wt_live_minutes 15' kit/ops/lib/workspace.sh
  - grep -q 'worktree archived → .polaris/wt-archive/' kit/ops/lib/workspace.sh
  - grep -q 'worktree LEFT' kit/ops/lib/workspace.sh
  - grep -q 'worktree removed' kit/ops/lib/workspace.sh
  - grep -q 'wt-archive' kit/ops/lib/workspace.sh
  - test "$(grep -c 'worktree remove --force' kit/ops/lib/workspace.sh)" = 0
  - grep -q 'polaris/park-' kit/ops/lib/workspace.sh
  - grep -q -- '--any' kit/ops/lib/workspace.sh
  - grep -q 'did not exist — recreated from' kit/ops/lib/workspace.sh
  - grep -q 'polaris-beat' kit/ops/lib/workspace.sh
  - test "$(wc -l < kit/ops/lib/workspace.sh | tr -d ' ')" -le 350
map_delta: "lib/workspace.sh gains beat_touch/beat_age/beat_live (the worktree liveness signal, $GCD/worktrees/<ID>/polaris-beat) and wt_remove — the ONLY worktree-removal primitive (rc 0 removed / 1 left / 2 archived to .polaris/wt-archive/, never --force)"
landed: ff868a851268d1818d4db3b36198f971d7543eaf
---
## Why
A sibling session's cleanup ran `git worktree remove --force` on a worktree someone else was still
typing in, and everything uncommitted died. Nothing in the kit can tell a live worktree from a dead
one, so every remover guesses. This task gives the kit the ONE way to ask ("is this worktree live?")
and the ONE way to remove ("wt_remove") — and wt_remove never uses `--force`: a clean idle worktree
is removed, a dirty one is moved to `.polaris/wt-archive/<ID>-<epoch>` with its bytes intact, and a
live one is left alone with a note that says what to do next. The liveness signal is a beat file
inside git's own per-worktree dir (`$GCD/worktrees/<ID>/polaris-beat`), touched by the CLI and the
hooks; `wt_live_minutes` (default 15, KEYS row registered by T-096) says how long a beat counts.
Same file, same sprint, two smaller fixes from the plan: the integration-lease steal becomes
pid-aware (a slow but alive integrator is never stolen from), park stashes carry
`<epoch>-<sid8|pid>` so `unpark` pops only YOUR park (`--any` for crash recovery) and `park` prints
what it stashed, and `wt_add <ID> resume` warns when it had to recreate a vanished branch.
Everything is pinned in ops/contracts/worktree-liveness.md — signatures, the caller×dirty×live
decision table, the three output prefixes and the archive layout. Callers (done/release/sweep/
resume) are other tasks in wave 2; this task ships the primitives only.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `beat_touch <ID>` writes an epoch to `$GCD/worktrees/<ID>/polaris-beat` (mkdir -p first), rc 0 always; `beat_age` prints seconds (999999 when absent/unreadable, content-first then `stat -c %Y || stat -f %m`); `beat_live` rc 0 iff age < `cfg wt_live_minutes 15` × 60 (non-numeric ⇒ 15)
- [ ] `wt_remove <ID> <done|release|sweep>` implements the contract's decision table exactly: rc 0 removed · 1 LEFT · 2 archived; dirty = `git status --porcelain` non-empty (untracked included); archive = mv to `$PRIMARY/.polaris/wt-archive/<ID>-<epoch>` + `rm -f <archive>/.git` + `git worktree prune`; any mv/git failure ⇒ rc 1 + one note, never die, never partial; own-lane release EBUSY ⇒ LEFT + the pinned `cd out of .polaris/wt/<ID> and run: bash ops/polaris sweep --fix` note
- [ ] output prefixes byte-exact: `worktree removed` · `worktree LEFT` · `worktree archived → .polaris/wt-archive/<ID>-<epoch>`; `grep -c 'worktree remove --force'` on workspace.sh is 0
- [ ] lease steal (`int_on`): iff age > sm×60 AND (pid absent OR dead OR age > 2×sm×60); dead = `kill -0` fails, or on Windows the pid is absent from ONE `ps -W` listing; wait/queue behavior otherwise byte-identical (busyint drill stays green)
- [ ] park: stash name `polaris/park-<epoch>-<sid8|pid>`, prints the stashed paths (3-space indent); `unpark` pops only OUR newest park, says how many foreign parks exist when none is ours; `unpark --any` pops the newest of anyone (drill park stays green — extend it only inside its body if a name assertion needs the new suffix, and say so in Notes)
- [ ] `wt_add <ID> resume` with no local `feat/<ID>` creates it from `<base>` and prints the pinned `⚠ feat/<ID> did not exist — recreated from <base>; …` note; plain `wt_add <ID>` unchanged
- [ ] proven in a throwaway kit copy + fixture repo (the T-089 pattern): a dirty worktree with a backdated beat (`echo 1 > <beat>`) archives with bytes intact; a fresh beat leaves it; a clean idle one is removed — rc asserted, not just the words (drill `wtreap` in W3 automates this)
- [ ] no new fn beyond the four pinned; `bash kit/ops/polaris doctor --selftest --only park,busyint` green (foreground, ≥600000 ms timeout)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: exactly FOUR new fns (`beat_age beat_live beat_touch wt_remove`) — T-096 owns `ops/tests/api-kit.expected` this wave and writes those rows; any other new fn at any depth, `#` line or KEYS row is a violation (key-registry.md §7).
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-092` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- Scratch dirs are per-lane: scratchpad/T-092/ only; never delete a shared scratch path. Never edit kit/ops/polaris or kit/ops/lib/*.sh while a selftest is RUNNING. Drills foreground with an explicit >=600000 ms timeout or `bg run`; never `check --only` from a worktree. bash 3.2 (CI runs macOS /bin/bash): no `case` inside `$(...)`, no `mapfile`. Windows: `python`, never `python3`.
- The re-stamp of the lease epoch during long suite steps belongs to integrate.sh (T-099, W2) — not this task. The mutex steal belongs to core.sh (T-097). Callers of `wt_remove` are T-098/T-099/T-100 (W2).
- assumed: park's sid-less suffix is `$PPID`, not `$$` (default-safe) — two CLI calls never share `$$`, so a sid-less `unpark` (CI, a bare terminal) could never find its own park and drill park would go red; the shell that ran both IS "this session" there. Under Claude Code the sid8 wins as pinned.
- beat_touch writes `date +%s 2>/dev/null > <beat>` — the 2>/dev/null BEFORE the >: bash applies redirections left to right, so the contract's order leaked "No such file or directory" when the dir was missing (T-093 found it live; reordered form proven silent + still writes).
- own-lane dirty `release`: on this Windows box `mv` of the shell's own cwd SUCCEEDED (rc 2, bytes archived) — EBUSY is not universal; the LEFT + pinned cd-out note path is proven by forcing the mv to fail (wt-archive as a file), rc 1, nothing moved.
- wt_remove on an ABSENT worktree dir: prune + rc 0, silent — so T-099's `done` may call it unguarded on hand-made review tasks (the drills' hint shape) and still reach `branch -D`; rc 1 there would have kept every such branch forever.
- lease steal on Windows: `ps -W` has no PID 1, so busyint (b)'s `pid 1` holder reads dead on every OS (non-root `kill -0 1` is EPERM elsewhere) — the drill stays green with no edit; drill park untouched too (`parked as polaris/park-` still matches).
- proof: scratchpad/T-092/prove.sh — 89 rc + file-state checks over a fixture repo with core.sh + workspace.sh sourced directly (wt_remove has no CLI caller until W2): beats, all four callers × dirty × live, forced mv failure, pid-aware steal (dead / alive / 2× stale / no pid), sid-owned parks + --any, resume recreate note.
