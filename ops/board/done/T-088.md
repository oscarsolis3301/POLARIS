---
id: T-088
title: "landing: self — a builder queues behind the lease and lands its own task"
type: feature
scope: landing
epic: enforced-isolation
plan: enforced-isolation
points: 5
value: 8
time_criticality: 3
risk_opportunity: 5
wsjf: 3.2
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-088
files_owned:
  - kit/ops/lib/builder.sh
  - kit/ops/lib/workspace.sh
  - kit/ops/lib/observe.sh
  - kit/ops/KEYS.tsv
  - kit/ops/roles/BUILDER.md
  - kit/ops/roles/SOLO.md
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/PROTOCOL.md
  - kit/CLAUDE.md
  - ops/tests/api-kit.expected
context_files:
  - kit/ops/lib/integrate.sh
  - ops/contracts/key-registry.md
  - ops/contracts/shared-checkout.md
  - kit/ops/roles/INTEGRATOR.md
depends_on:
  - T-087
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - bash -n kit/ops/lib/workspace.sh
  - bash -n kit/ops/lib/observe.sh
  - grep -q self_land kit/ops/lib/builder.sh
  - grep -q 'risk: high never self-lands' kit/ops/lib/builder.sh
  - awk -F "\t" "\$1==\"landing\" && \$3==\"self\" {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"autolaunch_max\" && \$3==5 {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"integration_wait_minutes\" && \$3==10 {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - awk -F "\t" "\$1==\"integration_wait_minutes\" && \$4 ~ /600/ {ok=1} END {exit !ok}" kit/ops/KEYS.tsv
  - grep -q 'cfg integration_wait_minutes 10' kit/ops/lib/workspace.sh
  - grep -q 'cfg autolaunch_max 5' kit/ops/lib/observe.sh
  - grep -q 'Only the integration-lease holder merges' kit/CLAUDE.md
  - grep -q 'bg run ship-' kit/ops/roles/BUILDER.md
  - grep -q self_land ops/tests/api-kit.expected
map_delta: "CONVENTIONS key landing (self|integrator, default self in code) — handoff continues into land through the shared lease; autolaunch_max default 5; Invariant 9 reworded in kit/CLAUDE.md (the lease holder IS the Integrator)"
landed: 487037e6fd971f7d3c910b9c80b24233f56c5560
---
## Why
Root cause #8 of the collisions: nothing wakes an integrator — handoff prints prose and the
board sits in `review/` forever when no CONDUCTOR runs. The lease already provides "wait your
turn behind the other agents" (int_on: atomic lease, 2s poll, stale steal, queued:/rc 3), so
close the loop: new knob `landing` (KEYS.tsv, since 6.1.0, default `self` IN CODE — the 6.0.0
lesson: update never rewrites CONVENTIONS, so unset must compose to the new behavior). Under
self, cmd_handoff continues into `land <ID>` via ONE new top-level fn `self_land`; the
all-review condition handoff already computes also triggers `seal` (last lane out seals the
wave). HARD STOPS no knob softens: `risk: high` and STOP-AND-ASK tasks never self-land — pinned
refusal, classic integrate notice, today's behavior byte-for-byte (same under
`landing: integrator`). Re-size autolaunch_max 3 to 5 (KEYS row + the code
fallback + CONDUCTOR.md "default 3"); integration_wait_minutes STAYS 10 — human decision
2026-08-23: int_on polls in the FOREGROUND, so a 10-minute bound is already exactly the
harness's 600s tool cap, and a bigger one makes the tool call return nothing and lose the whole
wait. Instead, KEYS.tsv's prose for that key gains the 600s-cap warning (never raise past 10),
and the self-land tail DETACHES: bg run ship-<ID>, collected with chunked bg wait --max 300
(contract v2.5) — the recipe the role files carry. Also reword Invariant 9 in kit/CLAUDE.md with
the human-approved VERBATIM text pinned in contract v2.5 (the lease holder IS the Integrator —
what wave_on always did); replace the existing line 9, touch nothing else in that file. Document self-landing in kit/ops/PROTOCOL.md
section LANES as the SOLO / land --express precedent generalized — edit INSIDE the section, no
new heading. Update BUILDER/SOLO/CONDUCTOR so the builder's life reads claim, build, verify,
handoff, then (self) land.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] landing unset or self: handoff of a normal task continues into land; task reaches done/;
      lease taken and released via int_on/int_off unchanged; busy lease takes the queued:/rc 3
      path — never a question, never a die
- [ ] last lane out (all-review) also seals the wave; mid-wave lands do not seal
- [ ] risk: high NEVER self-lands: pinned ONE-LINE refusal from contract v2.5 + classic
      integrate notice; landing: integrator is byte-for-byte today's handoff
- [ ] self-land runs NO full suite (integration: batch economics unchanged — land is
      squash + audit; the suite stays per-wave)
- [ ] KEYS.tsv gains the landing row; autolaunch_max default 5 everywhere the old default
      appears in code, KEYS and role docs; integration_wait_minutes stays 10 EVERYWHERE and its
      KEYS.tsv prose carries the 600s-cap warning (never raise past 10)
- [ ] kit/CLAUDE.md invariant 9 replaced with the contract v2.5 verbatim text, byte-exact; no
      other line of that file changes
- [ ] the role files' self-land tail uses bg run ship-<ID> + chunked bg wait --max 300 — never a
      foreground wait that can cross the 600s cap, never a background notification
- [ ] exactly ONE new top-level fn (self_land, builder.sh); api-kit.expected delta is exactly
      that line, sorted in place (you own the golden this wave)
- [ ] PROTOCOL section LANES documents self-landing; no new H2 headings anywhere
- [ ] busyint + express + claimguard drills stay green

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Never edit kit/ops/lib/*.sh while a selftest runs (Learned log); a failure point that moves
  between runs is corruption — re-run in a clean dir before believing it.
- Kit driver, foreground >=600000ms timeout on drill runs; scratch under scratchpad/T-088/ only.
- bash 3.2: no `case` inside `$(...)`; split `local` declarations.
- Lock ordering (contract section Lock ordering): int_on OUTERMOST, never while holding the
  board mutex — self_land runs AFTER handoff's mutex_off.
- STOP-AND-ASK detection: gate on the task's risk frontmatter plus ask-rule approval
  annotations; do not invent new classifiers.
- assumed: self_land is FAIL-CLOSED on a task with no risk: frontmatter (silent classic path) — an unclassified task cannot prove it is not stop-and-ask material, and this keeps every pre-6.1 drill fixture (none carry risk:) byte-identical; T-089's selfland fixtures must declare risk explicitly (default-safe)
- api-kit records KEYS.tsv keys too: the landing row is a second REAL golden line beyond the pinned self_land fn — proven via the POLARIS_ROOT index recipe; diff vs base = exactly those two lines
- smoke-proven in scratch: risk-normal handoff → land+seal+done end to end · risk: high → pinned refusal, stays review/ · landing: integrator → classic, zero new output · bare fixture (no risk key) → classic, zero new output
- fast tier (fmlist,tcm,brain,grant) + busyint,express,claimguard all SELFTEST-PASS with the new tail — spine handoffs take the fail-closed path by design
