---
id: T-100
title: "observe.sh — sweep reaps idle worktrees and reports live ones, orphan grace, qa stamps after, doctor sees keep-awake, finish and fleet learn the handover"
type: feature
scope: observe
epic: cant-eat-itself
plan: cant-eat-itself
points: 5
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 3.6
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-100
files_owned:
  - kit/ops/lib/observe.sh
approved:
context_files:
  - ops/contracts/worktree-liveness.md
  - ops/contracts/keep-awake.md
  - ops/contracts/role-handover.md
  - ops/contracts/bg-jobs.md
  - kit/ops/lib/workspace.sh
depends_on:
  - T-092
  - T-096
  - T-097
contract: ops/contracts/worktree-liveness.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/lib/observe.sh)" = "$(git show main:kit/ops/lib/observe.sh | grep -c '^[a-z_]*()')"
  - grep -q 'LIVE worktree: .polaris/wt/' kit/ops/lib/observe.sh
  - grep -q 'IDLE worktree: .polaris/wt/' kit/ops/lib/observe.sh
  - grep -q 'wt_remove .* sweep' kit/ops/lib/observe.sh
  - grep -q 'younger than 120s, left alone: a claim may be mid-flight' kit/ops/lib/observe.sh
  - grep -q 'last activity' kit/ops/lib/observe.sh
  - grep -q 'session alive' kit/ops/lib/observe.sh
  - grep -q 'session gone' kit/ops/lib/observe.sh
  - grep -q 'keep-awake not armed on this machine — ops/polaris awake install' kit/ops/lib/observe.sh
  - grep -q 'keep-awake is DISABLED (ops/polaris awake enable)' kit/ops/lib/observe.sh
  - grep -q 'stamp withheld' kit/ops/lib/observe.sh
  - grep -q 'then bash ops/polaris next and follow it' kit/ops/lib/observe.sh
  - test "$(grep -c 'Stop at the review handoff' kit/ops/lib/observe.sh)" = 0
  - grep -q 'Touching a visual: path? run the shot: line pack printed' kit/ops/lib/observe.sh
  - grep -q '\.polaris/handover' kit/ops/lib/observe.sh
  - grep -q '\.archive' kit/ops/lib/observe.sh
  - grep -q 'park: ' kit/ops/lib/observe.sh
  - bash -c "$(cat ops/tests/startup-budget.cmd)" | diff - ops/tests/startup-budget.expected
  - bash -c "$(cat ops/tests/keys-drift.cmd)" | diff - ops/tests/keys-drift.expected
map_delta: "sweep gains the worktree pass (LIVE reported, IDLE clean removed / dirty archived with --fix), 120 s orphan-lock grace, last-activity + session-alive lines, .archive and handover-dir pruning; doctor warns when keep-awake is unarmed/disabled; qa stamps only when HEAD and the tree are unchanged AFTER the suite; finish stamps the session's finished marker; fleet kickoff ends in polaris next"
landed: 93cd6e4bb2ce9b1e4b7f94db23d69d8736ca132b
---
## Why
The observing commands learn what the primitives now make true. `sweep` gains a worktree pass:
every `.polaris/wt/<ID>` is reported as LIVE (beat age, left alone) or IDLE (task column, age,
clean/dirty), and `--fix` removes idle clean ones (plus `branch -D` when the task is in done/) and
archives idle dirty ones through `wt_remove <ID> sweep` — which is what finally makes `finish`'s
caveat and `uninstall`'s "run sweep --fix" remedy true. Stale-lock lines gain "last activity <m>m
ago · session alive|gone" (lock meta line 5 via ONE `ps -W` on Windows, else `kill -0`); an orphan
lock younger than 120 s is reported but never dropped (a claim may be mid-flight); today's
`integrate/<date>` is never swept; `.polaris/bg/.archive/*` and `.polaris/handover/<sid>/` older than
24 h are pruned by `--fix`; `status` lists parks with age. `qa` writes its suite stamp only when HEAD
is unmoved AND the tree is clean AFTER the suite (`stamp withheld` otherwise) — a stamp taken before
a sibling's land could green a `finish` on code nobody tested. `doctor` warns when the keep-awake
hooks are not merged into `~/.claude/settings.json` or the daemon is disabled (gated on
`~/.claude` existing). `finish` rc 0 writes `.polaris/handover/<sid>/finished` so the Stop hook
never hops a finished session. The fleet kickoff carries the visual sentence and ends with
`then bash ops/polaris next and follow it` instead of "Stop at the review handoff". Specs:
worktree-liveness.md (sweep lines, grace), keep-awake.md (doctor lines), role-handover.md (state
dir, kickoff), bg-jobs.md v2 (archive pruning), visual-check.md (kickoff sentence).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `cmd_sweep` worktree pass over `git worktree list --porcelain` paths under `.polaris/wt/`: LIVE/IDLE lines byte-exact per contract; `--fix` ⇒ `wt_remove <ID> sweep`, `branch -D` on rc 0 when the task is in done/; `integrate/<date>` of today untouched; `--fix` prunes `.polaris/bg/.archive/*` and `.polaris/handover/<sid>/` older than 24 h
- [ ] orphan lock < 120 s ⇒ the pinned "younger than 120s" line, never dropped; STALE line gains ` · last activity <m>m ago · session alive|gone` (line 5 pid; `-` ⇒ gone); ONE `ps -W` per sweep on Windows
- [ ] `cmd_qa`: stamp written only when `git rev-parse HEAD` before == after AND `git status --porcelain` empty after; else one `⚠ … stamp withheld` line
- [ ] `cmd_doctor`: the two pinned keep-awake lines, gated on `[ -d "$HOME/.claude" ]`; silent when armed and enabled; `keys-drift` golden byte-identical
- [ ] `cmd_finish` rc 0 ⇒ `.polaris/handover/<sid>/finished` (epoch, best-effort, only with a sid); `cmd_status` lists parks `park: <name> · <age>m · <why>`
- [ ] `cmd_fleet` kickoff `msg` (:1869): visual sentence appended (single quotes only inside `msg`), `Stop at the review handoff.` → `then bash ops/polaris next and follow it.`; `--launch`/tmux quoting unchanged
- [ ] no new fn; `startup-budget` unchanged; `bash kit/ops/polaris doctor --selftest --only finish,qa,drift,hint` green (foreground, ≥600000 ms timeout)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: SURFACE-FROZEN — no new fn at any depth, no new `#` line, no new KEYS row. T-101 owns `ops/tests/api-kit.expected` this wave (key-registry.md §7).
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-100` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- observe.sh is 1,900+ lines; `find`/`show` before any Grep (`bash ops/polaris show kit/ops/lib/observe.sh#cmd_sweep`). The `triage-lane` golden runs ~9 s and is proven by `check` on the primary after landing — not a verify: line here.
- Scratch dirs per-lane: scratchpad/T-100/. Never edit kit/ops/lib/*.sh while a selftest is RUNNING. Windows/Git Bash: never assert on CR-sensitive output via grep/sed. bash 3.2.
- `status` keeps a `parked: <n> stash(es) — …` summary line ABOVE the pinned `park: <name> · <age>m · <why>` list: policy.sh:250 (drill_finish, NOT ours this wave) asserts `status | grep -q '^parked: '`, and dropping that prefix would red the `finish` drill this task's own acceptance requires green. T-104 may fold the summary away once it owns that assertion.
- sweep's `ps -W` is lazy — taken only when a STALE lock actually names a numeric pid, still at most once per sweep. Paying 0.4s on every sweep that finds nothing stale is the fork this repo keeps deleting.
- A worktree with NO beat file reads 999999s, so `sweep` prints `beat 16666m ago` and `--fix` would reap it. Correct per contract (beat-only liveness) but it means `sweep --fix` is unsafe until every beat writer has landed (T-101's preamble, T-093's hooks): before then a live lane looks idle.
- `qa`'s stamp gate is now HEAD-before == HEAD-after AND a clean tree AFTER; a dirty tree at the start also withholds and now SAYS so, where it used to skip the stamp in silence.
- Today's `integrate/<date>` is skipped in the remote-stray pass (a pipelined wave merges into base after every task and reuses the branch, so "merged" never means "finished").
- api-kit is red mid-wave on `kit/ops/hooks/handover-hook.sh fn hh_*` rows only — T-110's file does not exist yet while T-101's `.expected` already lists it. Nothing from observe.sh is in the diff; never `check --update` here.
