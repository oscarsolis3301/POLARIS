---
id: T-051
title: "`approved:` field on TASK.md + the guard's rc-1 message names the remedy"
type: chore
scope: board
epic: ask-rule-kind
plan: ask-rule-kind
points: 2
value: 8
time_criticality: 3
risk_opportunity: 3
wsjf: 7.0
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-051
files_owned:
  - kit/ops/templates/TASK.md
  - kit/ops/hooks/ownership-guard.sh
context_files:
  - ops/contracts/ask-approval.md
  - kit/ops/templates/TASK.md
depends_on: []
contract: ops/contracts/ask-approval.md
verify:
  - grep -q '^approved:' kit/ops/templates/TASK.md
  - bash -n kit/ops/hooks/ownership-guard.sh
  - grep -q 'polaris approve <ID> <scope>' kit/ops/hooks/ownership-guard.sh
  - bash ops/polaris board-fm >/dev/null
map_delta:
landed: 37ebc944bb5c9c101a83d7304ddfa2c072441b6d
---
## Why
Two small surfaces, both on the path a human or an agent actually walks.

The template is where the `approved:` field becomes discoverable — a field nobody can see in the
template is a field the Planner never writes and `approve` writes into nothing.

The guard's rc-1 message is what an agent reads at the exact moment it is blocked. Today it says
"if the rule is wrong, that is a HUMAN decision: propose the change, do not work around it" — correct
and complete when every rule meant *never*. With `ask` there is now a legitimate third answer, and it
is the one that would have saved the ARC sprint: the human may have already said yes, and if so the
yes belongs on the task. The message must name that remedy AND make clear the Builder does not run it.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `kit/ops/templates/TASK.md` gains the optional `approved:` field directly after `files_owned:`,
      exactly as shaped in `ops/contracts/ask-approval.md` § 2 (field line + one commented `-` item
      showing `<scope> — <who>, <date>: <why>`)
- [ ] the comment says `polaris approve` writes these — a Planner never hand-edits them
- [ ] a task file with no `approved:` field, or an empty one, stays valid: `bash ops/polaris board-fm`
      is unchanged and every existing task still parses
- [ ] the guard's rc-1 branch in `kit/ops/hooks/ownership-guard.sh` adds the contract's pinned remedy
      line VERBATIM: `` If a human has already approved this, it belongs on the task: `polaris approve
      <ID> <scope> -m "why"` — a Builder cannot run it. ``
- [ ] the existing rc-1 lines are kept — "rules bind even inside files_owned" and "if the rule is
      wrong, that is a HUMAN decision" are still true and still needed
- [ ] rc 2 (the value returned to Claude Code) and the rc 0/1/3 handling are unchanged; no new
      subprocess is added to the hook's hot path

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- The hook runs on EVERY Edit/Write against a 10s timeout and already sits at ~940ms per write. This
  is a message-text change only — do not add a `polaris` call, a git call, or a file read to it.
- The template's `files_owned:` already uses a bare `-` placeholder item; follow that exact shape so
  `fm_list` treats an unfilled `approved:` as empty.
- `kit/ops/templates/` is the PRODUCT copy. `ops/templates/` is the installed twin and is
  RULES-guarded — read it, never write it.
- `fm_list` needed no change: absent `approved:` and empty `approved:` both return zero items, and
  neither disturbs the `context_files` block that follows it.
- The pinned remedy line must be `echo '...'` (single quotes) — its backticks around `polaris approve`
  would be command substitution inside the `echo "..."` the neighbouring rc-1 lines use.
