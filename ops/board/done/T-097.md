---
id: T-097
title: "core.sh — lock meta learns the session (sid + claude pid), pid-aware mutex steal, ls-remote board tip, evt writes the handover last-event"
type: feature
scope: core
epic: cant-eat-itself
plan: cant-eat-itself
points: 2
value: 5
time_criticality: 3
risk_opportunity: 3
wsjf: 5.5
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-097
files_owned:
  - kit/ops/lib/core.sh
approved:
context_files:
  - ops/contracts/worktree-liveness.md
  - ops/contracts/role-handover.md
  - kit/ops/lib/workspace.sh
  - kit/ops/lib/builder.sh
depends_on: []
contract: ops/contracts/worktree-liveness.md
verify:
  - bash -n kit/ops/lib/core.sh
  - test "$(grep -c '^[a-z_]*()' kit/ops/lib/core.sh)" = 38
  - grep -q 'CLAUDE_CODE_SESSION_ID' kit/ops/lib/core.sh
  - grep -q 'CLAUDE_PID' kit/ops/lib/core.sh
  - grep -q 'ls-remote origin refs/heads/polaris/board' kit/ops/lib/core.sh
  - test "$(grep -c 'rev-parse -q --verify FETCH_HEAD' kit/ops/lib/core.sh)" = 0
  - grep -q -- '-gt 1200' kit/ops/lib/core.sh
  - grep -q '\.polaris/handover' kit/ops/lib/core.sh
  - grep -q 'last-event' kit/ops/lib/core.sh
  - grep -q 'avoid' kit/ops/lib/core.sh
  - grep -q 'MUTEX/epoch' kit/ops/lib/core.sh
  - bash -c "$(cat ops/tests/startup-budget.cmd)" | diff - ops/tests/startup-budget.expected
  - bash -c "$(cat ops/tests/triage-lane.cmd)" | diff - ops/tests/triage-lane.expected
map_delta:
landed: a5bb2dc9aa4c3dbe33b3b5d4edffb98c1f3356cf
---
## Why
Three small changes to the board's core, all pinned by contract, none adding a function.
(1) The task lock's `meta` file gains two lines: the session id (`$CLAUDE_CODE_SESSION_ID`) and the
harness pid (`$CLAUDE_PID`) — both live in every Bash-tool environment and stay stable for the whole
session, unlike `$$`. They never decide liveness (the beat does — T-092); they let `resume` recognize
its own session after a compaction, `sweep` say "session alive/gone", and `next` know whose lock it
is. Every 3-line reader keeps working; a missing line reads `-`. (2) The board mutex steal at
`mutex_on` becomes pid-aware — a holder that is still alive is not stolen from after 120 s of a slow
board write; a dead or absent pid still is, and 20 minutes steals regardless — and `sync_board`
re-stamps the mutex epoch on each retry and reads the remote tip with `ls-remote`, never a bare
`FETCH_HEAD` another fetch may have moved. (3) `evt()` records the event it just wrote into the
session's handover state dir (`.polaris/handover/<sid>/last-event`, plus `started` on first write and
`avoid` on release/blocked/kickback) when a session id is set — the file the handover hook (T-110)
and `polaris next` (T-109) read. Specs: worktree-liveness.md § lock meta / § steals;
role-handover.md § session state.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `lock_take` writes 5 lines: epoch · who · id · `$CLAUDE_CODE_SESSION_ID` or `-` · `$CLAUDE_PID` or `-`; `lock_age` and every other 3-line reader unchanged; probe in a fixture repo: `CLAUDE_CODE_SESSION_ID=sid-x CLAUDE_PID=4242 bash <kit> claim T-1` then `sed -n 4p .git/polaris-locks/T-1/meta` prints `sid-x`, line 5 `4242`; unset env ⇒ `-`
- [ ] `mutex_on` steal: iff age > 120 AND (pid absent OR dead), OR age > 1200; `mutex_off` unchanged
- [ ] `sync_board`: re-stamps `$MUTEX/epoch` at the top of each retry when `$MUTEX/pid` == `$$`; remote tip via `git ls-remote origin refs/heads/polaris/board | cut -f1`; no `FETCH_HEAD` read remains
- [ ] `evt()`: when `$CLAUDE_CODE_SESSION_ID` is set, `mkdir -p "$PRIMARY/.polaris/handover/<sid>"`, overwrite `last-event` with the exact `<ts> <kind> <id>` just appended, create `started` on first write, append the id to `avoid` for kinds release/blocked/kickback — all best-effort (`2>/dev/null || true`), never a failed evt; unset sid ⇒ no state write
- [ ] no new fn (38 before and after); `startup-budget`, `triage-lane` goldens green from the worktree
- [ ] `bash kit/ops/polaris doctor --selftest --only syncrace,claimguard,busyint` green (foreground, ≥600000 ms timeout)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: SURFACE-FROZEN — no new fn at any depth, no new `#` line, no new KEYS row (key-registry.md §7). T-096 owns `ops/tests/api-kit.expected` this wave.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-097` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- core.sh is on the write-guard's hot path (`_match|_rules|_guard` load core+ownership only): add NO fork to any fn the guard calls; `evt()` runs only on board mutations. Never edit kit/ops/lib/*.sh while a selftest is RUNNING. Scratch dirs per-lane: scratchpad/T-097/. bash 3.2 (CI runs macOS /bin/bash).
- `cmd_resume`'s own 3-line meta rewrite (builder.sh:592) is T-098's to extend to 5 lines — not this task's.
- `mutex_off` already wrote `$MUTEX/pid`; its comment claimed the steal was "deliberately pid-blind" — updated in place, since the steal now reads that same pid. No new file, no new fn.
- `evt()` captures `date +%s` into `$ts` once and reuses it for both the ndjson line and `last-event`, so the state write costs ZERO extra forks beyond one `mkdir -p` (and only when a session id is set).
- `sync_board` keeps the `fetch` (it is what brings the objects local); `ls-remote` only names the tip. A fetch that fails while `ls-remote` answers now skips the old 0.3s backoff — 5 fast retries, then the same die.
- probed in a fixture repo: `sid`/`pid` land on meta lines 4/5, unset ⇒ `-`/`-`; steal fires for dead-pid/absent-pid at >120s and for a LIVE pid only past 1200s (live holder at 300s waits out the 150-iteration timeout instead).
