---
id: T-176
title: "update-hook costs nothing on a normal start — a checked-today test in plain bash before the CLI starts, and no more leaked temp copies"
type: bug
scope: hooks
plan: v4-fast
points: 1
value: 3
time_criticality: 2
risk_opportunity: 3
wsjf: 8.0
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-176
files_owned:
  - kit/ops/hooks/update-hook.sh
  - kit/ops/lib/admin.sh
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/auto-update.md
depends_on: []
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/hooks/update-hook.sh
  - bash -n kit/ops/lib/admin.sh
  - F="$(mktemp -d)"; git init -q "$F"; mkdir -p "$F/ops" "$F/.polaris"; : > "$F/ops/polaris"; printf 'version: 6.5.0\n' > "$F/ops/VERSION"; printf 'checked: %s\nlatest: 6.5.0\n' "$(date +%Y-%m-%d)" > "$F/.polaris/update-cache"; out="$(printf '{"cwd":"%s"}' "$F" | bash kit/ops/hooks/update-hook.sh --test)"; rm -rf "$F"; test -z "$out"
  - F="$(mktemp -d)"; git init -q "$F"; mkdir -p "$F/ops" "$F/.polaris" "$F/kit/ops"; : > "$F/ops/polaris"; : > "$F/kit/ops/pack.py"; printf 'version: 6.5.0\n' > "$F/ops/VERSION"; out="$(printf '{"cwd":"%s"}' "$F" | bash kit/ops/hooks/update-hook.sh --test)"; rm -rf "$F"; test -z "$out"
  - F="$(mktemp -d)"; git init -q "$F"; mkdir -p "$F/ops" "$F/.polaris"; : > "$F/ops/polaris"; printf 'version: 6.5.0\n' > "$F/ops/VERSION"; printf 'checked: 2000-01-01\n' > "$F/.polaris/update-cache"; out="$(printf '{"cwd":"%s"}' "$F" | bash kit/ops/hooks/update-hook.sh --test)"; rm -rf "$F"; case "$out" in *'would run'*) true;; *) false;; esac
  - bash kit/ops/polaris doctor --selftest --only upgrade,autoupdate
  - bash ops/tests/perm-tools.cmd 2>/dev/null | diff - ops/tests/perm-tools.expected
  - bash ops/tests/output-style-installed.cmd 2>/dev/null | diff - ops/tests/output-style-installed.expected
  - bash ops/tests/adhd-skill-installed.cmd 2>/dev/null | diff - ops/tests/adhd-skill-installed.expected
  - bash ops/tests/startup-budget.cmd 2>/dev/null | diff - ops/tests/startup-budget.expected
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta:
surface:
landed: 5243c337399ab81ecb3915d206b522bb63269775
---
## Why
At every session start, `update-hook.sh` launches the full POLARIS CLI (twice) only to discover
that it already checked for updates today. That is about 1 s here and 2–3 s in installed repos,
paid on every new chat. Worse, when `update --auto` re-runs itself from a temporary copy, it never
deletes that copy: 82 of them sit in `%TEMP%` right now, about 90 MB.

Do the cheap check first, in plain bash with no child processes: if this is the kit repo itself
(`kit/ops/pack.py` exists), stop; if `.polaris/update-cache` says `checked:` today and its
`latest:` is not newer than `version:` in `ops/VERSION`, stop. Only otherwise start the CLI, exactly
as today. The check sits BEFORE the `--test` print, so `--test` proves it. In `admin.sh`, the
re-exec removes its temporary copy when it exits.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] Fresh cache (checked today, latest ≤ version) → the hook stops before starting the CLI; `--test` prints nothing.
- [ ] Self-hosting (`kit/ops/pack.py` present) → silent.
- [ ] Stale or missing cache → unchanged: `--test` still prints `would run`.
- [ ] No process starts before the cache decision: `read` builtins and `printf '%(%Y-%m-%d)T' -1` for today (the cache writes local time with `date +%Y-%m-%d`).
- [ ] `cmd_update`'s re-exec removes its temp copy on exit; count the `%TEMP%` copies before and after one `update --auto` run and record both in Notes.
- [ ] No new api rows (speed.md § 5).

## Notes
- The comparison is semver, in bash: a plain string compare gets `6.10.0` vs `6.9.0` wrong.
- Evidence (plans/audit-0924.md, verbatim):
  - **hooks-8** · OVERSTATED · update-hook starts the full CLI twice at every session start before it checks the once-a-day cache — ≈0.9 s per session start here, and an estimated ~2–3 s in installed repos at normal load (two startups plus a 1.1 MB copy). The pure-bash cache pre-check stays worthwhile. The fix should also remove the leaked temp copy (~90 MB so far). — fix: Add a pure-bash pre-check to kit/ops/hooks/update-hook.sh before it launches polaris: 1. Exit 0 if `$p/kit/ops/pack.py` exists (self-hosting). 2. Read `$p/.polaris/update-cache` with `read` builtins. If `checked:` is today and `latest:` ≤ the `version:` in `$p/ops/VERSION`, exit 0. A pure-bash semv… (kit/ops/hooks/update-hook.sh, kit/ops/lib/admin.sh)
  - **`update --auto` never deletes its re-exec temp copy** (kit/ops/lib/admin.sh:413-419: mktemp -d, cp polaris, cp -R lib, exec). The session-start update hook therefore leaves ~1.1 MB in %TEMP% each time. 82 copies exist now, 3–11 per day; their md5 matches the installed 6.5.0 CLI in another install.
- %TEMP% copies (`$TMPDIR/tmp.*/polaris`) around one `update --auto` in a fixture repo: HEAD's kit 82 → 83 (leaks one); this branch 83 → 83 (removed on exit; an explicit `update` that dies keeps rc 1 and also leaves 83 → 83). The one measurement leak was deleted; the 82 older copies were left alone.
- Re-exec cleanup is `exec bash -c '…; rm -rf "$d"; exit "$rc"'`: keeps today's exec (the original process holds no ops/polaris handle) and deletes only after the copy has exited, since Windows will not remove a script bash still has open.
- The hook's `in="$(cat)"` became `IFS= read -r -d ''` — it was a process before the cache decision too.
- bash < 4.2 (macOS /bin/bash 3.2) has no `printf '%(…)T'`: the hook falls back to one `date` there rather than to the CLI.
- Measured on a loaded box, fresh cache: old hook ≈6.6 s per session start, new ≈0.44 s against a bare `bash -c true` floor of ≈0.26 s.
