---
id: T-157
title: "polaris amend <ID> --verify — grant's sibling for the acceptance list: replace, drop or add one verify line of a claimed task from the primary, never from a builder, never a bare suite"
type: feature
scope: board
plan: sprint-c
points: 3
value: 5
time_criticality: 3
risk_opportunity: 5
wsjf: 4.3
risk: normal
model: mid
status: done
owner: Owner@DESKTOP
branch: feat/T-157
files_owned:
  - kit/ops/lib/integrate.sh
  - kit/ops/lib/selftest/board.sh
  - kit/ops/lib/selftest/fast.sh
approved:
context_files:
  - ops/contracts/grant.md
  - kit/ops/lib/builder.sh
  - kit/ops/lib/ownership.sh
  - ops/contracts/sprint-report.md
depends_on: [T-155, T-153, T-154]
contract: ops/contracts/grant.md
verify:
  - bash -n kit/ops/lib/integrate.sh
  - bash -n kit/ops/lib/selftest/board.sh
  - bash -n kit/ops/lib/selftest/fast.sh
  - bash kit/ops/polaris help >/dev/null
  - grep -q '^cmd_amend()' kit/ops/lib/integrate.sh
  - grep -q '^amend_verify()' kit/ops/lib/integrate.sh
  - grep -q 'chore(board): amend' kit/ops/lib/integrate.sh
  - grep -q 'a Builder never rewrites its own gate' kit/ops/lib/integrate.sh
  - grep -q 'ft_section amend' kit/ops/lib/selftest/fast.sh
  - grep -q 'chore(board): learned' kit/ops/lib/selftest/board.sh
  - bash kit/ops/polaris amend T-157 --verify 1 -m x -- true 2>&1 | grep -q 'a Builder never rewrites its own gate'
  - diff <(POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/integrate.sh' | tr -d '\r' | grep -Ev 'cmd_amend|amend_verify') <(grep '^kit/ops/lib/integrate.sh' ops/tests/api-kit.expected | grep -Ev 'cmd_amend|amend_verify')
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/selftest/board.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/selftest/board.sh' ops/tests/api-kit.expected)
  - POLARIS_ROOT="$PWD" python kit/ops/index.py find --api 'kit/ops/lib/selftest/fast.sh' | tr -d '\r' | diff - <(grep '^kit/ops/lib/selftest/fast.sh' ops/tests/api-kit.expected)
  - bash kit/ops/polaris doctor --fast
  - bash ops/tests/startup-budget.cmd | diff - ops/tests/startup-budget.expected
map_delta:
  - "integrate.sh: cmd_amend (amend <ID> --verify <n>|--add|--drop -m why — the sanctioned verify: amendment for a CLAIMED task; refuses on feat/*, refuses a bare full-suite command, one chore(board): amend <ID> verify commit) + amend_verify (the pure list surgery, fast-tier section amend); grant.md v2"
landed: e6f8d29133d88cf4581ca0fd7f0e3ed041c8dd28
---
## Why
`grant` widens a claimed task's `files_owned` and nothing widens or corrects its `verify:`. A
cross-lane golden owner is handed a verify line that is unsatisfiable by construction — its branch
is based on base, so the sibling's rows read as diff hunks until the sibling lands — and every
occurrence (T-122, T-125, T-136, T-139) cost a round trip to a human or a conductor to hand-edit a
board file. A calibration note fixes future carves and cannot reach work already on the board; a
command can. `amend` is grant's sibling with grant's discipline: a claimed task only, a reason on
the record, one board commit, and refusals that mutate nothing. Two guards keep it honest: it
refuses on any `feat/*` branch (a builder never rewrites its own gate — approve's rule) and it
refuses a bare full-suite command (`verify`'s own predicate).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `cmd_amend` in integrate.sh exactly as grant.md v2 pins it: the three forms, the five preconditions, the effects (in-place list edit, the Notes line, the `amend` event, `chore(board): amend <ID> verify`, `sync_board`); every refusal rc 1 with the file byte-identical; the feat/* refusal text is `⛔ amend refused on <branch> — a Builder never rewrites its own gate; run it from the primary checkout`
- [ ] `amend_verify <taskfile> <n|add> <newline|->` pure (awk over the frontmatter, rc 1 out of range, no board side-effects)
- [ ] `drill_grant` (board.sh): the contract's amend assertions (replace · drop · add · feat/* refusal · out-of-range · bare suite — rc + bytes) AND sprint-report.md v3's `learned` assertions (`learned -m x` from inside a feat/* worktree: last bullet of `## Learned`, the event, the pinned board-commit subject) — T-153 landed the fn and T-155 the dispatch before you
- [ ] `selftest_fast` section `amend` (fast.sh): replace / drop / add / out-of-range on a fixture file
- [ ] the drill green in your worktree: `bash ops/polaris bg run t157 -- bash kit/ops/polaris doctor --selftest --only grant` + chunked `bg wait t157 --max 300`; paste the `selftest passed` line into Notes
- [ ] `polaris verify` green

## Notes
- The dispatch line `amend)` and its usage entry already exist (T-155) — you add the function they call. `bash kit/ops/polaris amend …` from your worktree hits the feat/* refusal first, which is exactly what the verify line asserts.
- `cmd_grant`/`cmd_approve` (builder.sh) are the shape to copy: flag loop, `-m` required, `task_file`, `mutex_on`, the Notes append, `evt`, `board_commit`, `sync_board`, `mutex_off; trap - EXIT`. `_norm_cmd` + `cfg test`/`cfg build` (ownership.sh `run_verify_cmds`) is the bare-suite predicate — reuse, do not copy.
- `fm_list verify` reads the list; the writer is yours (`fm_append_item` appends only). Frontmatter lists are `  - <item>` lines under `verify:`; keep every other frontmatter byte untouched (the express drill diffs task files).
- Per-lane scratch (`mktemp -d`); trust rc + bytes (T-089).
- drill green in the worktree: `bash kit/ops/polaris doctor --selftest --only grant` → `✅ selftest passed (subset: grant — 1 of 36 labeled drills; spine always runs)` (9m, via bg run t157)
- amend_verify handles the block-list shape only (what the TASK template and the Planner emit); an inline `verify: [a, b]` has no line to edit in place and refuses with the bytes untouched, rather than guessing
- the feat/* refusal runs BEFORE any board read, so a Builder in its own worktree always hits that one message whatever else is wrong with the invocation — which is what the task own verify line asserts
- drill ordering: T-AM leaves the disk before the learned drill, whose board commit rebuilds the ref tree from disk and so drops it — nothing downstream can claim or count it
- the learned drill saves/restores ops/SPRINT.md (the drill_handover idiom): drill_express asserts the burndown row is the FILE last line, and a stray Learned section would have broken it in any shard that runs both
