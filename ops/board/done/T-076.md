---
id: T-076
title: "Doctor reports config drift — one line naming the keys an install is missing"
type: feature
scope: doctor
epic: autonomy-by-default
plan: autonomy-by-default
points: 3
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 6.0
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-076
files_owned:
  - kit/ops/lib/observe.sh
  - ops/tests/keys-drift.cmd
  - ops/tests/keys-drift.expected
context_files:
  - ops/contracts/key-registry.md
  - ops/tests/triage-lane.cmd
  - kit/ops/KEYS.tsv
depends_on:
  - T-074
  - T-075
contract: ops/contracts/key-registry.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - bash ops/polaris check --only keys-drift
  - bash kit/ops/polaris doctor --selftest --only notify
map_delta: "doctor reads ops/KEYS.tsv and reports CONVENTIONS keys absent from the live file as ONE summary line naming the remedy (ops/polaris adopt) — the class of drift the CLAUDE.md stamp check covers for exactly one file (key-registry.md § 2)"
landed: b923cf2754e4da45f020875c6916137ffb84f142
---
## Why
The only drift detector of this class is the CLAUDE.md `[kit X.Y.Z]` stamp (observe.sh:427-443),
added after a repo sat three weeks stale while doctor called it healthy — and that lesson was
applied to exactly one file. This applies it to the config surface: after the CONVENTIONS presence
check (:251), read `ops/KEYS.tsv` and report absent keys as ONE line in the stamp check's tone.
One line, not a warning storm; a commented stub counts as present, which is how `adopt` (T-077)
silences the line without changing behavior.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `$OPS/KEYS.tsv` missing → say nothing (pre-6.0 installed copy)
- [ ] presence test per key-registry.md § 2: line-start `key:` OR a `# key:` stub both count as
      present; anything else is absent
- [ ] absent keys → exactly the pinned one-liner (first six absent keys in KEYS.tsv order,
      `+<r> more` tail only when >6): `⚠ CONVENTIONS.md lacks <n> of <m> known keys (…) — see
      what each unlocks: ops/polaris adopt`
- [ ] zero absent keys → no output from this check at all
- [ ] no kit version number in the line (derived-surface golden trap)
- [ ] golden pair `ops/tests/keys-drift.{cmd,expected}` — HERMETIC (fixture repo + fixture
      KEYS.tsv with FAKE keys, the triage-lane pattern) proving both the firing and the silent
      case; byte-identical on a second run from any board state
- [ ] surface-frozen (key-registry.md § 5): implemented inline in cmd_doctor, no new top-level fn

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Serial chain on observe.sh: T-075 lands first — build on its flipped block, don't touch it.
- The fixture writes its own `ops/KEYS.tsv` in a throwaway repo, so the REAL registry can grow
  without redding the golden. Never anchor to the live file.
- `--only notify` FOREGROUND with an explicit ≥600000ms timeout, or log-and-poll for
  `selftest passed`; kit driver only (`bash kit/ops/polaris …`).
- bash 3.2: no `case` inside `$(...)`.
- assumed: CONVENTIONS.md absent → the drift line stays SILENT too (default-safe). The presence
  check one line above already says "INIT has not run"; "lacks 37 of 37" on top of it is the storm
  the check exists to avoid. Pinned as state 0 of the golden, so reversing it is a one-line change.
- Presence is ONE awk over both files (core.sh:9's fork-cost lesson — a Windows fork is ~240ms and
  doctor is on the write-guard's path), keys read as "everything left of the first colon".
- `check` reads `$OPS/tests` and runs from `$PRIMARY` (observe.sh:1244,1251), so a NEW golden written
  in a Builder worktree is invisible to `polaris verify` — `--only keys-drift` reports "no goldens
  matched" and verify goes green vacuously. Proved this pair by hand instead (`bash -c "$(cat
  ops/tests/keys-drift.cmd)"` from the worktree, twice, byte-identical); it runs for real at the
  wave gate. Worth EVOLVE's attention: every golden a Builder adds ships unproven by its own verify.
- Live check against the real 37-row registry: this repo's CONVENTIONS.md reads
  `lacks 14 of 37 known keys (builders · plan_gate · builder_questions · evolve_apply · drain_slices
  · notify +8 more)` — the dormant-feature silence the sprint exists to end, now visible.
