---
id: T-070
title: "bg.sh — the background job runner: run/status/tail/wait, rc-file-first, .prev rotation, sweep"
type: feature
scope: cli
epic: routing-and-bg
plan: routing-and-bg
points: 5
value: 8
time_criticality: 5
risk_opportunity: 8
wsjf: 4.2
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-070
files_owned:
  - kit/ops/lib/bg.sh
  - kit/ops/polaris
  - kit/ops/lib/observe.sh
  - ops/tests/api-kit.expected
context_files:
  - ops/contracts/bg-jobs.md
  - ops/contracts/module-layout.md
  - kit/ops/lib/workspace.sh
  - ops/board/done/T-062.md
depends_on:
  - T-065
contract: ops/contracts/bg-jobs.md
verify:
  - bash -n kit/ops/lib/bg.sh
  - bash -n kit/ops/polaris
  - bash -n kit/ops/lib/observe.sh
  - bash kit/ops/polaris bg run t070-verify -- true
  - bash kit/ops/polaris bg wait t070-verify --max 60
  - bash kit/ops/polaris bg status t070-verify
  - bash kit/ops/polaris bg run t070-verify -- true
  - bash kit/ops/polaris bg wait t070-verify --max 60
  - grep -q cmd_bg kit/ops/polaris
  - grep -q bg_ kit/ops/lib/observe.sh
  - grep -q cmd_bg ops/tests/api-kit.expected
map_delta: "kit/ops/lib/bg.sh — background job runner: bg run/status/tail/wait, dir-per-job $PRIMARY/.polaris/bg/<name>/, rc-file-first verdicts, .prev rotation, sweep --fix rotates >24h (ops/contracts/bg-jobs.md) (5.24.0-unreleased)"
landed: a72d670f6ab8b34918a20e184fe71804f1459835
---
## Why
Suite-length commands (805s serial, `qa` 1225s) die at the harness's 600s foreground cap: they time
out, return NOTHING, and get re-run — pure token and wall-clock waste, plus two subagents last
sprint ending turns with a suite still running because the recipe lived in folklore. `bg` makes it
a command. New module `kit/ops/lib/bg.sh` per `ops/contracts/bg-jobs.md` — that contract IS the
spec (registry layout, rc semantics, rotation, cwd rules); code to it, invent nothing. The shape
that matters most, from the T-064 lesson: jobs get pid semantics FROM BIRTH, and every verdict is
rc-FILE-FIRST, then `kill -0` — a pid check alone never declares a verdict, because Windows reuses
pids. Suite keys run in the CALLER's cwd (a builder proves its own worktree); `qa` always runs in
the primary, so its green stamps `.polaris/suite-stamp` and a later `finish` skips the whole suite
via the EXISTING fast path — the warm-stamp integration costs zero new code. Wiring: kit/ops/polaris
gains the `bg` dispatch + usage entry and the loader's FULL-load list gains `bg` after `admin`
(module-layout v4 — the `_match|_rules|_guard` short path stays EXACTLY `core ownership`);
`cmd_sweep` (observe.sh:144) `--fix` learns to rotate job dirs whose `start` is >24h old.

This task owns the wave-2 api-kit delta: one `fn` line per bg.sh top-level function, hand-authored
byte-exact. You feed the surface AND own the golden, so there is no cross-lane pin — but keep the
census tight (`cmd_bg` + `bg_`-prefixed workers, ≤10 total).

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `bg run <name>` — suite keys per contract (caller cwd; `qa` → primary; empty key ⛔ rc 1),
      `-- <cmd…>` arbitrary form, duplicate-RUNNING refusal ⛔ rc 1 naming `bg status`, `--force`
      kill+rotate+rerun, stdin /dev/null, runner survives the parent tool call ending
- [ ] `bg status [<name>]` rc contract EXACT: 0 green · 1 red · 2 running · 3 unknown (dead pid,
      no rc — message says crashed-or-pid-reused and names the log); bare form lists name ·
      verdict · age, `no background jobs` rc 0 when empty
- [ ] `bg tail <name> [-n N]` last N (default 20) log lines, read-only
- [ ] `bg wait <name> [--max <s>]` — poll ~2s, `--max` default 300; finished → tail + verdict,
      exits with the job's rc; expiry → ONE `still running` resumable line, rc 2, NEVER a question
- [ ] completed same-name rotates to `<name>.prev` (ONE slot, archive-not-delete) before re-run;
      `sweep --fix` rotates >24h jobs; plain `sweep` only reports them
- [ ] registry is `$PRIMARY/.polaris/bg/<name>/` with cmd·cwd·pid·log·rc·start·end, rc written
      LAST; works identically when invoked from a worktree
- [ ] api-kit.expected wave-2 delta: exactly one line per new top-level fn, byte-exact (recipe in
      Notes); bg.sh ≤300 lines, function definitions only, bash 3.2-safe
- [ ] `bg` never writes the board, EVENTS.ndjson, or any lock

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- The installed `ops/polaris` is 5.23.0: `bg` does not exist at runtime — prove EVERY behavior with
  `bash kit/ops/polaris …` (the verify list above already does).
- Detach recipe that survives Git Bash: `( … ) </dev/null >log 2>&1 &` with `$!` captured to `pid`;
  test that the parent shell can exit while the job runs — that is the whole point. `nohup` is
  belt-and-braces if plain `&` proves fragile on Windows.
- api-kit golden, worktree-local proof (T-062, proven): `POLARIS_ROOT=<your worktree> python
  ops/index.py`, emit the `--api 'kit/*'` shape, byte-exact diff vs your hand-authored delta.
  `check`/`find` are PRIMARY-anchored — never prove your golden through them; the wave gate's uat
  is the referee.
- Your verify writes real job dirs (`t070-verify`) into the PRIMARY's `.polaris/bg/` — gitignored
  runtime state, harmless; leave them for `sweep` or rotate them, never `git add`.
- Never run `doctor --selftest` while your kit edits are half-written — a mid-edit suite fabricates
  a different spurious failure each run. Long suites FOREGROUND with an explicit ≥600000ms timeout;
  past the cap → log-and-poll INSIDE your turn (yes: the discipline this task exists to automate).
- bash 3.2: no `case` inside `$(...)`, no mapfile/assoc arrays, split `local` declarations.
- workspace.sh (context) is the house pattern for pid-guarded state + poll loops (int_on/int_off) —
  match its style, do not import its lease semantics (bg is lock-free by design).
- ps -ef on Git Bash is UID/PID/PPID: --force reaps direct children (awk '$3==pid') before the leader — an orphan's open log handle would fail the rotation mv on Windows
- assumed: `.prev` dirs are archives — skipped by bare `bg status` and by sweep, still addressable explicitly (status/tail); `bg run` refuses a `*.prev` job name (the slot namespace the contract reserves)
- sweep --fix never rotates a >24h job whose pid is ALIVE (reported with a collect hint instead) — rotating a live job's dir out from under its runner is destruction, not hygiene
- crashed jobs (no rc, dead pid) also archive to .prev on re-run — archive-not-delete extends to crashes; `bg wait` on one exits 3 immediately instead of burning --max
- bg wait normalizes finished jobs to exit 0/1 on purpose: a job that itself exited 2 must stay distinguishable from wait's still-running rc 2
- detach survival proven across tool calls: job spawned in one Bash call, alive and collectable in the next (notify_fire idiom — plain subshell &, no nohup/setsid)
- census is the contract's 8 + bg_age (humanized ages for listing/verdict lines) = 9 ≤ 10; api-kit delta proven byte-exact against POLARIS_ROOT=<wt> ops/index.py find --api 'kit/*' with AND without -n (508 lines, no default-limit truncation)
