---
id: T-179
title: "One suite per close — a green suite is kept when only leftover branches are red, and EVOLVE runs before the last qa instead of after it"
type: bug
scope: qa
plan: v4-fast
points: 2
value: 8
time_criticality: 5
risk_opportunity: 5
wsjf: 9.0
risk: normal
status: done
owner: Owner@DESKTOP
branch: feat/T-179
files_owned:
  - kit/ops/lib/observe.sh
  - kit/ops/lib/integrate.sh
  - kit/ops/roles/CONDUCTOR.md
  - kit/ops/lib/selftest/policy.sh
  - kit/ops/lib/selftest/history.sh
  - ops/tests/qa-stamp.cmd
  - ops/tests/qa-stamp.expected
approved:
context_files:
  - ops/contracts/speed.md
  - ops/contracts/run-finish.md
  - ops/contracts/worktree-liveness.md
  - ops/tests/handover-route.cmd
depends_on: [T-177]
contract: ops/contracts/speed.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - bash -n kit/ops/lib/integrate.sh
  - bash -n kit/ops/lib/selftest/policy.sh
  - bash -n kit/ops/lib/selftest/history.sh
  - bash ops/tests/qa-stamp.cmd 2>/dev/null | diff - ops/tests/qa-stamp.expected
  - grep -q 'mktemp -d' ops/tests/qa-stamp.cmd
  - bash kit/ops/polaris doctor --selftest --only qa,finish,express
  - bash ops/tests/startup-budget.cmd 2>/dev/null | diff - ops/tests/startup-budget.expected
  - POLARIS_ROOT="$PWD" python ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta: new golden ops/tests/qa-stamp — qa keeps a green suite's stamp when only CRUFT is red; CONDUCTOR step 7.5 runs EVOLVE before the final qa; land --express runs verify: before seal
surface:
landed: 1d8786cbc98ffc44601916fe5eea71313d19a258
---
## Why
The end of every conductor run pays for the full test suite twice, about 15 minutes each time.
The real cause is the order of the last steps: the final `qa` goes green, then EVOLVE commits its
tuning, which moves HEAD, so the next check has to run the whole suite again. On top of that, `qa`
throws away a green suite whenever the only red is leftover-branch housekeeping.

Three changes, and no gate gets weaker (`ops/contracts/speed.md` § 4):
1. CONDUCTOR step 7.5 runs EVOLVE BEFORE the final `qa`, so the last suite run certifies the tree
   that actually ships.
2. `qa` writes its suite stamp when every suite key is green and the only other reds are
   CRUFT-class (leftover branches). It still exits 1 on them, but the next `qa` at the same HEAD
   does not re-run the suite. MAP Deltas over 20 and LEARNED over 8 stay hard gates: no stamp.
3. `land --express` runs the task's `verify:` lines before `seal`, so a red one unwinds the landing
   instead of being discovered after the seal.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] CRUFT-only red with a green suite → the stamp equals HEAD, rc 1, and the next `qa` runs no suite key — in `drill_qa` and in `qa-stamp`.
- [ ] A MAP Deltas > 20 red → rc 1 and NO stamp (LEARNED > 8 the same).
- [ ] `land --express` runs `verify:` before seal; a red verify unwinds and kicks back — asserted in `drill_express`.
- [ ] CONDUCTOR.md step 7.5 puts EVOLVE before the final `qa`; no heading changes; BUILDER.md and SOLO.md need no change (EVOLVE placement is conductor-only).
- [ ] `drill_finish` green; every other drill green unchanged.
- [ ] No new api rows, drill functions or drill labels (speed.md § 5): new assertions go inside the existing drills.

## Notes
- You own the drill files `policy.sh` and `history.sh` alone this sprint; other drills in them (drift, checkoutguard, rules, route, selfland) must stay byte-for-byte green.
- T-177 rewrote drift/cruft/check in observe.sh before you; keep your change to `cmd_qa`'s stamp block.
- Evidence (plans/audit-0924.md, verbatim):
  - **telemetry-1** · CONFIRMED · The run's tail pays for the full suite twice: qa reds on MAP/LEARNED/CRUFT housekeeping only after the 15-min suite, and EVOLVE runs after qa and moves HEAD — About -16.5 to -17 min per affected close (one full qa, 992-1036s measured) plus about 3 min of diagnosis. The pure-CRUFT reds (qa3.prev) are already fixed by T-152, so the MAP and LEARNED classes are what remain. It happens more often than 'every 1-1.5 sprints': see missed item 3. Every EVOLVE aut… — fix: Three changes in cmd_qa and one in CONDUCTOR.md: 1. In kit/ops/lib/observe.sh cmd_qa, write the suite stamp as soon as every suite command is green, BEFORE cruft, drift and doctor. The stamp certifies the code commit, not board documents. Keep the rc 1 for drift and doctor. 2. Demote drift's doc-ov… (kit/ops/lib/observe.sh, kit/ops/roles/CONDUCTOR.md, ops/tests/*qa*.expected)
  - **close-1** · OVERSTATED · qa throws away a GREEN suite whenever drift reports a housekeeping note (MAP/LEARNED overflow), so the next qa/finish re-runs the whole ~13-17 min suite — About 0 s saved on its own, measured against the 4 registry events: the one no-commit case is already fixed in 6.5.0, and in the other 3 an EVOLVE commit moved HEAD. It is worth doing only as a prerequisite for a carry or input-keyed stamp (close-2 / missed-1). — fix: In cmd_qa, count suite reds separately: `sred_any` covers test/lint/typecheck/build/uat and `red` still covers everything. Move the stamp block (observe.sh:2187-2200) ABOVE the `die` and gate it on `sred_any -eq 0` plus the existing HEAD/dirty before-and-after checks. Stamping is safe: drift and do… (kit/ops/lib/observe.sh)
- The contract wins over the evidence's suggested fix: telemetry-1's step 2 (demote drift's doc-overflow notes) is NOT part of this task — MAP Deltas > 20 and LEARNED > 8 stay hard gates with no stamp (speed.md § 4). The stamp is decided after drift, because it needs to know whether every non-suite red is CRUFT-class.
- qa: the stamp is decided after drift/doctor and BEFORE the red verdict — `sred_any` (a suite key red) and `hred` (a drift finding not starting `CRUFT`, a drift red with no finding, or doctor red) withhold it; a CRUFT-only red stamps, notes "suite stamp kept at <sha7>", and still exits 1.
- land --express: verify: moved from after seal to between the suite and seal (not duplicated), run in a subshell so a die inside is a red rc; red → same unwind + kickback + die as a red suite, note `express verify red: <tail>`. ops/contracts/express-lane.md still lists the old order — speed.md § 4 supersedes it.
- drill_qa now COMMITS its CONVENTIONS fixture (an untracked file kept the tree dirty, so no stamp could ever be observed there) and commits the teardown, leaving CONVENTIONS.md absent and the tree clean for drill_finish.
- qa-stamp golden: ~22s (five qa runs on a hermetic fixture); reds against the pre-T-179 kit on cruft-only/cruft-again only.
- rider (conductor): the remaining in-loop `$(basename …)` forks in observe.sh became builtins — status_brief ids, status active/ready/blocked, board-fm, triage (x3), finish fin_ids/locks/bg/worktrees, fleet; the sweep bg loop was already builtin (T-177). status/board-fm/triage/fleet output byte-identical old vs new on the live board; triage-lane, board-fm-shape, startup-budget goldens green.
