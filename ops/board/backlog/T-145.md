---
id: T-145
title: "Teach the roles — INIT asks its questions in one call, the Planner scaffolds at the plan gate, the install skill knows both lines"
type: feature
scope: roles
plan: spend-less
points: 5
value: 8
time_criticality: 8
risk_opportunity: 5
wsjf: 4.2
risk: normal
model:
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/roles/
  - kit/.claude/skills/polaris-install/SKILL.md
  - kit/ops/PROTOCOL.md
  - ops/tests/api-kit.expected
approved:
context_files:
  - ops/contracts/first-run.md
  - ops/contracts/test-surfaces.md
  - ops/contracts/output-style.md
  - ops/contracts/cli-docs-parity.md
depends_on: [T-141, T-142]
contract: ops/contracts/first-run.md
verify:
  - grep -q '^### 2a. Interaction 1 — the preferences POLARIS asks for, in ONE call$' kit/ops/roles/INIT.md
  - grep -q '^### 2c. Interactions 2 and 3 — the only things a repo cannot tell you$' kit/ops/roles/INIT.md && grep -q '^### 2b. DERIVE — silently, from the survey you already did. Ask none of this.$' kit/ops/roles/INIT.md
  - grep -q 'bash ops/polaris interview' kit/ops/roles/INIT.md && grep -q 'interview --set' kit/ops/roles/INIT.md && grep -q '^adhd: off' kit/ops/roles/INIT.md && grep -q 'surfaces --scaffold --apply' kit/ops/roles/INIT.md
  - test "$(grep -c 'Will you run agents on one computer, or several' kit/ops/roles/INIT.md)" = 0
  - grep -q 'No rows yet and the repo has tests' kit/ops/roles/PLANNER.md
  - grep -q 'surfaces: none mapped' kit/.claude/skills/polaris-install/SKILL.md && grep -q 'preferences never set here' kit/.claude/skills/polaris-install/SKILL.md
  - test "$(grep -c '^## ' kit/.claude/skills/polaris-install/SKILL.md)" = "$(git show main:kit/.claude/skills/polaris-install/SKILL.md | grep -c '^## ')"
  - grep -q 'surfaces \[--scaffold \[--apply\]\]' kit/ops/PROTOCOL.md
  - test "$(grep -c '^#' kit/ops/PROTOCOL.md)" = "$(git show main:kit/ops/PROTOCOL.md | grep -c '^#')" && test "$(grep -c '^#' kit/ops/roles/PLANNER.md)" = "$(git show main:kit/ops/roles/PLANNER.md | grep -c '^#')"
  - bash -c "$(cat ops/tests/plain-voice.cmd)" | diff - ops/tests/plain-voice.expected
  - bash -c "$(cat ops/tests/output-style-installed.cmd)" | diff - ops/tests/output-style-installed.expected
  - bash -c "$(cat ops/tests/adhd-skill-installed.cmd)" | diff - ops/tests/adhd-skill-installed.expected
  - POLARIS_ROOT=$PWD python kit/ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta: "roles: INIT's interview is ONE AskUserQuestion generated from KEYS.tsv's ask column (2a renamed, 2c loses the claim question, the skeleton gains adhd:, step 3 runs interview --set then surfaces --scaffold --apply); PLANNER 5b scaffolds at the plan gate; the install skill acts on 'surfaces: none mapped' and 'preferences never set here'; PROTOCOL's tool row names surfaces --scaffold"
---
## Why
The code landed in W1 and W2; nothing tells an agent to use it. INIT still asks its own hand-written
voice question and a `claim:` question in the long form — now both come from `polaris interview`
and are asked in ONE call, with the ADHD question beside them (first-run.md § 4 pins the new 2a
heading, the 2c cut, the `adhd: off` skeleton line and the `interview --set` call in step 3, followed
by `surfaces --scaffold --apply` so a fresh repo is mapped before its first sprint). The PLANNER's
step 5b gains its first sentence: an empty map plus tests means scaffold at the plan gate and commit
the two files with the contracts — a conductor-entered planner reports what it wrote rather than
asking (test-surfaces.md v2 § 19). The install skill learns the two lines an update or a doctor can
now print and what to do with each, in one question at most. PROTOCOL's tool row names the new flag.
You are W3's single owner of `ops/tests/api-kit.expected`: INIT.md's renamed heading is a row change,
and it is the only heading change in this task — every other edit is a paragraph, a list item or a
table row (assert the `^#` counts of PROTOCOL.md and PLANNER.md are unchanged, and the skill's `## `
count too). T-144, beside you, adds no fn and no heading.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] INIT.md § 2 per first-run.md § 4: 2a renamed and rewritten (run `interview`, ask everything it prints in ONE AskUserQuestion, voice first; before CONVENTIONS exists read the questions off `ops/KEYS.tsv`'s ask column), 2b untouched, 2c heading untouched and its question 2 removed; § 3 skeleton gains `adhd: off` under `voice:`, step 3 runs `bash ops/polaris interview --set …` then `bash ops/polaris surfaces --scaffold --apply` (test-surfaces v2 § 19 wording); the 3-interaction cap sentence still holds
- [ ] PLANNER.md step 5b's first sentence verbatim from test-surfaces v2 § 19; no other PLANNER change
- [ ] polaris-install SKILL.md § Update gains the two pinned paragraphs (surfaces, preferences); `§ After the install` unchanged; no heading added or renamed
- [ ] PROTOCOL.md THE TOOL row updated; no heading change; MANUAL.md, CLAUDE.md, BUILDER/SOLO/INTEGRATOR/CONDUCTOR/EVOLVE untouched
- [ ] `api-kit.expected`: INIT.md's heading row updated by content diff — a strict diff is green in your worktree (you own every hunk this wave); `plain-voice`, `output-style-installed`, `adhd-skill-installed` green
- [ ] `bash kit/ops/polaris doctor --selftest --only claudemd,brief,hint` green in the worktree (explicit 600000 ms timeout)

## Notes
- The roles trio (BUILDER/SOLO/CONDUCTOR) co-changes 6–8× historically — this task deliberately leaves all three alone; if a sentence in one of them turns out to owe an edit, one IDEAS.md line, not a drive-by.
- The `output-style-installed` and `plain-voice` goldens count numbered rules and diff VOICE lines — you touch neither file, but run them anyway (they are in verify) because PROTOCOL.md is theirs to read.
- Scratch paths under `scratchpad/T-145/…` only; a subagent never ends its turn with a job running.
- Trust rc and file state (T-089).
