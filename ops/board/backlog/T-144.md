---
id: T-144
title: "Prove the activation — fast-tier sections for the engine and the interview, and the surfaces drill scaffolds, applies and asks"
type: feature
scope: selftest
plan: spend-less
points: 5
value: 8
time_criticality: 8
risk_opportunity: 8
wsjf: 4.8
risk: normal
model:
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/lib/selftest/fast.sh
  - kit/ops/lib/selftest/policy.sh
approved:
context_files:
  - ops/contracts/test-surfaces.md
  - ops/contracts/first-run.md
  - ops/contracts/fast-tier.md
  - kit/ops/lib/surfaces.sh
  - kit/ops/lib/admin.sh
depends_on: [T-141, T-142]
contract: ops/contracts/test-surfaces.md
verify:
  - bash -n kit/ops/lib/selftest/fast.sh && bash -n kit/ops/lib/selftest/policy.sh
  - for s in surfaces-runner surfaces-pairs surfaces-proposal interview-pending; do grep -q "ft_section $s " kit/ops/lib/selftest/fast.sh || exit 1; done
  - test "$(grep -c '^[a-z_][a-z_]*() *{' kit/ops/lib/selftest/fast.sh)" = "$(git show main:kit/ops/lib/selftest/fast.sh | grep -c '^[a-z_][a-z_]*() *{')"
  - test "$(grep -c '^drill_[a-z_]*()' kit/ops/lib/selftest/policy.sh)" = 12
  - grep -q 'no test runner this kit can scope' kit/ops/lib/selftest/policy.sh && grep -q '\[scaffold\]' kit/ops/lib/selftest/policy.sh && grep -q 'preferences never set here' kit/ops/lib/selftest/policy.sh && grep -q 'interview --set' kit/ops/lib/selftest/policy.sh
  - grep -q 'surfaces' kit/ops/lib/selftest/spine.sh
  - bash kit/ops/polaris doctor --fast | tail -1 | grep -q 'fast tier passed'
  - POLARIS_ROOT=$PWD python kit/ops/index.py find --api 'kit/*' | grep -v '^kit/\.claude/skills/i-have-adhd/' | diff - ops/tests/api-kit.expected
map_delta: "fast tier gains surfaces-runner · surfaces-pairs · surfaces-proposal · interview-pending; the surfaces drill gains steps 8–10 (NORUNNER on the bare fixture · scaffold --apply end to end + feat/* refusal · doctor's pending-preferences line before and after interview --set)"
---
## Why
T-140's engine and T-141's interview are pure over files, which is exactly what the in-process tier
is for: prove the runner table, the pairing filters, the RUNNER/ROW/SKIP grammar and the pending
computation in milliseconds, on every change, with fixtures under `$FT_TMP` and `PRIMARY`/`OPS`/`CONV`
overridden inside each section's subshell — no CLI re-invocation, no scratch repo. The drill proves
the real entry point: the fixture that has no runner gets NORUNNER, the fixture that gains a pytest
layout gets one `[scaffold]` row and a `test_select:` line from `--scaffold --apply`, the refusal
from a task worktree fires, and doctor's `preferences never set here:` line appears and then
disappears once `interview --set` has answered. Contract test-surfaces.md v2 § 17 and first-run.md
§ Executable check pin the sections, the steps and their assertions. No new top-level function
anywhere: sections live inside `selftest_fast`, steps inside `drill_surfaces` — this wave's golden
owner is T-145 and you add no api-kit row.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] fast.sh sections `surfaces-runner` · `surfaces-pairs` · `surfaces-proposal` · `interview-pending`, ≥ 4 asserts each, inside `selftest_fast`; fixtures written with builtins; the whole fast tier still under its budget (`doctor --fast` prints the pass line with the check count)
- [ ] `drill_surfaces` steps 8–10 exactly per § 17: (8) NORUNNER, rc 0, header-only map; (9) pytest layout ⇒ one row ending ` [scaffold]`, `^test_select: pytest {tests}` in CONVENTIONS, `surfaces` rc 0, rerun ⇒ `nothing to propose`, feat/* worktree ⇒ rc 1; (10) `ops/KEYS.tsv` copied from the kit, CONVENTIONS with only `voice:` + `test:` ⇒ doctor has `preferences never set here: adhd · claim`; after `interview --set adhd=off --set claim=local-lock` it does not
- [ ] hermetic: the drill's existing cleanup restores the header-only map and removes the CONVENTIONS keys it added — steps 8–10 leave nothing behind for step 11 or the next drill; assertions are rc + file state, never qa's rc
- [ ] `bash kit/ops/polaris doctor --selftest --only surfaces,qa,finish` green in the worktree (explicit 600000 ms timeout); the sharded `test:` green at the wave gate via `bg run test`; re-measure the surfaces drill's cost and record it in Notes for EVOLVE (CONVENTIONS' `test:` comment budgets ~44 s per drill)

## Notes
- Read fast.sh's `surfaces-select` section first — the `_SURFACES_CACHED=""` reset and the section-subshell override pattern are the house style; `PRIMARY` is overridden the same way for the engine.
- `interview_pending` reads `$OPS/KEYS.tsv` and `$CONV`; override both inside the section subshell with fixture files (a 5-column and a 4-column registry, a CONVENTIONS with live/stub/absent keys).
- Scratch paths under `scratchpad/T-144/…` only; never run `doctor --selftest` from the primary while T-145 builds — in your worktree via `bg run`; a subagent never ends its turn with a job running.
- Bash ≥ 3.2: no `case` inside `$(...)`, no mapfile, no assoc arrays. Trust rc and file state (T-089); never qa's rc inside a fixture (T-131).
