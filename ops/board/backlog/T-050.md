---
id: T-050
title: "Six drill assertions for `ask` + approve in drill_rules"
type: chore
scope: selftest
epic: ask-rule-kind
plan: autonomy-by-default
points: 3
value: 8
time_criticality: 5
risk_opportunity: 8
wsjf: 7.0
risk: normal
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/lib/selftest/policy.sh
context_files:
  - ops/contracts/ask-approval.md
  - kit/ops/lib/selftest/board.sh
  - kit/ops/lib/ownership.sh
depends_on:
  - T-047
  - T-048
  - T-049
contract: ops/contracts/ask-approval.md
verify:
  - bash -n kit/ops/lib/selftest/policy.sh
  - bash kit/ops/polaris doctor --selftest --only rules,drift
map_delta:
---
## Why
`ask` is a mechanism for lifting a denial. Every one of its containments is a claim that only a
machine can keep honest — especially "a `path` rule is NOT cleared by an approval" and "a Builder
cannot approve itself", which are the two that stop `ask` from becoming a legal way to dissolve any
rule that blocks you. Prose cannot hold those. These six assertions can.

`drill_rules` already has exactly the right shape: it appends a temp rule, exercises it, and cleans
up so the fixture is left byte-identical (T-046 hermeticity). Follow that shape exactly — a drill
that leaks a rule or a ready task makes a *different* drill go red one shard over, which is a whole
wasted fix wave.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] 1. an `ask` rule + no approval → `_guard <path> <ID>` denies (rc 1)
- [ ] 2. after `polaris approve <ID> <scope> -m "drill"` → the same `_guard` call passes (rc 0)
- [ ] 3. `_rules <path>` with no ID still denies, even with the approval on the task
- [ ] 4. `approve` from a `feat/<ID>` branch refuses AND the board ref is unmoved (compare
      `git rev-parse refs/heads/polaris/board` before/after, as the grant drill does)
- [ ] 5. a `path` rule is NOT cleared by an approval covering the same scope
- [ ] 6. a `ready/` task owning an unapproved `ask` scope → `drift --strict` exits 1 with the finding
- [ ] HERMETIC: on return, `ops/RULES.tsv` is pristine, no drill task file survives in any column,
      no `feat/` branch survives, `git status --porcelain` is empty
- [ ] `doctor --selftest --only rules,drift` green — and green again on a second run (proves the
      fixture really was restored)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Assertion 6 needs `drift` to see the seeded rule; `drill_drift` self-provisions its own `T-2`
  precisely because `drill_rules` removes its own. Keep both drills' fixtures independent.
- Assertion 4 needs a `feat/*` branch: `claim` the drill task and run `approve` from inside
  `.polaris/wt/<ID>` (the `finish` drill does the same thing for its worktree refusal).
- Spine state reaches these functions by bash dynamic scoping — NO `local` declarations in drill
  bodies. Existing drills use bare names (`gpre`, `fnpre`); follow that.
- bash 3.2: no `case` inside `$(...)`.
- `--only rules,drift` is a proven-safe pair; `rules` + `qa` WITHOUT `drift` reds falsely. Run it
  FOREGROUND with an explicit ≥600000ms timeout, or log-and-poll for `selftest passed`.
- re-groomed 2026-08-03 by the Planner (plan `autonomy-by-default`). Surface-frozen per
  key-registry.md § 5: all six assertions live INSIDE the existing drill_rules — no new top-level
  fn, never touch ops/tests/api-kit.expected (T-080 owns it in your wave).
