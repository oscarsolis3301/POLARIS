---
id: T-006
title: Staleness checks that actually fire — the zip warning and the version cache
type: bug
points: 2
value: 3
time_criticality: 3
risk_opportunity: 3
wsjf: 4.5
risk: normal
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/polaris
context_files:
  - ops/contracts/self-hosting.md
  - kit/ops/pack.py
depends_on: [T-005]
contract: ops/contracts/self-hosting.md
verify:
  - bash kit/ops/polaris doctor --selftest
  - bash kit/ops/polaris doctor
map_delta:
---
## Why this exists
Two staleness checks lie quietly:
1. doctor's stale-zip warning (kit/ops/polaris:532) gates on `$OPS/pack.py` — a pre-split path that no
   longer exists, so the warning README promises can never fire. The last shipped zip rotted exactly
   this way. Gate on the self-hosting tell (`kit/ops/pack.py`, per contract) and fix the rebuild hint
   (`python kit/ops/pack.py`).
2. Field report, 5.6.0, bug 2: explicit `version` (kit/ops/polaris:848) answers from the once-a-day
   update cache and told a user "up to date" while 3 releases behind on release day.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] Stale-zip check detects the kit source tree exactly per ops/contracts/self-hosting.md (`kit/ops/pack.py`); with a stale `polaris-v5.zip` present it warns, naming `python kit/ops/pack.py` as the rebuild.
- [ ] Explicit `version` and `update` always query the channel fresh (keep the 5s timeout; unreachable → fall back to cached value with a note, exactly as graceful as today). Only the passive end-of-command nag keeps the once-a-day throttle.
- [ ] Ordinary (non-self-hosting) repos see no new output from doctor.
- [ ] `doctor --selftest` stays green; Bash 3.2 compatible; no new dependencies (the existing `command -v unzip` guard pattern stays — Git Bash ships no unzip).

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- `kit_zip_version()` (kit/ops/polaris:859) already reads a zip via python zipfile — consider reusing it instead of the unzip dependency in the stale-zip block.
- Chained behind T-005 (same hotspot file). Kit/ops/polaris chain updated 2026-07-18: T-004 → T-013 → T-005 → T-006.
