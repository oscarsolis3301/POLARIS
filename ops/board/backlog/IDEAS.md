# IDEAS — one line each. The Planner grooms these; Builders append, never plan.

- The zip stopped shipping `README.md` after the `kit/` split — a manual `unzip` now gives you no orientation. Ship `kit/README.md`?
- `install.sh`'s fresh and live-board paths are ~40 lines apart and share a copy list. One drifted from the other once (the `ops/*.md` glob). Fold them — AFTER T-001 lands, so the drill guards the refactor.
- EVOLVE has never run here. After sprint 1, feed it the kickbacks and let it calibrate points.
- FIELD REPORT (external repo on 5.6.0, 2026-07-14) — items 1, 2, 5 became T-004, T-006, T-005; remaining, deferred to next sprint on capacity:
- (3) GAP nothing gates `map_delta` — Definition of Done demands it, `done` applies it, nothing enforces it; 4/5 tasks landed blank in one external sprint and MAP rotted. Handoff should refuse when the diff touches mapped files and map_delta is blank.
- (4) GAP `claim` builds a bare worktree (no deps installed) so every `verify:` fails until manual install; external Planner hand-wrote `pnpm install` boilerplate into all 5 tasks. Add `bootstrap:` key in CONVENTIONS.md, run by claim.
- (6) GAP `integration: paranoid` has no concept of a pre-existing flake; a 15% flake bounced good work and the Integrator improvised a wrong heuristic. Either a `flaky:` quarantine list or a rule: re-run failing file in isolation AND against base before any kickback.
- QA scout 2026-07-18: bad-arg errors from `${N:?usage...}` leak raw bash "line NNN" noise instead of the ⛔ format across ~10 subcommands (task-commit-msg/land/rollback/history --tasks inherited it from release/kickback/audit/run-verify/done/why) — worth one systemic usage-guard helper.
- QA scout 2026-07-18: `history --tasks 1 extra` silently ignores the stray arg while plain `history` rejects strays — align the branches.
- EVOLVE 2026-07-18: self-hosted repos RULES-guard ops/roles/ and ops/templates/, blocking EVOLVE's own legal targets (PLANNER.md §Pointing notes, TASK.md field guidance) — kit/ops/roles/EVOLVE.md needs a documented fallback home (CONVENTIONS § Planner calibration) for when those paths are guarded.
- Testbed 2026-07-20: bare `polaris audit` leaks raw `line 785: 1: usage` garble instead of a clean usage line — and kit CLAUDE.md's command table implies bare `audit` checks the review queue; align behavior + doc (fits the existing systemic usage-guard-helper idea above).
- Testbed 2026-07-20: pr-mode seal on a non-bitbucket origin prints title/instructions but no PR URL — verify once against a real bitbucket.org origin before trusting the URL path.
- Testbed 2026-07-20: quiet-board mode — kit PLANNER.md should state explicitly that the Planner's contract commit on base rides the NEXT wave's PR on protected mains (implicit today, surprises the first pr-mode Planner).
- EVOLVE 2026-07-20: paranoid per-land `build:` was classifier-blocked in subagent shells all sprint 4 (6 waves ran selftest-only per land; build proven only at qa) — give the kit a classifier-safe entrypoint, e.g. a `polaris build` passthrough running CONVENTIONS `build:`, or arm the installer's permission rule for `python kit/ops/pack.py`.
- EVOLVE 2026-07-20: update notice — raw.githubusercontent VERSION channel lags main by minutes post-release; version check answers stale "latest"; consider tag/release-asset as the channel of record (testbed, 5.15.0)
- EVOLVE 2026-07-20: publish: pr — express refusal fires only at land time, after build work is spent; claim/handoff should hint "express needs publish: direct" early (testbed, 5.15.0)
- Golden EOL pins for USER repos: the kit scaffolds ops/tests/ goldens (harness/init) into user repos with no eol=lf pins — the same Windows CRLF latent-red T-056 fixed in THIS repo applies there (no kit/ops/tests/ in-repo; the gap is in what install/harness write, incl. the .gitattributes the installer arms). Next sprint planning. (fix-wave planner, 2026-08-03)
- Sprint-8 planner 2026-08-03: `pack.py --dogfood` installs the PUBLISHED release only — no local/unpublished path exists, so mid-program dogfoods are impossible (plan I-8 dropped; both isolation+routing sprints run installed 5.23.0 mechanics per the installed-CLI-lags-source rule). If a `--local` install flag is ever wanted, it is its own small task with its own ownership, never a release-step side quest.
- Sprint-12 planner 2026-09-01: primary-index retry hardening (plans/v2.md WS5 #5) — `find --api` and `check` are primary-anchored and a read during a sibling's index refresh can see an empty or partial db; retry once before believing an empty answer.
- Sprint-12 planner 2026-09-01: a dedicated integrate worktree so the primary never leaves base (WS5 #7 + the G5 auto-park root cause) — an architecture change with its own contract; 6.2.0 only names the park that stashed your dirt.
- Sprint-12 planner 2026-09-01: a PowerShell-tool matcher for checkout-guard — the hook is wired on matcher `Bash` only; install's merge identity is per script path, so a second `PowerShell` entry + the hook reading the same `tool_input.command` closes the gap in ~10 lines.
- Sprint-12 planner 2026-09-01: per-repo keep-awake opt-out ONLY if a repo ever needs one — 6.2.0 deliberately has no CONVENTIONS key (machine-level `disabled` flag, `awake disable`); do not add a key on speculation.
- Sprint-12 planner 2026-09-01: `finish` run through `bg run finish` self-blocks — the finish guard sees its own runner job as a pending job; finish should exempt the job that is running it.
- Sprint-12 planner 2026-09-02: `handoff` accepts a branch that carries NO commits — `verify` counts UNTRACKED files in its ownership diff, so a builder whose whole task is a NEW file passes verify, hands off an empty `feat/<ID>`, and only discovers it at `land` ("brings no changes over integrate/<date>"). Cost the T-109 lane a full ship cycle. Fix: `handoff` refuses when `feat/<ID>` has no commits over the wave branch, or counts untracked owned files as dirty.
- Sprint-12 planner 2026-09-02: `scratchpad/` is not in `.gitignore`, so a lane told to use `scratchpad/<ID>/` for working files leaves the tree dirty and `handoff` refuses — the T-103 lane had to work outside the repo instead. Either ignore `scratchpad/` or stop pointing lanes at it (contracts and role prose both name it today).
- T-117 solo 2026-09-02: **`doctor --selftest` is RED in the 6.2 kit — this, not the `--no-permissions` bug, is what failed the release smoke and left 6.2.0 unpublished.** Two things that landed in the same release contradict each other: `kit/ops/lib/builder.sh:378` (worktree-liveness v1.2 Guard 2) makes a self-landing lane SKIP `done` on its OWN task and announce "stays in review/", while the `handover` drill at `kit/ops/lib/selftest/board.sh:260` still asserts `ops/board/done/T-HO1.md` exists → `HANDOVER SELFLAND FAIL (landing: self must carry the task to done/)`. Deterministic, platform-independent, reproduced locally on the built 6.2.1 artifact. Someone has to decide WHICH is right — either the drill relaxes to "review/ + a landed sha + the announce line", or the landing tail gains a safe own-task `done` (e.g. re-exec from the primary's script so removing the worktree cannot delete the running file). Out of T-117's scope: neither file is owned by it, and it is a design call, not a typo.
- 2026-09-08 (T-129, SOLO): `awake_ensure` fires from claim/status/doctor/bg-run inside the selftest drills with POLARIS_AWAKE_HOME set to a temp dir, but a WMI child does not inherit env, so each drill spawns a daemon that reads the REAL registry (a lock loser, ~1s) — hidden now, still wasteful. Forward POLARIS_AWAKE_* to the WMI child on its command line, or have the drills export POLARIS_AWAKE_SPAWN=inline.
- 2026-09-08 (T-130, SOLO): `next_promote` (kit/ops/lib/handover.sh) drops a candidate SILENTLY on three of its four gate checks — a named-but-missing contract, points in {8,13,''}, and any dep not in done/ all hit `continue` BEFORE the `NX_HELD` accounting, so the task appears in neither `NX_ELIGIBLE` nor `NX_HELD` and `next` just says `finish`. Only the overlap/ask check reports a reason. T-130 fixed the unset-contract predicate that made this bite; the silent-drop itself is a wider change (a held-reason string per check, plus the drill assertions for each) and was left alone. Whoever takes it: the "eligible/held" output is the only window a human has into why planned work is not moving.
- 2026-09-14 (T-136, builder): kit/ops/polaris dispatches `qa) cmd_qa "${1:-}"` — only the first flag reaches cmd_qa, so `qa --force --full` silently drops `--full`. cmd_qa parses all of "$@" since 6.4; the fix is forwarding "$@" in that one dispatch line (finish passes a single flag and is unaffected). Entry-owned, so left for a task that owns kit/ops/polaris.
- 2026-09-14 (T-136 + conductor, sprint 14): `grant` is the sanctioned amendment for a CLAIMED task's `files_owned`, guarded by an overlap refusal — but nothing equivalent exists for `verify:`. A cross-lane golden owner in a parallel wave can be handed a verify line that is structurally unsatisfiable: its branch is based on base, so a row the contract REQUIRES it to write can only ever read as a diff hunk until the sibling lands. Sprint 13 hit this twice (T-122, T-125); sprint 14 hit it again on T-136, costing a round trip to the conductor. Either add `polaris amend <ID> --verify` with a refusal shape like grant, or teach the Planner not to emit strict cross-lane golden diffs in `verify:` at carve time.
- 2026-09-14 (conductor, sprint 14): CONDUCTOR.md step 7 says integration promotes this-plan dependents and the loop continues, but it names NO command — and `polaris next` prints `next: promote`, which is not an implemented command (it is an unstarted ROADMAP line). The mechanical path that DOES exist is `polaris next --do` (`next_promote`, kit/ops/lib/handover.sh). Because step 7 never names it, this run spent an entire planner context moving three files between folders. Fix: name `next --do` in CONDUCTOR.md step 7, and either implement `promote` as an alias or stop `next` advertising it.
- 2026-09-14 (conductor, sprint 14): `next_promote` does not filter candidates on `plan:`, while PLANNER.md step 13 says the conductor drain promotes only tasks whose `plan:` matches the run. Harmless in sprint 14 (every task carried `plan: spend-less`) but a doc/code mismatch that will silently adopt foreign work the first time a board holds two plans at once — exactly what `drain: plan` exists to prevent.
- EVOLVE 2026-09-14: the Integrator's pen went silent when integration became a command — `ops/SPRINT.md` burndown tables for sprints 12, 13 and 14 are EMPTY and Learned has no bullet newer than sprint 11 W4 (T-089), while CONVENTIONS write-routing still names the Integrator as the only writer of both. Since 6.1.0 every landing runs through code (`landing: self`, `land --express`, `next`) that writes `docs(map)`, the report and the seal — never a burndown row or a Learned bullet — so EVOLVE's "repeated Learned themes" input is dry and the three kickbacks read "(no reason recorded)" in brain/learned.md. Kit decision for a human: either `seal`/`done` appends the burndown row (it already has the numbers) and `report` is declared the burndown of record, plus a ≤3-bullet Learned prompt at self-land/express `done`; or retire the two SPRINT.md sections explicitly and reroute the write-routing row. Not an EVOLVE amendment: it is kit code + role prose.
- 2026-09-14 (conductor, sprint 14, observed TWICE in one day): `qa` reds on `drift` CRUFT findings — leftover `feat/<ID>` branches for done tasks — that `sweep --fix` clears in under a second. Because the suite ran first and went red, no suite-stamp is written, so the following `finish` re-runs the whole 12-minute suite for a housekeeping nit that was already fixed. Both of today's runs paid the suite twice for exactly this. The lane leaves its own branch behind by design (worktree-liveness: it is running from inside it), so this is structural, not a lane bug. Fix candidates: have `qa` run the cruft sweep before `drift` rather than reporting it; or let `drift --strict` treat auto-clearable CRUFT as a warning that does not red the suite; or have the conductor/integrator recipe call `sweep --fix` before `qa`, not after. Cheapest is probably the recipe, but the first is the one that helps every caller.
- Sprint-B planner 2026-09-14: THIS repo's own SURFACES rows — Sprint B's tasks carry no `surface:` items on purpose: the installed `done` is 6.3.1 and drops them silently (T-137's writer lands here only at the 6.4.0 dogfood), and `surfaces --scaffold` answers NORUNNER for a bash kit whose runner takes drill LABELS, not paths. First task of the NEXT sprint (1–2 pts): map the kit's modules to their drills with complete `cmd:` items, e.g. `kit/ops/lib/integrate.sh tests: kit/ops/lib/selftest/history.sh cmd: bash kit/ops/polaris doctor --selftest --only express,tcm,busyint,selfland,wtreap`, and set `test_select: bash kit/ops/polaris doctor --selftest --only {tests}` in CONVENTIONS — the rows land via 6.4.0's `done`.
- Sprint-B planner 2026-09-14: `update`'s explicit epilogue was going to nudge `surfaces --scaffold` when the map is empty, but `cmd_update` re-execs from a tmp copy of the PRE-update lib, so the line could only appear on the update AFTER the one that ships it — dropped rather than shipped inert; doctor + PLANNER 5b + the install skill carry activation (test-surfaces.md v2 § 15). Revisit if the re-exec ever loads the NEW lib.
- Sprint-B planner 2026-09-14: `dash` cannot follow you across machines by design (foreground loopback, no pidfile); B4 ships the READ side (`board_pull`, T-148) so `status`/`next`/`board-fm` under claim-branch see origin's board. A dash that refreshes from origin per request is a small follow-up in dashboard.py (human-maintained code, CONVENTIONS write routing).
- Sprint-B planner 2026-09-14: run bounds — the conductor quoted `run_max_tasks 12 / run_max_minutes 90` (the KEYS.tsv defaults); CONVENTIONS here says 24 / 360 ("raised for 6.2.0, revisit downward"). Sprint B fits the effective bounds in one run (9 tasks, ~2.5 h with the release ritual); under 12/90 it is two runs cut after W2. EVOLVE: settle which the owner wants after this sprint.
- T-145 builder 2026-09-14: PLANNER.md step 5b (pinned by test-surfaces v2 § 19) restates the runner list `(pytest · jest · vitest · go)` that `surfaces.sh` owns — the fourth copy after the usage entry, the NORUNNER line and the contract; next touch, drop the list and point at `surfaces --scaffold` (a list in prose rots, the command cannot)
- T-145 builder 2026-09-14: `ops/KEYS.tsv` keeps `claim` ABOVE `voice`, so `polaris interview` prints claim first and `--pending` reads `claim · adhd` when only voice is live — first-run.md § 1 says the order is voice · adhd · claim and T-144 step 10 greps `adhd · claim`; a data fix (move the claim row below adhd, mind `keys-drift`) or a string fix, decided by a human — prose cannot reach a drill already carved
