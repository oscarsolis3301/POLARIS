---
id: T-060
title: "finish/status/doctor/update learn the shared checkout — lease and parks surfaced, update parks instead of dying"
type: feature
scope: observe
epic: n-chats-one-repo
plan: n-chats-one-repo
points: 3
value: 8
time_criticality: 5
risk_opportunity: 3
wsjf: 5.3
risk: normal
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/lib/observe.sh
  - kit/ops/lib/admin.sh
  - kit/ops/lib/selftest/policy.sh
context_files:
  - ops/contracts/shared-checkout.md
  - kit/ops/lib/workspace.sh
depends_on:
  - T-057
contract: ops/contracts/shared-checkout.md
verify:
  - bash -n kit/ops/lib/observe.sh
  - bash -n kit/ops/lib/admin.sh
  - bash -n kit/ops/lib/selftest/policy.sh
  - bash kit/ops/polaris doctor --selftest --only finish,hardening
map_delta:
---
## Why
A second chat's first question is "what is going on here?" — and today status/finish cannot answer
it: they know nothing about the integration lease or parked stashes, `finish`'s dirty-tree pending
line names no remedy, and `update` still dies on a dirty configured repo. Per shared-checkout v1:
`cmd_status` gains one line for the lease when held (holder · age) and one per `polaris/park-*`
stash, so the first read explains the world. `cmd_finish` gains a pending line when the lease is
held by another live session, names `park` as the dirty-tree remedy, and lists parked stashes as
caveats (never gates). `cmd_doctor` validates `integration_wait_minutes` /
`integration_stale_minutes` when set (positive integers, one ⚠ line otherwise) and warns once when
git < 2.13 (`stash push`). `cmd_update`'s configured-repo dirty die (admin.sh:359) becomes park +
caveat + proceed (park failure → today's die; the never-configured branch :350-358 is untouched).
finish/hardening drill assertions (selftest/policy.sh) update where output changed.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] status shows lease holder + age when held, and each parked stash; silent when neither exists
      (byte-identical to today on a quiet repo)
- [ ] finish: lease held by another session → ⛔ pending naming holder + age; dirty tree pending
      line names `park`; parked stashes → caveat lines, never gates
- [ ] doctor: bad knob values → one ⚠ line each; unset knobs → silent; git < 2.13 → one warn
- [ ] update on a dirty CONFIGURED repo parks + proceeds + caveat; park failure → today's die;
      never-configured branch byte-identical
- [ ] finish + hardening drills green with updated assertions
- [ ] no new top-level function in observe.sh/admin.sh — inline the glue (the api-kit surface
      golden is frozen this wave)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- Relevant seams: cmd_status (observe.sh:64) · cmd_finish (observe.sh:1325, pending/caveat helpers
  inside) · cmd_doctor (observe.sh:188) · cmd_update dirty gate (admin.sh:349-360).
- The installed `ops/polaris` lags your kit edits all sprint: prove behavior with
  `bash kit/ops/polaris …`.
- bash 3.2: no `case` inside `$(...)`; split `local` declarations.
- Never run `doctor --selftest` while your kit edits are half-written — a mid-edit suite fabricates
  a different spurious failure each run.
- Long suites FOREGROUND with an explicit ≥600000ms tool timeout (test_fast ≈320s); past the cap →
  log to a file and poll for `selftest passed`. Never end a turn waiting on a notification.
