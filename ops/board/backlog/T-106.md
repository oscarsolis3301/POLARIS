---
id: T-106
title: "Golden awake-hook — every hook subcommand's --test line, a stub-presser tick, the disabled flag, silence on the live path"
type: chore
scope: tests
epic: cant-eat-itself
plan: cant-eat-itself
points: 1
value: 2
time_criticality: 2
risk_opportunity: 1
wsjf: 5.0
risk: normal
status: backlog
owner: null
branch: null
files_owned:
  - ops/tests/awake-hook.cmd
  - ops/tests/awake-hook.expected
approved:
context_files:
  - ops/contracts/keep-awake.md
  - ops/tests/checkout-guard-denies.cmd
  - kit/ops/hooks/awake-hook.sh
depends_on:
  - T-102
contract: ops/contracts/keep-awake.md
verify:
  - test -f ops/tests/awake-hook.cmd
  - test -f ops/tests/awake-hook.expected
  - grep -q 'POLARIS_AWAKE_HOME' ops/tests/awake-hook.cmd
  - grep -q 'POLARIS_AWAKE_PRESSER' ops/tests/awake-hook.cmd
  - grep -q '^start: ' ops/tests/awake-hook.expected
  - grep -q '(kept)' ops/tests/awake-hook.expected
  - grep -q '^busy: ' ops/tests/awake-hook.expected
  - grep -q '^idle: ' ops/tests/awake-hook.expected
  - grep -q '^end: .* removed' ops/tests/awake-hook.expected
  - grep -q '^tick: active pressed' ops/tests/awake-hook.expected
  - grep -q '^tick: active disabled' ops/tests/awake-hook.expected
  - grep -q '^tick: quiet no-press' ops/tests/awake-hook.expected
  - grep -q 'live-stdout-bytes: 0' ops/tests/awake-hook.expected
  - test "$(grep -c '[0-9]\.[0-9]\.[0-9]' ops/tests/awake-hook.expected)" = 0
  - bash -c "$(cat ops/tests/awake-hook.cmd)" | diff - ops/tests/awake-hook.expected
map_delta:
---
## Why
The keep-awake hook's ONE hard contract is silence — a byte on stdout from UserPromptSubmit lands
in the model's context, an rc 2 from Stop tells the harness to keep going — and the `--test` words
are what the drill and the humans read. This 1-point golden pins both, cheaply, on every `check`:
each of `start busy idle end` via `--test` (with `start` run twice to prove `(kept)` never
downgrades busy, and a Windows-escaped `transcript_path` in the JSON), `--test tick` with the stub
presser (`tick: active pressed`), with the `disabled` flag (`tick: active disabled`), and with every
session idle (`tick: quiet no-press`); then the LIVE subcommands once each with stdout byte-counted
(`live-stdout-bytes: 0`) and rc printed. Registry paths normalized to `<home>`. Hermetic under a
mktemp `POLARIS_AWAKE_HOME`; never touches the owner's registry. Spec: keep-awake.md § golden.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] the `.cmd` sets `POLARIS_AWAKE_HOME=<mktemp>`, `POLARIS_AWAKE_PRESSER="touch $H/pressed"`, `POLARIS_AWAKE_SPAWN=inline`, `POLARIS_AWAKE_TICK=1`; never spawns a daemon (only `--test tick`, which does one pass); cleans up with `trap … EXIT`
- [ ] the pinned lines for each case exactly as keep-awake.md § `--test` specifies; `live-stdout-bytes: 0` and `rc 0` for the four live subcommands; paths normalized to `<home>`
- [ ] runtime < 3 s; byte-identical on a second run; no kit version number in the expected file
- [ ] proven from the worktree with the `.cmd`-body diff; sabotaged red once (a stray `echo` in `ah_hook_idle` in a throwaway copy) before trusting green

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- api-kit: SURFACE-FROZEN — `ops/tests/` adds no row; no kit file is yours. T-104 owns `ops/tests/api-kit.expected` this wave.
- LIVE RACE (installed CLI is 6.1.0 until dogfood): after `bg wait ship-T-106` returns 0, `cd "<PRIMARY>"` and keep nothing uncommitted in the worktree; never `git worktree remove`, `git clean`, `rm -rf .polaris`, or kill by name.
- `polaris check` is PRIMARY-anchored — the `.cmd`-body form is your proof; new goldens are auto-LF-pinned. Never point `POLARIS_AWAKE_HOME` at your real `~/.claude/polaris/awake`.
- Scratch dirs per-lane: scratchpad/T-106/. Windows: `python`, never `python3`.
