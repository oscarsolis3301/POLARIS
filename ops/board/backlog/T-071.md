---
id: T-071
title: "route + bg drills and the two hermetic goldens (route-tier, bg-lifecycle)"
type: test
scope: selftest
epic: routing-and-bg
plan: routing-and-bg
points: 3
value: 5
time_criticality: 3
risk_opportunity: 5
wsjf: 4.3
risk: normal
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/lib/selftest/spine.sh
  - kit/ops/lib/selftest/policy.sh
  - ops/tests/route-tier.cmd
  - ops/tests/route-tier.expected
  - ops/tests/bg-lifecycle.cmd
  - ops/tests/bg-lifecycle.expected
  - ops/tests/api-kit.expected
context_files:
  - ops/contracts/bg-jobs.md
  - ops/contracts/model-routing.md
  - ops/tests/triage-lane.cmd
  - ops/board/done/T-062.md
depends_on:
  - T-065
  - T-070
contract: ops/contracts/bg-jobs.md
verify:
  - bash -n kit/ops/lib/selftest/spine.sh
  - bash -n kit/ops/lib/selftest/policy.sh
  - bash ops/tests/route-tier.cmd | diff - ops/tests/route-tier.expected
  - bash ops/tests/bg-lifecycle.cmd | diff - ops/tests/bg-lifecycle.expected
  - grep -q drill_route ops/tests/api-kit.expected
  - grep -q drill_bg ops/tests/api-kit.expected
  - bash kit/ops/polaris doctor --selftest --only route,bg
map_delta:
---
## Why
Routing and bg landed in waves 1-2; this task is the proof that keeps them true forever, in both
test tiers. Two labeled DRILLS (deep, throwaway-repo): `route` asserts the tier_for table via flag
forms, the `model:` note appearing exactly when a fixture CONVENTIONS sets a knob, the task-level
`model:` override (tier word AND literal), and `fleet --dry-run` carrying `--model` for the ready
queue's max tier; `bg` asserts the full lifecycle — green run, red run (honest rc 1),
duplicate-RUNNING refusal, `--force` replace, tail, `.prev` rotation, `sweep --fix` rotating a
>24h job (backdate `start`), the `finish` pending line on an rc-less job, and the status
rc contract 0/1/2/3 including the dead-pid unknown. Labels register on spine.sh's one-line
SELFTEST_LABELS literal (:46); both drill fns live in policy.sh (one fn per label, no stray
`local` — dynamic scoping reaches spine state). Two GOLDENS (cheap, every `check`/`qa` run):
`route-tier` and `bg-lifecycle`, HERMETIC BY CONSTRUCTION per the T-062 pattern — each .cmd builds
its own throwaway fixture repo and runs `bash kit/ops/polaris` from INSIDE it (the CLI anchors to
the worktree-list primary, which becomes the fixture), so live-board writes and this repo's future
knob edits can never red them; bg-lifecycle uses fast commands only (`true`/`false`/`echo`, small
`--max`), never a real suite. Running either .cmd twice from any board state is byte-identical.

This task owns the wave-3 api-kit delta: exactly the `drill_route` + `drill_bg` fn lines,
hand-authored byte-exact.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] both labels in SELFTEST_LABELS (one line, literal); an unknown label still dies before the
      spine; `--only route,bg` green; full serial selftest stays green (proven at the wave gate)
- [ ] drill_route + drill_bg in policy.sh assert every bullet above and leave the throwaway repo
      assertions-clean (T-046 hermeticity — no stray rules, tasks, refs, job dirs)
- [ ] route-tier.cmd: fixture repo with KNOWN knobs; asserts line-1 vocabulary, the three-space
      `   model:` note shape, override precedence, and the fleet token — byte-identical on re-run
- [ ] bg-lifecycle.cmd: fixture repo; green/red/duplicate/tail/rotation asserted through the
      PUBLIC rc contract (0/1/2/3), total runtime <30s
- [ ] api-kit.expected gains exactly the two drill fn lines, byte-exact, sorted position
- [ ] goldens are LF (auto-pinned by .gitattributes since T-056) and CR-safe: no CR-sensitive
      asserts via grep/sed — awk/od only

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- The installed `ops/polaris` is 5.23.0: route/bg exist only in kit — every proof runs
  `bash kit/ops/polaris …`; inside your fixture repos, copy/point at THIS worktree's kit.
- Hermetic-fixture recipe: mirror ops/tests/triage-lane.cmd (T-062's re-founding) — init a
  throwaway git repo, minimal fixture files, run the CLI from inside it; polaris anchors to the
  fixture's primary, so live state cannot leak in.
- Scoped drill timing: spine 144s + ~44s/drill → `--only route,bg` ≈ 230s. FOREGROUND with an
  explicit ≥600000ms tool timeout; never end a turn with a run in flight (the doctrine your bg
  drill itself proves). Never run the suite while your kit edits are half-written.
- api-kit golden, worktree-local proof: `POLARIS_ROOT=<your worktree> python ops/index.py`, emit
  the `--api 'kit/*'` shape, byte-exact diff (T-062 Notes). `check` is PRIMARY-anchored — the wave
  gate's uat is the referee; your verify diffs the .cmd output directly instead.
- Drill fns add NO `local` declarations that shadow spine state (module-layout invariant);
  bash 3.2 throughout.
- sweep's >24h case: backdate the fixture job's `start` file, never sleep.
