---
id: T-149
title: "Skills POLARIS writes for itself — the writer, the budget and the eviction (RE-CARVE after the T-146 spike; never claimable as is)"
type: feature
scope: skills
plan: spend-less
points: 13
value: 8
time_criticality: 3
risk_opportunity: 5
wsjf: 1.2
risk: normal
model:
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/lib/knowledge.sh
  - kit/ops/roles/EVOLVE.md
  - kit/ops/templates/SKILL.md
approved:
context_files:
  - plans/self-skills.md
  - plans/v3.md
  - kit/ops/lib/admin.sh
depends_on: [T-146]
contract:
verify:
  - test -f ops/contracts/self-skills.md
map_delta:
---
## Why
plans/v3.md § B3: a surface the agents rediscover every session deserves a skill, committed in the
repo's `.claude/skills/<name>/SKILL.md`, so it travels with the repo. The cost side is the whole
design problem — every skill's frontmatter rides in every session's system prompt — and no
mechanism for writing, budgeting or evicting such skills exists anywhere in the kit. T-146 is the
design pass; this placeholder holds the sprint's intent on the board and nothing more. It is pointed
13 and carries no contract ON PURPOSE: the ready gate holds it (points ∉ {8, 13} and a contract
that must exist), and `next --do` will report it held — that is correct. When T-146 lands, the
Planner lifts `plans/self-skills.md`'s proposed contract into `ops/contracts/self-skills.md`, splits
this into the ≤ 5-point leaves the spike proposes with disjoint `files_owned`, and retires this file.
The `files_owned` above are provisional guesses to keep the frontmatter parseable — the spike decides
the real homes.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] re-carved by the Planner from T-146's `## Proposed carve` into leaves ≤ 5 points, each with a contract, disjoint ownership and executable verify lines
- [ ] `ops/contracts/self-skills.md` exists before any leaf enters `ready/`
- [ ] this placeholder is moved to `done/` by the Planner's re-carve commit (`chore(board): plan …`) or deleted from `backlog/` in the same commit — never claimed, never built

## Notes
- `next_promote` drops a 13-point or contract-less candidate SILENTLY (IDEAS.md, T-130) — an ID missing from both `promoted:` and `held:` here is expected, not a bug.
