---
id: T-048
title: "`polaris approve <ID> <scope> -m \"why\"` — the sibling of grant"
type: feature
scope: approve
epic: ask-rule-kind
plan: ask-rule-kind
points: 5
value: 13
time_criticality: 8
risk_opportunity: 8
wsjf: 5.8
risk: high
status: backlog
owner: null
branch: null
files_owned:
  - kit/ops/lib/builder.sh
  - kit/ops/polaris
context_files:
  - ops/contracts/ask-approval.md
  - ops/contracts/grant.md
  - kit/ops/lib/selftest/board.sh
depends_on:
  - T-047
contract: ops/contracts/ask-approval.md
verify:
  - bash -n kit/ops/lib/builder.sh
  - bash kit/ops/polaris help | grep -q 'approve <ID> <scope>'
  - bash ops/polaris check --only cli-help-parity
  - bash ops/polaris check --only triage-lane
  - bash kit/ops/polaris doctor --selftest --only grant
map_delta: "polaris gains `approve <ID> <scope> -m \"why\"` — records a human's yes to an `ask` rule on one task (ops/contracts/ask-approval.md)"
---
## Why
An `ask` rule is only half a mechanism without a sanctioned way to record the human's yes. This is
that way, and it is deliberately the twin of `grant`: same shape, same refusal discipline, same
single board commit. **They stay distinct commands** — `grant` widens ownership, `approve` clears a
policy gate, and only `approve` needs a human.

The generalization matters as much as the command: `grant_append_owned` becomes a field-name-taking
append-only front-matter writer that both callers share, rather than a second near-copy of 25 lines
of awk drifting away from the first.

The `feat/*` refusal is the load-bearing containment. An approval mechanism is exactly what a stuck
agent rationalizes its way into, so a Builder approving itself must be *mechanically impossible*, not
merely discouraged. Read `ops/contracts/ask-approval.md` § 3.

## Acceptance criteria (binary — each becomes a test; put the runnable ones in verify:)
- [ ] `grant_append_owned` generalized to take a field name; `grant` calls it with `files_owned` and
      behaves exactly as before (the `grant` drill is unchanged and green)
- [ ] the generalized writer refuses (rc non-zero, file untouched) when the named field is absent —
      it never invents a field
- [ ] `cmd_approve` exists in `kit/ops/lib/builder.sh`, modelled on `cmd_grant`: same option parsing,
      same `-m` requirement, same `mutex_on → append → evt → board_commit → sync_board` sequence
- [ ] refuses, board untouched, when `<ID>` is not on the board
- [ ] refuses, board untouched, when `-m "why"` is missing or empty
- [ ] refuses and SAYS SO when `<scope>` matches no `ask`-kind rule (approving something ungated is a
      no-op, never a silent write) — uses T-047's helper, does not re-implement the scan
- [ ] refuses when `git rev-parse --abbrev-ref HEAD` is `feat/*`
- [ ] every refusal mutates nothing: no partial write, no board commit, no event, clean tree
- [ ] success appends `<scope> — <who>, <date>: <why>` to the task's `approved:` list, one
      `- approve: <scope> — <why>` Notes line, one `approve` event, and ONE board commit with subject
      `chore(board): approve <ID> <scope>`
- [ ] `approve` dispatched in `kit/ops/polaris` and documented in `help` in the `grant` house style
- [ ] `bash ops/polaris check --only cli-help-parity` and `--only triage-lane` stay green (the latter
      greps this file's help text for `1 task ≤3pts`)

## Notes
<!-- Builders append one-line discoveries here; Integrator pastes failures here (path:line only). -->
- `kit/ops/polaris` is the entry point and a hotspot; only this task touches it this sprint. Keep the
  edit to the dispatch line + the usage block — the `help` fast path must stay above the module
  loader (`startup-budget` golden asserts it).
- `ops/tests/cli-help.expected` is generated from the INSTALLED `ops/polaris`, so it will not red
  during this sprint. It must be regenerated at the release dogfood; that is the run's close, not
  your job.
- bash 3.2: no `case` inside `$(...)`.
- Long suites FOREGROUND with an explicit ≥600000ms timeout, or log-and-poll for `selftest passed`.
- `risk: high` — this command is the escape hatch. The Integrator needs explicit human approval in
  the conversation before merging.
- re-parked 2026-08-03 by the Planner per the approved isolation plan (`n-chats-one-repo`): sprint 8
  reworks `kit/ops/lib/builder.sh` and `kit/ops/polaris`, which this task owns. Re-groom after the
  5.24.0 release — 5.24.0 is now the COMBINED two-sprint release at the program's end; sprint 7's
  "5.24.0 at the close" target is superseded, and this task rides a later release.
- Integrator advisory (sprint 7 close): when re-groomed, this task MUST also own
  `ops/tests/api-kit.expected` — `cmd_approve` + the generalized field writer are new top-level fns
  and that golden records every one (T-047's kickback lesson: the surface-changer owns the recorder).
- Integrator advisory (sprint 7 close): close T-047's recorded gap here — `check_rules` at its
  builder.sh call sites (`cmd_verify`, `cmd_handoff`) still runs ID-less, so an approved `ask` scope
  dies at a Builder's verify/handoff; thread the task ID exactly as `cmd_audit` already does.
- Integrator (sprint 9 W1 close): hold CONFIRMED for a second, independent reason — sprint 9's T-070
  owns `kit/ops/polaris`, which this task also owns, so promoting on satisfied deps alone would have
  broken W2's disjointness. Deps (T-047) are green; ownership is what parks it.
