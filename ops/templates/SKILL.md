---
name: <name>
description: TODO(<glob>) — ≤ 290 chars: TRIGGER when …; DO NOT TRIGGER when …
disable-model-invocation: true
metadata:
  polaris: { paths: [<glob>], since: <YYYY-MM-DD>, tier: 0, evidence: "<n>/<2W> tasks · <k> kickbacks" }
---

# <glob> — what POLARIS already knows
<!-- producer: the <glob> `skill propose` was given; every section below is generated from board + brain + index, and a section with no brain behind it reads `(no brain — run: ops/polaris brain)` -->

## What it is
<!-- producer: the code-map.md entry for the dir(s) + the SURFACES row's note -->

## Public surface
<!-- producer: find --api <glob>, ≤ 25 lines -->

## What keeps going wrong
<!-- producer: WHOLE Learned/gotchas bullets + kickback notes naming these paths, ≤ 40 lines -->

## Files that move together
<!-- producer: learned.md co-change pairs touching these paths -->

## Tests that cover it
<!-- producer: SURFACES rows + the verify: lines of the last 3 done tasks here -->

## Last worked
<!-- producer: the last 5 done task IDs + titles -->
