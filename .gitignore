polaris-v5.zip
__pycache__/
*.pyc
# The update check writes .polaris/update-cache on any `doctor`/`status`. Untracked, that
# made pack.py refuse the tree as dirty — the kit could not repack itself after a doctor run.
.polaris/
