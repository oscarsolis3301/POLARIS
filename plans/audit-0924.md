# Audit digest — 2026-09-24 (POLARIS × Opus 5.5)

One line per finding: **id** · VERDICT · title — corrected impact — fix (files). Skeptic-corrected numbers
win over the finder's. `refuted` rows are kept only so nobody re-proposes them. Source: the read-only
audit workflow (7 finders, 7 skeptics); plan: `plans/v4.md`. Numbers were measured on this machine
while sibling audit agents ran, so absolute timings are inflated — ratios and process counts hold.

## hooks

- **hooks-1** · CONFIRMED · ownership-guard regressed from 976 ms to ~3–4.9 s per Edit/Write: one content rule for one file sends every write through python — ≈1.4–1.8 s saved per Edit/Write in this repo (up to ~3 s under heavy load). session-H's 55 writes ≈ 80–100 s of total wait. Repos with `*`-scoped content rules get nothing from this fix. To help them, pull new_string/content out with the existing bash jstr and scan it with `grep -E`, falling back to… — fix: In kit/ops/hooks/ownership-guard.sh: 1. Parse file_path/cwd with the existing pure-bash `jstr` first. 2. Compute REL. 3. Set NEED_BODY=1 only when a content rule's scope matches REL, using `. $TOP/ops/lib/ownership.sh; match_one "$REL" "$scope"` inside a builtin `while IFS=$'\t' read` loop over RUL… (kit/ops/hooks/ownership-guard.sh, ops/RULES.tsv)
- **hooks-2** · CONFIRMED · Make the ownership-guard common path cheap: one git call and an in-hook RULES prefilter, with `polaris _guard` only when a rule could fire — About 2.3–2.8 s saved per non-Builder write, measured with my candidate; ~0.4–0.6 s is plausible with the git calls merged. This number already includes hooks-1's saving, so do not add the two. Once it lands, the wait on an Edit becomes model-guard's 0.4–2.2 s, which runs in parallel, unless hooks-… — fix: kit/ops/hooks/ownership-guard.sh: 1. Merge the three git calls into one `rev-parse --show-toplevel --git-common-dir --abbrev-ref HEAD`. 2. Source `$TOP/ops/lib/ownership.sh` (function definitions only, zero forks) and prefilter with the same `match_one` the CLI uses. 3. Exit 0 right away when ID='-… (kit/ops/hooks/ownership-guard.sh, kit/ops/polaris, kit/ops/lib/ownership.sh)
- **hooks-3** · OVERSTATED · model-guard (matcher "*", every tool call on the machine) spends 0.7–1.4 s in a pure-bash scan of 64 KB; reading 4 KB first gives the same answer 3x faster — Grep-only saves ≈0.2–1.9 s per non-edit call, depending on the transcript. For session-H that is ≈0.35 s × 812 ≈ 5 min of total hook time; since subagents run in parallel, wall-clock time drops by much less than the 5–15 min claimed. More important, it keeps model-guard under its 5 s limit at fan-ou… — fix: Edit kit/ops/hooks/model-guard.sh (the installer re-copies it to ~/.claude/polaris). 1. `TAIL=$(tail -c 4096 "$TP")`. If it contains `"model":"`, extract the last occurrence with the existing loop, which is only 1–4 iterations on 4 KB. 2. Otherwise fall back to `tail -c 65536 "$TP" | grep -o '"mode… (kit/ops/hooks/model-guard.sh, ~/.claude/polaris/model-guard.sh)
- **hooks-4** · CONFIRMED · model-guard cannot see a subagent's model: a Haiku subagent (built-in claude-code-guide) passes — Closes a latent hole in the absolute ban (0 cases observed since the guard shipped). A side benefit: a subagent's own transcript is small when it starts, so reading it instead of the parent's 64 KB tail also removes the fan-out cost behind today's 6 timeouts. — fix: In kit/ops/hooks/model-guard.sh: 1. `jstr agent_id`. When present, point TP at `${TP%.jsonl}/subagents/agent-$AID.jsonl`, or at the first match of the glob `${TP%.jsonl}/subagents/workflows/*/agent-$AID.jsonl`. If neither exists, fail open as today. 2. When tool_name is Agent or Task, deny with no … (kit/ops/hooks/model-guard.sh, ~/.claude/polaris/model-guard.sh)
- **hooks-5** · OVERSTATED · The compaction re-anchor always times out: `polaris next --brief` takes 18–28 s against the anchor hook's 10 s limit, because next_route forks basename for eac… — Fixing it cuts each `polaris next` in the wait or finish state from ~15 s (up to ~28 s under load) to about 1 s. That covers the Stop hook's call and agent-run calls: 36 in transcripts ever, ~5–9 per sprint day, so roughly 1–2 min per run. It is not a demonstrated cause of the 'Opus missing somethi… — fix: In kit/ops/lib/handover.sh:237: 1. Test `case "$f" in *.prev/) continue;; esac` first. 2. Then `n="${f%/}"; n="${n##*/}"`, with no fork. 3. Also replace the `$(cat pid|tr)` / `$(cat cwd|tr|tr)` pipelines with `read -r` builtins. In the same file, `basename` is forked per task at :22, :62, :97, :233… (kit/ops/lib/handover.sh, kit/ops/hooks/handover-hook.sh, kit/ops/lib/bg.sh)
- **hooks-6** · OVERSTATED · checkout-guard misses newline-separated commands and denies harmless text quoted after a `;` — 0 real bypasses observed in POLARIS repos. The false-positive fix (quote-state scanning) is the more useful half. The newline fix needs heredoc skipping and awareness of a `cd` inside the command, or it trades a theoretical miss for real false denials. — fix: In mutating_git and mutating_other (kit/ops/hooks/checkout-guard.sh): 1. Replace the whitespace `set -- $cmd` split with a single-pass character scanner that tracks quote state across tokens. readonly-allow.sh's `scan` already has one (its `st` state machine at ~:380-410); reuse it. 2. Treat an unq… (kit/ops/hooks/checkout-guard.sh, ops/tests/checkout-guard-denies.cmd, ops/tests/checkout-guard-denies.expected)
- **hooks-7** · CONFIRMED · The PowerShell tool bypasses checkout-guard completely; its parser already understands most PowerShell forms — No incidents so far. It protects the shared-checkout guarantee if Opus starts using PowerShell more often. Cost: one ~0.1–0.2 s bash start per PowerShell call, run in parallel with model-guard. — fix: 1. In kit/.claude/settings.json, change the checkout-guard entry's matcher to "Bash|PowerShell". Leave readonly-allow on Bash. 2. In checkout-guard's mutating_git, treat `{` as a command-position reset like `;`, and treat `if`/`elseif`/`foreach` + `(…)` as non-command tokens, so `A; if ($?) { git s… (kit/.claude/settings.json, kit/ops/hooks/checkout-guard.sh, kit/ops/install.sh)
- **hooks-8** · OVERSTATED · update-hook starts the full CLI twice at every session start before it checks the once-a-day cache — ≈0.9 s per session start here, and an estimated ~2–3 s in installed repos at normal load (two startups plus a 1.1 MB copy). The pure-bash cache pre-check stays worthwhile. The fix should also remove the leaked temp copy (~90 MB so far). — fix: Add a pure-bash pre-check to kit/ops/hooks/update-hook.sh before it launches polaris: 1. Exit 0 if `$p/kit/ops/pack.py` exists (self-hosting). 2. Read `$p/.polaris/update-cache` with `read` builtins. If `checked:` is today and `latest:` ≤ the `version:` in `$p/ops/VERSION`, exit 0. A pure-bash semv… (kit/ops/hooks/update-hook.sh, kit/ops/lib/admin.sh)
- **hooks-9** · OVERSTATED · Guards fail open without any trace: the silent guards are never logged, and recorded timeouts show they do hit their limits — The bench is the part that catches slowdowns like hooks-1. The hook_cancelled scan catches fail-opens, which today means the model-guard ban going unenforced at fan-out. — fix: Add a read-only `polaris doctor` or `metrics` line that scans `<transcripts> repo>/**/*.jsonl` for `hook_cancelled` or `timedOut:true` records whose command runs `ops/hooks/`, and prints count, worst ms and date. Add a zero-token bench to ops/bench.sh that feeds each guard the canonical … (kit/ops/lib/observe.sh, kit/ops/bench.sh)

Skeptic — missed in hooks:
- **model-guard fails open at subagent fan-out.** It hit its 5 s timeout 6 times today: 5 at 17:39:03–04Z as this workflow's subagents started, and 1 at 18:23Z in a another install subagent (hook_cancelled, timedOut:true). So the absolute ban on Fable and Haiku is not enforced exactly when many agents start at once, and each affected call waits 5 s. I reproduced it: I re…
- **The 4 KB-first hybrid proposed in hooks-3 is slower in a common case.** When the last transcript line is a large tool_use whose model field sits more than 4 KB before the line's end (a Workflow launch or a big Write), it takes 558–716 ms against the current 325–344 ms. A plain `tail -c 65536 | grep -ao '"model":"[^"]*' | tail -n 1` costs 386–518 ms on all…
- **`update --auto` never deletes its re-exec temp copy** (kit/ops/lib/admin.sh:413-419: mktemp -d, cp polaris, cp -R lib, exec). The session-start update hook therefore leaves ~1.1 MB in %TEMP% each time. 82 copies exist now, 3–11 per day; their md5 matches the installed 6.5.0 CLI in another install.
- **The ownership-guard fast path is itself about twice the recorded 976 ms.** Even with no content rule it takes 1.86–2.19 s at moderate load. `polaris _guard` alone takes 1.0–1.16 s, against the 537 ms in the hook's own header trace. The auditor blamed the whole slowdown on the content rule.
- **another project's app runs Haiku despite the ban.** 111 of 136 of that project's transcripts are Haiku sessions (claude-haiku-4-5 via sdk-cli), the latest today at 13:12Z. They are text-only turns, so model-guard, which fires only on tool calls, can never see them. The owner has to decide whether 'every project' includes this app.
- **awake-hook's cost per prompt is unmeasured.** It runs machine-wide on every UserPromptSubmit (10 s timeout, blocking the prompt) and on Stop (5 s; one 5,456 ms timeout recorded 2026-09-15). Its busy path forks cygpath to resolve PowerShell and may spawn the daemon. Neither the auditor nor I timed it, because running awake-hook.sh is forbidden in this audi…
- **The summary table's per-session totals are not wall-clock time.** They add up hook time across subagents that ran in parallel (e.g. 1,253 s for session-H with 18 subagents), and they use standalone timings taken under heavy load. Real wall-clock delay is the hook wait along each agent's own sequence of calls, which is much smaller.

## close

- **close-1** · OVERSTATED · qa throws away a GREEN suite whenever drift reports a housekeeping note (MAP/LEARNED overflow), so the next qa/finish re-runs the whole ~13-17 min suite — About 0 s saved on its own, measured against the 4 registry events: the one no-commit case is already fixed in 6.5.0, and in the other 3 an EVOLVE commit moved HEAD. It is worth doing only as a prerequisite for a carry or input-keyed stamp (close-2 / missed-1). — fix: In cmd_qa, count suite reds separately: `sred_any` covers test/lint/typecheck/build/uat and `red` still covers everything. Move the stamp block (observe.sh:2187-2200) ABOVE the `die` and gate it on `sred_any -eq 0` plus the existing HEAD/dirty before-and-after checks. Stamping is safe: drift and do… (kit/ops/lib/observe.sh)
- **close-2** · OVERSTATED · The suite stamp is keyed on HEAD, and the carry step is locked behind test_select (unset here), so a finish after an EVOLVE commit or after any prose-only chan… — About 800 s per CONDUCTOR run in which EVOLVE auto-applies a commit (3 measured in about 4 weeks, about 2400 s), and only together with close-1. No per-express-land saving in this repo (batch integration). — fix: Stamp by what each check actually reads, not by HEAD. Per suite key, keep `.polaris/suite-stamp.<key>` = `<inputs-fingerprint> <epoch> <scope>`, where the fingerprint is the text of `git ls-tree -r HEAD -- <inputs>` (one git call; compare as text, or pipe to `git hash-object --stdin`) plus the key'… (kit/ops/lib/observe.sh, kit/ops/lib/integrate.sh, kit/ops/lib/core.sh, kit/ops/KEYS.tsv, ops/CONVENTIONS.md)
- **close-3** · CONFIRMED · drift's section 7 (dependency-graph check over ALL columns incl. 171 done tasks) is 94% of drift, and drift runs about 3+3W times per run — Section 7 costs about 430 s per drift call on today's box with no load from sibling agents, and about 100 s or less on a fast day (the 09-17 tail was 114 s for cruft+drift+doctor together). A one-pass awk brings it to about 0.4 s. The per-run total is an estimate. — fix: Replace observe.sh:981-993 with ONE awk over `$BOARD/{active,ready,review,blocked,backlog,done}/*.md` (list files with a builtin glob loop; skip any column with no files). It reads the depends_on block and flow lists exactly as fm_list does, then in END prints 'DEP MISSING' for any id not seen and … (kit/ops/lib/observe.sh, kit/ops/lib/selftest/board.sh)
- **close-4** · CONFIRMED · `polaris next` takes 24.5 s: a `basename` process per .polaris/bg job folder (153, never pruned). That exceeds the 10 s anchor-hook timeout and approaches the … — `next` takes 23-28 s and `next --brief` 29.5 s on today's box, against 10 s (anchor) and 30 s (stop) hook timeouts. The builtin fix brings that loop from about 24 s to about 0.02 s. Anchor re-orientation is lost only for sessions that reach row 5. — fix: In all four scans (handover.sh:236-244, observe.sh:2311-2314, observe.sh:330-333, bg.sh:146-149) replace `$(basename "$d")` with `n="${d%/}"; n="${n##*/}"` and do the `*.prev` case before any other work, exactly as hh_bg_live does. Better still, iterate `"$root"/*/` but `case "$d" in *.prev/) conti… (kit/ops/lib/handover.sh, kit/ops/lib/observe.sh, kit/ops/lib/bg.sh)
- **close-5** · CONFIRMED · Other loops that start a `basename` process per file: branch cleanup and drift's cruft check pay 171 per qa when ANY feat/* branch exists; check --only pays 31… — About 41-68 s per qa on today's box (about 7 s on a fast day) while any feat/* ref exists, which has been every qa here since 2026-07-14. About 4-5 s per `check --only`. — fix: (a) cruft_clear, drift's cruft section and sweep: loop over the refs, not the done folder. `for r in $crefs; do id=${r#feat/}; [ -f "$BOARD/done/$id.md" ] || continue; …` is O(branches) with zero process starts. (b) cmd_check: `name="${f##*/}"; name="${name%.cmd}"`, and filter before anything else.… (kit/ops/lib/observe.sh, kit/ops/lib/handover.sh, kit/ops/lib/knowledge.sh, kit/ops/lib/builder.sh)
- **close-6** · UNVERIFIABLE · qa and land --express run test → build → uat one after another, then cruft/drift/doctor after them; only `test` is long, and none of them depend on each other — Unmeasured. At most about 1-2 min per qa before contention, and possibly close to zero on a box bound by process starts. It needs a timed prototype before it can be ranked. — fix: In cmd_qa: start drift --strict and doctor in background subshells writing to their own temp files at t0, run `test` in the foreground, and start build and uat in the background concurrently (each with its own out file and rc file). Then `wait` and report in the fixed order test, lint, typecheck, b… (kit/ops/lib/observe.sh, kit/ops/lib/integrate.sh)
- **close-7** · CONFIRMED · Parallel lanes run selftests at the same time and overload the Windows box: one drill ran 10x slower than it does alone, and sibling drift runs doubled each ot… — When 5-6 heavy jobs overlap, each one stretches at least 4.9x (not 10.4x) in the one measured wave. The semaphore gain is still an estimate. — fix: Add a machine-wide heavy-job semaphore: `slot_on`/`slot_off` in core.sh using `mkdir "$LOCKS/.heavy-$i"` for i in 1..N (N = cfg heavy_slots, default 2, with a stale-pid steal like mutex_on). Take it around `doctor --selftest` (not --fast) and around `bg run` of suite keys, printing one `queued: wai… (kit/ops/lib/core.sh, kit/ops/lib/bg.sh, kit/ops/lib/observe.sh, ops/CONVENTIONS.md)
- **close-8** · CONFIRMED · Change-aware test selection is not configured here, and turning it on would buy little: 59% of code commits touch modules every drill uses, and each row arms t… — As stated. Any gain comes from a stamp carry (close-2 / missed-1), not from SURFACES rows. — fix: Do not map SURFACES rows for the kit. Get the prose/golden savings from input-keyed stamps (close-2) instead. If leaf-module speed-ups are wanted later, add rows only for handover.sh/awake.sh/bg.sh/skills.sh/visual.sh → `bash kit/ops/polaris doctor --selftest --only <label>` with tests=<the drill's… (ops/CONVENTIONS.md, ops/SURFACES.tsv)
- **close-9** · OVERSTATED · pack starts a whole new polaris + python + index-refresh process chain for EVERY owned path — An estimate of about 1-2 s per Builder cold start for a typical task (about 1.85 fewer find calls), and 0 s for the third of tasks that own one path. — fix: Let index.py `find --api` take several globs (`--api g1 g2 …`, one build(), print in order), and call it once from cmd_pack with the full owned list. Keep the per-path grouping in the output so the pack-visual golden stays byte-identical. (kit/ops/lib/builder.sh, kit/ops/index.py)

Skeptic — missed in close:
- ROOT CAUSE OF THE DOUBLE SUITE, not attributed by the auditor: CONDUCTOR.md:294-310 runs EVOLVE after the final green qa and before finish. With evolve_apply: auto-reversible (the default since 6.0; EVOLVE.md:18-25), EVOLVE applies MAP folds, Learned prunes and a CONVENTIONS changelog line on its own, then commits `chore(polaris): evolve`. That moves HEAD, …
- An orphan feat/* branch is invisible and costs every qa: feat/voice-standard-reports (7e5ce59, 2026-07-14, not an ancestor of main) matches no task. drift, sweep and cruft_clear all look only at done/ ids, so they never report or clear it. Yet because it exists, all three run their 171-iteration loops with a process start per file on every qa (about 41-68 s…
- `next --brief`, which the SessionStart compact|resume anchor hook runs under a 10 s timeout, measured 29.5 s today (about 3x the timeout). The auditor timed only bare `next` (23-28 s).
- Environment: with no POLARIS processes running, process starts on this box currently cost about 156 ms (basename x20 = 3.12 s) and git rev-parse about 158 ms, against the 57 ms baseline at ops/CONVENTIONS.md:33, about 2.8x slower. On 09-17 the whole cruft+drift+doctor tail took 114 s; today drift section 7 alone takes 431 s. Every loop that starts a process…
- PLANNER.md:7 and step 13b run `drift` in the foreground (13b says 're-run until clean'), and EVOLVE.md:5 needs drift output too. At about 431 s per call today, one Planner kickoff pays about 14 min of drift and brushes the 600 s tool cap on each call. The close-3 awk rewrite removes all of this, so it should rank above close-2 for latency the user actually …

## context

- **context-1** · OVERSTATED · Conductor context never compacts under Opus 5.5's 1M window — median 443k tokens re-read per conductor call — Saves 2-12% of total run spend on long conductor runs, and about 2% on a sprint-16-sized run. It is not about 50%. Short Opus 5.5 sessions lose. The cheaper fix is a harness setting (autoCompactWindow of about 300k in kit/.claude/settings.json, or the env var). This still needs verifying on 2.1.281… — fix: 1. Add a context budget to the conductor loop. - kit/ops/hooks/handover-hook.sh `stop` and `prompt` modes already run every turn. - Tail-read the last assistant `usage` from transcript_path (model-guard.sh already parses transcript_path in pure bash) and compute cache_read + cache_creation + input.…
- **context-2** · CONFIRMED · `route` before every spawn costs a full conductor turn and returns a tier word nothing uses — About 1.56M IE across 5 sessions, plus 1.6-1.9 s of shell time per spawn. On a large-context conductor that is about 40k IE per removed turn. The fix is 1 point and low risk, and it removes the last written Fable instruction. — fix: 1. Delete the "Route every spawn" preamble (CONDUCTOR.md:83-87) and the "(routed per the preamble rule)" and "Route each lane first" clauses at :120 and :172-174. 2. Replace PROTOCOL.md § MODEL ROUTING (:211-239, 2.55 KB) with two lines: "Never pass a `model` param; every spawn inherits the session…
- **context-3** · CONFIRMED · claude-flow machinery rides every context unused, and `slim` both mis-classifies it and misses the biggest piece — About 9.8k tokens per general-purpose subagent per call and about 9.0k per main call. About 7M cache-read tokens per sprint-16-sized run, roughly double the auditor's 3.85M. The slim classifier fix is also larger than stated: its `agents/` rows reach subagents too. — fix: 1. In slim_scan's emit(), apply `mach` before the corpus match for the agents/ and commands/ trees, and require a whole-token match (e.g. /(^|[^a-z0-9-])name([^a-z0-9-]|$)/) for skills. 2. Make cmd_slim --restore skip `$arc/RESTORE.md`. 3. Add a machinery row for ~/CLAUDE.md: detect the '# Claude C…
- **context-4** · OVERSTATED · `pack` prints the first 120 lines of a contract, and 84% of builders then re-read the contract anyway — Across all 8 sessions, about 0.8 MB of addressable re-reads, about 200k tokens persisted. Per sprint the saving is probably 0.2-0.3M IE (about 1-2%), not 0.5M (3%). This is an estimate: a selective pack could also cause extra re-reads when the section match misses. — fix: Change the cmd_pack contract section (builder.sh:667-669): 1. Print a heading index: every `^## ` line with its line range (a few hundred bytes). 2. Print in full only the sections whose heading or body mentions the task ID or one of its files_owned paths, capped at about 60 lines. 3. Print a one-l…
- **context-5** · OVERSTATED · Eight contradictions between always-loaded rules and role, skill and plugin text — an Opus-class model follows them literally — Worth fixing as cheap quality hygiene: 2 points, low risk. The +100-150k-token misroute and the 'subagent runs finish' behavior are unmeasured, and the second is contradicted by 0 occurrences. Items 1 (bare `start` → CONDUCTOR, skipping triage) and 2 (EVOLVE's RULES stance) are the ones a literal-m… — fix: 1. Rewrite polaris/SKILL.md step 1 to one rule: "bare start phrase or work request → run `bash ops/polaris triage`; line 1 is the lane". Keep INIT detection and the question guard. Also update the kit's installed copy; the ~/.claude user copy is refreshed by `update`. 2. EVOLVE.md:30 → "RULES follo…
- **context-6** · UNVERIFIABLE · Spawn POLARIS roles as custom agent types with a tight tool list and the role file as the system prompt — The auditor's own estimate is 8-11k tokens per call, about 0.6M IE per sprint. If both listings drop, it could reach about 20k per call, but that is unverified. The auditor's probe (same task, compare first-call usage) is the correct gate before committing 3 points. — fix: 1. Ship kit/.claude/agents/polaris-{builder,planner,integrator,solo,evolve}.md, each with: - frontmatter `tools: Bash, Read, Edit, Write, Grep, Glob` (plus AskUserQuestion only where a role may ask); - no `model:` key, so it inherits the session; - the role file body as the agent prompt, generated …
- **context-7** · OVERSTATED · Role-file and PROTOCOL prose trim plan: about 55 KB of narrative and duplication, with blast radius — Well under 1% of run spend per run. The real payoff is quality: fewer contradictions and less duplicated doctrine, which overlaps context-5. Split it: do the contradiction and obsolete-section cuts (MODEL ROUTING, stale LANES figures) first, and defer the broad 55 KB rewrite. — fix: Edit the kit/ copies only. ops/RULES.tsv path-guards ops/roles/ and ops/PROTOCOL.md; refresh them via the dogfood step, handed to the human as 🚩. PINNED (keep these strings verbatim): - ops/tests/plain-voice.cmd: in PROTOCOL § VOICE, 'never used git or run a test', '**Pre-send check', and the 7 num…
- **context-8** · CONFIRMED · The kit's cost model says ~7,300 tokens per context; measured is 44-47k per subagent and 66-68k in main — No direct token cost. The note understates the solo-vs-full gap 4-8x, which only strengthens the existing solo choice. Any widening of the envelope is a separate owner call. Nothing in it forces a policy change today. — fix: 1. Replace the constant with measured numbers: about 45,000 per subagent, about 67,000 for main. 2. Better: have `slim` (or a new `polaris tax`) read the newest subagent jsonl under <transcripts> and print the first-call usage (cache_read / cache_creation), so the number stays tru…
- **context-9** · CONFIRMED · No POLARIS check measures CLAUDE.md or MEMORY.md, which are the largest per-context items in real installs — In a another install-sized install, about 34k tokens per context per call come from files POLARIS never measures. That is more than all of POLARIS's own injected text. A 2-point report and nudge is well justified. — fix: 1. Extend `slim` (or `doctor`) with a 'per-context injection' block covering: - ~/CLAUDE.md, repo CLAUDE.md (split into the POLARIS block and the project part), nested CLAUDE.md, MEMORY.md, enabled plugins' skill bytes, and the output style; - measured from the files, with a transcript-measured tot…

Skeptic — missed in context:
- The auditor's source table is wrong that the agent listing reaches the main session only. 46 of 46 general-purpose subagents in session-H and session-E carry agent_listing_delta (25,439 B: 96 types, of which 20,625 B are non-built-in ~/.claude agents). So each subagent's controllable prefix is about 19.8k tokens, not 13.3k. This also raises the ceiling for co…
- The compaction problem has a harness-level lever. The installed Claude Code binary (npm 2.1.251, bin/claude.exe) has an `autoCompactWindow` setting and a `CLAUDE_CODE_AUTO_COMPACT_WINDOW` env var that accept 100k-1M. Neither is set in ~/.claude/settings.json or either Polaris settings.json. A project-level autoCompactWindow of about 300k in kit/.claude/sett…
- Subagents, not the conductor, dominate run spend: 67-82% of input-equivalents in every measured conductor run (session-H: 14.88M of 18.1M). Levers on each subagent's prefix (context-3, context-6) and on its in-run reads (context-4) therefore beat conductor compaction on sprint-16-shaped runs. The findings list ranks compaction first.
- The first-call prefix of general-purpose subagents varies by session: median 39.9k (session-F) up to 58.0k (session-A) and 54.6k (session-D). The ~45k figure is not stable across CC versions and sessions, which argues for the transcript-measured 'tax' number that context-8 proposes instead of any new constant.
- In another install, MEMORY.md is 23,350 B (68 lines) and is injected into every context alongside the 107,899-character CLAUDE.md. The per-project auto-memory index can on its own exceed POLARIS's whole CLAUDE.md, and nothing reports it.
- Of the 249 contract reads after pack, 119 target a contract OTHER than the task's own. pack surfaces no cross-contract pointers (for example, the headings of contracts that the task's contract or files_owned reference), so builders go hunting. A section-selecting pack alone leaves that half untouched.

## conflicts

- **conflicts-1** · CONFIRMED · POLARIS has no precedence clause; add a ~300 B one to the managed CLAUDE.md block — Cost: about +300 B × 6-8 contexts, roughly +0.5k tokens per run (estimate). Benefit: one sentence removes a 4-way adjudication (RuFlo, superpowers, harness, POLARIS) that a literal-reading Opus 5.5 would otherwise face every turn. The deliberation saved cannot be measured: 535 of 14,758 thinking bl… — fix: In kit/CLAUDE.md, directly under the title and inside POLARIS:BEGIN/END so `update` ships it (heal's efficiency block is append-once and would never reach existing repos), add: "**Precedence.** Here POLARIS is the process. Where any other source disagrees — an ancestor or user CLAUDE.md, a plugin's… (kit/CLAUDE.md)
- **conflicts-2** · CONFIRMED · RuFlo ancestor CLAUDE.md: never followed, loaded into every context in every repo — About 1.7k tokens (6,787 chars / 4, estimate) per context. It is written to cache once per context and re-read on every turn: about 11.5k general-purpose subagent turns plus 5.9k POLARIS main turns measured since August, roughly 30M cache-read tokens (estimate). Per 8-context run that is about 13.6… — fix: HUMAN DECISION (a user-level file outside every repo). Option (a): archive, never delete, ~/CLAUDE.md to ~/.claude/.polaris-archived/ancestor/CLAUDE.md with a RESTORE line. Nothing on the machine has used RuFlo since at least 08-01. Also drop the inert CLAUDE_FLOW_* env va… (~/CLAUDE.md, ~/.claude/settings.json, ~/.claude/settings.local.json, kit/ops/lib/admin.sh)
- **conflicts-3** · CONFIRMED · superpowers is loaded into 100% of POLARIS sessions and used in <1%; slim cannot see it and disabling it per repo needs consent — About 1.5k tokens per main session (3,431 B of SessionStart text plus ~2.4-2.8 KB of listing), paid again on each compact/clear, plus about 600 tokens per subagent. Payback is 3 invocations in 152 sessions (2%). — fix: 1. In kit/ops/lib/admin.sh slim_scan and cmd_slim, add report-only rows. One row for enabled plugin skills (read enabledPlugins plus installed_plugins.json installPath, then skills/*/SKILL.md frontmatter with the same awk). One row for the last transcript's plugin SessionStart hook_additional_conte… (kit/ops/lib/admin.sh, ops/KEYS.tsv, ~/.claude/plugins/cache/claude-plugins-official/superpowers/6.4.1/hooks/session-start)
- **conflicts-4** · OVERSTATED · POLARIS gives 4 different routes for the same kickoff; the skill and plan paths skip triage and pay for 3 contexts on SOLO-sized work — The routing ambiguity is real but its measured cost is unknown: no SOLO-sized job was observed going through the planner. Where the route does add a PLANNER, it costs about 33 requests, 79k output and 5.2M cache-read, not 97/92k/15.5M. The extra role-file read in the plan path is ~4.4k tokens (esti… — fix: Make `triage` the only router. 1. SKILL.md step 1: collapse the start and work-request bullets into one line: 'start phrase, work request, or approved plan → author the task(s) if the board has none (ops/templates/TASK.md), then `bash ops/polaris triage`; line 1 is your lane per CLAUDE.md'. This de… (kit/.claude/skills/polaris/SKILL.md, kit/CLAUDE.md, kit/ops/roles/CONDUCTOR.md, kit/ops/lib/observe.sh)
- **conflicts-5** · OVERSTATED · Harness plan-mode subagents can't see POLARIS: 58 of 59 Explore/Plan agents never used the brain — Per plan-mode session: about 5-7 agents, ~250-300 tool calls, ~100-170k output and ~7-10M cache-read (deduplicated), not 21M. The share that brain/find would avoid is still an estimate. — fix: 1. Add one line (about 200 B) to kit/CLAUDE.md ROLE DISPATCH: "**Plan mode:** research it yourself — `.polaris/brain/INDEX.md`, `bash ops/polaris find|show`. An Explore/Plan subagent receives NO CLAUDE.md and greps blind; spawn one only with the brain-first line pasted into its prompt. Write the pl… (kit/CLAUDE.md, kit/ops/hooks/, kit/.claude/settings.json)
- **conflicts-6** · CONFIRMED · 'Route every spawn' is a no-op on this machine and contradicts 'never name a model' — Per spawn: 1 tool call, ~1.1-1.6 s, and one extra conductor request at a 210-390k-token context (~0.2-0.4M cache-read). session-E's 25 route calls cost about 9.7M cache-read tokens (estimate). The wall-time saving is smaller than claimed; the token saving is larger. — fix: 1. Delete the 'Route every spawn' paragraph at CONDUCTOR.md:83-87 (about 500 B) and replace it with 'Spawns never pass `model` — they inherit this session.' 2. In PROTOCOL.md § MODEL ROUTING, delete :229's 'THIS repo pins…' and rewrite :231's consumer line: the conductor does not call route; `fleet… (kit/ops/roles/CONDUCTOR.md, kit/ops/PROTOCOL.md)
- **conflicts-7** · OVERSTATED · POLARIS's own rules disagree on asking, interview rounds and contexts per task, and triage understates cold-start cost ~2x — Items 1 and 3 affect quality only (unmeasured). Item 2: ~2k tokens (the 2,338 B slice) plus the rest of an 18.9 KB file, in about 2 of 7 conductor runs. Item 4: cosmetic, with no effect on lane choice. — fix: 1. PROTOCOL.md:331 → 'Missing detail: default the reversible choice, record it as `- assumed:` in Notes; stop only for the STOP-AND-ASK list.' 2. Move PLANNER 0b/0c into a small shared ops/roles/INTERVIEW.md that both roles read. State the 3-round cap once there. 3. PROTOCOL.md:206 → 'One task per … (kit/ops/PROTOCOL.md, kit/ops/roles/CONDUCTOR.md, kit/ops/roles/PLANNER.md, kit/ops/lib/observe.sh)
- **conflicts-8** · CONFIRMED · voice: standard is broken in half the kit repo's replies; commit-message style has 3 conflicting sources — In the kit repo the rule costs a pre-send rewrite pass per reply and still fails half the time. The owner gets inconsistent register, which is the opposite of the ADHD-shaped predictability the style exists for. Token cost is small (an estimate). — fix: HUMAN DECISION on the kit repo's ops/CONVENTIONS.md:5: `voice: technical` for the repo whose reader wrote the kit, or keep standard and accept it. Then reconcile polaris.md:10 with the owner's commit-message preference in one clause. Suggested wording: 'commit subjects: `type(scope): "plain-languag… (ops/CONVENTIONS.md, kit/.claude/output-styles/polaris.md)
- **conflicts-9** · CONFIRMED · The statusline shows a RuFlo dashboard (Swarm 0/15, DDD 0/5) instead of POLARIS state — No token cost; the statusline runs outside the model. The UX is a permanent display of a product nobody uses, which competes with POLARIS's own 'where things stand' promise, plus about 0.6 s of node startup per refresh. — fix: 1. Ship kit/ops/hooks/statusline.sh: pure bash, no ops/polaris startup. It prints 'POLARIS · next: <role/ID> · ready N · active N · review N' from a tiny cache file (e.g. .polaris/next-brief) that `next`, `claim`, `done` and `land` rewrite. Outside a POLARIS repo it prints nothing, or delegates to … (kit/ops/hooks/, kit/ops/lib/handover.sh, ~/.claude/settings.json)
- **conflicts-10** · OVERSTATED · After plan approval a fresh PLANNER throws away the context the plan-mode session just paid for — The saving is at most one PLANNER's fixed injection (~13k tokens, estimate) plus part of 5.2M cache-read and 1-3 minutes, minus the extra conductor context re-read on every later request. The net could be negative on long runs. It needs a measured spike before the owner amends Invariant 5. — fix: Add a second sanctioned in-context hop: 'plan-mode → PLANNER'. The session whose plan was just approved carves the board itself (PLANNER.md from step 1 on; 0b and 0c are satisfied by the approval), runs `drift` and the ready gate through the CLI as usual, then hands off via `polaris next`. Log the … (kit/CLAUDE.md, kit/ops/roles/CONDUCTOR.md, kit/ops/roles/PLANNER.md, ops/contracts/role-handover.md)

Skeptic — missed in conflicts:
- METHOD ERROR that affects other findings: Claude Code writes each API response as 2-4 transcript records that carry the SAME usage block. In session-H, 40 of 54 requests span multiple records and cache_read is identical within every one. Summing per record inflates cache-read and 'turns' 2.5-3x; output_tokens is roughly unaffected. conflicts-4, -5 and -10 us…
- BIGGEST MEASURED SLOWDOWN (cross-dimension, hooks): the machine-wide PreToolUse hook in ~/.claude/settings.json (matcher '*', ~/.claude/polaris/model-guard.sh, identical to kit/ops/hooks/model-guard.sh from 68cae37) runs before EVERY tool call in EVERY repo, including non-POLARIS ones. Natural experiment on tool_use→tool_result latency across a…
- Route-per-spawn is mostly a token cost, not a wall-clock one. Each route call forces one extra conductor API request at median 210-387k context (measured at the call site in 3 sessions); session-E made 25 route calls, about 9.7M cache-read tokens. This strengthens conflicts-6's fix, and the same accounting applies to every extra serial CLI call a CONDUCTOR r…
- Plan-mode subagent prompts never carry POLARIS guidance, yet the fix works when present: 137 of 180 general-purpose agents used brain/find/pack, and 135 of those prompts told them to, against 0 of 76 Explore/Plan agents, which were never told. That is direct evidence that conflicts-5's 'paste the brain-first line into the spawn prompt' fix works, and it mak…
- Duplicate skill listing in the kit repo: kit/.claude/skills is auto-discovered as the scoped skills `kit:polaris` and `kit:polaris-install` next to the installed `polaris` and `polaris-install`. Every context in the kit repo pays both descriptions (~1.8 KB extra, estimate from the listed descriptions), and the model had to choose between them twice (kit:pol…
- Unmeasured: ~/.claude/settings.json env sets CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1, which exposes another orchestration layer (ListAgents/SendMessage-style team tools appear in this subagent's deferred tool list) alongside CONDUCTOR. POLARIS never mentions it, so it is a possible fifth source of competing instructions. It deserves a check of whether it add…
- SKEPTIC DISCLOSURE (my own actions, not the audit's): I accidentally ran `bash ops/polaris version` once while timing. It calls update_check_maybe force: one curl to the release channel, and it rewrote the gitignored .polaris/update-cache ('checked: 2026-09-24 / latest: 6.5.0', same-day content). I also briefly wrote and deleted one temp file under /tmp whi…

## harness

- **harness-1** · OVERSTATED · No compaction ever happens on 1M windows, so the conductor ('reads only summaries') costs 18–33% of every run — About 3–12% of run eq on the three big runs, before post-compaction re-read overhead, with real quality risk because the knob is repo-wide. Starting a fresh session per program (finish, then a new chat) gets most of the gain without the env var. — fix: (1) Add `"env": {"CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "<n>"}` to kit/.claude/settings.json and to the settings merge in kit/ops/bootstrap.py and admin.sh. Choose n so compaction fires near 250k on a 1M model. (2) Reword Invariant 5 in kit/CLAUDE.md:90 and CONDUCTOR.md:75-80 so a compaction reset is a… (kit/.claude/settings.json, kit/ops/bootstrap.py, kit/ops/lib/admin.sh, kit/CLAUDE.md, kit/ops/roles/CONDUCTOR.md, kit/ops/hooks/handover-hook.sh)
- **harness-2** · OVERSTATED · Conductor hand-rolls orchestration (spawn → route → notification → relay → poll per lane). A saved Workflow would move all of it out of the model's context. — About 4–6% of full-run eq, overlapping harness-3 and harness-7. About 2–5 minutes of wall clock, not 10–15. Questions to the human would arrive later. — fix: Add a new kit/.claude/workflows/polaris-wave.js, shipped through pack.py KIT_CODE and install.sh, with uninstall and slim accounting. - Args: the ready rows from `board-fm ready` (id, points, risk, visual); lanes = min(ready, autolaunch_max, 14), where 14 = min(16, this box's 16 CPUs − 2); run caps… (kit/.claude/workflows/polaris-wave.js, kit/.claude/skills/polaris/SKILL.md, kit/ops/roles/CONDUCTOR.md, kit/ops/pack.py, kit/ops/install.sh, kit/ops/lib/admin.…)
- **harness-3** · OVERSTATED · The rule against ending a turn to wait on a background job is still correct for subagents, but it is enforced at the top level too, where the Stop hook forces … — Conductor: about 0.5–1.0M eq per full run. Builders: about 2%. Wall clock unchanged. — fix: (a) Top-level sessions (conductor, top-level SOLO, fleet panes): run `Bash({command:'bash ops/polaris bg wait <job> --max 3000', run_in_background:true})` and end the turn; a single completion notification wakes the session. Drop the sleep timers: Agent completion notifications already wake the con… (kit/ops/roles/CONDUCTOR.md, kit/ops/roles/SOLO.md, kit/ops/roles/BUILDER.md, kit/ops/PROTOCOL.md, kit/ops/hooks/handover-hook.sh, kit/ops/lib/bg.sh)
- **harness-4** · OVERSTATED · A returned question, a red result or a queued landing respawns a cold agent; SendMessage would continue the warm one — About 0–0.5M eq per run, since events that need a re-dispatch are rare (~3%). The rule change is still cheap, but its impact is small. — fix: CONDUCTOR.md step 5: - Question: always SendMessage(<lane>, answer). - RED or kickback: the one allowed retry becomes a SendMessage carrying the failing lines. If the task is still red, move it to blocked/ as today. A fresh agent is used only when the warm one is unreachable. Step 6, rc 3: SendMess… (kit/ops/roles/CONDUCTOR.md, kit/ops/PROTOCOL.md)
- **harness-5** · OVERSTATED · Every lane is spawned as general-purpose, whose prefix is 47.6k tokens; restricted agent types start at 21–23k — Estimated at about 90–100k eq per builder (~9–10%), not measured. — fix: Extend the prior plan's spike to lanes. Ship kit/.claude/agents/polaris-lane.md with tools: Bash, Read, Edit, Write, Glob, Grep, and NO model field (it inherits; model-guard/core.sh model_denied keeps Fable and Haiku out). Its body is the kickoff-invariant core of BUILDER.md/SOLO.md, or a one-line … (kit/.claude/agents/polaris-lane.md, kit/ops/roles/CONDUCTOR.md, kit/ops/install.sh, kit/ops/lib/admin.sh)
- **harness-6** · OVERSTATED · With a 1M window: one task per builder context still pays (~9×), but in-context hops and multi-task SOLO shed nothing, and triage's arithmetic is off — Plausible for multi-task SOLO in the main chat, but how often that happens is unmeasured, and there is a UX cost. The 7,300 note should be corrected (it is at least 15k). — fix: When an Agent tool exists, the `solo` lane runs as ONE SOLO subagent per task, run in sequence. The main chat receives only each report. Change the `solo` row in kit/CLAUDE.md § ROLE DISPATCH, SOLO.md Entry, and CONDUCTOR.md:120-132 (per task rather than per batch). In BUILDER.md Loop mode, a top-l… (kit/CLAUDE.md, kit/ops/roles/SOLO.md, kit/ops/roles/BUILDER.md, kit/ops/roles/CONDUCTOR.md, kit/ops/lib/observe.sh)
- **harness-7** · CONFIRMED · Calling `route` before every spawn now costs about one builder per run and does nothing; `effort` on mechanical stages is too small to be worth a rule — 0.07–1.2M eq per run, already planned in crystalline-puppy. — fix: Delete the per-spawn route step. The prior plan's route-ritual item already covers this in CONDUCTOR.md, and the fable pin in PROTOCOL.md § MODEL ROUTING goes with it. If effort is ever tried, map tier to effort inside polaris-wave.js from the points and risk the script already receives, which cost… (kit/ops/roles/CONDUCTOR.md, kit/ops/PROTOCOL.md, kit/ops/lib/observe.sh)
- **harness-8** · UNVERIFIABLE · After ExitPlanMode approves a plan, the planner subagent re-researches what the plan-mode session already read — Estimate: turning plan-entered planners into transcribers could save about 1M eq per program run and 10–20 minutes before the first builder starts. — fix: Plan-mode plans carry a fixed '## Carve' block, one row per task: ID, title, points, risk, files_owned, depends_on, verify, open?. The read-only plan session writes it while it still holds the research. CONDUCTOR.md 'Entered from an approved plan?' (:102) then kicks off the planner as: 'transcribe … (kit/ops/roles/CONDUCTOR.md, kit/ops/roles/PLANNER.md, kit/ops/templates/TASK.md)
- **harness-9** · OVERSTATED · Human gates are silent when the owner is away; sprint results live only in a Markdown file or a localhost dashboard (PushNotification and Artifacts unused) — Wall clock: every gate that currently waits for the owner to wander back gets a push. Reports become a private link the owner can share. Token cost is near zero if the push rides in the same message as the question or close, and the page is rendered by the CLI. — fix: CONDUCTOR.md: add a PushNotification in the SAME message as the gate question at step 3 (confirm), step 5 (question), step 6 (risk: high), and the step 8 close when finish exits 0. Name what the owner must act on. It is additive to notify-gate, never a substitute. Artifacts: add `polaris report --h… (kit/ops/roles/CONDUCTOR.md, kit/ops/lib/observe.sh, kit/.claude/output-styles/polaris.md)

Skeptic — missed in harness:
- The audit never touched Opus 5.5 lane runs. Main-session models: session-A, session-E, session-F and session-H ran claude-opus-5; session-D ran fable-5-1 plus opus-5; session-C ran fable-5. Opus 5.5 appears only in session-I (a plan session that used workflows only) and this audit. Subagent message-line tallies: fable-5 1011, fable-5-1 4239, opus-5 8173, opus-5-5 …
- The global effort setting is unexamined. ~/.claude/settings.json sets effortLevel 'xhigh', plus modelSettings {claude-opus-5-5: xhigh}, so every conductor bookkeeping turn and every lane thinks at xhigh. Output, which includes thinking, is 23% of builder cost and 8–10% of conductor cost (mainsim.py/subs.py). This bears directly on the owner's 'slower' compl…
- The skill_listing attachment is injected into every subagent, including Explore and Plan. It measures 27.2k chars in a session-E builder and 34.4k in this run's workflow agents, so it grew. It lists about 150 RuFlo/claude-flow skills and commands that POLARIS never uses. ~/.claude holds 88 command files, 75 skill files and 98 agent files, so the `slim` archi…
- Every context's `instructions` attachment (22.5k chars) includes ~/CLAUDE.md, the 6,804-byte RuFlo file. Its directives contradict POLARIS: 'ALWAYS spawn ALL agents... run_in_background', 'After spawning, STOP — do NOT add more tool calls', 'MUST initialize the swarm', and three-tier routing to Haiku. A highly literal model receives these on ev…
- The 'conductor' cost is really a long-lived owner chat running several programs. session-E lasted 34 hours and 372 turns, with 7 ExitPlanMode calls; session-D ran from 09-01 21:52 to 09-03 06:42. Most of the 717k growth is carry-over across programs, so closing the chat after `finish` and starting a fresh session per program is a zero-risk alternative to a re…
- Several findings count the same conductor turns more than once: route-only turns in harness-2 and harness-7, poll turns in harness-2 and harness-3, and main2.py's 'after-notification' category overlapping the others. Summing their impacts overstates the combined gain.
- Conductor kickoffs are long hand-written prose on top of `pack` (a median of 2.7–3.3k chars, 110k–160k chars per run). The output cost is under 1% of the run, so this is minor, but the prose duplicates what `pack` already provides.

## telemetry

- **telemetry-1** · CONFIRMED · The run's tail pays for the full suite twice: qa reds on MAP/LEARNED/CRUFT housekeeping only after the 15-min suite, and EVOLVE runs after qa and moves HEAD — About -16.5 to -17 min per affected close (one full qa, 992-1036s measured) plus about 3 min of diagnosis. The pure-CRUFT reds (qa3.prev) are already fixed by T-152, so the MAP and LEARNED classes are what remain. It happens more often than 'every 1-1.5 sprints': see missed item 3. Every EVOLVE aut… — fix: Three changes in cmd_qa and one in CONDUCTOR.md: 1. In kit/ops/lib/observe.sh cmd_qa, write the suite stamp as soon as every suite command is green, BEFORE cruft, drift and doctor. The stamp certifies the code commit, not board documents. Keep the rc 1 for drift and doctor. 2. Demote drift's doc-ov… (kit/ops/lib/observe.sh, kit/ops/roles/CONDUCTOR.md, ops/tests/*qa*.expected)
- **telemetry-2** · CONFIRMED · `next --do` runs a full drift after every promotion, putting a 2–10 min fork-bound audit on every wave handover as well as in every qa and finish — Skipping done/ in section 7 (both as cycle roots and in the DEP MISSING loop) cuts each drift from about 100-120s to an estimated 15-25s. S16 made 17 agent calls that ran drift, each 91-123s, about 30.5 min in total, plus the drifts inside qa×2 and finish. Estimated saving: about 28 min of cumulati… — fix: 1. In kit/ops/lib/handover.sh cmd_next --do, replace `( cmd_drift )` with a promotion audit covering only what a promotion can break: drift sections 1–2 (files_owned overlap and the ready gate) restricted to the promoted IDs against ready ∪ active. Put that in a new `drift --promoted <IDs>` path. 2… (kit/ops/lib/handover.sh, kit/ops/lib/observe.sh, ops/board/backlog/IDEAS.md)
- **telemetry-3** · OVERSTATED · The conductor main loop costs its whole context on every wake-up: 22% of run tokens, duplicate wakes, and multi-sprint sessions that never reset on a 1M window — Only the reset nudge has value. For the release window, about -65M cache-read tokens is about -6.5M input-equivalent (estimate). The duplicate-wake prose fix is about 0. The digest-instead-of-role-file fix saves about 7k tokens once per run. — fix: 1. kit/ops/hooks/handover-hook.sh, in the `prompt` (UserPromptSubmit) and `stop` branches: - Take the last assistant `usage` line from transcript_path with `tail` and pure-bash string surgery (same discipline as model-guard.sh). - When input-side tokens exceed a new KEYS.tsv key `context_reset_at` … (kit/ops/hooks/handover-hook.sh, kit/ops/KEYS.tsv, kit/ops/roles/CONDUCTOR.md)
- **telemetry-4** · OVERSTATED · Fan-out without parallelism: effective concurrency is about 1.0, and every chain task pays a fresh builder's cold start and 1.5–8.4 min of orientation on the s… — With drift removed (telemetry-2), the handover gap left is about 2 min × 5 = about 10 min per S16-shaped chain. The orientation saving from wider carving is unmeasured (estimate). — fix: 1. PLANNER.md and cmd_triage: compute and print, at the plan gate, the plan's longest depends_on path versus total points (for example '6-deep chain — expected concurrency ≈1.0'). When chain tasks share files_owned hotspots, carve fewer, wider tasks (still ≤5 pts; fold a golden or prose task into i… (kit/ops/roles/PLANNER.md, kit/ops/roles/CONDUCTOR.md, kit/ops/lib/observe.sh)
- **telemetry-5** · OVERSTATED · triage's cost model is off: it prices a context at ~7,300 tokens and ignores the conductor loop, which is the dominant cost of the full lane — Fixing the constant only makes the printed note accurate. It changes no routing. The token saving from routing S16-like chains to solo is unproven, and possibly negative. — fix: In kit/ops/lib/observe.sh cmd_triage and kit/ops/PROTOCOL.md § LANES: - Replace the 7,300 constant with measured figures (fresh context ≈45k first request, conductor loop ≈20–25M per run, builder orientation ≈1M). - Add a DAG-shape input: the share of plan points on the longest depends_on path, rea… (kit/ops/lib/observe.sh, kit/ops/PROTOCOL.md, ops/tests/*triage*.expected)
- **telemetry-6** · OVERSTATED · Planning is 17% of wall-clock: 43–59 min to the first claim, because one planner context writes every wave before any builder starts — Estimated -5 to -10 min to the first claim from pipelining. About -4 min comes more cheaply from the drift fix (telemetry-2). Planner token savings are unmeasured. — fix: Pipelined planning: 1. kit/ops/roles/PLANNER.md: write the contract(s) and wave-1 task files first, run `bash ops/polaris next --do` for them, and SendMessage the conductor 'wave 1 ready'. Then carve the later waves while wave 1 builds; the files are disjoint by construction. 2. kit/ops/roles/CONDU… (kit/ops/roles/PLANNER.md, kit/ops/roles/CONDUCTOR.md)
- **telemetry-7** · CONFIRMED · The Opus 5.5 gap: zero builds since 5.5 arrived. The 09-22 5.5 audit (about 160M tokens) died at the approval prompt and its findings were never saved to the r… — The raw figure overstates cost. 159.7M input-side is about 20M input-equivalent (cache reads at about 0.1×) plus 1.33M output. It is still the largest single spend with nothing landed. — fix: 1. kit/ops/lib/observe.sh cmd_triage and next_brief (plus the SessionStart anchor in handover-hook.sh): list ~/.claude/plans/*.md that are newer than this repo's last EVENTS entry and mention the repo path, as one line: 'unexecuted plan: <file> — say go to run it'. This is a read-only scan. 2. COND… (kit/ops/lib/observe.sh, kit/ops/hooks/handover-hook.sh, kit/ops/roles/CONDUCTOR.md, ~/.claude/plans/https-claude-dev-blog-getting-the-most-o-cryst…)
- **telemetry-8** · CONFIRMED · The board cannot see where the time goes: no events for run start, qa, EVOLVE or finish, and `metrics` reports all-time averages that are stale by 30× — As stated, but the gap is large mainly on landing. Building calibration is off by about 2.5×. — fix: Emit three events through the existing EVENTS writer: - `run` from cmd_triage (lane, plan slug); - `qa` from cmd_qa (seconds, rc, red_class = suite | drift:<kind> | doctor); - `finish` from cmd_finish (rc). Add `polaris metrics --last`: awk over EVENTS since the newest `run`, printing: - the phase … (kit/ops/lib/observe.sh, kit/ops/lib/core.sh, ops/tests/*metrics*.expected)
- **telemetry-9** · CONFIRMED · The kit repo is not a representative proving ground: the product installs are idle and stale, so the 6.3–6.6 speed work has never run on a product board — As stated. This is a method caveat, with no direct wall-clock impact. — fix: 1. Before tuning further against kit-repo numbers, run one small sprint in another install on the current kit with Opus 5.5 and the telemetry-8 events on, and compare its phase split with S16. 2. Bring the testbed up to 6.5/6.6 and use it for SOLO-vs-full timing probes. 3. Record the compa… (docs/sprints/, ops/ROADMAP.md)
- **telemetry-10** · REFUTED · Builders keep reading through Bash (sed/cat/grep) after `pack`: about 20× slower per call than Read, and twice pack's own bytes land in context — Switching from Bash to Read saves about 0.1s per call today. The token saving comes only from narrower reads, which the existing prose already asks for. It is unmeasured. — fix: Change BUILDER.md and the `pack` footer (kit/ops/lib/builder.sh) to: 'read a known path with Read (offset/limit); locate with `polaris find`, then Read the span — never sed/cat through Bash'. Have pack print file:line ranges for the owned symbols, so a builder Reads exact spans instead of re-cattin… (kit/ops/roles/BUILDER.md, kit/ops/lib/builder.sh)
- **telemetry-11** · CONFIRMED · The model ban has a hole: the built-in claude-code-guide agent type runs Haiku whatever the parent's model, and model-guard only checks the session's own model — Closes a policy hole of negligible token cost (about 0.7M tokens across 2 spawns in 3 weeks). It matters for owner policy, not for speed. — fix: In model-guard.sh (kit copy + refresh_machine_kit), add a check for tool_name Agent/Task whose tool_input.subagent_type is claude-code-guide: exit 2 with 'claude-code-guide runs a forbidden model — use a general-purpose subagent or WebFetch'. Add one line to PROTOCOL.md § MODEL ROUTING and a golden… (kit/ops/hooks/model-guard.sh, kit/ops/PROTOCOL.md, kit/ops/lib/admin.sh)

Skeptic — missed in telemetry:
- MACHINE-WIDE TOOL-CALL TAX: ~/.claude/settings.json registers model-guard.sh as PreToolUse matcher '*' (installed 09-15 16:06). Median Read time went 0.03s (09-14, n=111) → 0.85s (09-15) → 0.75s (09-17) → 0.83s (09-22, the Opus 5.5 audit). Grep went 0.09s → 0.23-1.12s. That is about +0.75s on EVERY tool call in EVERY project on this machine, and it is the o…
- DRIFT RUNS EVERYWHERE, NOT JUST IN next --do: S16 made 17 agent calls that ran drift, each 91-123s (about 30.5 min cumulative): 3 in the first planner (all before the first claim), 4 in the mid-run planner, 5 in next --do, 2 by the conductor on the tail, and 3 in EVOLVE. qa×2 and finish add 3 more. Section 7 (the all-columns dependency walk) takes about 85%…
- STRUCTURAL DOUBLE SUITE AT CLOSE: each of the last 3 evolve commits (2b827a4, 772228a, e078d68) touches ops/CONVENTIONS.md (Kit changelog) plus ops/MAP.md. Each commit moves HEAD after the green qa, so the next qa or finish re-runs the full suite. Observed three times: 08-04 (evolve 12:56 → evolve-qa 1075s from 12:56:59), 09-14 (evolve 12:41 → qa2 762s from…
- TOKEN ACCOUNTING UNIT: cache reads are 98-99% of every 'input-side' total in this audit (S16 main loop 23.07M of 23.37M, S16 subagents 82.5M of 84.9M, the 09-22 audit 155.9M of 159.7M) and bill at about 0.1× base input. Price-weighted, S16 is about 18M input-equivalent, and output (0.85M at about 5×) plus cache writes (2.65M at 1.25×) are about 40% of it. R…

## quality

- **quality-1** · CONFIRMED · Default landing: self seals and pushes an untested wave to origin/main — the suite first runs at finish, after the push — The mechanism is as stated: default waves reach origin/<base> before any combined-tree suite. The '0 kickbacks vs 20 fixes' comparison overstates how many escapes a pre-seal suite would have caught. Roughly 5 of the 20 fix tasks are plausible suite-catchable escapes, not a third of the work. — fix: Factor express step 3 (integrate.sh:652-700: the scoped/full suite loop with lease re-stamping) into a `wave_suite` function. Call it in self_land's last-lane branch (builder.sh, before `"$SELF" seal`), and also run run_verify_cmds for each wave task on integrate/<date> (<10s lines by rule). Green:… (kit/ops/lib/builder.sh, kit/ops/lib/integrate.sh, kit/ops/roles/CONDUCTOR.md, kit/ops/roles/SOLO.md, kit/ops/roles/BUILDER.md)
- **quality-2** · OVERSTATED · The 31 golden pairs run nowhere automatic — CI stayed green for ~47h while two goldens were red (IDEAS:57, still open) — Goldens are checked at every lane close. The gap is hand commits and releases cut outside lanes (the 6.5.0 hand release shipped with 2 stale goldens), plus the per-task feedback delay. The in-kit handoff fix would shorten per-task feedback, but it does nothing for the measured incident. — fix: (a) Add `check --changed [<ref>]` to cmd_check (observe.sh:1794). It runs the pairs whose .cmd/.expected changed in BASE...ref, plus pairs named by an ops/SURFACES.tsv cmd whose surface changed (reuse surface_change_set from core.sh). Call it from cmd_handoff after run_verify_cmds, worktree-local p… (kit/ops/lib/observe.sh, kit/ops/lib/builder.sh, .github/workflows/ci.yml)
- **quality-3** · OVERSTATED · `check --only` and `find --api` are primary-anchored, so verify: lines run from a builder worktree prove base, not the branch (IDEAS:58, still open) — About 2% of done tasks (3 of 171), not 14%, had a check line that was vacuous end to end before landing. The rest were vacuous only at builder verify and handoff. It is still a real tooling gap: pre-land feedback is wrong and lanes have to be told workarounds. — fix: In cmd_check: when $PWD is under $PRIMARY/.polaris/wt/<ID> (the same case test the entry uses at polaris:360), set dir=<worktree>/ops/tests and run each .cmd from the worktree root. When --only was given explicitly and n=0, exit 1 ('no golden named <x> — a named check that runs nothing is red'). A … (kit/ops/lib/observe.sh, kit/ops/lib/search.sh, kit/ops/lib/selftest/board.sh)
- **quality-4** · CONFIRMED · handoff can prove the wrong tree: an empty feat branch passes (IDEAS:26, still open), and `handoff <ID>` from the primary runs verify: against the primary — The mechanism is as stated. Per occurrence it costs a failed land plus a re-land, not a full claim→land cycle. The wrong-tree verify path is plausibly hit by conductor-spawned builders; that frequency is unmeasured. — fix: In cmd_handoff, before check_ownership: (1) die if `git -C "$PRIMARY" rev-list --count "$BASE..feat/$id"` is 0; (2) die if `git status --porcelain --untracked-files=all` in the task worktree lists any path matching files_owned ('commit it first'); (3) run run_verify_cmds from `(cd "$(wt_path "$id")… (kit/ops/lib/builder.sh, kit/ops/lib/ownership.sh, kit/ops/lib/integrate.sh)
- **quality-5** · CONFIRMED · Planned work can vanish silently: next_promote drops 3 of 4 gate failures, bare `next` never prints held lines, and `finish` never reads backlog/ (IDEAS:30/47,… — Frequency is unmeasured. There are 0 stranded tasks today, and one historical bite (the unset-contract case T-130 fixed, IDEAS:30). Impact stays an estimate. — fix: In next_promote, replace the three `ok=0` sets with held reasons ('held: <ID> — contract <path> missing', '— points <p> must be 1-5', '— waits on <dep> (<col>)') appended to NX_HELD, keeping promotion semantics identical. In next_route's finish branch, print NX_HELD after the verb line. In cmd_fini… (kit/ops/lib/handover.sh, kit/ops/lib/observe.sh, ops/tests/handover-route.cmd, ops/tests/handover-route.expected, kit/ops/roles/CONDUCTOR.md)
- **quality-6** · CONFIRMED · The quality feedback loop is blind: kickback rate is structurally 0, the brain's kickback parser can never match, and Learned is dry despite the 6.5.0 fix — The parser bug and the dry Learned log are real and cheap to fix. The escape rate is not measured. By subject reading, about 5 of the 20 fix tasks since 6.1.0 repair lane-landed work. — fix: (1) One line in knowledge.sh brain_learned: `if ($i=="id" && $(i+1)==":") id=$(i+2); if ($i=="note" && $(i+1)==":") note=$(i+2)`, or reuse next_brief's sed extraction (handover.sh:283-285). Add a brain drill assertion that a seeded kickback's id and note appear. (2) An escape signal: a `fixes: [T-x… (kit/ops/lib/knowledge.sh, kit/ops/lib/observe.sh, kit/ops/lib/integrate.sh, kit/ops/roles/EVOLVE.md, kit/ops/templates/TASK.md, kit/ops/lib/selftest/brain.sh)
- **quality-7** · CONFIRMED · Any non-completion event erases the Stop-hook hop licence, and 6.5.0's seal nudge now invites exactly that right after `done` (IDEAS:46, still open, now worse) — The backstop can be lost for one turn after a lane obeys the nudge. How often a run actually stalls is unmeasured, so this is an estimate. — fix: In evt (core.sh), write last-event only for completion and parking kinds: `case "$1" in handoff|done|all-review|kickback|release|blocked) printf … > "$hs/last-event";; esac`, keeping the avoid-list write as is. Add a handover drill assertion: `learned -m` after a handoff leaves the licence intact. … (kit/ops/lib/core.sh, kit/ops/lib/selftest/board.sh)
- **quality-8** · OVERSTATED · checkout-guard is not wired for the PowerShell tool (this machine's primary shell) and over-matches non-switching git commands (IDEAS:23, 56, both still open) — Keep: add a PowerShell matcher and allow lowercase `branch -d`. Reverse: close the `git restore` hole (deny worktree-affecting restore in the primary) instead of opening `checkout -- <path>`. — fix: Add a second PreToolUse entry `{matcher: "PowerShell", hooks: [checkout-guard.sh]}` in kit/.claude/settings.json, plus the installer's merge identity (the hook already reads tool_input.command). In the tokenizer, treat `checkout` followed by a `--` pathspec (with or without a ref) as a file restore… (kit/.claude/settings.json, kit/ops/hooks/checkout-guard.sh, kit/ops/install.sh, ops/tests/checkout-guard-denies.cmd, ops/tests/checkout-guard-denies.expected)
- **quality-9** · OVERSTATED · Visual gate is self-graded: a builder's own `--saw "FAIL …"` still hands off and self-lands, and nobody opens the before/after pair under landing: self — The narrower, real gap: under landing: self nobody does INTEGRATOR.md:17's pair review. A FAIL-word refusal needs a contract amendment (a Planner or human decision), not a 1-point code change. The impact is an unmeasured estimate. — fix: In visual_gate die mode, refuse when saw matches `\bFAIL\b` ('you graded it FAIL against ops/DESIGN.md — fix it, or release --to blocked'). In self_land, when the diff touched a visual: path and saw lacks `PASS`, stop in review/ with the same shape as the risk: high refusal, so the integrator or a … (kit/ops/lib/visual.sh, kit/ops/lib/builder.sh, kit/ops/roles/BUILDER.md)
- **quality-10** · CONFIRMED · Backlog re-verification ledger: remaining open items, with the one worth a task now being a retire verb (T-160 stuck in blocked/ 8 days) — The retire gap makes every close carry a false 'blocked' caveat and teaches the model to ignore caveats. The awake race produces a red that is not the change's on release runs (1h qa plus about 15m diagnosis once, IDEAS:53). The rest are cheap housekeeping. — fix: (1) Add `polaris retire <ID> -m "why"`: ready|backlog|blocked → done/ with a `retired: <date> <why>` stamp and `evt retired`, excluded from cycle and points metrics. Refuse active/ and review/. Then retire T-160. 2 pts. (2) drill_awake: size GRACE from a measured `awake-hook.sh --test tick` (≥3× on… (kit/ops/polaris, kit/ops/lib/builder.sh, kit/ops/lib/observe.sh, kit/ops/lib/selftest/policy.sh, kit/ops/lib/selftest/spine.sh, kit/ops/roles/INIT.md, kit/ops/…)
- **quality-11** · OVERSTATED · SOLO step 1 sends the model to hand-roll a 12-line git-plumbing board commit that `claim` already does, and SOLO tasks written straight into ready/ skip the re… — About 1-2 tool calls per self-authored SOLO task (an estimate). The better fix is a CLI verb that commits authored tasks under the ready-gate predicate; it would cover both SOLO and PLANNER. — fix: Replace SOLO.md:54 with 'claim commits it — no board commit by hand'. In cmd_claim, after the lock, run the same ready-gate predicate as next_promote (factor it out of handover.sh:118-128 as `ready_gate <taskfile>` returning the held reason) and refuse with that reason. The predicate is shared, so … (kit/ops/roles/SOLO.md, kit/ops/lib/builder.sh, kit/ops/lib/handover.sh)

Skeptic — missed in quality:
- checkout-guard's real hole is the opposite of quality-8's proposed fix. Measured with `checkout-guard.sh --test`: `git restore .` → allow and `git restore --staged --worktree .` → allow, while `git checkout -- .` → deny (pinned at checkout-guard-denies.expected:3). In the shared primary, `restore .` wipes every session's uncommitted and parked work. Fix: de…
- claim's printed entry note (kit/ops/lib/builder.sh:206) tells pinned-cwd subagents to 'run everything via absolute paths under .polaris/wt/<ID> (or cd there — the shell's cwd persists between calls)'. Subagent Bash calls reset cwd between calls, and run_verify_cmds executes in CWD (ownership.sh:56). So the recommended absolute-path `bash <wt>/ops/polaris ha…
- Hand commits on main bypass every lane gate, and they, not the lane flow, caused the measured red-golden window. On 2026-09-15, 4 hand commits (68cae37, be5f768, 99449d8, 89e68a8) touched 7 kit code files (lib, hooks, entry) with no verify, qa or check, and shipped as 6.5.0 (601f109). T-160 sitting in blocked/ is the leftover. The release ritual has no `qa`…
- Doc drift that Opus will trust: 'the suite stays per-wave' / 'the wave gate already runs the suite once' appears in builder.sh:334, BUILDER.md:51 and :63, ops/contracts/shared-checkout.md:276 and the run_verify_cmds comment at ownership.sh:58-60. Under the default landing: self it is false before the push. SOLO.md:106 is the only place that tells the truth.…
- The express lane runs the task's verify: lines (cmd_run_verify, integrate.sh:729-730) only AFTER cmd_seal has merged and pushed $BASE. A red verify there cannot unwind or kick back; it just dies after the push. Moving run_verify ahead of step 4 (seal) would bring verify: into the same unwind-plus-kickback path the suite already has at integrate.sh:680-688.

## Harness facts (Claude Code 2.1.281, docs + binary)

- **Q1a. When several PreToolUse hooks (user + project settings) match one tool call, do they run in parallel or one after another, and how are…** [documented] They run in parallel. The docs say: "All matching hooks run in parallel. If you define the same handler in more than one settings file, it runs once. A plugin's or skill's copy of the same handler stays separate." When their decisions differ, the order of precedence is deny > defer > ask > allow. An exit 2 counts as a deny. When several hooks return additionalContext, Claude receives every value. Consequence: there …
- **Q1b. Is there an async (non-blocking) hook option?** [documented] Yes, but only on type:"command" hooks. `"async": true` runs the hook in the background. It cannot block or decide anything: "response fields like decision, permissionDecision, and continue have no effect". timeout is not enforced on it. Its additionalContext and systemMessage reach Claude on the next conversation turn, or wait for the next user interaction if the session is idle. There is no dedup across repeated fi…
- **Q2a. Which input fields do PreToolUse, UserPromptSubmit, SessionStart, Stop and SubagentStart/SubagentStop hooks receive?** [documented] Common to all events: session_id, prompt_id (v2.1.196+), transcript_path, cwd, scratchpad_dir (v2.1.257+), permission_mode (not on every event) and hook_event_name. There is also `effort`, an object {level: low|medium|high|xhigh|max}, 'present for events that fire within a tool-use context, such as PreToolUse, PostToolUse, Stop, and SubagentStop'; the same value is exported as $CLAUDE_EFFORT to hooks and to Bash. In…
- **Q2b. Does any hook input carry the session's current model?** [documented] Only SessionStart, and not reliably. The docs say: "Only SessionStart hooks can receive a model field, and Claude Code doesn't always include it". It can be missing after /clear or after conversation recovery. "There is no $CLAUDE_MODEL environment variable", and $ANTHROPIC_MODEL does not follow /model switches. To track the model live, use PreModelSwitch/PostModelSwitch hooks (from_model/to_model; PreModelSwitch ca…
- **Q3a. Can a hook matcher target the PowerShell tool?** [documented] Yes. The matcher is the tool name 'PowerShell', and tool_input has the same fields as Bash (command, description, timeout, run_in_background). The docs say to 'Match `Bash|PowerShell` in hooks that inspect shell commands'. On Windows with Git Bash installed, the PowerShell tool is on by default for claude.ai/Console accounts ('Claude treats PowerShell as the primary shell'), and a hook that matches only Bash 'never …
- **Q3b. Can PreToolUse match Agent, Task, Workflow and MCP calls, and can the hook read tool_input.model and tool_input.subagent_type?** [documented] PreToolUse 'Matches on any tool name except EndConversation', and the docs explicitly list Agent and Workflow. - Agent: tool_input = {prompt, description, subagent_type, model (optional alias sonnet|opus|haiku|fable, present only when Claude passes one)}, so a hook can read and deny on subagent_type and on model. It cannot see a model that is pinned inside the agent definition (e.g. claude-code-guide → haiku); only …
- **Q4a. Do completion notifications for background Bash or background subagents reach the main session?** [documented] Yes. A background Bash command, a background subagent and a Workflow each deliver a <\task-notification> into the main conversation on a later turn. The Workflow tool 'returns immediately with a task ID, and a <\task-notification> arrives when the workflow completes'. A command started by the main conversation or by a background subagent keeps running after the final response. Subagents run in the background by defa…
- **Q4b. Does the notification reach a subagent that launched a background command, including after that subagent's turn has ended?** [observed] Yes for background subagents: 'A background subagent can leave a background Bash or PowerShell command running past the end of its turn. When that command ends, Claude Code sends the subagent a notification.' A command owned by a FOREGROUND subagent is killed when that subagent gives its final response. Observed (CC 2.1.255, session session-D, subagent <agent> 'solo-vendored-line'): the subagent ended its t…
- **Q5a. How does the Monitor tool work?** [documented] Monitor runs a command, or opens a WebSocket (ws), in the background. - Each stdout line is one event. Lines arriving within 200ms are batched into one notification. Stderr is not an event. - Exit ends the watch and reports the exit code. - Every watch has a deadline: 5 min by default, at most 30 min, and 10 min in a -p run. At the deadline it is killed and Claude gets one notice, so it can re-arm. - A monitor that …
- **Q5b. What is the Bash tool's maximum timeout?** [documented] The default is 120000 ms (BASH_DEFAULT_TIMEOUT_MS) and the ceiling is 600000 ms (BASH_MAX_TIMEOUT_MS). The effective ceiling is the larger of the two. Higher values are 'reduced to the maximum rather than rejected'. Since the docs now say an overrunning command is moved to the background instead of killed (except commands starting with `sleep`), the memory note's '10-min tool cap kills the suite' premise is outdated…
- **Q6. Does a custom output style reach subagents, and what does keep-coding-instructions do?** [documented] No. 'Output styles apply to the main conversation and to a fork ... Other subagents run their own system prompt, so styles don't change how they respond.' The sub-agents page lists output style under 'Some main-conversation state never reaches a non-fork subagent'. Workflow agents are non-fork subagents, so they are not styled either (inferred). keep-coding-instructions defaults to false: without it, a custom style …
- **Q7a. What does disable-model-invocation change about prompt cost?** [documented] With `disable-model-invocation: true` the description is not in context. The docs' table row reads 'Description not in context, full skill loads when you invoke'. Only the user can invoke the skill (with /name), and it can no longer be preloaded into subagents. `user-invocable: false` does the opposite: the description stays in context and only Claude can invoke it. Settings can do the same without editing SKILL.md …
- **Q7b. How do `.claude/rules/*.md` files with `paths:` load?** [documented] All .md files under .claude/rules/ are discovered recursively. Rules without `paths` load unconditionally at launch, like CLAUDE.md. Rules with YAML `paths:` globs 'trigger when Claude reads files matching the pattern, not on every tool use'. After compaction, path-scoped rules reload only when a matching file is read again. User-level ~/.claude/rules/ applies to every project. Rules are skipped if `project` is excl…
- **Q7c. How much of a plugin's skills and agents is injected into each context?** [documented] Skills: only name + description (+ when_to_use) go into the skill listing, each capped at 1,536 characters (configurable with skillListingMaxDescChars). The whole listing has a budget of '1% of the model's context window' (skillListingBudgetFraction or SLASH_COMMAND_TOOL_CHAR_BUDGET). When it overflows, the least-invoked skills lose their descriptions first. The full SKILL.md loads only when invoked, or at startup i…
- **Q7d. Can a project's settings.json `enabledPlugins: {...: false}` disable a user-enabled plugin for that project only?** [documented] Yes. enabledPlugins scope is 'Any file', keyed plugin@marketplace. 'Project settings take precedence over user settings', and the docs say that to keep a plugin out of one project you set it false in that project's committed .claude/settings.json. .claude/settings.local.json also works as a per-machine override. The only exception is plugins force-enabled by managed settings. Observed: this repo's .claude/settings.l…
- **Q8a. Can SendMessage continue a completed subagent with its context intact?** [documented] Yes. 'Resumed subagents retain their full conversation history, including all previous tool calls, results, and reasoning.' When Claude SendMessages a completed subagent by name or agentId, 'the subagent resumes in the background without a new Agent invocation', keeps its original tool set, and 'can keep reading the prompt cache the original run warmed'. Limits: - Explore and Plan are one-shot and return no agent ID…
- **Q8b. How does Agent isolation:"worktree" behave, and how is it cleaned up?** [documented] The subagent gets a temporary git worktree under .claude/worktrees/. It is branched by default from the repository's default branch on the remote (worktree.baseRef "fresh"), NOT from the parent's HEAD, unless worktree.baseRef is "head". Only tracked files are checked out (plus .worktreeinclude). The subagent's Bash and PowerShell commands whose cwd resolves to the main checkout fail, and Bash that redirects git into…
- **Q9a. What counts as opt-in for the Workflow tool? Does a skill whose instructions call Workflow count?** [observed] The Workflow tool description in 2.1.281 allows a call ONLY when one of these holds: 1. The user typed 'ultracode'. 2. Ultracode is on for the session. 3. The user directly asked for a workflow or multi-agent orchestration 'in the user's words'. 4. 'The user invoked a skill or slash command whose instructions tell you to call Workflow.' 5. The user asked to run a specific named or saved workflow. 'a task that would …
- **Q9b. Where are saved workflows stored, what approval do they need, and can effort or model be set per agent?** [documented] Saving (from /workflows, press s) goes to project .claude/workflows/ (the closest existing one up to the repo root) or to personal ~/.claude/workflows/. The project copy wins on a name clash. Plugins ship workflows in a workflows/ dir and run as /plugin:name. A saved workflow runs as /<name> and takes `args`. Every run's script is also written under the session dir in <transcripts> Approval: interactive Manua…
- **Q10. What effort settings are documented for the main session and for subagents?** [documented] Main session, highest priority first: 1. The CLAUDE_CODE_EFFORT_LEVEL env var, then --effort, then /effort. 2. Settings: modelSettings.<model>.effortLevel or effortLevel. 3. The model default: high, except Opus 5.5 = medium and Opus 4.7 = xhigh. 'a top-level effortLevel in your user settings file doesn't count for Opus 5.5'. A top-level effortLevel in project, local or managed settings applies to every model. Observ…
- **Q11. When a PreToolUse hook times out, does the tool run (fail-open) or is it blocked?** [documented] It fails open. 'A timed-out command, http, or mcp_tool hook doesn't block the tool call. The call continues through the normal permission flow, so don't count on a stalled hook to act as a gate.' Only Agent SDK callback hooks block on timeout. The default timeout is 600s for command hooks (30s on UserPromptSubmit), and the kit sets 5–20s explicitly. Exit code 1, or a hook that fails to start (e.g. 127), is also non-…
- **Q12a. What guidance exists on CLAUDE.md size, @-imports and nested CLAUDE.md loading?** [documented] Size: 'target under 200 lines per CLAUDE.md file. Longer files consume more context and reduce adherence'. A file up to 4 MiB loads in full; a larger one is skipped. @imports: they expand at launch and 'doesn't reduce context'. Relative paths resolve from the importing file, recursion goes at most 4 hops, and imports inside code spans are ignored. Ancestor CLAUDE.md files (e.g. ~/CLAUDE.md) load at laun…
- **Q12b. Which instruction files do subagents receive?** [observed] Non-fork subagents get 'every level of the CLAUDE.md hierarchy the main conversation loads' (including ancestor and ~/.claude/CLAUDE.md, rules and CLAUDE.local.md). Explore and Plan skip them; the 2.1.281 binary shows omitClaudeMd:!0 on both. Custom agents can set `omitClaudeMd: true` (v2.1.271+) to load only managed policy files, which is a lever for dropping the RuFlo ancestor file from POLARIS lanes. DOCS vs OBSE…
- **Q13a. Which built-in agent types pin their own model regardless of the parent?** [observed] From the 2.1.281 binary and the docs: - claude-code-guide: model:"haiku" (pinned). Observed 3 of 3 runs on claude-haiku-4-5-20251001, from opus-5 and opus-5-5 parents. - statusline-setup: model:"sonnet" (pinned). - Explore: model:"inherit", 'capped at Opus on the Claude API'. Observed runs on the parent's model. - Plan: model:"inherit". Observed 19/19 opus-5-5 under opus-5-5 parents; fable children under fable paren…
- **Q13b. Does a custom agent's `model:` frontmatter override the parent's model, and does `model: inherit` exist?** [documented] Yes to both. Accepted values are sonnet | opus | haiku | fable | a full model ID | inherit. The resolution order is: 1. The per-invocation `model` param. 2. Frontmatter `model` ('inherit selects the main conversation's model'). 3. CLAUDE_CODE_SUBAGENT_MODEL. 4. The main conversation's model. Before v2.1.251 the env var came first. A family alias that matches the parent's family (e.g. `opus` under Opus 5.5 [1m]) reso…
- **Extra (harness-1 needs_human). What controls auto-compaction on a 1M-token window?** [documented] By default, models with a native 1M window 'compact before the window fills, at about 967K tokens', which is consistent with the 0 compactions measured. The knobs: - `autoCompactWindow` setting: scope 'Any file' (so project .claude/settings.json works), 100000–1000000 tokens. - /autocompact <n> and the --autocompact flag. - CLAUDE_CODE_AUTO_COMPACT_WINDOW env var, plain integer only, which wins over the others. - CL…
- **Extra. Which Claude Code version produced these observations?** [observed] The live sessions, including this one, run 2.1.281. That is the VS Code extension binary at <the installed Claude Code binary> and the transcript version field is 2.1.281. The `claude` on PATH is npm @anthropic-ai/claude-code 2.1.251, whose sdk-tools.d.ts shows an older Monitor schema (persistent flag, 1h max). Documented version gates that m…
